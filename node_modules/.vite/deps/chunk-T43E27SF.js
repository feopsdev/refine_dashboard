import {
  O,
  T,
  d,
  p
} from "./chunk-SCGGGWO5.js";
import {
  require_pluralize
} from "./chunk-ZFKR73HT.js";
import {
  chunk_default,
  debounce_default,
  differenceWith_default,
  fromPairs_default,
  get_default,
  isEqual_default,
  unionWith_default,
  uniqBy_default,
  zip_default
} from "./chunk-BT45J3G4.js";
import {
  require_react_dom
} from "./chunk-GYWC62UC.js";
import {
  require_react
} from "./chunk-HS5T2ZWL.js";
import {
  __commonJS,
  __toESM
} from "./chunk-AUZ3RYOM.js";

// node_modules/use-sync-external-store/cjs/use-sync-external-store-shim.development.js
var require_use_sync_external_store_shim_development = __commonJS({
  "node_modules/use-sync-external-store/cjs/use-sync-external-store-shim.development.js"(exports) {
    "use strict";
    if (true) {
      (function() {
        "use strict";
        if (typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ !== "undefined" && typeof __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStart === "function") {
          __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStart(new Error());
        }
        var React11 = require_react();
        var ReactSharedInternals = React11.__SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED;
        function error(format) {
          {
            {
              for (var _len2 = arguments.length, args = new Array(_len2 > 1 ? _len2 - 1 : 0), _key2 = 1; _key2 < _len2; _key2++) {
                args[_key2 - 1] = arguments[_key2];
              }
              printWarning("error", format, args);
            }
          }
        }
        function printWarning(level, format, args) {
          {
            var ReactDebugCurrentFrame = ReactSharedInternals.ReactDebugCurrentFrame;
            var stack = ReactDebugCurrentFrame.getStackAddendum();
            if (stack !== "") {
              format += "%s";
              args = args.concat([stack]);
            }
            var argsWithFormat = args.map(function(item) {
              return String(item);
            });
            argsWithFormat.unshift("Warning: " + format);
            Function.prototype.apply.call(console[level], console, argsWithFormat);
          }
        }
        function is(x2, y2) {
          return x2 === y2 && (x2 !== 0 || 1 / x2 === 1 / y2) || x2 !== x2 && y2 !== y2;
        }
        var objectIs = typeof Object.is === "function" ? Object.is : is;
        var useState5 = React11.useState, useEffect6 = React11.useEffect, useLayoutEffect = React11.useLayoutEffect, useDebugValue = React11.useDebugValue;
        var didWarnOld18Alpha = false;
        var didWarnUncachedGetSnapshot = false;
        function useSyncExternalStore2(subscribe, getSnapshot, getServerSnapshot) {
          {
            if (!didWarnOld18Alpha) {
              if (React11.startTransition !== void 0) {
                didWarnOld18Alpha = true;
                error("You are using an outdated, pre-release alpha of React 18 that does not support useSyncExternalStore. The use-sync-external-store shim will not work correctly. Upgrade to a newer pre-release.");
              }
            }
          }
          var value = getSnapshot();
          {
            if (!didWarnUncachedGetSnapshot) {
              var cachedValue = getSnapshot();
              if (!objectIs(value, cachedValue)) {
                error("The result of getSnapshot should be cached to avoid an infinite loop");
                didWarnUncachedGetSnapshot = true;
              }
            }
          }
          var _useState = useState5({
            inst: {
              value,
              getSnapshot
            }
          }), inst = _useState[0].inst, forceUpdate = _useState[1];
          useLayoutEffect(function() {
            inst.value = value;
            inst.getSnapshot = getSnapshot;
            if (checkIfSnapshotChanged(inst)) {
              forceUpdate({
                inst
              });
            }
          }, [subscribe, value, getSnapshot]);
          useEffect6(function() {
            if (checkIfSnapshotChanged(inst)) {
              forceUpdate({
                inst
              });
            }
            var handleStoreChange = function() {
              if (checkIfSnapshotChanged(inst)) {
                forceUpdate({
                  inst
                });
              }
            };
            return subscribe(handleStoreChange);
          }, [subscribe]);
          useDebugValue(value);
          return value;
        }
        function checkIfSnapshotChanged(inst) {
          var latestGetSnapshot = inst.getSnapshot;
          var prevValue = inst.value;
          try {
            var nextValue = latestGetSnapshot();
            return !objectIs(prevValue, nextValue);
          } catch (error2) {
            return true;
          }
        }
        function useSyncExternalStore$12(subscribe, getSnapshot, getServerSnapshot) {
          return getSnapshot();
        }
        var canUseDOM = !!(typeof window !== "undefined" && typeof window.document !== "undefined" && typeof window.document.createElement !== "undefined");
        var isServerEnvironment = !canUseDOM;
        var shim = isServerEnvironment ? useSyncExternalStore$12 : useSyncExternalStore2;
        var useSyncExternalStore$2 = React11.useSyncExternalStore !== void 0 ? React11.useSyncExternalStore : shim;
        exports.useSyncExternalStore = useSyncExternalStore$2;
        if (typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ !== "undefined" && typeof __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStop === "function") {
          __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStop(new Error());
        }
      })();
    }
  }
});

// node_modules/use-sync-external-store/shim/index.js
var require_shim = __commonJS({
  "node_modules/use-sync-external-store/shim/index.js"(exports, module) {
    "use strict";
    if (false) {
      module.exports = null;
    } else {
      module.exports = require_use_sync_external_store_shim_development();
    }
  }
});

// node_modules/stackframe/stackframe.js
var require_stackframe = __commonJS({
  "node_modules/stackframe/stackframe.js"(exports, module) {
    (function(root, factory) {
      "use strict";
      if (typeof define === "function" && define.amd) {
        define("stackframe", [], factory);
      } else if (typeof exports === "object") {
        module.exports = factory();
      } else {
        root.StackFrame = factory();
      }
    })(exports, function() {
      "use strict";
      function _isNumber(n) {
        return !isNaN(parseFloat(n)) && isFinite(n);
      }
      function _capitalize(str) {
        return str.charAt(0).toUpperCase() + str.substring(1);
      }
      function _getter(p3) {
        return function() {
          return this[p3];
        };
      }
      var booleanProps = ["isConstructor", "isEval", "isNative", "isToplevel"];
      var numericProps = ["columnNumber", "lineNumber"];
      var stringProps = ["fileName", "functionName", "source"];
      var arrayProps = ["args"];
      var objectProps = ["evalOrigin"];
      var props = booleanProps.concat(numericProps, stringProps, arrayProps, objectProps);
      function StackFrame(obj) {
        if (!obj)
          return;
        for (var i3 = 0; i3 < props.length; i3++) {
          if (obj[props[i3]] !== void 0) {
            this["set" + _capitalize(props[i3])](obj[props[i3]]);
          }
        }
      }
      StackFrame.prototype = {
        getArgs: function() {
          return this.args;
        },
        setArgs: function(v2) {
          if (Object.prototype.toString.call(v2) !== "[object Array]") {
            throw new TypeError("Args must be an Array");
          }
          this.args = v2;
        },
        getEvalOrigin: function() {
          return this.evalOrigin;
        },
        setEvalOrigin: function(v2) {
          if (v2 instanceof StackFrame) {
            this.evalOrigin = v2;
          } else if (v2 instanceof Object) {
            this.evalOrigin = new StackFrame(v2);
          } else {
            throw new TypeError("Eval Origin must be an Object or StackFrame");
          }
        },
        toString: function() {
          var fileName = this.getFileName() || "";
          var lineNumber = this.getLineNumber() || "";
          var columnNumber = this.getColumnNumber() || "";
          var functionName = this.getFunctionName() || "";
          if (this.getIsEval()) {
            if (fileName) {
              return "[eval] (" + fileName + ":" + lineNumber + ":" + columnNumber + ")";
            }
            return "[eval]:" + lineNumber + ":" + columnNumber;
          }
          if (functionName) {
            return functionName + " (" + fileName + ":" + lineNumber + ":" + columnNumber + ")";
          }
          return fileName + ":" + lineNumber + ":" + columnNumber;
        }
      };
      StackFrame.fromString = function StackFrame$$fromString(str) {
        var argsStartIndex = str.indexOf("(");
        var argsEndIndex = str.lastIndexOf(")");
        var functionName = str.substring(0, argsStartIndex);
        var args = str.substring(argsStartIndex + 1, argsEndIndex).split(",");
        var locationString = str.substring(argsEndIndex + 1);
        if (locationString.indexOf("@") === 0) {
          var parts = /@(.+?)(?::(\d+))?(?::(\d+))?$/.exec(locationString, "");
          var fileName = parts[1];
          var lineNumber = parts[2];
          var columnNumber = parts[3];
        }
        return new StackFrame({
          functionName,
          args: args || void 0,
          fileName,
          lineNumber: lineNumber || void 0,
          columnNumber: columnNumber || void 0
        });
      };
      for (var i2 = 0; i2 < booleanProps.length; i2++) {
        StackFrame.prototype["get" + _capitalize(booleanProps[i2])] = _getter(booleanProps[i2]);
        StackFrame.prototype["set" + _capitalize(booleanProps[i2])] = function(p3) {
          return function(v2) {
            this[p3] = Boolean(v2);
          };
        }(booleanProps[i2]);
      }
      for (var j = 0; j < numericProps.length; j++) {
        StackFrame.prototype["get" + _capitalize(numericProps[j])] = _getter(numericProps[j]);
        StackFrame.prototype["set" + _capitalize(numericProps[j])] = function(p3) {
          return function(v2) {
            if (!_isNumber(v2)) {
              throw new TypeError(p3 + " must be a Number");
            }
            this[p3] = Number(v2);
          };
        }(numericProps[j]);
      }
      for (var k = 0; k < stringProps.length; k++) {
        StackFrame.prototype["get" + _capitalize(stringProps[k])] = _getter(stringProps[k]);
        StackFrame.prototype["set" + _capitalize(stringProps[k])] = function(p3) {
          return function(v2) {
            this[p3] = String(v2);
          };
        }(stringProps[k]);
      }
      return StackFrame;
    });
  }
});

// node_modules/error-stack-parser/error-stack-parser.js
var require_error_stack_parser = __commonJS({
  "node_modules/error-stack-parser/error-stack-parser.js"(exports, module) {
    (function(root, factory) {
      "use strict";
      if (typeof define === "function" && define.amd) {
        define("error-stack-parser", ["stackframe"], factory);
      } else if (typeof exports === "object") {
        module.exports = factory(require_stackframe());
      } else {
        root.ErrorStackParser = factory(root.StackFrame);
      }
    })(exports, function ErrorStackParser(StackFrame) {
      "use strict";
      var FIREFOX_SAFARI_STACK_REGEXP = /(^|@)\S+:\d+/;
      var CHROME_IE_STACK_REGEXP = /^\s*at .*(\S+:\d+|\(native\))/m;
      var SAFARI_NATIVE_CODE_REGEXP = /^(eval@)?(\[native code])?$/;
      return {
        /**
         * Given an Error object, extract the most information from it.
         *
         * @param {Error} error object
         * @return {Array} of StackFrames
         */
        parse: function ErrorStackParser$$parse(error) {
          if (typeof error.stacktrace !== "undefined" || typeof error["opera#sourceloc"] !== "undefined") {
            return this.parseOpera(error);
          } else if (error.stack && error.stack.match(CHROME_IE_STACK_REGEXP)) {
            return this.parseV8OrIE(error);
          } else if (error.stack) {
            return this.parseFFOrSafari(error);
          } else {
            throw new Error("Cannot parse given Error object");
          }
        },
        // Separate line and column numbers from a string of the form: (URI:Line:Column)
        extractLocation: function ErrorStackParser$$extractLocation(urlLike) {
          if (urlLike.indexOf(":") === -1) {
            return [urlLike];
          }
          var regExp = /(.+?)(?::(\d+))?(?::(\d+))?$/;
          var parts = regExp.exec(urlLike.replace(/[()]/g, ""));
          return [parts[1], parts[2] || void 0, parts[3] || void 0];
        },
        parseV8OrIE: function ErrorStackParser$$parseV8OrIE(error) {
          var filtered = error.stack.split("\n").filter(function(line) {
            return !!line.match(CHROME_IE_STACK_REGEXP);
          }, this);
          return filtered.map(function(line) {
            if (line.indexOf("(eval ") > -1) {
              line = line.replace(/eval code/g, "eval").replace(/(\(eval at [^()]*)|(,.*$)/g, "");
            }
            var sanitizedLine = line.replace(/^\s+/, "").replace(/\(eval code/g, "(").replace(/^.*?\s+/, "");
            var location = sanitizedLine.match(/ (\(.+\)$)/);
            sanitizedLine = location ? sanitizedLine.replace(location[0], "") : sanitizedLine;
            var locationParts = this.extractLocation(location ? location[1] : sanitizedLine);
            var functionName = location && sanitizedLine || void 0;
            var fileName = ["eval", "<anonymous>"].indexOf(locationParts[0]) > -1 ? void 0 : locationParts[0];
            return new StackFrame({
              functionName,
              fileName,
              lineNumber: locationParts[1],
              columnNumber: locationParts[2],
              source: line
            });
          }, this);
        },
        parseFFOrSafari: function ErrorStackParser$$parseFFOrSafari(error) {
          var filtered = error.stack.split("\n").filter(function(line) {
            return !line.match(SAFARI_NATIVE_CODE_REGEXP);
          }, this);
          return filtered.map(function(line) {
            if (line.indexOf(" > eval") > -1) {
              line = line.replace(/ line (\d+)(?: > eval line \d+)* > eval:\d+:\d+/g, ":$1");
            }
            if (line.indexOf("@") === -1 && line.indexOf(":") === -1) {
              return new StackFrame({
                functionName: line
              });
            } else {
              var functionNameRegex = /((.*".+"[^@]*)?[^@]*)(?:@)/;
              var matches = line.match(functionNameRegex);
              var functionName = matches && matches[1] ? matches[1] : void 0;
              var locationParts = this.extractLocation(line.replace(functionNameRegex, ""));
              return new StackFrame({
                functionName,
                fileName: locationParts[0],
                lineNumber: locationParts[1],
                columnNumber: locationParts[2],
                source: line
              });
            }
          }, this);
        },
        parseOpera: function ErrorStackParser$$parseOpera(e) {
          if (!e.stacktrace || e.message.indexOf("\n") > -1 && e.message.split("\n").length > e.stacktrace.split("\n").length) {
            return this.parseOpera9(e);
          } else if (!e.stack) {
            return this.parseOpera10(e);
          } else {
            return this.parseOpera11(e);
          }
        },
        parseOpera9: function ErrorStackParser$$parseOpera9(e) {
          var lineRE = /Line (\d+).*script (?:in )?(\S+)/i;
          var lines = e.message.split("\n");
          var result = [];
          for (var i2 = 2, len = lines.length; i2 < len; i2 += 2) {
            var match = lineRE.exec(lines[i2]);
            if (match) {
              result.push(new StackFrame({
                fileName: match[2],
                lineNumber: match[1],
                source: lines[i2]
              }));
            }
          }
          return result;
        },
        parseOpera10: function ErrorStackParser$$parseOpera10(e) {
          var lineRE = /Line (\d+).*script (?:in )?(\S+)(?:: In function (\S+))?$/i;
          var lines = e.stacktrace.split("\n");
          var result = [];
          for (var i2 = 0, len = lines.length; i2 < len; i2 += 2) {
            var match = lineRE.exec(lines[i2]);
            if (match) {
              result.push(
                new StackFrame({
                  functionName: match[3] || void 0,
                  fileName: match[2],
                  lineNumber: match[1],
                  source: lines[i2]
                })
              );
            }
          }
          return result;
        },
        // Opera 10.65+ Error.stack very similar to FF/Safari
        parseOpera11: function ErrorStackParser$$parseOpera11(error) {
          var filtered = error.stack.split("\n").filter(function(line) {
            return !!line.match(FIREFOX_SAFARI_STACK_REGEXP) && !line.match(/^Error created at/);
          }, this);
          return filtered.map(function(line) {
            var tokens = line.split("@");
            var locationParts = this.extractLocation(tokens.pop());
            var functionCall = tokens.shift() || "";
            var functionName = functionCall.replace(/<anonymous function(: (\w+))?>/, "$2").replace(/\([^)]*\)/g, "") || void 0;
            var argsRaw;
            if (functionCall.match(/\(([^)]*)\)/)) {
              argsRaw = functionCall.replace(/^[^(]+\(([^)]*)\)$/, "$1");
            }
            var args = argsRaw === void 0 || argsRaw === "[arguments not available]" ? void 0 : argsRaw.split(",");
            return new StackFrame({
              functionName,
              args,
              fileName: locationParts[0],
              lineNumber: locationParts[1],
              columnNumber: locationParts[2],
              source: line
            });
          }, this);
        }
      };
    });
  }
});

// node_modules/es-errors/index.js
var require_es_errors = __commonJS({
  "node_modules/es-errors/index.js"(exports, module) {
    "use strict";
    module.exports = Error;
  }
});

// node_modules/es-errors/eval.js
var require_eval = __commonJS({
  "node_modules/es-errors/eval.js"(exports, module) {
    "use strict";
    module.exports = EvalError;
  }
});

// node_modules/es-errors/range.js
var require_range = __commonJS({
  "node_modules/es-errors/range.js"(exports, module) {
    "use strict";
    module.exports = RangeError;
  }
});

// node_modules/es-errors/ref.js
var require_ref = __commonJS({
  "node_modules/es-errors/ref.js"(exports, module) {
    "use strict";
    module.exports = ReferenceError;
  }
});

// node_modules/es-errors/syntax.js
var require_syntax = __commonJS({
  "node_modules/es-errors/syntax.js"(exports, module) {
    "use strict";
    module.exports = SyntaxError;
  }
});

// node_modules/es-errors/type.js
var require_type = __commonJS({
  "node_modules/es-errors/type.js"(exports, module) {
    "use strict";
    module.exports = TypeError;
  }
});

// node_modules/es-errors/uri.js
var require_uri = __commonJS({
  "node_modules/es-errors/uri.js"(exports, module) {
    "use strict";
    module.exports = URIError;
  }
});

// node_modules/has-symbols/shams.js
var require_shams = __commonJS({
  "node_modules/has-symbols/shams.js"(exports, module) {
    "use strict";
    module.exports = function hasSymbols() {
      if (typeof Symbol !== "function" || typeof Object.getOwnPropertySymbols !== "function") {
        return false;
      }
      if (typeof Symbol.iterator === "symbol") {
        return true;
      }
      var obj = {};
      var sym = Symbol("test");
      var symObj = Object(sym);
      if (typeof sym === "string") {
        return false;
      }
      if (Object.prototype.toString.call(sym) !== "[object Symbol]") {
        return false;
      }
      if (Object.prototype.toString.call(symObj) !== "[object Symbol]") {
        return false;
      }
      var symVal = 42;
      obj[sym] = symVal;
      for (sym in obj) {
        return false;
      }
      if (typeof Object.keys === "function" && Object.keys(obj).length !== 0) {
        return false;
      }
      if (typeof Object.getOwnPropertyNames === "function" && Object.getOwnPropertyNames(obj).length !== 0) {
        return false;
      }
      var syms = Object.getOwnPropertySymbols(obj);
      if (syms.length !== 1 || syms[0] !== sym) {
        return false;
      }
      if (!Object.prototype.propertyIsEnumerable.call(obj, sym)) {
        return false;
      }
      if (typeof Object.getOwnPropertyDescriptor === "function") {
        var descriptor = Object.getOwnPropertyDescriptor(obj, sym);
        if (descriptor.value !== symVal || descriptor.enumerable !== true) {
          return false;
        }
      }
      return true;
    };
  }
});

// node_modules/has-symbols/index.js
var require_has_symbols = __commonJS({
  "node_modules/has-symbols/index.js"(exports, module) {
    "use strict";
    var origSymbol = typeof Symbol !== "undefined" && Symbol;
    var hasSymbolSham = require_shams();
    module.exports = function hasNativeSymbols() {
      if (typeof origSymbol !== "function") {
        return false;
      }
      if (typeof Symbol !== "function") {
        return false;
      }
      if (typeof origSymbol("foo") !== "symbol") {
        return false;
      }
      if (typeof Symbol("bar") !== "symbol") {
        return false;
      }
      return hasSymbolSham();
    };
  }
});

// node_modules/has-proto/index.js
var require_has_proto = __commonJS({
  "node_modules/has-proto/index.js"(exports, module) {
    "use strict";
    var test = {
      __proto__: null,
      foo: {}
    };
    var $Object = Object;
    module.exports = function hasProto() {
      return { __proto__: test }.foo === test.foo && !(test instanceof $Object);
    };
  }
});

// node_modules/function-bind/implementation.js
var require_implementation = __commonJS({
  "node_modules/function-bind/implementation.js"(exports, module) {
    "use strict";
    var ERROR_MESSAGE = "Function.prototype.bind called on incompatible ";
    var toStr = Object.prototype.toString;
    var max = Math.max;
    var funcType = "[object Function]";
    var concatty = function concatty2(a, b) {
      var arr = [];
      for (var i2 = 0; i2 < a.length; i2 += 1) {
        arr[i2] = a[i2];
      }
      for (var j = 0; j < b.length; j += 1) {
        arr[j + a.length] = b[j];
      }
      return arr;
    };
    var slicy = function slicy2(arrLike, offset) {
      var arr = [];
      for (var i2 = offset || 0, j = 0; i2 < arrLike.length; i2 += 1, j += 1) {
        arr[j] = arrLike[i2];
      }
      return arr;
    };
    var joiny = function(arr, joiner) {
      var str = "";
      for (var i2 = 0; i2 < arr.length; i2 += 1) {
        str += arr[i2];
        if (i2 + 1 < arr.length) {
          str += joiner;
        }
      }
      return str;
    };
    module.exports = function bind(that) {
      var target = this;
      if (typeof target !== "function" || toStr.apply(target) !== funcType) {
        throw new TypeError(ERROR_MESSAGE + target);
      }
      var args = slicy(arguments, 1);
      var bound;
      var binder = function() {
        if (this instanceof bound) {
          var result = target.apply(
            this,
            concatty(args, arguments)
          );
          if (Object(result) === result) {
            return result;
          }
          return this;
        }
        return target.apply(
          that,
          concatty(args, arguments)
        );
      };
      var boundLength = max(0, target.length - args.length);
      var boundArgs = [];
      for (var i2 = 0; i2 < boundLength; i2++) {
        boundArgs[i2] = "$" + i2;
      }
      bound = Function("binder", "return function (" + joiny(boundArgs, ",") + "){ return binder.apply(this,arguments); }")(binder);
      if (target.prototype) {
        var Empty = function Empty2() {
        };
        Empty.prototype = target.prototype;
        bound.prototype = new Empty();
        Empty.prototype = null;
      }
      return bound;
    };
  }
});

// node_modules/function-bind/index.js
var require_function_bind = __commonJS({
  "node_modules/function-bind/index.js"(exports, module) {
    "use strict";
    var implementation = require_implementation();
    module.exports = Function.prototype.bind || implementation;
  }
});

// node_modules/hasown/index.js
var require_hasown = __commonJS({
  "node_modules/hasown/index.js"(exports, module) {
    "use strict";
    var call = Function.prototype.call;
    var $hasOwn = Object.prototype.hasOwnProperty;
    var bind = require_function_bind();
    module.exports = bind.call(call, $hasOwn);
  }
});

// node_modules/get-intrinsic/index.js
var require_get_intrinsic = __commonJS({
  "node_modules/get-intrinsic/index.js"(exports, module) {
    "use strict";
    var undefined2;
    var $Error = require_es_errors();
    var $EvalError = require_eval();
    var $RangeError = require_range();
    var $ReferenceError = require_ref();
    var $SyntaxError = require_syntax();
    var $TypeError = require_type();
    var $URIError = require_uri();
    var $Function = Function;
    var getEvalledConstructor = function(expressionSyntax) {
      try {
        return $Function('"use strict"; return (' + expressionSyntax + ").constructor;")();
      } catch (e) {
      }
    };
    var $gOPD = Object.getOwnPropertyDescriptor;
    if ($gOPD) {
      try {
        $gOPD({}, "");
      } catch (e) {
        $gOPD = null;
      }
    }
    var throwTypeError = function() {
      throw new $TypeError();
    };
    var ThrowTypeError = $gOPD ? function() {
      try {
        arguments.callee;
        return throwTypeError;
      } catch (calleeThrows) {
        try {
          return $gOPD(arguments, "callee").get;
        } catch (gOPDthrows) {
          return throwTypeError;
        }
      }
    }() : throwTypeError;
    var hasSymbols = require_has_symbols()();
    var hasProto = require_has_proto()();
    var getProto = Object.getPrototypeOf || (hasProto ? function(x2) {
      return x2.__proto__;
    } : null);
    var needsEval = {};
    var TypedArray = typeof Uint8Array === "undefined" || !getProto ? undefined2 : getProto(Uint8Array);
    var INTRINSICS = {
      __proto__: null,
      "%AggregateError%": typeof AggregateError === "undefined" ? undefined2 : AggregateError,
      "%Array%": Array,
      "%ArrayBuffer%": typeof ArrayBuffer === "undefined" ? undefined2 : ArrayBuffer,
      "%ArrayIteratorPrototype%": hasSymbols && getProto ? getProto([][Symbol.iterator]()) : undefined2,
      "%AsyncFromSyncIteratorPrototype%": undefined2,
      "%AsyncFunction%": needsEval,
      "%AsyncGenerator%": needsEval,
      "%AsyncGeneratorFunction%": needsEval,
      "%AsyncIteratorPrototype%": needsEval,
      "%Atomics%": typeof Atomics === "undefined" ? undefined2 : Atomics,
      "%BigInt%": typeof BigInt === "undefined" ? undefined2 : BigInt,
      "%BigInt64Array%": typeof BigInt64Array === "undefined" ? undefined2 : BigInt64Array,
      "%BigUint64Array%": typeof BigUint64Array === "undefined" ? undefined2 : BigUint64Array,
      "%Boolean%": Boolean,
      "%DataView%": typeof DataView === "undefined" ? undefined2 : DataView,
      "%Date%": Date,
      "%decodeURI%": decodeURI,
      "%decodeURIComponent%": decodeURIComponent,
      "%encodeURI%": encodeURI,
      "%encodeURIComponent%": encodeURIComponent,
      "%Error%": $Error,
      "%eval%": eval,
      // eslint-disable-line no-eval
      "%EvalError%": $EvalError,
      "%Float32Array%": typeof Float32Array === "undefined" ? undefined2 : Float32Array,
      "%Float64Array%": typeof Float64Array === "undefined" ? undefined2 : Float64Array,
      "%FinalizationRegistry%": typeof FinalizationRegistry === "undefined" ? undefined2 : FinalizationRegistry,
      "%Function%": $Function,
      "%GeneratorFunction%": needsEval,
      "%Int8Array%": typeof Int8Array === "undefined" ? undefined2 : Int8Array,
      "%Int16Array%": typeof Int16Array === "undefined" ? undefined2 : Int16Array,
      "%Int32Array%": typeof Int32Array === "undefined" ? undefined2 : Int32Array,
      "%isFinite%": isFinite,
      "%isNaN%": isNaN,
      "%IteratorPrototype%": hasSymbols && getProto ? getProto(getProto([][Symbol.iterator]())) : undefined2,
      "%JSON%": typeof JSON === "object" ? JSON : undefined2,
      "%Map%": typeof Map === "undefined" ? undefined2 : Map,
      "%MapIteratorPrototype%": typeof Map === "undefined" || !hasSymbols || !getProto ? undefined2 : getProto((/* @__PURE__ */ new Map())[Symbol.iterator]()),
      "%Math%": Math,
      "%Number%": Number,
      "%Object%": Object,
      "%parseFloat%": parseFloat,
      "%parseInt%": parseInt,
      "%Promise%": typeof Promise === "undefined" ? undefined2 : Promise,
      "%Proxy%": typeof Proxy === "undefined" ? undefined2 : Proxy,
      "%RangeError%": $RangeError,
      "%ReferenceError%": $ReferenceError,
      "%Reflect%": typeof Reflect === "undefined" ? undefined2 : Reflect,
      "%RegExp%": RegExp,
      "%Set%": typeof Set === "undefined" ? undefined2 : Set,
      "%SetIteratorPrototype%": typeof Set === "undefined" || !hasSymbols || !getProto ? undefined2 : getProto((/* @__PURE__ */ new Set())[Symbol.iterator]()),
      "%SharedArrayBuffer%": typeof SharedArrayBuffer === "undefined" ? undefined2 : SharedArrayBuffer,
      "%String%": String,
      "%StringIteratorPrototype%": hasSymbols && getProto ? getProto(""[Symbol.iterator]()) : undefined2,
      "%Symbol%": hasSymbols ? Symbol : undefined2,
      "%SyntaxError%": $SyntaxError,
      "%ThrowTypeError%": ThrowTypeError,
      "%TypedArray%": TypedArray,
      "%TypeError%": $TypeError,
      "%Uint8Array%": typeof Uint8Array === "undefined" ? undefined2 : Uint8Array,
      "%Uint8ClampedArray%": typeof Uint8ClampedArray === "undefined" ? undefined2 : Uint8ClampedArray,
      "%Uint16Array%": typeof Uint16Array === "undefined" ? undefined2 : Uint16Array,
      "%Uint32Array%": typeof Uint32Array === "undefined" ? undefined2 : Uint32Array,
      "%URIError%": $URIError,
      "%WeakMap%": typeof WeakMap === "undefined" ? undefined2 : WeakMap,
      "%WeakRef%": typeof WeakRef === "undefined" ? undefined2 : WeakRef,
      "%WeakSet%": typeof WeakSet === "undefined" ? undefined2 : WeakSet
    };
    if (getProto) {
      try {
        null.error;
      } catch (e) {
        errorProto = getProto(getProto(e));
        INTRINSICS["%Error.prototype%"] = errorProto;
      }
    }
    var errorProto;
    var doEval = function doEval2(name) {
      var value;
      if (name === "%AsyncFunction%") {
        value = getEvalledConstructor("async function () {}");
      } else if (name === "%GeneratorFunction%") {
        value = getEvalledConstructor("function* () {}");
      } else if (name === "%AsyncGeneratorFunction%") {
        value = getEvalledConstructor("async function* () {}");
      } else if (name === "%AsyncGenerator%") {
        var fn2 = doEval2("%AsyncGeneratorFunction%");
        if (fn2) {
          value = fn2.prototype;
        }
      } else if (name === "%AsyncIteratorPrototype%") {
        var gen = doEval2("%AsyncGenerator%");
        if (gen && getProto) {
          value = getProto(gen.prototype);
        }
      }
      INTRINSICS[name] = value;
      return value;
    };
    var LEGACY_ALIASES = {
      __proto__: null,
      "%ArrayBufferPrototype%": ["ArrayBuffer", "prototype"],
      "%ArrayPrototype%": ["Array", "prototype"],
      "%ArrayProto_entries%": ["Array", "prototype", "entries"],
      "%ArrayProto_forEach%": ["Array", "prototype", "forEach"],
      "%ArrayProto_keys%": ["Array", "prototype", "keys"],
      "%ArrayProto_values%": ["Array", "prototype", "values"],
      "%AsyncFunctionPrototype%": ["AsyncFunction", "prototype"],
      "%AsyncGenerator%": ["AsyncGeneratorFunction", "prototype"],
      "%AsyncGeneratorPrototype%": ["AsyncGeneratorFunction", "prototype", "prototype"],
      "%BooleanPrototype%": ["Boolean", "prototype"],
      "%DataViewPrototype%": ["DataView", "prototype"],
      "%DatePrototype%": ["Date", "prototype"],
      "%ErrorPrototype%": ["Error", "prototype"],
      "%EvalErrorPrototype%": ["EvalError", "prototype"],
      "%Float32ArrayPrototype%": ["Float32Array", "prototype"],
      "%Float64ArrayPrototype%": ["Float64Array", "prototype"],
      "%FunctionPrototype%": ["Function", "prototype"],
      "%Generator%": ["GeneratorFunction", "prototype"],
      "%GeneratorPrototype%": ["GeneratorFunction", "prototype", "prototype"],
      "%Int8ArrayPrototype%": ["Int8Array", "prototype"],
      "%Int16ArrayPrototype%": ["Int16Array", "prototype"],
      "%Int32ArrayPrototype%": ["Int32Array", "prototype"],
      "%JSONParse%": ["JSON", "parse"],
      "%JSONStringify%": ["JSON", "stringify"],
      "%MapPrototype%": ["Map", "prototype"],
      "%NumberPrototype%": ["Number", "prototype"],
      "%ObjectPrototype%": ["Object", "prototype"],
      "%ObjProto_toString%": ["Object", "prototype", "toString"],
      "%ObjProto_valueOf%": ["Object", "prototype", "valueOf"],
      "%PromisePrototype%": ["Promise", "prototype"],
      "%PromiseProto_then%": ["Promise", "prototype", "then"],
      "%Promise_all%": ["Promise", "all"],
      "%Promise_reject%": ["Promise", "reject"],
      "%Promise_resolve%": ["Promise", "resolve"],
      "%RangeErrorPrototype%": ["RangeError", "prototype"],
      "%ReferenceErrorPrototype%": ["ReferenceError", "prototype"],
      "%RegExpPrototype%": ["RegExp", "prototype"],
      "%SetPrototype%": ["Set", "prototype"],
      "%SharedArrayBufferPrototype%": ["SharedArrayBuffer", "prototype"],
      "%StringPrototype%": ["String", "prototype"],
      "%SymbolPrototype%": ["Symbol", "prototype"],
      "%SyntaxErrorPrototype%": ["SyntaxError", "prototype"],
      "%TypedArrayPrototype%": ["TypedArray", "prototype"],
      "%TypeErrorPrototype%": ["TypeError", "prototype"],
      "%Uint8ArrayPrototype%": ["Uint8Array", "prototype"],
      "%Uint8ClampedArrayPrototype%": ["Uint8ClampedArray", "prototype"],
      "%Uint16ArrayPrototype%": ["Uint16Array", "prototype"],
      "%Uint32ArrayPrototype%": ["Uint32Array", "prototype"],
      "%URIErrorPrototype%": ["URIError", "prototype"],
      "%WeakMapPrototype%": ["WeakMap", "prototype"],
      "%WeakSetPrototype%": ["WeakSet", "prototype"]
    };
    var bind = require_function_bind();
    var hasOwn = require_hasown();
    var $concat = bind.call(Function.call, Array.prototype.concat);
    var $spliceApply = bind.call(Function.apply, Array.prototype.splice);
    var $replace = bind.call(Function.call, String.prototype.replace);
    var $strSlice = bind.call(Function.call, String.prototype.slice);
    var $exec = bind.call(Function.call, RegExp.prototype.exec);
    var rePropName = /[^%.[\]]+|\[(?:(-?\d+(?:\.\d+)?)|(["'])((?:(?!\2)[^\\]|\\.)*?)\2)\]|(?=(?:\.|\[\])(?:\.|\[\]|%$))/g;
    var reEscapeChar = /\\(\\)?/g;
    var stringToPath = function stringToPath2(string) {
      var first = $strSlice(string, 0, 1);
      var last = $strSlice(string, -1);
      if (first === "%" && last !== "%") {
        throw new $SyntaxError("invalid intrinsic syntax, expected closing `%`");
      } else if (last === "%" && first !== "%") {
        throw new $SyntaxError("invalid intrinsic syntax, expected opening `%`");
      }
      var result = [];
      $replace(string, rePropName, function(match, number, quote, subString) {
        result[result.length] = quote ? $replace(subString, reEscapeChar, "$1") : number || match;
      });
      return result;
    };
    var getBaseIntrinsic = function getBaseIntrinsic2(name, allowMissing) {
      var intrinsicName = name;
      var alias;
      if (hasOwn(LEGACY_ALIASES, intrinsicName)) {
        alias = LEGACY_ALIASES[intrinsicName];
        intrinsicName = "%" + alias[0] + "%";
      }
      if (hasOwn(INTRINSICS, intrinsicName)) {
        var value = INTRINSICS[intrinsicName];
        if (value === needsEval) {
          value = doEval(intrinsicName);
        }
        if (typeof value === "undefined" && !allowMissing) {
          throw new $TypeError("intrinsic " + name + " exists, but is not available. Please file an issue!");
        }
        return {
          alias,
          name: intrinsicName,
          value
        };
      }
      throw new $SyntaxError("intrinsic " + name + " does not exist!");
    };
    module.exports = function GetIntrinsic(name, allowMissing) {
      if (typeof name !== "string" || name.length === 0) {
        throw new $TypeError("intrinsic name must be a non-empty string");
      }
      if (arguments.length > 1 && typeof allowMissing !== "boolean") {
        throw new $TypeError('"allowMissing" argument must be a boolean');
      }
      if ($exec(/^%?[^%]*%?$/, name) === null) {
        throw new $SyntaxError("`%` may not be present anywhere but at the beginning and end of the intrinsic name");
      }
      var parts = stringToPath(name);
      var intrinsicBaseName = parts.length > 0 ? parts[0] : "";
      var intrinsic = getBaseIntrinsic("%" + intrinsicBaseName + "%", allowMissing);
      var intrinsicRealName = intrinsic.name;
      var value = intrinsic.value;
      var skipFurtherCaching = false;
      var alias = intrinsic.alias;
      if (alias) {
        intrinsicBaseName = alias[0];
        $spliceApply(parts, $concat([0, 1], alias));
      }
      for (var i2 = 1, isOwn = true; i2 < parts.length; i2 += 1) {
        var part = parts[i2];
        var first = $strSlice(part, 0, 1);
        var last = $strSlice(part, -1);
        if ((first === '"' || first === "'" || first === "`" || (last === '"' || last === "'" || last === "`")) && first !== last) {
          throw new $SyntaxError("property names with quotes must have matching quotes");
        }
        if (part === "constructor" || !isOwn) {
          skipFurtherCaching = true;
        }
        intrinsicBaseName += "." + part;
        intrinsicRealName = "%" + intrinsicBaseName + "%";
        if (hasOwn(INTRINSICS, intrinsicRealName)) {
          value = INTRINSICS[intrinsicRealName];
        } else if (value != null) {
          if (!(part in value)) {
            if (!allowMissing) {
              throw new $TypeError("base intrinsic for " + name + " exists, but the property is not available.");
            }
            return void 0;
          }
          if ($gOPD && i2 + 1 >= parts.length) {
            var desc = $gOPD(value, part);
            isOwn = !!desc;
            if (isOwn && "get" in desc && !("originalValue" in desc.get)) {
              value = desc.get;
            } else {
              value = value[part];
            }
          } else {
            isOwn = hasOwn(value, part);
            value = value[part];
          }
          if (isOwn && !skipFurtherCaching) {
            INTRINSICS[intrinsicRealName] = value;
          }
        }
      }
      return value;
    };
  }
});

// node_modules/es-define-property/index.js
var require_es_define_property = __commonJS({
  "node_modules/es-define-property/index.js"(exports, module) {
    "use strict";
    var GetIntrinsic = require_get_intrinsic();
    var $defineProperty = GetIntrinsic("%Object.defineProperty%", true) || false;
    if ($defineProperty) {
      try {
        $defineProperty({}, "a", { value: 1 });
      } catch (e) {
        $defineProperty = false;
      }
    }
    module.exports = $defineProperty;
  }
});

// node_modules/gopd/index.js
var require_gopd = __commonJS({
  "node_modules/gopd/index.js"(exports, module) {
    "use strict";
    var GetIntrinsic = require_get_intrinsic();
    var $gOPD = GetIntrinsic("%Object.getOwnPropertyDescriptor%", true);
    if ($gOPD) {
      try {
        $gOPD([], "length");
      } catch (e) {
        $gOPD = null;
      }
    }
    module.exports = $gOPD;
  }
});

// node_modules/define-data-property/index.js
var require_define_data_property = __commonJS({
  "node_modules/define-data-property/index.js"(exports, module) {
    "use strict";
    var $defineProperty = require_es_define_property();
    var $SyntaxError = require_syntax();
    var $TypeError = require_type();
    var gopd = require_gopd();
    module.exports = function defineDataProperty(obj, property, value) {
      if (!obj || typeof obj !== "object" && typeof obj !== "function") {
        throw new $TypeError("`obj` must be an object or a function`");
      }
      if (typeof property !== "string" && typeof property !== "symbol") {
        throw new $TypeError("`property` must be a string or a symbol`");
      }
      if (arguments.length > 3 && typeof arguments[3] !== "boolean" && arguments[3] !== null) {
        throw new $TypeError("`nonEnumerable`, if provided, must be a boolean or null");
      }
      if (arguments.length > 4 && typeof arguments[4] !== "boolean" && arguments[4] !== null) {
        throw new $TypeError("`nonWritable`, if provided, must be a boolean or null");
      }
      if (arguments.length > 5 && typeof arguments[5] !== "boolean" && arguments[5] !== null) {
        throw new $TypeError("`nonConfigurable`, if provided, must be a boolean or null");
      }
      if (arguments.length > 6 && typeof arguments[6] !== "boolean") {
        throw new $TypeError("`loose`, if provided, must be a boolean");
      }
      var nonEnumerable = arguments.length > 3 ? arguments[3] : null;
      var nonWritable = arguments.length > 4 ? arguments[4] : null;
      var nonConfigurable = arguments.length > 5 ? arguments[5] : null;
      var loose = arguments.length > 6 ? arguments[6] : false;
      var desc = !!gopd && gopd(obj, property);
      if ($defineProperty) {
        $defineProperty(obj, property, {
          configurable: nonConfigurable === null && desc ? desc.configurable : !nonConfigurable,
          enumerable: nonEnumerable === null && desc ? desc.enumerable : !nonEnumerable,
          value,
          writable: nonWritable === null && desc ? desc.writable : !nonWritable
        });
      } else if (loose || !nonEnumerable && !nonWritable && !nonConfigurable) {
        obj[property] = value;
      } else {
        throw new $SyntaxError("This environment does not support defining a property as non-configurable, non-writable, or non-enumerable.");
      }
    };
  }
});

// node_modules/has-property-descriptors/index.js
var require_has_property_descriptors = __commonJS({
  "node_modules/has-property-descriptors/index.js"(exports, module) {
    "use strict";
    var $defineProperty = require_es_define_property();
    var hasPropertyDescriptors = function hasPropertyDescriptors2() {
      return !!$defineProperty;
    };
    hasPropertyDescriptors.hasArrayLengthDefineBug = function hasArrayLengthDefineBug() {
      if (!$defineProperty) {
        return null;
      }
      try {
        return $defineProperty([], "length", { value: 1 }).length !== 1;
      } catch (e) {
        return true;
      }
    };
    module.exports = hasPropertyDescriptors;
  }
});

// node_modules/set-function-length/index.js
var require_set_function_length = __commonJS({
  "node_modules/set-function-length/index.js"(exports, module) {
    "use strict";
    var GetIntrinsic = require_get_intrinsic();
    var define2 = require_define_data_property();
    var hasDescriptors = require_has_property_descriptors()();
    var gOPD = require_gopd();
    var $TypeError = require_type();
    var $floor = GetIntrinsic("%Math.floor%");
    module.exports = function setFunctionLength(fn2, length) {
      if (typeof fn2 !== "function") {
        throw new $TypeError("`fn` is not a function");
      }
      if (typeof length !== "number" || length < 0 || length > 4294967295 || $floor(length) !== length) {
        throw new $TypeError("`length` must be a positive 32-bit integer");
      }
      var loose = arguments.length > 2 && !!arguments[2];
      var functionLengthIsConfigurable = true;
      var functionLengthIsWritable = true;
      if ("length" in fn2 && gOPD) {
        var desc = gOPD(fn2, "length");
        if (desc && !desc.configurable) {
          functionLengthIsConfigurable = false;
        }
        if (desc && !desc.writable) {
          functionLengthIsWritable = false;
        }
      }
      if (functionLengthIsConfigurable || functionLengthIsWritable || !loose) {
        if (hasDescriptors) {
          define2(
            /** @type {Parameters<define>[0]} */
            fn2,
            "length",
            length,
            true,
            true
          );
        } else {
          define2(
            /** @type {Parameters<define>[0]} */
            fn2,
            "length",
            length
          );
        }
      }
      return fn2;
    };
  }
});

// node_modules/call-bind/index.js
var require_call_bind = __commonJS({
  "node_modules/call-bind/index.js"(exports, module) {
    "use strict";
    var bind = require_function_bind();
    var GetIntrinsic = require_get_intrinsic();
    var setFunctionLength = require_set_function_length();
    var $TypeError = require_type();
    var $apply = GetIntrinsic("%Function.prototype.apply%");
    var $call = GetIntrinsic("%Function.prototype.call%");
    var $reflectApply = GetIntrinsic("%Reflect.apply%", true) || bind.call($call, $apply);
    var $defineProperty = require_es_define_property();
    var $max = GetIntrinsic("%Math.max%");
    module.exports = function callBind(originalFunction) {
      if (typeof originalFunction !== "function") {
        throw new $TypeError("a function is required");
      }
      var func = $reflectApply(bind, $call, arguments);
      return setFunctionLength(
        func,
        1 + $max(0, originalFunction.length - (arguments.length - 1)),
        true
      );
    };
    var applyBind = function applyBind2() {
      return $reflectApply(bind, $apply, arguments);
    };
    if ($defineProperty) {
      $defineProperty(module.exports, "apply", { value: applyBind });
    } else {
      module.exports.apply = applyBind;
    }
  }
});

// node_modules/call-bind/callBound.js
var require_callBound = __commonJS({
  "node_modules/call-bind/callBound.js"(exports, module) {
    "use strict";
    var GetIntrinsic = require_get_intrinsic();
    var callBind = require_call_bind();
    var $indexOf = callBind(GetIntrinsic("String.prototype.indexOf"));
    module.exports = function callBoundIntrinsic(name, allowMissing) {
      var intrinsic = GetIntrinsic(name, !!allowMissing);
      if (typeof intrinsic === "function" && $indexOf(name, ".prototype.") > -1) {
        return callBind(intrinsic);
      }
      return intrinsic;
    };
  }
});

// (disabled):node_modules/object-inspect/util.inspect
var require_util = __commonJS({
  "(disabled):node_modules/object-inspect/util.inspect"() {
  }
});

// node_modules/object-inspect/index.js
var require_object_inspect = __commonJS({
  "node_modules/object-inspect/index.js"(exports, module) {
    var hasMap = typeof Map === "function" && Map.prototype;
    var mapSizeDescriptor = Object.getOwnPropertyDescriptor && hasMap ? Object.getOwnPropertyDescriptor(Map.prototype, "size") : null;
    var mapSize = hasMap && mapSizeDescriptor && typeof mapSizeDescriptor.get === "function" ? mapSizeDescriptor.get : null;
    var mapForEach = hasMap && Map.prototype.forEach;
    var hasSet = typeof Set === "function" && Set.prototype;
    var setSizeDescriptor = Object.getOwnPropertyDescriptor && hasSet ? Object.getOwnPropertyDescriptor(Set.prototype, "size") : null;
    var setSize = hasSet && setSizeDescriptor && typeof setSizeDescriptor.get === "function" ? setSizeDescriptor.get : null;
    var setForEach = hasSet && Set.prototype.forEach;
    var hasWeakMap = typeof WeakMap === "function" && WeakMap.prototype;
    var weakMapHas = hasWeakMap ? WeakMap.prototype.has : null;
    var hasWeakSet = typeof WeakSet === "function" && WeakSet.prototype;
    var weakSetHas = hasWeakSet ? WeakSet.prototype.has : null;
    var hasWeakRef = typeof WeakRef === "function" && WeakRef.prototype;
    var weakRefDeref = hasWeakRef ? WeakRef.prototype.deref : null;
    var booleanValueOf = Boolean.prototype.valueOf;
    var objectToString = Object.prototype.toString;
    var functionToString = Function.prototype.toString;
    var $match = String.prototype.match;
    var $slice = String.prototype.slice;
    var $replace = String.prototype.replace;
    var $toUpperCase = String.prototype.toUpperCase;
    var $toLowerCase = String.prototype.toLowerCase;
    var $test = RegExp.prototype.test;
    var $concat = Array.prototype.concat;
    var $join = Array.prototype.join;
    var $arrSlice = Array.prototype.slice;
    var $floor = Math.floor;
    var bigIntValueOf = typeof BigInt === "function" ? BigInt.prototype.valueOf : null;
    var gOPS = Object.getOwnPropertySymbols;
    var symToString = typeof Symbol === "function" && typeof Symbol.iterator === "symbol" ? Symbol.prototype.toString : null;
    var hasShammedSymbols = typeof Symbol === "function" && typeof Symbol.iterator === "object";
    var toStringTag = typeof Symbol === "function" && Symbol.toStringTag && (typeof Symbol.toStringTag === hasShammedSymbols ? "object" : "symbol") ? Symbol.toStringTag : null;
    var isEnumerable = Object.prototype.propertyIsEnumerable;
    var gPO = (typeof Reflect === "function" ? Reflect.getPrototypeOf : Object.getPrototypeOf) || ([].__proto__ === Array.prototype ? function(O3) {
      return O3.__proto__;
    } : null);
    function addNumericSeparator(num, str) {
      if (num === Infinity || num === -Infinity || num !== num || num && num > -1e3 && num < 1e3 || $test.call(/e/, str)) {
        return str;
      }
      var sepRegex = /[0-9](?=(?:[0-9]{3})+(?![0-9]))/g;
      if (typeof num === "number") {
        var int = num < 0 ? -$floor(-num) : $floor(num);
        if (int !== num) {
          var intStr = String(int);
          var dec = $slice.call(str, intStr.length + 1);
          return $replace.call(intStr, sepRegex, "$&_") + "." + $replace.call($replace.call(dec, /([0-9]{3})/g, "$&_"), /_$/, "");
        }
      }
      return $replace.call(str, sepRegex, "$&_");
    }
    var utilInspect = require_util();
    var inspectCustom = utilInspect.custom;
    var inspectSymbol = isSymbol(inspectCustom) ? inspectCustom : null;
    module.exports = function inspect_(obj, options, depth, seen) {
      var opts = options || {};
      if (has(opts, "quoteStyle") && (opts.quoteStyle !== "single" && opts.quoteStyle !== "double")) {
        throw new TypeError('option "quoteStyle" must be "single" or "double"');
      }
      if (has(opts, "maxStringLength") && (typeof opts.maxStringLength === "number" ? opts.maxStringLength < 0 && opts.maxStringLength !== Infinity : opts.maxStringLength !== null)) {
        throw new TypeError('option "maxStringLength", if provided, must be a positive integer, Infinity, or `null`');
      }
      var customInspect = has(opts, "customInspect") ? opts.customInspect : true;
      if (typeof customInspect !== "boolean" && customInspect !== "symbol") {
        throw new TypeError("option \"customInspect\", if provided, must be `true`, `false`, or `'symbol'`");
      }
      if (has(opts, "indent") && opts.indent !== null && opts.indent !== "	" && !(parseInt(opts.indent, 10) === opts.indent && opts.indent > 0)) {
        throw new TypeError('option "indent" must be "\\t", an integer > 0, or `null`');
      }
      if (has(opts, "numericSeparator") && typeof opts.numericSeparator !== "boolean") {
        throw new TypeError('option "numericSeparator", if provided, must be `true` or `false`');
      }
      var numericSeparator = opts.numericSeparator;
      if (typeof obj === "undefined") {
        return "undefined";
      }
      if (obj === null) {
        return "null";
      }
      if (typeof obj === "boolean") {
        return obj ? "true" : "false";
      }
      if (typeof obj === "string") {
        return inspectString(obj, opts);
      }
      if (typeof obj === "number") {
        if (obj === 0) {
          return Infinity / obj > 0 ? "0" : "-0";
        }
        var str = String(obj);
        return numericSeparator ? addNumericSeparator(obj, str) : str;
      }
      if (typeof obj === "bigint") {
        var bigIntStr = String(obj) + "n";
        return numericSeparator ? addNumericSeparator(obj, bigIntStr) : bigIntStr;
      }
      var maxDepth = typeof opts.depth === "undefined" ? 5 : opts.depth;
      if (typeof depth === "undefined") {
        depth = 0;
      }
      if (depth >= maxDepth && maxDepth > 0 && typeof obj === "object") {
        return isArray(obj) ? "[Array]" : "[Object]";
      }
      var indent = getIndent(opts, depth);
      if (typeof seen === "undefined") {
        seen = [];
      } else if (indexOf(seen, obj) >= 0) {
        return "[Circular]";
      }
      function inspect(value, from, noIndent) {
        if (from) {
          seen = $arrSlice.call(seen);
          seen.push(from);
        }
        if (noIndent) {
          var newOpts = {
            depth: opts.depth
          };
          if (has(opts, "quoteStyle")) {
            newOpts.quoteStyle = opts.quoteStyle;
          }
          return inspect_(value, newOpts, depth + 1, seen);
        }
        return inspect_(value, opts, depth + 1, seen);
      }
      if (typeof obj === "function" && !isRegExp(obj)) {
        var name = nameOf(obj);
        var keys = arrObjKeys(obj, inspect);
        return "[Function" + (name ? ": " + name : " (anonymous)") + "]" + (keys.length > 0 ? " { " + $join.call(keys, ", ") + " }" : "");
      }
      if (isSymbol(obj)) {
        var symString = hasShammedSymbols ? $replace.call(String(obj), /^(Symbol\(.*\))_[^)]*$/, "$1") : symToString.call(obj);
        return typeof obj === "object" && !hasShammedSymbols ? markBoxed(symString) : symString;
      }
      if (isElement(obj)) {
        var s = "<" + $toLowerCase.call(String(obj.nodeName));
        var attrs = obj.attributes || [];
        for (var i2 = 0; i2 < attrs.length; i2++) {
          s += " " + attrs[i2].name + "=" + wrapQuotes(quote(attrs[i2].value), "double", opts);
        }
        s += ">";
        if (obj.childNodes && obj.childNodes.length) {
          s += "...";
        }
        s += "</" + $toLowerCase.call(String(obj.nodeName)) + ">";
        return s;
      }
      if (isArray(obj)) {
        if (obj.length === 0) {
          return "[]";
        }
        var xs2 = arrObjKeys(obj, inspect);
        if (indent && !singleLineValues(xs2)) {
          return "[" + indentedJoin(xs2, indent) + "]";
        }
        return "[ " + $join.call(xs2, ", ") + " ]";
      }
      if (isError2(obj)) {
        var parts = arrObjKeys(obj, inspect);
        if (!("cause" in Error.prototype) && "cause" in obj && !isEnumerable.call(obj, "cause")) {
          return "{ [" + String(obj) + "] " + $join.call($concat.call("[cause]: " + inspect(obj.cause), parts), ", ") + " }";
        }
        if (parts.length === 0) {
          return "[" + String(obj) + "]";
        }
        return "{ [" + String(obj) + "] " + $join.call(parts, ", ") + " }";
      }
      if (typeof obj === "object" && customInspect) {
        if (inspectSymbol && typeof obj[inspectSymbol] === "function" && utilInspect) {
          return utilInspect(obj, { depth: maxDepth - depth });
        } else if (customInspect !== "symbol" && typeof obj.inspect === "function") {
          return obj.inspect();
        }
      }
      if (isMap(obj)) {
        var mapParts = [];
        if (mapForEach) {
          mapForEach.call(obj, function(value, key) {
            mapParts.push(inspect(key, obj, true) + " => " + inspect(value, obj));
          });
        }
        return collectionOf("Map", mapSize.call(obj), mapParts, indent);
      }
      if (isSet(obj)) {
        var setParts = [];
        if (setForEach) {
          setForEach.call(obj, function(value) {
            setParts.push(inspect(value, obj));
          });
        }
        return collectionOf("Set", setSize.call(obj), setParts, indent);
      }
      if (isWeakMap(obj)) {
        return weakCollectionOf("WeakMap");
      }
      if (isWeakSet(obj)) {
        return weakCollectionOf("WeakSet");
      }
      if (isWeakRef(obj)) {
        return weakCollectionOf("WeakRef");
      }
      if (isNumber(obj)) {
        return markBoxed(inspect(Number(obj)));
      }
      if (isBigInt(obj)) {
        return markBoxed(inspect(bigIntValueOf.call(obj)));
      }
      if (isBoolean(obj)) {
        return markBoxed(booleanValueOf.call(obj));
      }
      if (isString(obj)) {
        return markBoxed(inspect(String(obj)));
      }
      if (typeof window !== "undefined" && obj === window) {
        return "{ [object Window] }";
      }
      if (obj === global) {
        return "{ [object globalThis] }";
      }
      if (!isDate(obj) && !isRegExp(obj)) {
        var ys = arrObjKeys(obj, inspect);
        var isPlainObject2 = gPO ? gPO(obj) === Object.prototype : obj instanceof Object || obj.constructor === Object;
        var protoTag = obj instanceof Object ? "" : "null prototype";
        var stringTag = !isPlainObject2 && toStringTag && Object(obj) === obj && toStringTag in obj ? $slice.call(toStr(obj), 8, -1) : protoTag ? "Object" : "";
        var constructorTag = isPlainObject2 || typeof obj.constructor !== "function" ? "" : obj.constructor.name ? obj.constructor.name + " " : "";
        var tag = constructorTag + (stringTag || protoTag ? "[" + $join.call($concat.call([], stringTag || [], protoTag || []), ": ") + "] " : "");
        if (ys.length === 0) {
          return tag + "{}";
        }
        if (indent) {
          return tag + "{" + indentedJoin(ys, indent) + "}";
        }
        return tag + "{ " + $join.call(ys, ", ") + " }";
      }
      return String(obj);
    };
    function wrapQuotes(s, defaultStyle, opts) {
      var quoteChar = (opts.quoteStyle || defaultStyle) === "double" ? '"' : "'";
      return quoteChar + s + quoteChar;
    }
    function quote(s) {
      return $replace.call(String(s), /"/g, "&quot;");
    }
    function isArray(obj) {
      return toStr(obj) === "[object Array]" && (!toStringTag || !(typeof obj === "object" && toStringTag in obj));
    }
    function isDate(obj) {
      return toStr(obj) === "[object Date]" && (!toStringTag || !(typeof obj === "object" && toStringTag in obj));
    }
    function isRegExp(obj) {
      return toStr(obj) === "[object RegExp]" && (!toStringTag || !(typeof obj === "object" && toStringTag in obj));
    }
    function isError2(obj) {
      return toStr(obj) === "[object Error]" && (!toStringTag || !(typeof obj === "object" && toStringTag in obj));
    }
    function isString(obj) {
      return toStr(obj) === "[object String]" && (!toStringTag || !(typeof obj === "object" && toStringTag in obj));
    }
    function isNumber(obj) {
      return toStr(obj) === "[object Number]" && (!toStringTag || !(typeof obj === "object" && toStringTag in obj));
    }
    function isBoolean(obj) {
      return toStr(obj) === "[object Boolean]" && (!toStringTag || !(typeof obj === "object" && toStringTag in obj));
    }
    function isSymbol(obj) {
      if (hasShammedSymbols) {
        return obj && typeof obj === "object" && obj instanceof Symbol;
      }
      if (typeof obj === "symbol") {
        return true;
      }
      if (!obj || typeof obj !== "object" || !symToString) {
        return false;
      }
      try {
        symToString.call(obj);
        return true;
      } catch (e) {
      }
      return false;
    }
    function isBigInt(obj) {
      if (!obj || typeof obj !== "object" || !bigIntValueOf) {
        return false;
      }
      try {
        bigIntValueOf.call(obj);
        return true;
      } catch (e) {
      }
      return false;
    }
    var hasOwn = Object.prototype.hasOwnProperty || function(key) {
      return key in this;
    };
    function has(obj, key) {
      return hasOwn.call(obj, key);
    }
    function toStr(obj) {
      return objectToString.call(obj);
    }
    function nameOf(f2) {
      if (f2.name) {
        return f2.name;
      }
      var m2 = $match.call(functionToString.call(f2), /^function\s*([\w$]+)/);
      if (m2) {
        return m2[1];
      }
      return null;
    }
    function indexOf(xs2, x2) {
      if (xs2.indexOf) {
        return xs2.indexOf(x2);
      }
      for (var i2 = 0, l2 = xs2.length; i2 < l2; i2++) {
        if (xs2[i2] === x2) {
          return i2;
        }
      }
      return -1;
    }
    function isMap(x2) {
      if (!mapSize || !x2 || typeof x2 !== "object") {
        return false;
      }
      try {
        mapSize.call(x2);
        try {
          setSize.call(x2);
        } catch (s) {
          return true;
        }
        return x2 instanceof Map;
      } catch (e) {
      }
      return false;
    }
    function isWeakMap(x2) {
      if (!weakMapHas || !x2 || typeof x2 !== "object") {
        return false;
      }
      try {
        weakMapHas.call(x2, weakMapHas);
        try {
          weakSetHas.call(x2, weakSetHas);
        } catch (s) {
          return true;
        }
        return x2 instanceof WeakMap;
      } catch (e) {
      }
      return false;
    }
    function isWeakRef(x2) {
      if (!weakRefDeref || !x2 || typeof x2 !== "object") {
        return false;
      }
      try {
        weakRefDeref.call(x2);
        return true;
      } catch (e) {
      }
      return false;
    }
    function isSet(x2) {
      if (!setSize || !x2 || typeof x2 !== "object") {
        return false;
      }
      try {
        setSize.call(x2);
        try {
          mapSize.call(x2);
        } catch (m2) {
          return true;
        }
        return x2 instanceof Set;
      } catch (e) {
      }
      return false;
    }
    function isWeakSet(x2) {
      if (!weakSetHas || !x2 || typeof x2 !== "object") {
        return false;
      }
      try {
        weakSetHas.call(x2, weakSetHas);
        try {
          weakMapHas.call(x2, weakMapHas);
        } catch (s) {
          return true;
        }
        return x2 instanceof WeakSet;
      } catch (e) {
      }
      return false;
    }
    function isElement(x2) {
      if (!x2 || typeof x2 !== "object") {
        return false;
      }
      if (typeof HTMLElement !== "undefined" && x2 instanceof HTMLElement) {
        return true;
      }
      return typeof x2.nodeName === "string" && typeof x2.getAttribute === "function";
    }
    function inspectString(str, opts) {
      if (str.length > opts.maxStringLength) {
        var remaining = str.length - opts.maxStringLength;
        var trailer = "... " + remaining + " more character" + (remaining > 1 ? "s" : "");
        return inspectString($slice.call(str, 0, opts.maxStringLength), opts) + trailer;
      }
      var s = $replace.call($replace.call(str, /(['\\])/g, "\\$1"), /[\x00-\x1f]/g, lowbyte);
      return wrapQuotes(s, "single", opts);
    }
    function lowbyte(c) {
      var n = c.charCodeAt(0);
      var x2 = {
        8: "b",
        9: "t",
        10: "n",
        12: "f",
        13: "r"
      }[n];
      if (x2) {
        return "\\" + x2;
      }
      return "\\x" + (n < 16 ? "0" : "") + $toUpperCase.call(n.toString(16));
    }
    function markBoxed(str) {
      return "Object(" + str + ")";
    }
    function weakCollectionOf(type) {
      return type + " { ? }";
    }
    function collectionOf(type, size, entries, indent) {
      var joinedEntries = indent ? indentedJoin(entries, indent) : $join.call(entries, ", ");
      return type + " (" + size + ") {" + joinedEntries + "}";
    }
    function singleLineValues(xs2) {
      for (var i2 = 0; i2 < xs2.length; i2++) {
        if (indexOf(xs2[i2], "\n") >= 0) {
          return false;
        }
      }
      return true;
    }
    function getIndent(opts, depth) {
      var baseIndent;
      if (opts.indent === "	") {
        baseIndent = "	";
      } else if (typeof opts.indent === "number" && opts.indent > 0) {
        baseIndent = $join.call(Array(opts.indent + 1), " ");
      } else {
        return null;
      }
      return {
        base: baseIndent,
        prev: $join.call(Array(depth + 1), baseIndent)
      };
    }
    function indentedJoin(xs2, indent) {
      if (xs2.length === 0) {
        return "";
      }
      var lineJoiner = "\n" + indent.prev + indent.base;
      return lineJoiner + $join.call(xs2, "," + lineJoiner) + "\n" + indent.prev;
    }
    function arrObjKeys(obj, inspect) {
      var isArr = isArray(obj);
      var xs2 = [];
      if (isArr) {
        xs2.length = obj.length;
        for (var i2 = 0; i2 < obj.length; i2++) {
          xs2[i2] = has(obj, i2) ? inspect(obj[i2], obj) : "";
        }
      }
      var syms = typeof gOPS === "function" ? gOPS(obj) : [];
      var symMap;
      if (hasShammedSymbols) {
        symMap = {};
        for (var k = 0; k < syms.length; k++) {
          symMap["$" + syms[k]] = syms[k];
        }
      }
      for (var key in obj) {
        if (!has(obj, key)) {
          continue;
        }
        if (isArr && String(Number(key)) === key && key < obj.length) {
          continue;
        }
        if (hasShammedSymbols && symMap["$" + key] instanceof Symbol) {
          continue;
        } else if ($test.call(/[^\w$]/, key)) {
          xs2.push(inspect(key, obj) + ": " + inspect(obj[key], obj));
        } else {
          xs2.push(key + ": " + inspect(obj[key], obj));
        }
      }
      if (typeof gOPS === "function") {
        for (var j = 0; j < syms.length; j++) {
          if (isEnumerable.call(obj, syms[j])) {
            xs2.push("[" + inspect(syms[j]) + "]: " + inspect(obj[syms[j]], obj));
          }
        }
      }
      return xs2;
    }
  }
});

// node_modules/side-channel/index.js
var require_side_channel = __commonJS({
  "node_modules/side-channel/index.js"(exports, module) {
    "use strict";
    var GetIntrinsic = require_get_intrinsic();
    var callBound = require_callBound();
    var inspect = require_object_inspect();
    var $TypeError = require_type();
    var $WeakMap = GetIntrinsic("%WeakMap%", true);
    var $Map = GetIntrinsic("%Map%", true);
    var $weakMapGet = callBound("WeakMap.prototype.get", true);
    var $weakMapSet = callBound("WeakMap.prototype.set", true);
    var $weakMapHas = callBound("WeakMap.prototype.has", true);
    var $mapGet = callBound("Map.prototype.get", true);
    var $mapSet = callBound("Map.prototype.set", true);
    var $mapHas = callBound("Map.prototype.has", true);
    var listGetNode = function(list, key) {
      var prev = list;
      var curr;
      for (; (curr = prev.next) !== null; prev = curr) {
        if (curr.key === key) {
          prev.next = curr.next;
          curr.next = /** @type {NonNullable<typeof list.next>} */
          list.next;
          list.next = curr;
          return curr;
        }
      }
    };
    var listGet = function(objects, key) {
      var node = listGetNode(objects, key);
      return node && node.value;
    };
    var listSet = function(objects, key, value) {
      var node = listGetNode(objects, key);
      if (node) {
        node.value = value;
      } else {
        objects.next = /** @type {import('.').ListNode<typeof value>} */
        {
          // eslint-disable-line no-param-reassign, no-extra-parens
          key,
          next: objects.next,
          value
        };
      }
    };
    var listHas = function(objects, key) {
      return !!listGetNode(objects, key);
    };
    module.exports = function getSideChannel() {
      var $wm;
      var $m;
      var $o2;
      var channel = {
        assert: function(key) {
          if (!channel.has(key)) {
            throw new $TypeError("Side channel does not contain " + inspect(key));
          }
        },
        get: function(key) {
          if ($WeakMap && key && (typeof key === "object" || typeof key === "function")) {
            if ($wm) {
              return $weakMapGet($wm, key);
            }
          } else if ($Map) {
            if ($m) {
              return $mapGet($m, key);
            }
          } else {
            if ($o2) {
              return listGet($o2, key);
            }
          }
        },
        has: function(key) {
          if ($WeakMap && key && (typeof key === "object" || typeof key === "function")) {
            if ($wm) {
              return $weakMapHas($wm, key);
            }
          } else if ($Map) {
            if ($m) {
              return $mapHas($m, key);
            }
          } else {
            if ($o2) {
              return listHas($o2, key);
            }
          }
          return false;
        },
        set: function(key, value) {
          if ($WeakMap && key && (typeof key === "object" || typeof key === "function")) {
            if (!$wm) {
              $wm = new $WeakMap();
            }
            $weakMapSet($wm, key, value);
          } else if ($Map) {
            if (!$m) {
              $m = new $Map();
            }
            $mapSet($m, key, value);
          } else {
            if (!$o2) {
              $o2 = { key: {}, next: null };
            }
            listSet($o2, key, value);
          }
        }
      };
      return channel;
    };
  }
});

// node_modules/qs/lib/formats.js
var require_formats = __commonJS({
  "node_modules/qs/lib/formats.js"(exports, module) {
    "use strict";
    var replace = String.prototype.replace;
    var percentTwenties = /%20/g;
    var Format = {
      RFC1738: "RFC1738",
      RFC3986: "RFC3986"
    };
    module.exports = {
      "default": Format.RFC3986,
      formatters: {
        RFC1738: function(value) {
          return replace.call(value, percentTwenties, "+");
        },
        RFC3986: function(value) {
          return String(value);
        }
      },
      RFC1738: Format.RFC1738,
      RFC3986: Format.RFC3986
    };
  }
});

// node_modules/qs/lib/utils.js
var require_utils = __commonJS({
  "node_modules/qs/lib/utils.js"(exports, module) {
    "use strict";
    var formats = require_formats();
    var has = Object.prototype.hasOwnProperty;
    var isArray = Array.isArray;
    var hexTable = function() {
      var array = [];
      for (var i2 = 0; i2 < 256; ++i2) {
        array.push("%" + ((i2 < 16 ? "0" : "") + i2.toString(16)).toUpperCase());
      }
      return array;
    }();
    var compactQueue = function compactQueue2(queue) {
      while (queue.length > 1) {
        var item = queue.pop();
        var obj = item.obj[item.prop];
        if (isArray(obj)) {
          var compacted = [];
          for (var j = 0; j < obj.length; ++j) {
            if (typeof obj[j] !== "undefined") {
              compacted.push(obj[j]);
            }
          }
          item.obj[item.prop] = compacted;
        }
      }
    };
    var arrayToObject = function arrayToObject2(source, options) {
      var obj = options && options.plainObjects ? /* @__PURE__ */ Object.create(null) : {};
      for (var i2 = 0; i2 < source.length; ++i2) {
        if (typeof source[i2] !== "undefined") {
          obj[i2] = source[i2];
        }
      }
      return obj;
    };
    var merge = function merge2(target, source, options) {
      if (!source) {
        return target;
      }
      if (typeof source !== "object") {
        if (isArray(target)) {
          target.push(source);
        } else if (target && typeof target === "object") {
          if (options && (options.plainObjects || options.allowPrototypes) || !has.call(Object.prototype, source)) {
            target[source] = true;
          }
        } else {
          return [target, source];
        }
        return target;
      }
      if (!target || typeof target !== "object") {
        return [target].concat(source);
      }
      var mergeTarget = target;
      if (isArray(target) && !isArray(source)) {
        mergeTarget = arrayToObject(target, options);
      }
      if (isArray(target) && isArray(source)) {
        source.forEach(function(item, i2) {
          if (has.call(target, i2)) {
            var targetItem = target[i2];
            if (targetItem && typeof targetItem === "object" && item && typeof item === "object") {
              target[i2] = merge2(targetItem, item, options);
            } else {
              target.push(item);
            }
          } else {
            target[i2] = item;
          }
        });
        return target;
      }
      return Object.keys(source).reduce(function(acc, key) {
        var value = source[key];
        if (has.call(acc, key)) {
          acc[key] = merge2(acc[key], value, options);
        } else {
          acc[key] = value;
        }
        return acc;
      }, mergeTarget);
    };
    var assign = function assignSingleSource(target, source) {
      return Object.keys(source).reduce(function(acc, key) {
        acc[key] = source[key];
        return acc;
      }, target);
    };
    var decode = function(str, decoder, charset) {
      var strWithoutPlus = str.replace(/\+/g, " ");
      if (charset === "iso-8859-1") {
        return strWithoutPlus.replace(/%[0-9a-f]{2}/gi, unescape);
      }
      try {
        return decodeURIComponent(strWithoutPlus);
      } catch (e) {
        return strWithoutPlus;
      }
    };
    var encode = function encode2(str, defaultEncoder, charset, kind, format) {
      if (str.length === 0) {
        return str;
      }
      var string = str;
      if (typeof str === "symbol") {
        string = Symbol.prototype.toString.call(str);
      } else if (typeof str !== "string") {
        string = String(str);
      }
      if (charset === "iso-8859-1") {
        return escape(string).replace(/%u[0-9a-f]{4}/gi, function($0) {
          return "%26%23" + parseInt($0.slice(2), 16) + "%3B";
        });
      }
      var out = "";
      for (var i2 = 0; i2 < string.length; ++i2) {
        var c = string.charCodeAt(i2);
        if (c === 45 || c === 46 || c === 95 || c === 126 || c >= 48 && c <= 57 || c >= 65 && c <= 90 || c >= 97 && c <= 122 || format === formats.RFC1738 && (c === 40 || c === 41)) {
          out += string.charAt(i2);
          continue;
        }
        if (c < 128) {
          out = out + hexTable[c];
          continue;
        }
        if (c < 2048) {
          out = out + (hexTable[192 | c >> 6] + hexTable[128 | c & 63]);
          continue;
        }
        if (c < 55296 || c >= 57344) {
          out = out + (hexTable[224 | c >> 12] + hexTable[128 | c >> 6 & 63] + hexTable[128 | c & 63]);
          continue;
        }
        i2 += 1;
        c = 65536 + ((c & 1023) << 10 | string.charCodeAt(i2) & 1023);
        out += hexTable[240 | c >> 18] + hexTable[128 | c >> 12 & 63] + hexTable[128 | c >> 6 & 63] + hexTable[128 | c & 63];
      }
      return out;
    };
    var compact = function compact2(value) {
      var queue = [{ obj: { o: value }, prop: "o" }];
      var refs = [];
      for (var i2 = 0; i2 < queue.length; ++i2) {
        var item = queue[i2];
        var obj = item.obj[item.prop];
        var keys = Object.keys(obj);
        for (var j = 0; j < keys.length; ++j) {
          var key = keys[j];
          var val = obj[key];
          if (typeof val === "object" && val !== null && refs.indexOf(val) === -1) {
            queue.push({ obj, prop: key });
            refs.push(val);
          }
        }
      }
      compactQueue(queue);
      return value;
    };
    var isRegExp = function isRegExp2(obj) {
      return Object.prototype.toString.call(obj) === "[object RegExp]";
    };
    var isBuffer = function isBuffer2(obj) {
      if (!obj || typeof obj !== "object") {
        return false;
      }
      return !!(obj.constructor && obj.constructor.isBuffer && obj.constructor.isBuffer(obj));
    };
    var combine = function combine2(a, b) {
      return [].concat(a, b);
    };
    var maybeMap = function maybeMap2(val, fn2) {
      if (isArray(val)) {
        var mapped = [];
        for (var i2 = 0; i2 < val.length; i2 += 1) {
          mapped.push(fn2(val[i2]));
        }
        return mapped;
      }
      return fn2(val);
    };
    module.exports = {
      arrayToObject,
      assign,
      combine,
      compact,
      decode,
      encode,
      isBuffer,
      isRegExp,
      maybeMap,
      merge
    };
  }
});

// node_modules/qs/lib/stringify.js
var require_stringify = __commonJS({
  "node_modules/qs/lib/stringify.js"(exports, module) {
    "use strict";
    var getSideChannel = require_side_channel();
    var utils = require_utils();
    var formats = require_formats();
    var has = Object.prototype.hasOwnProperty;
    var arrayPrefixGenerators = {
      brackets: function brackets(prefix) {
        return prefix + "[]";
      },
      comma: "comma",
      indices: function indices(prefix, key) {
        return prefix + "[" + key + "]";
      },
      repeat: function repeat(prefix) {
        return prefix;
      }
    };
    var isArray = Array.isArray;
    var push = Array.prototype.push;
    var pushToArray = function(arr, valueOrArray) {
      push.apply(arr, isArray(valueOrArray) ? valueOrArray : [valueOrArray]);
    };
    var toISO = Date.prototype.toISOString;
    var defaultFormat = formats["default"];
    var defaults = {
      addQueryPrefix: false,
      allowDots: false,
      charset: "utf-8",
      charsetSentinel: false,
      delimiter: "&",
      encode: true,
      encoder: utils.encode,
      encodeValuesOnly: false,
      format: defaultFormat,
      formatter: formats.formatters[defaultFormat],
      // deprecated
      indices: false,
      serializeDate: function serializeDate(date) {
        return toISO.call(date);
      },
      skipNulls: false,
      strictNullHandling: false
    };
    var isNonNullishPrimitive = function isNonNullishPrimitive2(v2) {
      return typeof v2 === "string" || typeof v2 === "number" || typeof v2 === "boolean" || typeof v2 === "symbol" || typeof v2 === "bigint";
    };
    var sentinel = {};
    var stringify = function stringify2(object, prefix, generateArrayPrefix, commaRoundTrip, strictNullHandling, skipNulls, encoder, filter, sort, allowDots, serializeDate, format, formatter, encodeValuesOnly, charset, sideChannel) {
      var obj = object;
      var tmpSc = sideChannel;
      var step = 0;
      var findFlag = false;
      while ((tmpSc = tmpSc.get(sentinel)) !== void 0 && !findFlag) {
        var pos = tmpSc.get(object);
        step += 1;
        if (typeof pos !== "undefined") {
          if (pos === step) {
            throw new RangeError("Cyclic object value");
          } else {
            findFlag = true;
          }
        }
        if (typeof tmpSc.get(sentinel) === "undefined") {
          step = 0;
        }
      }
      if (typeof filter === "function") {
        obj = filter(prefix, obj);
      } else if (obj instanceof Date) {
        obj = serializeDate(obj);
      } else if (generateArrayPrefix === "comma" && isArray(obj)) {
        obj = utils.maybeMap(obj, function(value2) {
          if (value2 instanceof Date) {
            return serializeDate(value2);
          }
          return value2;
        });
      }
      if (obj === null) {
        if (strictNullHandling) {
          return encoder && !encodeValuesOnly ? encoder(prefix, defaults.encoder, charset, "key", format) : prefix;
        }
        obj = "";
      }
      if (isNonNullishPrimitive(obj) || utils.isBuffer(obj)) {
        if (encoder) {
          var keyValue = encodeValuesOnly ? prefix : encoder(prefix, defaults.encoder, charset, "key", format);
          return [formatter(keyValue) + "=" + formatter(encoder(obj, defaults.encoder, charset, "value", format))];
        }
        return [formatter(prefix) + "=" + formatter(String(obj))];
      }
      var values = [];
      if (typeof obj === "undefined") {
        return values;
      }
      var objKeys;
      if (generateArrayPrefix === "comma" && isArray(obj)) {
        if (encodeValuesOnly && encoder) {
          obj = utils.maybeMap(obj, encoder);
        }
        objKeys = [{ value: obj.length > 0 ? obj.join(",") || null : void 0 }];
      } else if (isArray(filter)) {
        objKeys = filter;
      } else {
        var keys = Object.keys(obj);
        objKeys = sort ? keys.sort(sort) : keys;
      }
      var adjustedPrefix = commaRoundTrip && isArray(obj) && obj.length === 1 ? prefix + "[]" : prefix;
      for (var j = 0; j < objKeys.length; ++j) {
        var key = objKeys[j];
        var value = typeof key === "object" && typeof key.value !== "undefined" ? key.value : obj[key];
        if (skipNulls && value === null) {
          continue;
        }
        var keyPrefix = isArray(obj) ? typeof generateArrayPrefix === "function" ? generateArrayPrefix(adjustedPrefix, key) : adjustedPrefix : adjustedPrefix + (allowDots ? "." + key : "[" + key + "]");
        sideChannel.set(object, step);
        var valueSideChannel = getSideChannel();
        valueSideChannel.set(sentinel, sideChannel);
        pushToArray(values, stringify2(
          value,
          keyPrefix,
          generateArrayPrefix,
          commaRoundTrip,
          strictNullHandling,
          skipNulls,
          generateArrayPrefix === "comma" && encodeValuesOnly && isArray(obj) ? null : encoder,
          filter,
          sort,
          allowDots,
          serializeDate,
          format,
          formatter,
          encodeValuesOnly,
          charset,
          valueSideChannel
        ));
      }
      return values;
    };
    var normalizeStringifyOptions = function normalizeStringifyOptions2(opts) {
      if (!opts) {
        return defaults;
      }
      if (opts.encoder !== null && typeof opts.encoder !== "undefined" && typeof opts.encoder !== "function") {
        throw new TypeError("Encoder has to be a function.");
      }
      var charset = opts.charset || defaults.charset;
      if (typeof opts.charset !== "undefined" && opts.charset !== "utf-8" && opts.charset !== "iso-8859-1") {
        throw new TypeError("The charset option must be either utf-8, iso-8859-1, or undefined");
      }
      var format = formats["default"];
      if (typeof opts.format !== "undefined") {
        if (!has.call(formats.formatters, opts.format)) {
          throw new TypeError("Unknown format option provided.");
        }
        format = opts.format;
      }
      var formatter = formats.formatters[format];
      var filter = defaults.filter;
      if (typeof opts.filter === "function" || isArray(opts.filter)) {
        filter = opts.filter;
      }
      return {
        addQueryPrefix: typeof opts.addQueryPrefix === "boolean" ? opts.addQueryPrefix : defaults.addQueryPrefix,
        allowDots: typeof opts.allowDots === "undefined" ? defaults.allowDots : !!opts.allowDots,
        charset,
        charsetSentinel: typeof opts.charsetSentinel === "boolean" ? opts.charsetSentinel : defaults.charsetSentinel,
        delimiter: typeof opts.delimiter === "undefined" ? defaults.delimiter : opts.delimiter,
        encode: typeof opts.encode === "boolean" ? opts.encode : defaults.encode,
        encoder: typeof opts.encoder === "function" ? opts.encoder : defaults.encoder,
        encodeValuesOnly: typeof opts.encodeValuesOnly === "boolean" ? opts.encodeValuesOnly : defaults.encodeValuesOnly,
        filter,
        format,
        formatter,
        serializeDate: typeof opts.serializeDate === "function" ? opts.serializeDate : defaults.serializeDate,
        skipNulls: typeof opts.skipNulls === "boolean" ? opts.skipNulls : defaults.skipNulls,
        sort: typeof opts.sort === "function" ? opts.sort : null,
        strictNullHandling: typeof opts.strictNullHandling === "boolean" ? opts.strictNullHandling : defaults.strictNullHandling
      };
    };
    module.exports = function(object, opts) {
      var obj = object;
      var options = normalizeStringifyOptions(opts);
      var objKeys;
      var filter;
      if (typeof options.filter === "function") {
        filter = options.filter;
        obj = filter("", obj);
      } else if (isArray(options.filter)) {
        filter = options.filter;
        objKeys = filter;
      }
      var keys = [];
      if (typeof obj !== "object" || obj === null) {
        return "";
      }
      var arrayFormat;
      if (opts && opts.arrayFormat in arrayPrefixGenerators) {
        arrayFormat = opts.arrayFormat;
      } else if (opts && "indices" in opts) {
        arrayFormat = opts.indices ? "indices" : "repeat";
      } else {
        arrayFormat = "indices";
      }
      var generateArrayPrefix = arrayPrefixGenerators[arrayFormat];
      if (opts && "commaRoundTrip" in opts && typeof opts.commaRoundTrip !== "boolean") {
        throw new TypeError("`commaRoundTrip` must be a boolean, or absent");
      }
      var commaRoundTrip = generateArrayPrefix === "comma" && opts && opts.commaRoundTrip;
      if (!objKeys) {
        objKeys = Object.keys(obj);
      }
      if (options.sort) {
        objKeys.sort(options.sort);
      }
      var sideChannel = getSideChannel();
      for (var i2 = 0; i2 < objKeys.length; ++i2) {
        var key = objKeys[i2];
        if (options.skipNulls && obj[key] === null) {
          continue;
        }
        pushToArray(keys, stringify(
          obj[key],
          key,
          generateArrayPrefix,
          commaRoundTrip,
          options.strictNullHandling,
          options.skipNulls,
          options.encode ? options.encoder : null,
          options.filter,
          options.sort,
          options.allowDots,
          options.serializeDate,
          options.format,
          options.formatter,
          options.encodeValuesOnly,
          options.charset,
          sideChannel
        ));
      }
      var joined = keys.join(options.delimiter);
      var prefix = options.addQueryPrefix === true ? "?" : "";
      if (options.charsetSentinel) {
        if (options.charset === "iso-8859-1") {
          prefix += "utf8=%26%2310003%3B&";
        } else {
          prefix += "utf8=%E2%9C%93&";
        }
      }
      return joined.length > 0 ? prefix + joined : "";
    };
  }
});

// node_modules/qs/lib/parse.js
var require_parse = __commonJS({
  "node_modules/qs/lib/parse.js"(exports, module) {
    "use strict";
    var utils = require_utils();
    var has = Object.prototype.hasOwnProperty;
    var isArray = Array.isArray;
    var defaults = {
      allowDots: false,
      allowPrototypes: false,
      allowSparse: false,
      arrayLimit: 20,
      charset: "utf-8",
      charsetSentinel: false,
      comma: false,
      decoder: utils.decode,
      delimiter: "&",
      depth: 5,
      ignoreQueryPrefix: false,
      interpretNumericEntities: false,
      parameterLimit: 1e3,
      parseArrays: true,
      plainObjects: false,
      strictNullHandling: false
    };
    var interpretNumericEntities = function(str) {
      return str.replace(/&#(\d+);/g, function($0, numberStr) {
        return String.fromCharCode(parseInt(numberStr, 10));
      });
    };
    var parseArrayValue = function(val, options) {
      if (val && typeof val === "string" && options.comma && val.indexOf(",") > -1) {
        return val.split(",");
      }
      return val;
    };
    var isoSentinel = "utf8=%26%2310003%3B";
    var charsetSentinel = "utf8=%E2%9C%93";
    var parseValues = function parseQueryStringValues(str, options) {
      var obj = { __proto__: null };
      var cleanStr = options.ignoreQueryPrefix ? str.replace(/^\?/, "") : str;
      var limit = options.parameterLimit === Infinity ? void 0 : options.parameterLimit;
      var parts = cleanStr.split(options.delimiter, limit);
      var skipIndex = -1;
      var i2;
      var charset = options.charset;
      if (options.charsetSentinel) {
        for (i2 = 0; i2 < parts.length; ++i2) {
          if (parts[i2].indexOf("utf8=") === 0) {
            if (parts[i2] === charsetSentinel) {
              charset = "utf-8";
            } else if (parts[i2] === isoSentinel) {
              charset = "iso-8859-1";
            }
            skipIndex = i2;
            i2 = parts.length;
          }
        }
      }
      for (i2 = 0; i2 < parts.length; ++i2) {
        if (i2 === skipIndex) {
          continue;
        }
        var part = parts[i2];
        var bracketEqualsPos = part.indexOf("]=");
        var pos = bracketEqualsPos === -1 ? part.indexOf("=") : bracketEqualsPos + 1;
        var key, val;
        if (pos === -1) {
          key = options.decoder(part, defaults.decoder, charset, "key");
          val = options.strictNullHandling ? null : "";
        } else {
          key = options.decoder(part.slice(0, pos), defaults.decoder, charset, "key");
          val = utils.maybeMap(
            parseArrayValue(part.slice(pos + 1), options),
            function(encodedVal) {
              return options.decoder(encodedVal, defaults.decoder, charset, "value");
            }
          );
        }
        if (val && options.interpretNumericEntities && charset === "iso-8859-1") {
          val = interpretNumericEntities(val);
        }
        if (part.indexOf("[]=") > -1) {
          val = isArray(val) ? [val] : val;
        }
        if (has.call(obj, key)) {
          obj[key] = utils.combine(obj[key], val);
        } else {
          obj[key] = val;
        }
      }
      return obj;
    };
    var parseObject = function(chain, val, options, valuesParsed) {
      var leaf = valuesParsed ? val : parseArrayValue(val, options);
      for (var i2 = chain.length - 1; i2 >= 0; --i2) {
        var obj;
        var root = chain[i2];
        if (root === "[]" && options.parseArrays) {
          obj = [].concat(leaf);
        } else {
          obj = options.plainObjects ? /* @__PURE__ */ Object.create(null) : {};
          var cleanRoot = root.charAt(0) === "[" && root.charAt(root.length - 1) === "]" ? root.slice(1, -1) : root;
          var index = parseInt(cleanRoot, 10);
          if (!options.parseArrays && cleanRoot === "") {
            obj = { 0: leaf };
          } else if (!isNaN(index) && root !== cleanRoot && String(index) === cleanRoot && index >= 0 && (options.parseArrays && index <= options.arrayLimit)) {
            obj = [];
            obj[index] = leaf;
          } else if (cleanRoot !== "__proto__") {
            obj[cleanRoot] = leaf;
          }
        }
        leaf = obj;
      }
      return leaf;
    };
    var parseKeys = function parseQueryStringKeys(givenKey, val, options, valuesParsed) {
      if (!givenKey) {
        return;
      }
      var key = options.allowDots ? givenKey.replace(/\.([^.[]+)/g, "[$1]") : givenKey;
      var brackets = /(\[[^[\]]*])/;
      var child = /(\[[^[\]]*])/g;
      var segment = options.depth > 0 && brackets.exec(key);
      var parent = segment ? key.slice(0, segment.index) : key;
      var keys = [];
      if (parent) {
        if (!options.plainObjects && has.call(Object.prototype, parent)) {
          if (!options.allowPrototypes) {
            return;
          }
        }
        keys.push(parent);
      }
      var i2 = 0;
      while (options.depth > 0 && (segment = child.exec(key)) !== null && i2 < options.depth) {
        i2 += 1;
        if (!options.plainObjects && has.call(Object.prototype, segment[1].slice(1, -1))) {
          if (!options.allowPrototypes) {
            return;
          }
        }
        keys.push(segment[1]);
      }
      if (segment) {
        keys.push("[" + key.slice(segment.index) + "]");
      }
      return parseObject(keys, val, options, valuesParsed);
    };
    var normalizeParseOptions = function normalizeParseOptions2(opts) {
      if (!opts) {
        return defaults;
      }
      if (opts.decoder !== null && opts.decoder !== void 0 && typeof opts.decoder !== "function") {
        throw new TypeError("Decoder has to be a function.");
      }
      if (typeof opts.charset !== "undefined" && opts.charset !== "utf-8" && opts.charset !== "iso-8859-1") {
        throw new TypeError("The charset option must be either utf-8, iso-8859-1, or undefined");
      }
      var charset = typeof opts.charset === "undefined" ? defaults.charset : opts.charset;
      return {
        allowDots: typeof opts.allowDots === "undefined" ? defaults.allowDots : !!opts.allowDots,
        allowPrototypes: typeof opts.allowPrototypes === "boolean" ? opts.allowPrototypes : defaults.allowPrototypes,
        allowSparse: typeof opts.allowSparse === "boolean" ? opts.allowSparse : defaults.allowSparse,
        arrayLimit: typeof opts.arrayLimit === "number" ? opts.arrayLimit : defaults.arrayLimit,
        charset,
        charsetSentinel: typeof opts.charsetSentinel === "boolean" ? opts.charsetSentinel : defaults.charsetSentinel,
        comma: typeof opts.comma === "boolean" ? opts.comma : defaults.comma,
        decoder: typeof opts.decoder === "function" ? opts.decoder : defaults.decoder,
        delimiter: typeof opts.delimiter === "string" || utils.isRegExp(opts.delimiter) ? opts.delimiter : defaults.delimiter,
        // eslint-disable-next-line no-implicit-coercion, no-extra-parens
        depth: typeof opts.depth === "number" || opts.depth === false ? +opts.depth : defaults.depth,
        ignoreQueryPrefix: opts.ignoreQueryPrefix === true,
        interpretNumericEntities: typeof opts.interpretNumericEntities === "boolean" ? opts.interpretNumericEntities : defaults.interpretNumericEntities,
        parameterLimit: typeof opts.parameterLimit === "number" ? opts.parameterLimit : defaults.parameterLimit,
        parseArrays: opts.parseArrays !== false,
        plainObjects: typeof opts.plainObjects === "boolean" ? opts.plainObjects : defaults.plainObjects,
        strictNullHandling: typeof opts.strictNullHandling === "boolean" ? opts.strictNullHandling : defaults.strictNullHandling
      };
    };
    module.exports = function(str, opts) {
      var options = normalizeParseOptions(opts);
      if (str === "" || str === null || typeof str === "undefined") {
        return options.plainObjects ? /* @__PURE__ */ Object.create(null) : {};
      }
      var tempObj = typeof str === "string" ? parseValues(str, options) : str;
      var obj = options.plainObjects ? /* @__PURE__ */ Object.create(null) : {};
      var keys = Object.keys(tempObj);
      for (var i2 = 0; i2 < keys.length; ++i2) {
        var key = keys[i2];
        var newObj = parseKeys(key, tempObj[key], options, typeof str === "string");
        obj = utils.merge(obj, newObj, options);
      }
      if (options.allowSparse === true) {
        return obj;
      }
      return utils.compact(obj);
    };
  }
});

// node_modules/qs/lib/index.js
var require_lib = __commonJS({
  "node_modules/qs/lib/index.js"(exports, module) {
    "use strict";
    var stringify = require_stringify();
    var parse = require_parse();
    var formats = require_formats();
    module.exports = {
      formats,
      parse,
      stringify
    };
  }
});

// node_modules/warn-once/index.js
var require_warn_once = __commonJS({
  "node_modules/warn-once/index.js"(exports, module) {
    var DEV = true;
    var warnings = /* @__PURE__ */ new Set();
    function warnOnce(condition, ...rest) {
      if (DEV && condition) {
        const key = rest.join(" ");
        if (warnings.has(key)) {
          return;
        }
        warnings.add(key);
        console.warn(...rest);
      }
    }
    module.exports = warnOnce;
  }
});

// node_modules/papaparse/papaparse.min.js
var require_papaparse_min = __commonJS({
  "node_modules/papaparse/papaparse.min.js"(exports, module) {
    !function(e, t) {
      "function" == typeof define && define.amd ? define([], t) : "object" == typeof module && "undefined" != typeof exports ? module.exports = t() : e.Papa = t();
    }(exports, function s() {
      "use strict";
      var f2 = "undefined" != typeof self ? self : "undefined" != typeof window ? window : void 0 !== f2 ? f2 : {};
      var n = !f2.document && !!f2.postMessage, o2 = f2.IS_PAPA_WORKER || false, a = {}, u = 0, b = { parse: function(e, t) {
        var r2 = (t = t || {}).dynamicTyping || false;
        J2(r2) && (t.dynamicTypingFunction = r2, r2 = {});
        if (t.dynamicTyping = r2, t.transform = !!J2(t.transform) && t.transform, t.worker && b.WORKERS_SUPPORTED) {
          var i2 = function() {
            if (!b.WORKERS_SUPPORTED)
              return false;
            var e2 = (r3 = f2.URL || f2.webkitURL || null, i3 = s.toString(), b.BLOB_URL || (b.BLOB_URL = r3.createObjectURL(new Blob(["var global = (function() { if (typeof self !== 'undefined') { return self; } if (typeof window !== 'undefined') { return window; } if (typeof global !== 'undefined') { return global; } return {}; })(); global.IS_PAPA_WORKER=true; ", "(", i3, ")();"], { type: "text/javascript" })))), t2 = new f2.Worker(e2);
            var r3, i3;
            return t2.onmessage = _2, t2.id = u++, a[t2.id] = t2;
          }();
          return i2.userStep = t.step, i2.userChunk = t.chunk, i2.userComplete = t.complete, i2.userError = t.error, t.step = J2(t.step), t.chunk = J2(t.chunk), t.complete = J2(t.complete), t.error = J2(t.error), delete t.worker, void i2.postMessage({ input: e, config: t, workerId: i2.id });
        }
        var n2 = null;
        b.NODE_STREAM_INPUT, "string" == typeof e ? (e = function(e2) {
          if (65279 === e2.charCodeAt(0))
            return e2.slice(1);
          return e2;
        }(e), n2 = t.download ? new l2(t) : new p3(t)) : true === e.readable && J2(e.read) && J2(e.on) ? n2 = new g2(t) : (f2.File && e instanceof File || e instanceof Object) && (n2 = new c(t));
        return n2.stream(e);
      }, unparse: function(e, t) {
        var n2 = false, _3 = true, m3 = ",", y3 = "\r\n", s2 = '"', a2 = s2 + s2, r2 = false, i2 = null, o3 = false;
        !function() {
          if ("object" != typeof t)
            return;
          "string" != typeof t.delimiter || b.BAD_DELIMITERS.filter(function(e2) {
            return -1 !== t.delimiter.indexOf(e2);
          }).length || (m3 = t.delimiter);
          ("boolean" == typeof t.quotes || "function" == typeof t.quotes || Array.isArray(t.quotes)) && (n2 = t.quotes);
          "boolean" != typeof t.skipEmptyLines && "string" != typeof t.skipEmptyLines || (r2 = t.skipEmptyLines);
          "string" == typeof t.newline && (y3 = t.newline);
          "string" == typeof t.quoteChar && (s2 = t.quoteChar);
          "boolean" == typeof t.header && (_3 = t.header);
          if (Array.isArray(t.columns)) {
            if (0 === t.columns.length)
              throw new Error("Option columns is empty");
            i2 = t.columns;
          }
          void 0 !== t.escapeChar && (a2 = t.escapeChar + s2);
          ("boolean" == typeof t.escapeFormulae || t.escapeFormulae instanceof RegExp) && (o3 = t.escapeFormulae instanceof RegExp ? t.escapeFormulae : /^[=+\-@\t\r].*$/);
        }();
        var u2 = new RegExp(Q(s2), "g");
        "string" == typeof e && (e = JSON.parse(e));
        if (Array.isArray(e)) {
          if (!e.length || Array.isArray(e[0]))
            return h2(null, e, r2);
          if ("object" == typeof e[0])
            return h2(i2 || Object.keys(e[0]), e, r2);
        } else if ("object" == typeof e)
          return "string" == typeof e.data && (e.data = JSON.parse(e.data)), Array.isArray(e.data) && (e.fields || (e.fields = e.meta && e.meta.fields || i2), e.fields || (e.fields = Array.isArray(e.data[0]) ? e.fields : "object" == typeof e.data[0] ? Object.keys(e.data[0]) : []), Array.isArray(e.data[0]) || "object" == typeof e.data[0] || (e.data = [e.data])), h2(e.fields || [], e.data || [], r2);
        throw new Error("Unable to serialize unrecognized input");
        function h2(e2, t2, r3) {
          var i3 = "";
          "string" == typeof e2 && (e2 = JSON.parse(e2)), "string" == typeof t2 && (t2 = JSON.parse(t2));
          var n3 = Array.isArray(e2) && 0 < e2.length, s3 = !Array.isArray(t2[0]);
          if (n3 && _3) {
            for (var a3 = 0; a3 < e2.length; a3++)
              0 < a3 && (i3 += m3), i3 += v3(e2[a3], a3);
            0 < t2.length && (i3 += y3);
          }
          for (var o4 = 0; o4 < t2.length; o4++) {
            var u3 = n3 ? e2.length : t2[o4].length, h3 = false, f3 = n3 ? 0 === Object.keys(t2[o4]).length : 0 === t2[o4].length;
            if (r3 && !n3 && (h3 = "greedy" === r3 ? "" === t2[o4].join("").trim() : 1 === t2[o4].length && 0 === t2[o4][0].length), "greedy" === r3 && n3) {
              for (var d4 = [], l3 = 0; l3 < u3; l3++) {
                var c2 = s3 ? e2[l3] : l3;
                d4.push(t2[o4][c2]);
              }
              h3 = "" === d4.join("").trim();
            }
            if (!h3) {
              for (var p4 = 0; p4 < u3; p4++) {
                0 < p4 && !f3 && (i3 += m3);
                var g3 = n3 && s3 ? e2[p4] : p4;
                i3 += v3(t2[o4][g3], p4);
              }
              o4 < t2.length - 1 && (!r3 || 0 < u3 && !f3) && (i3 += y3);
            }
          }
          return i3;
        }
        function v3(e2, t2) {
          if (null == e2)
            return "";
          if (e2.constructor === Date)
            return JSON.stringify(e2).slice(1, 25);
          var r3 = false;
          o3 && "string" == typeof e2 && o3.test(e2) && (e2 = "'" + e2, r3 = true);
          var i3 = e2.toString().replace(u2, a2);
          return (r3 = r3 || true === n2 || "function" == typeof n2 && n2(e2, t2) || Array.isArray(n2) && n2[t2] || function(e3, t3) {
            for (var r4 = 0; r4 < t3.length; r4++)
              if (-1 < e3.indexOf(t3[r4]))
                return true;
            return false;
          }(i3, b.BAD_DELIMITERS) || -1 < i3.indexOf(m3) || " " === i3.charAt(0) || " " === i3.charAt(i3.length - 1)) ? s2 + i3 + s2 : i3;
        }
      } };
      if (b.RECORD_SEP = String.fromCharCode(30), b.UNIT_SEP = String.fromCharCode(31), b.BYTE_ORDER_MARK = "\uFEFF", b.BAD_DELIMITERS = ["\r", "\n", '"', b.BYTE_ORDER_MARK], b.WORKERS_SUPPORTED = !n && !!f2.Worker, b.NODE_STREAM_INPUT = 1, b.LocalChunkSize = 10485760, b.RemoteChunkSize = 5242880, b.DefaultDelimiter = ",", b.Parser = E2, b.ParserHandle = r, b.NetworkStreamer = l2, b.FileStreamer = c, b.StringStreamer = p3, b.ReadableStreamStreamer = g2, f2.jQuery) {
        var d3 = f2.jQuery;
        d3.fn.parse = function(o3) {
          var r2 = o3.config || {}, u2 = [];
          return this.each(function(e2) {
            if (!("INPUT" === d3(this).prop("tagName").toUpperCase() && "file" === d3(this).attr("type").toLowerCase() && f2.FileReader) || !this.files || 0 === this.files.length)
              return true;
            for (var t = 0; t < this.files.length; t++)
              u2.push({ file: this.files[t], inputElem: this, instanceConfig: d3.extend({}, r2) });
          }), e(), this;
          function e() {
            if (0 !== u2.length) {
              var e2, t, r3, i2, n2 = u2[0];
              if (J2(o3.before)) {
                var s2 = o3.before(n2.file, n2.inputElem);
                if ("object" == typeof s2) {
                  if ("abort" === s2.action)
                    return e2 = "AbortError", t = n2.file, r3 = n2.inputElem, i2 = s2.reason, void (J2(o3.error) && o3.error({ name: e2 }, t, r3, i2));
                  if ("skip" === s2.action)
                    return void h2();
                  "object" == typeof s2.config && (n2.instanceConfig = d3.extend(n2.instanceConfig, s2.config));
                } else if ("skip" === s2)
                  return void h2();
              }
              var a2 = n2.instanceConfig.complete;
              n2.instanceConfig.complete = function(e3) {
                J2(a2) && a2(e3, n2.file, n2.inputElem), h2();
              }, b.parse(n2.file, n2.instanceConfig);
            } else
              J2(o3.complete) && o3.complete();
          }
          function h2() {
            u2.splice(0, 1), e();
          }
        };
      }
      function h(e) {
        this._handle = null, this._finished = false, this._completed = false, this._halted = false, this._input = null, this._baseIndex = 0, this._partialLine = "", this._rowCount = 0, this._start = 0, this._nextChunk = null, this.isFirstChunk = true, this._completeResults = { data: [], errors: [], meta: {} }, (function(e2) {
          var t = w(e2);
          t.chunkSize = parseInt(t.chunkSize), e2.step || e2.chunk || (t.chunkSize = null);
          this._handle = new r(t), (this._handle.streamer = this)._config = t;
        }).call(this, e), this.parseChunk = function(e2, t) {
          if (this.isFirstChunk && J2(this._config.beforeFirstChunk)) {
            var r2 = this._config.beforeFirstChunk(e2);
            void 0 !== r2 && (e2 = r2);
          }
          this.isFirstChunk = false, this._halted = false;
          var i2 = this._partialLine + e2;
          this._partialLine = "";
          var n2 = this._handle.parse(i2, this._baseIndex, !this._finished);
          if (!this._handle.paused() && !this._handle.aborted()) {
            var s2 = n2.meta.cursor;
            this._finished || (this._partialLine = i2.substring(s2 - this._baseIndex), this._baseIndex = s2), n2 && n2.data && (this._rowCount += n2.data.length);
            var a2 = this._finished || this._config.preview && this._rowCount >= this._config.preview;
            if (o2)
              f2.postMessage({ results: n2, workerId: b.WORKER_ID, finished: a2 });
            else if (J2(this._config.chunk) && !t) {
              if (this._config.chunk(n2, this._handle), this._handle.paused() || this._handle.aborted())
                return void (this._halted = true);
              n2 = void 0, this._completeResults = void 0;
            }
            return this._config.step || this._config.chunk || (this._completeResults.data = this._completeResults.data.concat(n2.data), this._completeResults.errors = this._completeResults.errors.concat(n2.errors), this._completeResults.meta = n2.meta), this._completed || !a2 || !J2(this._config.complete) || n2 && n2.meta.aborted || (this._config.complete(this._completeResults, this._input), this._completed = true), a2 || n2 && n2.meta.paused || this._nextChunk(), n2;
          }
          this._halted = true;
        }, this._sendError = function(e2) {
          J2(this._config.error) ? this._config.error(e2) : o2 && this._config.error && f2.postMessage({ workerId: b.WORKER_ID, error: e2, finished: false });
        };
      }
      function l2(e) {
        var i2;
        (e = e || {}).chunkSize || (e.chunkSize = b.RemoteChunkSize), h.call(this, e), this._nextChunk = n ? function() {
          this._readChunk(), this._chunkLoaded();
        } : function() {
          this._readChunk();
        }, this.stream = function(e2) {
          this._input = e2, this._nextChunk();
        }, this._readChunk = function() {
          if (this._finished)
            this._chunkLoaded();
          else {
            if (i2 = new XMLHttpRequest(), this._config.withCredentials && (i2.withCredentials = this._config.withCredentials), n || (i2.onload = v2(this._chunkLoaded, this), i2.onerror = v2(this._chunkError, this)), i2.open(this._config.downloadRequestBody ? "POST" : "GET", this._input, !n), this._config.downloadRequestHeaders) {
              var e2 = this._config.downloadRequestHeaders;
              for (var t in e2)
                i2.setRequestHeader(t, e2[t]);
            }
            if (this._config.chunkSize) {
              var r2 = this._start + this._config.chunkSize - 1;
              i2.setRequestHeader("Range", "bytes=" + this._start + "-" + r2);
            }
            try {
              i2.send(this._config.downloadRequestBody);
            } catch (e3) {
              this._chunkError(e3.message);
            }
            n && 0 === i2.status && this._chunkError();
          }
        }, this._chunkLoaded = function() {
          4 === i2.readyState && (i2.status < 200 || 400 <= i2.status ? this._chunkError() : (this._start += this._config.chunkSize ? this._config.chunkSize : i2.responseText.length, this._finished = !this._config.chunkSize || this._start >= function(e2) {
            var t = e2.getResponseHeader("Content-Range");
            if (null === t)
              return -1;
            return parseInt(t.substring(t.lastIndexOf("/") + 1));
          }(i2), this.parseChunk(i2.responseText)));
        }, this._chunkError = function(e2) {
          var t = i2.statusText || e2;
          this._sendError(new Error(t));
        };
      }
      function c(e) {
        var i2, n2;
        (e = e || {}).chunkSize || (e.chunkSize = b.LocalChunkSize), h.call(this, e);
        var s2 = "undefined" != typeof FileReader;
        this.stream = function(e2) {
          this._input = e2, n2 = e2.slice || e2.webkitSlice || e2.mozSlice, s2 ? ((i2 = new FileReader()).onload = v2(this._chunkLoaded, this), i2.onerror = v2(this._chunkError, this)) : i2 = new FileReaderSync(), this._nextChunk();
        }, this._nextChunk = function() {
          this._finished || this._config.preview && !(this._rowCount < this._config.preview) || this._readChunk();
        }, this._readChunk = function() {
          var e2 = this._input;
          if (this._config.chunkSize) {
            var t = Math.min(this._start + this._config.chunkSize, this._input.size);
            e2 = n2.call(e2, this._start, t);
          }
          var r2 = i2.readAsText(e2, this._config.encoding);
          s2 || this._chunkLoaded({ target: { result: r2 } });
        }, this._chunkLoaded = function(e2) {
          this._start += this._config.chunkSize, this._finished = !this._config.chunkSize || this._start >= this._input.size, this.parseChunk(e2.target.result);
        }, this._chunkError = function() {
          this._sendError(i2.error);
        };
      }
      function p3(e) {
        var r2;
        h.call(this, e = e || {}), this.stream = function(e2) {
          return r2 = e2, this._nextChunk();
        }, this._nextChunk = function() {
          if (!this._finished) {
            var e2, t = this._config.chunkSize;
            return t ? (e2 = r2.substring(0, t), r2 = r2.substring(t)) : (e2 = r2, r2 = ""), this._finished = !r2, this.parseChunk(e2);
          }
        };
      }
      function g2(e) {
        h.call(this, e = e || {});
        var t = [], r2 = true, i2 = false;
        this.pause = function() {
          h.prototype.pause.apply(this, arguments), this._input.pause();
        }, this.resume = function() {
          h.prototype.resume.apply(this, arguments), this._input.resume();
        }, this.stream = function(e2) {
          this._input = e2, this._input.on("data", this._streamData), this._input.on("end", this._streamEnd), this._input.on("error", this._streamError);
        }, this._checkIsFinished = function() {
          i2 && 1 === t.length && (this._finished = true);
        }, this._nextChunk = function() {
          this._checkIsFinished(), t.length ? this.parseChunk(t.shift()) : r2 = true;
        }, this._streamData = v2(function(e2) {
          try {
            t.push("string" == typeof e2 ? e2 : e2.toString(this._config.encoding)), r2 && (r2 = false, this._checkIsFinished(), this.parseChunk(t.shift()));
          } catch (e3) {
            this._streamError(e3);
          }
        }, this), this._streamError = v2(function(e2) {
          this._streamCleanUp(), this._sendError(e2);
        }, this), this._streamEnd = v2(function() {
          this._streamCleanUp(), i2 = true, this._streamData("");
        }, this), this._streamCleanUp = v2(function() {
          this._input.removeListener("data", this._streamData), this._input.removeListener("end", this._streamEnd), this._input.removeListener("error", this._streamError);
        }, this);
      }
      function r(m3) {
        var a2, o3, u2, i2 = Math.pow(2, 53), n2 = -i2, s2 = /^\s*-?(\d+\.?|\.\d+|\d+\.\d+)([eE][-+]?\d+)?\s*$/, h2 = /^((\d{4}-[01]\d-[0-3]\dT[0-2]\d:[0-5]\d:[0-5]\d\.\d+([+-][0-2]\d:[0-5]\d|Z))|(\d{4}-[01]\d-[0-3]\dT[0-2]\d:[0-5]\d:[0-5]\d([+-][0-2]\d:[0-5]\d|Z))|(\d{4}-[01]\d-[0-3]\dT[0-2]\d:[0-5]\d([+-][0-2]\d:[0-5]\d|Z)))$/, t = this, r2 = 0, f3 = 0, d4 = false, e = false, l3 = [], c2 = { data: [], errors: [], meta: {} };
        if (J2(m3.step)) {
          var p4 = m3.step;
          m3.step = function(e2) {
            if (c2 = e2, _3())
              g3();
            else {
              if (g3(), 0 === c2.data.length)
                return;
              r2 += e2.data.length, m3.preview && r2 > m3.preview ? o3.abort() : (c2.data = c2.data[0], p4(c2, t));
            }
          };
        }
        function y3(e2) {
          return "greedy" === m3.skipEmptyLines ? "" === e2.join("").trim() : 1 === e2.length && 0 === e2[0].length;
        }
        function g3() {
          return c2 && u2 && (k("Delimiter", "UndetectableDelimiter", "Unable to auto-detect delimiting character; defaulted to '" + b.DefaultDelimiter + "'"), u2 = false), m3.skipEmptyLines && (c2.data = c2.data.filter(function(e2) {
            return !y3(e2);
          })), _3() && function() {
            if (!c2)
              return;
            function e2(e3, t3) {
              J2(m3.transformHeader) && (e3 = m3.transformHeader(e3, t3)), l3.push(e3);
            }
            if (Array.isArray(c2.data[0])) {
              for (var t2 = 0; _3() && t2 < c2.data.length; t2++)
                c2.data[t2].forEach(e2);
              c2.data.splice(0, 1);
            } else
              c2.data.forEach(e2);
          }(), function() {
            if (!c2 || !m3.header && !m3.dynamicTyping && !m3.transform)
              return c2;
            function e2(e3, t3) {
              var r3, i3 = m3.header ? {} : [];
              for (r3 = 0; r3 < e3.length; r3++) {
                var n3 = r3, s3 = e3[r3];
                m3.header && (n3 = r3 >= l3.length ? "__parsed_extra" : l3[r3]), m3.transform && (s3 = m3.transform(s3, n3)), s3 = v3(n3, s3), "__parsed_extra" === n3 ? (i3[n3] = i3[n3] || [], i3[n3].push(s3)) : i3[n3] = s3;
              }
              return m3.header && (r3 > l3.length ? k("FieldMismatch", "TooManyFields", "Too many fields: expected " + l3.length + " fields but parsed " + r3, f3 + t3) : r3 < l3.length && k("FieldMismatch", "TooFewFields", "Too few fields: expected " + l3.length + " fields but parsed " + r3, f3 + t3)), i3;
            }
            var t2 = 1;
            !c2.data.length || Array.isArray(c2.data[0]) ? (c2.data = c2.data.map(e2), t2 = c2.data.length) : c2.data = e2(c2.data, 0);
            m3.header && c2.meta && (c2.meta.fields = l3);
            return f3 += t2, c2;
          }();
        }
        function _3() {
          return m3.header && 0 === l3.length;
        }
        function v3(e2, t2) {
          return r3 = e2, m3.dynamicTypingFunction && void 0 === m3.dynamicTyping[r3] && (m3.dynamicTyping[r3] = m3.dynamicTypingFunction(r3)), true === (m3.dynamicTyping[r3] || m3.dynamicTyping) ? "true" === t2 || "TRUE" === t2 || "false" !== t2 && "FALSE" !== t2 && (function(e3) {
            if (s2.test(e3)) {
              var t3 = parseFloat(e3);
              if (n2 < t3 && t3 < i2)
                return true;
            }
            return false;
          }(t2) ? parseFloat(t2) : h2.test(t2) ? new Date(t2) : "" === t2 ? null : t2) : t2;
          var r3;
        }
        function k(e2, t2, r3, i3) {
          var n3 = { type: e2, code: t2, message: r3 };
          void 0 !== i3 && (n3.row = i3), c2.errors.push(n3);
        }
        this.parse = function(e2, t2, r3) {
          var i3 = m3.quoteChar || '"';
          if (m3.newline || (m3.newline = function(e3, t3) {
            e3 = e3.substring(0, 1048576);
            var r4 = new RegExp(Q(t3) + "([^]*?)" + Q(t3), "gm"), i4 = (e3 = e3.replace(r4, "")).split("\r"), n4 = e3.split("\n"), s4 = 1 < n4.length && n4[0].length < i4[0].length;
            if (1 === i4.length || s4)
              return "\n";
            for (var a3 = 0, o4 = 0; o4 < i4.length; o4++)
              "\n" === i4[o4][0] && a3++;
            return a3 >= i4.length / 2 ? "\r\n" : "\r";
          }(e2, i3)), u2 = false, m3.delimiter)
            J2(m3.delimiter) && (m3.delimiter = m3.delimiter(e2), c2.meta.delimiter = m3.delimiter);
          else {
            var n3 = function(e3, t3, r4, i4, n4) {
              var s4, a3, o4, u3;
              n4 = n4 || [",", "	", "|", ";", b.RECORD_SEP, b.UNIT_SEP];
              for (var h3 = 0; h3 < n4.length; h3++) {
                var f4 = n4[h3], d5 = 0, l4 = 0, c3 = 0;
                o4 = void 0;
                for (var p5 = new E2({ comments: i4, delimiter: f4, newline: t3, preview: 10 }).parse(e3), g4 = 0; g4 < p5.data.length; g4++)
                  if (r4 && y3(p5.data[g4]))
                    c3++;
                  else {
                    var _4 = p5.data[g4].length;
                    l4 += _4, void 0 !== o4 ? 0 < _4 && (d5 += Math.abs(_4 - o4), o4 = _4) : o4 = _4;
                  }
                0 < p5.data.length && (l4 /= p5.data.length - c3), (void 0 === a3 || d5 <= a3) && (void 0 === u3 || u3 < l4) && 1.99 < l4 && (a3 = d5, s4 = f4, u3 = l4);
              }
              return { successful: !!(m3.delimiter = s4), bestDelimiter: s4 };
            }(e2, m3.newline, m3.skipEmptyLines, m3.comments, m3.delimitersToGuess);
            n3.successful ? m3.delimiter = n3.bestDelimiter : (u2 = true, m3.delimiter = b.DefaultDelimiter), c2.meta.delimiter = m3.delimiter;
          }
          var s3 = w(m3);
          return m3.preview && m3.header && s3.preview++, a2 = e2, o3 = new E2(s3), c2 = o3.parse(a2, t2, r3), g3(), d4 ? { meta: { paused: true } } : c2 || { meta: { paused: false } };
        }, this.paused = function() {
          return d4;
        }, this.pause = function() {
          d4 = true, o3.abort(), a2 = J2(m3.chunk) ? "" : a2.substring(o3.getCharIndex());
        }, this.resume = function() {
          t.streamer._halted ? (d4 = false, t.streamer.parseChunk(a2, true)) : setTimeout(t.resume, 3);
        }, this.aborted = function() {
          return e;
        }, this.abort = function() {
          e = true, o3.abort(), c2.meta.aborted = true, J2(m3.complete) && m3.complete(c2), a2 = "";
        };
      }
      function Q(e) {
        return e.replace(/[.*+?^${}()|[\]\\]/g, "\\$&");
      }
      function E2(j) {
        var z2, M = (j = j || {}).delimiter, P2 = j.newline, U = j.comments, q = j.step, N = j.preview, B = j.fastMode, K = z2 = void 0 === j.quoteChar || null === j.quoteChar ? '"' : j.quoteChar;
        if (void 0 !== j.escapeChar && (K = j.escapeChar), ("string" != typeof M || -1 < b.BAD_DELIMITERS.indexOf(M)) && (M = ","), U === M)
          throw new Error("Comment character same as delimiter");
        true === U ? U = "#" : ("string" != typeof U || -1 < b.BAD_DELIMITERS.indexOf(U)) && (U = false), "\n" !== P2 && "\r" !== P2 && "\r\n" !== P2 && (P2 = "\n");
        var W = 0, H = false;
        this.parse = function(i2, t, r2) {
          if ("string" != typeof i2)
            throw new Error("Input must be a string");
          var n2 = i2.length, e = M.length, s2 = P2.length, a2 = U.length, o3 = J2(q), u2 = [], h2 = [], f3 = [], d4 = W = 0;
          if (!i2)
            return L();
          if (j.header && !t) {
            var l3 = i2.split(P2)[0].split(M), c2 = [], p4 = {}, g3 = false;
            for (var _3 in l3) {
              var m3 = l3[_3];
              J2(j.transformHeader) && (m3 = j.transformHeader(m3, _3));
              var y3 = m3, v3 = p4[m3] || 0;
              for (0 < v3 && (g3 = true, y3 = m3 + "_" + v3), p4[m3] = v3 + 1; c2.includes(y3); )
                y3 = y3 + "_" + v3;
              c2.push(y3);
            }
            if (g3) {
              var k = i2.split(P2);
              k[0] = c2.join(M), i2 = k.join(P2);
            }
          }
          if (B || false !== B && -1 === i2.indexOf(z2)) {
            for (var b2 = i2.split(P2), E3 = 0; E3 < b2.length; E3++) {
              if (f3 = b2[E3], W += f3.length, E3 !== b2.length - 1)
                W += P2.length;
              else if (r2)
                return L();
              if (!U || f3.substring(0, a2) !== U) {
                if (o3) {
                  if (u2 = [], I2(f3.split(M)), F(), H)
                    return L();
                } else
                  I2(f3.split(M));
                if (N && N <= E3)
                  return u2 = u2.slice(0, N), L(true);
              }
            }
            return L();
          }
          for (var w2 = i2.indexOf(M, W), R2 = i2.indexOf(P2, W), C2 = new RegExp(Q(K) + Q(z2), "g"), S2 = i2.indexOf(z2, W); ; )
            if (i2[W] !== z2)
              if (U && 0 === f3.length && i2.substring(W, W + a2) === U) {
                if (-1 === R2)
                  return L();
                W = R2 + s2, R2 = i2.indexOf(P2, W), w2 = i2.indexOf(M, W);
              } else if (-1 !== w2 && (w2 < R2 || -1 === R2))
                f3.push(i2.substring(W, w2)), W = w2 + e, w2 = i2.indexOf(M, W);
              else {
                if (-1 === R2)
                  break;
                if (f3.push(i2.substring(W, R2)), D(R2 + s2), o3 && (F(), H))
                  return L();
                if (N && u2.length >= N)
                  return L(true);
              }
            else
              for (S2 = W, W++; ; ) {
                if (-1 === (S2 = i2.indexOf(z2, S2 + 1)))
                  return r2 || h2.push({ type: "Quotes", code: "MissingQuotes", message: "Quoted field unterminated", row: u2.length, index: W }), T2();
                if (S2 === n2 - 1)
                  return T2(i2.substring(W, S2).replace(C2, z2));
                if (z2 !== K || i2[S2 + 1] !== K) {
                  if (z2 === K || 0 === S2 || i2[S2 - 1] !== K) {
                    -1 !== w2 && w2 < S2 + 1 && (w2 = i2.indexOf(M, S2 + 1)), -1 !== R2 && R2 < S2 + 1 && (R2 = i2.indexOf(P2, S2 + 1));
                    var O3 = A2(-1 === R2 ? w2 : Math.min(w2, R2));
                    if (i2.substr(S2 + 1 + O3, e) === M) {
                      f3.push(i2.substring(W, S2).replace(C2, z2)), i2[W = S2 + 1 + O3 + e] !== z2 && (S2 = i2.indexOf(z2, W)), w2 = i2.indexOf(M, W), R2 = i2.indexOf(P2, W);
                      break;
                    }
                    var x2 = A2(R2);
                    if (i2.substring(S2 + 1 + x2, S2 + 1 + x2 + s2) === P2) {
                      if (f3.push(i2.substring(W, S2).replace(C2, z2)), D(S2 + 1 + x2 + s2), w2 = i2.indexOf(M, W), S2 = i2.indexOf(z2, W), o3 && (F(), H))
                        return L();
                      if (N && u2.length >= N)
                        return L(true);
                      break;
                    }
                    h2.push({ type: "Quotes", code: "InvalidQuotes", message: "Trailing quote on quoted field is malformed", row: u2.length, index: W }), S2++;
                  }
                } else
                  S2++;
              }
          return T2();
          function I2(e2) {
            u2.push(e2), d4 = W;
          }
          function A2(e2) {
            var t2 = 0;
            if (-1 !== e2) {
              var r3 = i2.substring(S2 + 1, e2);
              r3 && "" === r3.trim() && (t2 = r3.length);
            }
            return t2;
          }
          function T2(e2) {
            return r2 || (void 0 === e2 && (e2 = i2.substring(W)), f3.push(e2), W = n2, I2(f3), o3 && F()), L();
          }
          function D(e2) {
            W = e2, I2(f3), f3 = [], R2 = i2.indexOf(P2, W);
          }
          function L(e2) {
            return { data: u2, errors: h2, meta: { delimiter: M, linebreak: P2, aborted: H, truncated: !!e2, cursor: d4 + (t || 0) } };
          }
          function F() {
            q(L()), u2 = [], h2 = [];
          }
        }, this.abort = function() {
          H = true;
        }, this.getCharIndex = function() {
          return W;
        };
      }
      function _2(e) {
        var t = e.data, r2 = a[t.workerId], i2 = false;
        if (t.error)
          r2.userError(t.error, t.file);
        else if (t.results && t.results.data) {
          var n2 = { abort: function() {
            i2 = true, m2(t.workerId, { data: [], errors: [], meta: { aborted: true } });
          }, pause: y2, resume: y2 };
          if (J2(r2.userStep)) {
            for (var s2 = 0; s2 < t.results.data.length && (r2.userStep({ data: t.results.data[s2], errors: t.results.errors, meta: t.results.meta }, n2), !i2); s2++)
              ;
            delete t.results;
          } else
            J2(r2.userChunk) && (r2.userChunk(t.results, n2, t.file), delete t.results);
        }
        t.finished && !i2 && m2(t.workerId, t.results);
      }
      function m2(e, t) {
        var r2 = a[e];
        J2(r2.userComplete) && r2.userComplete(t), r2.terminate(), delete a[e];
      }
      function y2() {
        throw new Error("Not implemented.");
      }
      function w(e) {
        if ("object" != typeof e || null === e)
          return e;
        var t = Array.isArray(e) ? [] : {};
        for (var r2 in e)
          t[r2] = w(e[r2]);
        return t;
      }
      function v2(e, t) {
        return function() {
          e.apply(t, arguments);
        };
      }
      function J2(e) {
        return "function" == typeof e;
      }
      return o2 && (f2.onmessage = function(e) {
        var t = e.data;
        void 0 === b.WORKER_ID && t && (b.WORKER_ID = t.workerId);
        if ("string" == typeof t.input)
          f2.postMessage({ workerId: b.WORKER_ID, results: b.parse(t.input, t.config), finished: true });
        else if (f2.File && t.input instanceof File || t.input instanceof Object) {
          var r2 = b.parse(t.input, t.config);
          r2 && f2.postMessage({ workerId: b.WORKER_ID, results: r2, finished: true });
        }
      }), (l2.prototype = Object.create(h.prototype)).constructor = l2, (c.prototype = Object.create(h.prototype)).constructor = c, (p3.prototype = Object.create(p3.prototype)).constructor = p3, (g2.prototype = Object.create(h.prototype)).constructor = g2, b;
    });
  }
});

// node_modules/@refinedev/core/dist/esm/index.js
var import_react2 = __toESM(require_react());

// node_modules/@tanstack/query-core/build/lib/subscribable.mjs
var Subscribable = class {
  constructor() {
    this.listeners = /* @__PURE__ */ new Set();
    this.subscribe = this.subscribe.bind(this);
  }
  subscribe(listener) {
    const identity = {
      listener
    };
    this.listeners.add(identity);
    this.onSubscribe();
    return () => {
      this.listeners.delete(identity);
      this.onUnsubscribe();
    };
  }
  hasListeners() {
    return this.listeners.size > 0;
  }
  onSubscribe() {
  }
  onUnsubscribe() {
  }
};

// node_modules/@tanstack/query-core/build/lib/utils.mjs
var isServer = typeof window === "undefined" || "Deno" in window;
function noop() {
  return void 0;
}
function functionalUpdate(updater, input) {
  return typeof updater === "function" ? updater(input) : updater;
}
function isValidTimeout(value) {
  return typeof value === "number" && value >= 0 && value !== Infinity;
}
function timeUntilStale(updatedAt, staleTime) {
  return Math.max(updatedAt + (staleTime || 0) - Date.now(), 0);
}
function parseQueryArgs(arg1, arg2, arg3) {
  if (!isQueryKey(arg1)) {
    return arg1;
  }
  if (typeof arg2 === "function") {
    return {
      ...arg3,
      queryKey: arg1,
      queryFn: arg2
    };
  }
  return {
    ...arg2,
    queryKey: arg1
  };
}
function parseMutationArgs(arg1, arg2, arg3) {
  if (isQueryKey(arg1)) {
    if (typeof arg2 === "function") {
      return {
        ...arg3,
        mutationKey: arg1,
        mutationFn: arg2
      };
    }
    return {
      ...arg2,
      mutationKey: arg1
    };
  }
  if (typeof arg1 === "function") {
    return {
      ...arg2,
      mutationFn: arg1
    };
  }
  return {
    ...arg1
  };
}
function parseFilterArgs(arg1, arg2, arg3) {
  return isQueryKey(arg1) ? [{
    ...arg2,
    queryKey: arg1
  }, arg3] : [arg1 || {}, arg2];
}
function matchQuery(filters, query) {
  const {
    type = "all",
    exact,
    fetchStatus,
    predicate,
    queryKey,
    stale
  } = filters;
  if (isQueryKey(queryKey)) {
    if (exact) {
      if (query.queryHash !== hashQueryKeyByOptions(queryKey, query.options)) {
        return false;
      }
    } else if (!partialMatchKey(query.queryKey, queryKey)) {
      return false;
    }
  }
  if (type !== "all") {
    const isActive = query.isActive();
    if (type === "active" && !isActive) {
      return false;
    }
    if (type === "inactive" && isActive) {
      return false;
    }
  }
  if (typeof stale === "boolean" && query.isStale() !== stale) {
    return false;
  }
  if (typeof fetchStatus !== "undefined" && fetchStatus !== query.state.fetchStatus) {
    return false;
  }
  if (predicate && !predicate(query)) {
    return false;
  }
  return true;
}
function matchMutation(filters, mutation) {
  const {
    exact,
    fetching,
    predicate,
    mutationKey
  } = filters;
  if (isQueryKey(mutationKey)) {
    if (!mutation.options.mutationKey) {
      return false;
    }
    if (exact) {
      if (hashQueryKey(mutation.options.mutationKey) !== hashQueryKey(mutationKey)) {
        return false;
      }
    } else if (!partialMatchKey(mutation.options.mutationKey, mutationKey)) {
      return false;
    }
  }
  if (typeof fetching === "boolean" && mutation.state.status === "loading" !== fetching) {
    return false;
  }
  if (predicate && !predicate(mutation)) {
    return false;
  }
  return true;
}
function hashQueryKeyByOptions(queryKey, options) {
  const hashFn = (options == null ? void 0 : options.queryKeyHashFn) || hashQueryKey;
  return hashFn(queryKey);
}
function hashQueryKey(queryKey) {
  return JSON.stringify(queryKey, (_2, val) => isPlainObject(val) ? Object.keys(val).sort().reduce((result, key) => {
    result[key] = val[key];
    return result;
  }, {}) : val);
}
function partialMatchKey(a, b) {
  return partialDeepEqual(a, b);
}
function partialDeepEqual(a, b) {
  if (a === b) {
    return true;
  }
  if (typeof a !== typeof b) {
    return false;
  }
  if (a && b && typeof a === "object" && typeof b === "object") {
    return !Object.keys(b).some((key) => !partialDeepEqual(a[key], b[key]));
  }
  return false;
}
function replaceEqualDeep(a, b) {
  if (a === b) {
    return a;
  }
  const array = isPlainArray(a) && isPlainArray(b);
  if (array || isPlainObject(a) && isPlainObject(b)) {
    const aSize = array ? a.length : Object.keys(a).length;
    const bItems = array ? b : Object.keys(b);
    const bSize = bItems.length;
    const copy = array ? [] : {};
    let equalItems = 0;
    for (let i2 = 0; i2 < bSize; i2++) {
      const key = array ? i2 : bItems[i2];
      copy[key] = replaceEqualDeep(a[key], b[key]);
      if (copy[key] === a[key]) {
        equalItems++;
      }
    }
    return aSize === bSize && equalItems === aSize ? a : copy;
  }
  return b;
}
function shallowEqualObjects(a, b) {
  if (a && !b || b && !a) {
    return false;
  }
  for (const key in a) {
    if (a[key] !== b[key]) {
      return false;
    }
  }
  return true;
}
function isPlainArray(value) {
  return Array.isArray(value) && value.length === Object.keys(value).length;
}
function isPlainObject(o2) {
  if (!hasObjectPrototype(o2)) {
    return false;
  }
  const ctor = o2.constructor;
  if (typeof ctor === "undefined") {
    return true;
  }
  const prot = ctor.prototype;
  if (!hasObjectPrototype(prot)) {
    return false;
  }
  if (!prot.hasOwnProperty("isPrototypeOf")) {
    return false;
  }
  return true;
}
function hasObjectPrototype(o2) {
  return Object.prototype.toString.call(o2) === "[object Object]";
}
function isQueryKey(value) {
  return Array.isArray(value);
}
function sleep(timeout) {
  return new Promise((resolve) => {
    setTimeout(resolve, timeout);
  });
}
function scheduleMicrotask(callback) {
  sleep(0).then(callback);
}
function getAbortController() {
  if (typeof AbortController === "function") {
    return new AbortController();
  }
  return;
}
function replaceData(prevData, data, options) {
  if (options.isDataEqual != null && options.isDataEqual(prevData, data)) {
    return prevData;
  } else if (typeof options.structuralSharing === "function") {
    return options.structuralSharing(prevData, data);
  } else if (options.structuralSharing !== false) {
    return replaceEqualDeep(prevData, data);
  }
  return data;
}

// node_modules/@tanstack/query-core/build/lib/focusManager.mjs
var FocusManager = class extends Subscribable {
  constructor() {
    super();
    this.setup = (onFocus) => {
      if (!isServer && window.addEventListener) {
        const listener = () => onFocus();
        window.addEventListener("visibilitychange", listener, false);
        window.addEventListener("focus", listener, false);
        return () => {
          window.removeEventListener("visibilitychange", listener);
          window.removeEventListener("focus", listener);
        };
      }
      return;
    };
  }
  onSubscribe() {
    if (!this.cleanup) {
      this.setEventListener(this.setup);
    }
  }
  onUnsubscribe() {
    if (!this.hasListeners()) {
      var _this$cleanup;
      (_this$cleanup = this.cleanup) == null ? void 0 : _this$cleanup.call(this);
      this.cleanup = void 0;
    }
  }
  setEventListener(setup) {
    var _this$cleanup2;
    this.setup = setup;
    (_this$cleanup2 = this.cleanup) == null ? void 0 : _this$cleanup2.call(this);
    this.cleanup = setup((focused) => {
      if (typeof focused === "boolean") {
        this.setFocused(focused);
      } else {
        this.onFocus();
      }
    });
  }
  setFocused(focused) {
    const changed = this.focused !== focused;
    if (changed) {
      this.focused = focused;
      this.onFocus();
    }
  }
  onFocus() {
    this.listeners.forEach(({
      listener
    }) => {
      listener();
    });
  }
  isFocused() {
    if (typeof this.focused === "boolean") {
      return this.focused;
    }
    if (typeof document === "undefined") {
      return true;
    }
    return [void 0, "visible", "prerender"].includes(document.visibilityState);
  }
};
var focusManager = new FocusManager();

// node_modules/@tanstack/query-core/build/lib/onlineManager.mjs
var onlineEvents = ["online", "offline"];
var OnlineManager = class extends Subscribable {
  constructor() {
    super();
    this.setup = (onOnline) => {
      if (!isServer && window.addEventListener) {
        const listener = () => onOnline();
        onlineEvents.forEach((event) => {
          window.addEventListener(event, listener, false);
        });
        return () => {
          onlineEvents.forEach((event) => {
            window.removeEventListener(event, listener);
          });
        };
      }
      return;
    };
  }
  onSubscribe() {
    if (!this.cleanup) {
      this.setEventListener(this.setup);
    }
  }
  onUnsubscribe() {
    if (!this.hasListeners()) {
      var _this$cleanup;
      (_this$cleanup = this.cleanup) == null ? void 0 : _this$cleanup.call(this);
      this.cleanup = void 0;
    }
  }
  setEventListener(setup) {
    var _this$cleanup2;
    this.setup = setup;
    (_this$cleanup2 = this.cleanup) == null ? void 0 : _this$cleanup2.call(this);
    this.cleanup = setup((online) => {
      if (typeof online === "boolean") {
        this.setOnline(online);
      } else {
        this.onOnline();
      }
    });
  }
  setOnline(online) {
    const changed = this.online !== online;
    if (changed) {
      this.online = online;
      this.onOnline();
    }
  }
  onOnline() {
    this.listeners.forEach(({
      listener
    }) => {
      listener();
    });
  }
  isOnline() {
    if (typeof this.online === "boolean") {
      return this.online;
    }
    if (typeof navigator === "undefined" || typeof navigator.onLine === "undefined") {
      return true;
    }
    return navigator.onLine;
  }
};
var onlineManager = new OnlineManager();

// node_modules/@tanstack/query-core/build/lib/retryer.mjs
function defaultRetryDelay(failureCount) {
  return Math.min(1e3 * 2 ** failureCount, 3e4);
}
function canFetch(networkMode) {
  return (networkMode != null ? networkMode : "online") === "online" ? onlineManager.isOnline() : true;
}
var CancelledError = class {
  constructor(options) {
    this.revert = options == null ? void 0 : options.revert;
    this.silent = options == null ? void 0 : options.silent;
  }
};
function isCancelledError(value) {
  return value instanceof CancelledError;
}
function createRetryer(config) {
  let isRetryCancelled = false;
  let failureCount = 0;
  let isResolved = false;
  let continueFn;
  let promiseResolve;
  let promiseReject;
  const promise = new Promise((outerResolve, outerReject) => {
    promiseResolve = outerResolve;
    promiseReject = outerReject;
  });
  const cancel = (cancelOptions) => {
    if (!isResolved) {
      reject(new CancelledError(cancelOptions));
      config.abort == null ? void 0 : config.abort();
    }
  };
  const cancelRetry = () => {
    isRetryCancelled = true;
  };
  const continueRetry = () => {
    isRetryCancelled = false;
  };
  const shouldPause = () => !focusManager.isFocused() || config.networkMode !== "always" && !onlineManager.isOnline();
  const resolve = (value) => {
    if (!isResolved) {
      isResolved = true;
      config.onSuccess == null ? void 0 : config.onSuccess(value);
      continueFn == null ? void 0 : continueFn();
      promiseResolve(value);
    }
  };
  const reject = (value) => {
    if (!isResolved) {
      isResolved = true;
      config.onError == null ? void 0 : config.onError(value);
      continueFn == null ? void 0 : continueFn();
      promiseReject(value);
    }
  };
  const pause = () => {
    return new Promise((continueResolve) => {
      continueFn = (value) => {
        const canContinue = isResolved || !shouldPause();
        if (canContinue) {
          continueResolve(value);
        }
        return canContinue;
      };
      config.onPause == null ? void 0 : config.onPause();
    }).then(() => {
      continueFn = void 0;
      if (!isResolved) {
        config.onContinue == null ? void 0 : config.onContinue();
      }
    });
  };
  const run = () => {
    if (isResolved) {
      return;
    }
    let promiseOrValue;
    try {
      promiseOrValue = config.fn();
    } catch (error) {
      promiseOrValue = Promise.reject(error);
    }
    Promise.resolve(promiseOrValue).then(resolve).catch((error) => {
      var _config$retry, _config$retryDelay;
      if (isResolved) {
        return;
      }
      const retry = (_config$retry = config.retry) != null ? _config$retry : 3;
      const retryDelay = (_config$retryDelay = config.retryDelay) != null ? _config$retryDelay : defaultRetryDelay;
      const delay = typeof retryDelay === "function" ? retryDelay(failureCount, error) : retryDelay;
      const shouldRetry = retry === true || typeof retry === "number" && failureCount < retry || typeof retry === "function" && retry(failureCount, error);
      if (isRetryCancelled || !shouldRetry) {
        reject(error);
        return;
      }
      failureCount++;
      config.onFail == null ? void 0 : config.onFail(failureCount, error);
      sleep(delay).then(() => {
        if (shouldPause()) {
          return pause();
        }
        return;
      }).then(() => {
        if (isRetryCancelled) {
          reject(error);
        } else {
          run();
        }
      });
    });
  };
  if (canFetch(config.networkMode)) {
    run();
  } else {
    pause().then(run);
  }
  return {
    promise,
    cancel,
    continue: () => {
      const didContinue = continueFn == null ? void 0 : continueFn();
      return didContinue ? promise : Promise.resolve();
    },
    cancelRetry,
    continueRetry
  };
}

// node_modules/@tanstack/query-core/build/lib/logger.mjs
var defaultLogger = console;

// node_modules/@tanstack/query-core/build/lib/notifyManager.mjs
function createNotifyManager() {
  let queue = [];
  let transactions = 0;
  let notifyFn = (callback) => {
    callback();
  };
  let batchNotifyFn = (callback) => {
    callback();
  };
  const batch = (callback) => {
    let result;
    transactions++;
    try {
      result = callback();
    } finally {
      transactions--;
      if (!transactions) {
        flush();
      }
    }
    return result;
  };
  const schedule = (callback) => {
    if (transactions) {
      queue.push(callback);
    } else {
      scheduleMicrotask(() => {
        notifyFn(callback);
      });
    }
  };
  const batchCalls = (callback) => {
    return (...args) => {
      schedule(() => {
        callback(...args);
      });
    };
  };
  const flush = () => {
    const originalQueue = queue;
    queue = [];
    if (originalQueue.length) {
      scheduleMicrotask(() => {
        batchNotifyFn(() => {
          originalQueue.forEach((callback) => {
            notifyFn(callback);
          });
        });
      });
    }
  };
  const setNotifyFunction = (fn2) => {
    notifyFn = fn2;
  };
  const setBatchNotifyFunction = (fn2) => {
    batchNotifyFn = fn2;
  };
  return {
    batch,
    batchCalls,
    schedule,
    setNotifyFunction,
    setBatchNotifyFunction
  };
}
var notifyManager = createNotifyManager();

// node_modules/@tanstack/query-core/build/lib/removable.mjs
var Removable = class {
  destroy() {
    this.clearGcTimeout();
  }
  scheduleGc() {
    this.clearGcTimeout();
    if (isValidTimeout(this.cacheTime)) {
      this.gcTimeout = setTimeout(() => {
        this.optionalRemove();
      }, this.cacheTime);
    }
  }
  updateCacheTime(newCacheTime) {
    this.cacheTime = Math.max(this.cacheTime || 0, newCacheTime != null ? newCacheTime : isServer ? Infinity : 5 * 60 * 1e3);
  }
  clearGcTimeout() {
    if (this.gcTimeout) {
      clearTimeout(this.gcTimeout);
      this.gcTimeout = void 0;
    }
  }
};

// node_modules/@tanstack/query-core/build/lib/query.mjs
var Query = class extends Removable {
  constructor(config) {
    super();
    this.abortSignalConsumed = false;
    this.defaultOptions = config.defaultOptions;
    this.setOptions(config.options);
    this.observers = [];
    this.cache = config.cache;
    this.logger = config.logger || defaultLogger;
    this.queryKey = config.queryKey;
    this.queryHash = config.queryHash;
    this.initialState = config.state || getDefaultState(this.options);
    this.state = this.initialState;
    this.scheduleGc();
  }
  get meta() {
    return this.options.meta;
  }
  setOptions(options) {
    this.options = {
      ...this.defaultOptions,
      ...options
    };
    this.updateCacheTime(this.options.cacheTime);
  }
  optionalRemove() {
    if (!this.observers.length && this.state.fetchStatus === "idle") {
      this.cache.remove(this);
    }
  }
  setData(newData, options) {
    const data = replaceData(this.state.data, newData, this.options);
    this.dispatch({
      data,
      type: "success",
      dataUpdatedAt: options == null ? void 0 : options.updatedAt,
      manual: options == null ? void 0 : options.manual
    });
    return data;
  }
  setState(state, setStateOptions) {
    this.dispatch({
      type: "setState",
      state,
      setStateOptions
    });
  }
  cancel(options) {
    var _this$retryer;
    const promise = this.promise;
    (_this$retryer = this.retryer) == null ? void 0 : _this$retryer.cancel(options);
    return promise ? promise.then(noop).catch(noop) : Promise.resolve();
  }
  destroy() {
    super.destroy();
    this.cancel({
      silent: true
    });
  }
  reset() {
    this.destroy();
    this.setState(this.initialState);
  }
  isActive() {
    return this.observers.some((observer) => observer.options.enabled !== false);
  }
  isDisabled() {
    return this.getObserversCount() > 0 && !this.isActive();
  }
  isStale() {
    return this.state.isInvalidated || !this.state.dataUpdatedAt || this.observers.some((observer) => observer.getCurrentResult().isStale);
  }
  isStaleByTime(staleTime = 0) {
    return this.state.isInvalidated || !this.state.dataUpdatedAt || !timeUntilStale(this.state.dataUpdatedAt, staleTime);
  }
  onFocus() {
    var _this$retryer2;
    const observer = this.observers.find((x2) => x2.shouldFetchOnWindowFocus());
    if (observer) {
      observer.refetch({
        cancelRefetch: false
      });
    }
    (_this$retryer2 = this.retryer) == null ? void 0 : _this$retryer2.continue();
  }
  onOnline() {
    var _this$retryer3;
    const observer = this.observers.find((x2) => x2.shouldFetchOnReconnect());
    if (observer) {
      observer.refetch({
        cancelRefetch: false
      });
    }
    (_this$retryer3 = this.retryer) == null ? void 0 : _this$retryer3.continue();
  }
  addObserver(observer) {
    if (!this.observers.includes(observer)) {
      this.observers.push(observer);
      this.clearGcTimeout();
      this.cache.notify({
        type: "observerAdded",
        query: this,
        observer
      });
    }
  }
  removeObserver(observer) {
    if (this.observers.includes(observer)) {
      this.observers = this.observers.filter((x2) => x2 !== observer);
      if (!this.observers.length) {
        if (this.retryer) {
          if (this.abortSignalConsumed) {
            this.retryer.cancel({
              revert: true
            });
          } else {
            this.retryer.cancelRetry();
          }
        }
        this.scheduleGc();
      }
      this.cache.notify({
        type: "observerRemoved",
        query: this,
        observer
      });
    }
  }
  getObserversCount() {
    return this.observers.length;
  }
  invalidate() {
    if (!this.state.isInvalidated) {
      this.dispatch({
        type: "invalidate"
      });
    }
  }
  fetch(options, fetchOptions) {
    var _this$options$behavio, _context$fetchOptions;
    if (this.state.fetchStatus !== "idle") {
      if (this.state.dataUpdatedAt && fetchOptions != null && fetchOptions.cancelRefetch) {
        this.cancel({
          silent: true
        });
      } else if (this.promise) {
        var _this$retryer4;
        (_this$retryer4 = this.retryer) == null ? void 0 : _this$retryer4.continueRetry();
        return this.promise;
      }
    }
    if (options) {
      this.setOptions(options);
    }
    if (!this.options.queryFn) {
      const observer = this.observers.find((x2) => x2.options.queryFn);
      if (observer) {
        this.setOptions(observer.options);
      }
    }
    if (true) {
      if (!Array.isArray(this.options.queryKey)) {
        this.logger.error("As of v4, queryKey needs to be an Array. If you are using a string like 'repoData', please change it to an Array, e.g. ['repoData']");
      }
    }
    const abortController = getAbortController();
    const queryFnContext = {
      queryKey: this.queryKey,
      pageParam: void 0,
      meta: this.meta
    };
    const addSignalProperty = (object) => {
      Object.defineProperty(object, "signal", {
        enumerable: true,
        get: () => {
          if (abortController) {
            this.abortSignalConsumed = true;
            return abortController.signal;
          }
          return void 0;
        }
      });
    };
    addSignalProperty(queryFnContext);
    const fetchFn = () => {
      if (!this.options.queryFn) {
        return Promise.reject("Missing queryFn for queryKey '" + this.options.queryHash + "'");
      }
      this.abortSignalConsumed = false;
      return this.options.queryFn(queryFnContext);
    };
    const context = {
      fetchOptions,
      options: this.options,
      queryKey: this.queryKey,
      state: this.state,
      fetchFn
    };
    addSignalProperty(context);
    (_this$options$behavio = this.options.behavior) == null ? void 0 : _this$options$behavio.onFetch(context);
    this.revertState = this.state;
    if (this.state.fetchStatus === "idle" || this.state.fetchMeta !== ((_context$fetchOptions = context.fetchOptions) == null ? void 0 : _context$fetchOptions.meta)) {
      var _context$fetchOptions2;
      this.dispatch({
        type: "fetch",
        meta: (_context$fetchOptions2 = context.fetchOptions) == null ? void 0 : _context$fetchOptions2.meta
      });
    }
    const onError = (error) => {
      if (!(isCancelledError(error) && error.silent)) {
        this.dispatch({
          type: "error",
          error
        });
      }
      if (!isCancelledError(error)) {
        var _this$cache$config$on, _this$cache$config, _this$cache$config$on2, _this$cache$config2;
        (_this$cache$config$on = (_this$cache$config = this.cache.config).onError) == null ? void 0 : _this$cache$config$on.call(_this$cache$config, error, this);
        (_this$cache$config$on2 = (_this$cache$config2 = this.cache.config).onSettled) == null ? void 0 : _this$cache$config$on2.call(_this$cache$config2, this.state.data, error, this);
        if (true) {
          this.logger.error(error);
        }
      }
      if (!this.isFetchingOptimistic) {
        this.scheduleGc();
      }
      this.isFetchingOptimistic = false;
    };
    this.retryer = createRetryer({
      fn: context.fetchFn,
      abort: abortController == null ? void 0 : abortController.abort.bind(abortController),
      onSuccess: (data) => {
        var _this$cache$config$on3, _this$cache$config3, _this$cache$config$on4, _this$cache$config4;
        if (typeof data === "undefined") {
          if (true) {
            this.logger.error("Query data cannot be undefined. Please make sure to return a value other than undefined from your query function. Affected query key: " + this.queryHash);
          }
          onError(new Error(this.queryHash + " data is undefined"));
          return;
        }
        this.setData(data);
        (_this$cache$config$on3 = (_this$cache$config3 = this.cache.config).onSuccess) == null ? void 0 : _this$cache$config$on3.call(_this$cache$config3, data, this);
        (_this$cache$config$on4 = (_this$cache$config4 = this.cache.config).onSettled) == null ? void 0 : _this$cache$config$on4.call(_this$cache$config4, data, this.state.error, this);
        if (!this.isFetchingOptimistic) {
          this.scheduleGc();
        }
        this.isFetchingOptimistic = false;
      },
      onError,
      onFail: (failureCount, error) => {
        this.dispatch({
          type: "failed",
          failureCount,
          error
        });
      },
      onPause: () => {
        this.dispatch({
          type: "pause"
        });
      },
      onContinue: () => {
        this.dispatch({
          type: "continue"
        });
      },
      retry: context.options.retry,
      retryDelay: context.options.retryDelay,
      networkMode: context.options.networkMode
    });
    this.promise = this.retryer.promise;
    return this.promise;
  }
  dispatch(action) {
    const reducer = (state) => {
      var _action$meta, _action$dataUpdatedAt;
      switch (action.type) {
        case "failed":
          return {
            ...state,
            fetchFailureCount: action.failureCount,
            fetchFailureReason: action.error
          };
        case "pause":
          return {
            ...state,
            fetchStatus: "paused"
          };
        case "continue":
          return {
            ...state,
            fetchStatus: "fetching"
          };
        case "fetch":
          return {
            ...state,
            fetchFailureCount: 0,
            fetchFailureReason: null,
            fetchMeta: (_action$meta = action.meta) != null ? _action$meta : null,
            fetchStatus: canFetch(this.options.networkMode) ? "fetching" : "paused",
            ...!state.dataUpdatedAt && {
              error: null,
              status: "loading"
            }
          };
        case "success":
          return {
            ...state,
            data: action.data,
            dataUpdateCount: state.dataUpdateCount + 1,
            dataUpdatedAt: (_action$dataUpdatedAt = action.dataUpdatedAt) != null ? _action$dataUpdatedAt : Date.now(),
            error: null,
            isInvalidated: false,
            status: "success",
            ...!action.manual && {
              fetchStatus: "idle",
              fetchFailureCount: 0,
              fetchFailureReason: null
            }
          };
        case "error":
          const error = action.error;
          if (isCancelledError(error) && error.revert && this.revertState) {
            return {
              ...this.revertState,
              fetchStatus: "idle"
            };
          }
          return {
            ...state,
            error,
            errorUpdateCount: state.errorUpdateCount + 1,
            errorUpdatedAt: Date.now(),
            fetchFailureCount: state.fetchFailureCount + 1,
            fetchFailureReason: error,
            fetchStatus: "idle",
            status: "error"
          };
        case "invalidate":
          return {
            ...state,
            isInvalidated: true
          };
        case "setState":
          return {
            ...state,
            ...action.state
          };
      }
    };
    this.state = reducer(this.state);
    notifyManager.batch(() => {
      this.observers.forEach((observer) => {
        observer.onQueryUpdate(action);
      });
      this.cache.notify({
        query: this,
        type: "updated",
        action
      });
    });
  }
};
function getDefaultState(options) {
  const data = typeof options.initialData === "function" ? options.initialData() : options.initialData;
  const hasData = typeof data !== "undefined";
  const initialDataUpdatedAt = hasData ? typeof options.initialDataUpdatedAt === "function" ? options.initialDataUpdatedAt() : options.initialDataUpdatedAt : 0;
  return {
    data,
    dataUpdateCount: 0,
    dataUpdatedAt: hasData ? initialDataUpdatedAt != null ? initialDataUpdatedAt : Date.now() : 0,
    error: null,
    errorUpdateCount: 0,
    errorUpdatedAt: 0,
    fetchFailureCount: 0,
    fetchFailureReason: null,
    fetchMeta: null,
    isInvalidated: false,
    status: hasData ? "success" : "loading",
    fetchStatus: "idle"
  };
}

// node_modules/@tanstack/query-core/build/lib/queryCache.mjs
var QueryCache = class extends Subscribable {
  constructor(config) {
    super();
    this.config = config || {};
    this.queries = [];
    this.queriesMap = {};
  }
  build(client, options, state) {
    var _options$queryHash;
    const queryKey = options.queryKey;
    const queryHash = (_options$queryHash = options.queryHash) != null ? _options$queryHash : hashQueryKeyByOptions(queryKey, options);
    let query = this.get(queryHash);
    if (!query) {
      query = new Query({
        cache: this,
        logger: client.getLogger(),
        queryKey,
        queryHash,
        options: client.defaultQueryOptions(options),
        state,
        defaultOptions: client.getQueryDefaults(queryKey)
      });
      this.add(query);
    }
    return query;
  }
  add(query) {
    if (!this.queriesMap[query.queryHash]) {
      this.queriesMap[query.queryHash] = query;
      this.queries.push(query);
      this.notify({
        type: "added",
        query
      });
    }
  }
  remove(query) {
    const queryInMap = this.queriesMap[query.queryHash];
    if (queryInMap) {
      query.destroy();
      this.queries = this.queries.filter((x2) => x2 !== query);
      if (queryInMap === query) {
        delete this.queriesMap[query.queryHash];
      }
      this.notify({
        type: "removed",
        query
      });
    }
  }
  clear() {
    notifyManager.batch(() => {
      this.queries.forEach((query) => {
        this.remove(query);
      });
    });
  }
  get(queryHash) {
    return this.queriesMap[queryHash];
  }
  getAll() {
    return this.queries;
  }
  find(arg1, arg2) {
    const [filters] = parseFilterArgs(arg1, arg2);
    if (typeof filters.exact === "undefined") {
      filters.exact = true;
    }
    return this.queries.find((query) => matchQuery(filters, query));
  }
  findAll(arg1, arg2) {
    const [filters] = parseFilterArgs(arg1, arg2);
    return Object.keys(filters).length > 0 ? this.queries.filter((query) => matchQuery(filters, query)) : this.queries;
  }
  notify(event) {
    notifyManager.batch(() => {
      this.listeners.forEach(({
        listener
      }) => {
        listener(event);
      });
    });
  }
  onFocus() {
    notifyManager.batch(() => {
      this.queries.forEach((query) => {
        query.onFocus();
      });
    });
  }
  onOnline() {
    notifyManager.batch(() => {
      this.queries.forEach((query) => {
        query.onOnline();
      });
    });
  }
};

// node_modules/@tanstack/query-core/build/lib/mutation.mjs
var Mutation = class extends Removable {
  constructor(config) {
    super();
    this.defaultOptions = config.defaultOptions;
    this.mutationId = config.mutationId;
    this.mutationCache = config.mutationCache;
    this.logger = config.logger || defaultLogger;
    this.observers = [];
    this.state = config.state || getDefaultState2();
    this.setOptions(config.options);
    this.scheduleGc();
  }
  setOptions(options) {
    this.options = {
      ...this.defaultOptions,
      ...options
    };
    this.updateCacheTime(this.options.cacheTime);
  }
  get meta() {
    return this.options.meta;
  }
  setState(state) {
    this.dispatch({
      type: "setState",
      state
    });
  }
  addObserver(observer) {
    if (!this.observers.includes(observer)) {
      this.observers.push(observer);
      this.clearGcTimeout();
      this.mutationCache.notify({
        type: "observerAdded",
        mutation: this,
        observer
      });
    }
  }
  removeObserver(observer) {
    this.observers = this.observers.filter((x2) => x2 !== observer);
    this.scheduleGc();
    this.mutationCache.notify({
      type: "observerRemoved",
      mutation: this,
      observer
    });
  }
  optionalRemove() {
    if (!this.observers.length) {
      if (this.state.status === "loading") {
        this.scheduleGc();
      } else {
        this.mutationCache.remove(this);
      }
    }
  }
  continue() {
    var _this$retryer$continu, _this$retryer;
    return (_this$retryer$continu = (_this$retryer = this.retryer) == null ? void 0 : _this$retryer.continue()) != null ? _this$retryer$continu : this.execute();
  }
  async execute() {
    const executeMutation = () => {
      var _this$options$retry;
      this.retryer = createRetryer({
        fn: () => {
          if (!this.options.mutationFn) {
            return Promise.reject("No mutationFn found");
          }
          return this.options.mutationFn(this.state.variables);
        },
        onFail: (failureCount, error) => {
          this.dispatch({
            type: "failed",
            failureCount,
            error
          });
        },
        onPause: () => {
          this.dispatch({
            type: "pause"
          });
        },
        onContinue: () => {
          this.dispatch({
            type: "continue"
          });
        },
        retry: (_this$options$retry = this.options.retry) != null ? _this$options$retry : 0,
        retryDelay: this.options.retryDelay,
        networkMode: this.options.networkMode
      });
      return this.retryer.promise;
    };
    const restored = this.state.status === "loading";
    try {
      var _this$mutationCache$c3, _this$mutationCache$c4, _this$options$onSucce, _this$options2, _this$mutationCache$c5, _this$mutationCache$c6, _this$options$onSettl, _this$options3;
      if (!restored) {
        var _this$mutationCache$c, _this$mutationCache$c2, _this$options$onMutat, _this$options;
        this.dispatch({
          type: "loading",
          variables: this.options.variables
        });
        await ((_this$mutationCache$c = (_this$mutationCache$c2 = this.mutationCache.config).onMutate) == null ? void 0 : _this$mutationCache$c.call(_this$mutationCache$c2, this.state.variables, this));
        const context = await ((_this$options$onMutat = (_this$options = this.options).onMutate) == null ? void 0 : _this$options$onMutat.call(_this$options, this.state.variables));
        if (context !== this.state.context) {
          this.dispatch({
            type: "loading",
            context,
            variables: this.state.variables
          });
        }
      }
      const data = await executeMutation();
      await ((_this$mutationCache$c3 = (_this$mutationCache$c4 = this.mutationCache.config).onSuccess) == null ? void 0 : _this$mutationCache$c3.call(_this$mutationCache$c4, data, this.state.variables, this.state.context, this));
      await ((_this$options$onSucce = (_this$options2 = this.options).onSuccess) == null ? void 0 : _this$options$onSucce.call(_this$options2, data, this.state.variables, this.state.context));
      await ((_this$mutationCache$c5 = (_this$mutationCache$c6 = this.mutationCache.config).onSettled) == null ? void 0 : _this$mutationCache$c5.call(_this$mutationCache$c6, data, null, this.state.variables, this.state.context, this));
      await ((_this$options$onSettl = (_this$options3 = this.options).onSettled) == null ? void 0 : _this$options$onSettl.call(_this$options3, data, null, this.state.variables, this.state.context));
      this.dispatch({
        type: "success",
        data
      });
      return data;
    } catch (error) {
      try {
        var _this$mutationCache$c7, _this$mutationCache$c8, _this$options$onError, _this$options4, _this$mutationCache$c9, _this$mutationCache$c10, _this$options$onSettl2, _this$options5;
        await ((_this$mutationCache$c7 = (_this$mutationCache$c8 = this.mutationCache.config).onError) == null ? void 0 : _this$mutationCache$c7.call(_this$mutationCache$c8, error, this.state.variables, this.state.context, this));
        if (true) {
          this.logger.error(error);
        }
        await ((_this$options$onError = (_this$options4 = this.options).onError) == null ? void 0 : _this$options$onError.call(_this$options4, error, this.state.variables, this.state.context));
        await ((_this$mutationCache$c9 = (_this$mutationCache$c10 = this.mutationCache.config).onSettled) == null ? void 0 : _this$mutationCache$c9.call(_this$mutationCache$c10, void 0, error, this.state.variables, this.state.context, this));
        await ((_this$options$onSettl2 = (_this$options5 = this.options).onSettled) == null ? void 0 : _this$options$onSettl2.call(_this$options5, void 0, error, this.state.variables, this.state.context));
        throw error;
      } finally {
        this.dispatch({
          type: "error",
          error
        });
      }
    }
  }
  dispatch(action) {
    const reducer = (state) => {
      switch (action.type) {
        case "failed":
          return {
            ...state,
            failureCount: action.failureCount,
            failureReason: action.error
          };
        case "pause":
          return {
            ...state,
            isPaused: true
          };
        case "continue":
          return {
            ...state,
            isPaused: false
          };
        case "loading":
          return {
            ...state,
            context: action.context,
            data: void 0,
            failureCount: 0,
            failureReason: null,
            error: null,
            isPaused: !canFetch(this.options.networkMode),
            status: "loading",
            variables: action.variables
          };
        case "success":
          return {
            ...state,
            data: action.data,
            failureCount: 0,
            failureReason: null,
            error: null,
            status: "success",
            isPaused: false
          };
        case "error":
          return {
            ...state,
            data: void 0,
            error: action.error,
            failureCount: state.failureCount + 1,
            failureReason: action.error,
            isPaused: false,
            status: "error"
          };
        case "setState":
          return {
            ...state,
            ...action.state
          };
      }
    };
    this.state = reducer(this.state);
    notifyManager.batch(() => {
      this.observers.forEach((observer) => {
        observer.onMutationUpdate(action);
      });
      this.mutationCache.notify({
        mutation: this,
        type: "updated",
        action
      });
    });
  }
};
function getDefaultState2() {
  return {
    context: void 0,
    data: void 0,
    error: null,
    failureCount: 0,
    failureReason: null,
    isPaused: false,
    status: "idle",
    variables: void 0
  };
}

// node_modules/@tanstack/query-core/build/lib/mutationCache.mjs
var MutationCache = class extends Subscribable {
  constructor(config) {
    super();
    this.config = config || {};
    this.mutations = [];
    this.mutationId = 0;
  }
  build(client, options, state) {
    const mutation = new Mutation({
      mutationCache: this,
      logger: client.getLogger(),
      mutationId: ++this.mutationId,
      options: client.defaultMutationOptions(options),
      state,
      defaultOptions: options.mutationKey ? client.getMutationDefaults(options.mutationKey) : void 0
    });
    this.add(mutation);
    return mutation;
  }
  add(mutation) {
    this.mutations.push(mutation);
    this.notify({
      type: "added",
      mutation
    });
  }
  remove(mutation) {
    this.mutations = this.mutations.filter((x2) => x2 !== mutation);
    this.notify({
      type: "removed",
      mutation
    });
  }
  clear() {
    notifyManager.batch(() => {
      this.mutations.forEach((mutation) => {
        this.remove(mutation);
      });
    });
  }
  getAll() {
    return this.mutations;
  }
  find(filters) {
    if (typeof filters.exact === "undefined") {
      filters.exact = true;
    }
    return this.mutations.find((mutation) => matchMutation(filters, mutation));
  }
  findAll(filters) {
    return this.mutations.filter((mutation) => matchMutation(filters, mutation));
  }
  notify(event) {
    notifyManager.batch(() => {
      this.listeners.forEach(({
        listener
      }) => {
        listener(event);
      });
    });
  }
  resumePausedMutations() {
    var _this$resuming;
    this.resuming = ((_this$resuming = this.resuming) != null ? _this$resuming : Promise.resolve()).then(() => {
      const pausedMutations = this.mutations.filter((x2) => x2.state.isPaused);
      return notifyManager.batch(() => pausedMutations.reduce((promise, mutation) => promise.then(() => mutation.continue().catch(noop)), Promise.resolve()));
    }).then(() => {
      this.resuming = void 0;
    });
    return this.resuming;
  }
};

// node_modules/@tanstack/query-core/build/lib/infiniteQueryBehavior.mjs
function infiniteQueryBehavior() {
  return {
    onFetch: (context) => {
      context.fetchFn = () => {
        var _context$fetchOptions, _context$fetchOptions2, _context$fetchOptions3, _context$fetchOptions4, _context$state$data, _context$state$data2;
        const refetchPage = (_context$fetchOptions = context.fetchOptions) == null ? void 0 : (_context$fetchOptions2 = _context$fetchOptions.meta) == null ? void 0 : _context$fetchOptions2.refetchPage;
        const fetchMore = (_context$fetchOptions3 = context.fetchOptions) == null ? void 0 : (_context$fetchOptions4 = _context$fetchOptions3.meta) == null ? void 0 : _context$fetchOptions4.fetchMore;
        const pageParam = fetchMore == null ? void 0 : fetchMore.pageParam;
        const isFetchingNextPage = (fetchMore == null ? void 0 : fetchMore.direction) === "forward";
        const isFetchingPreviousPage = (fetchMore == null ? void 0 : fetchMore.direction) === "backward";
        const oldPages = ((_context$state$data = context.state.data) == null ? void 0 : _context$state$data.pages) || [];
        const oldPageParams = ((_context$state$data2 = context.state.data) == null ? void 0 : _context$state$data2.pageParams) || [];
        let newPageParams = oldPageParams;
        let cancelled = false;
        const addSignalProperty = (object) => {
          Object.defineProperty(object, "signal", {
            enumerable: true,
            get: () => {
              var _context$signal;
              if ((_context$signal = context.signal) != null && _context$signal.aborted) {
                cancelled = true;
              } else {
                var _context$signal2;
                (_context$signal2 = context.signal) == null ? void 0 : _context$signal2.addEventListener("abort", () => {
                  cancelled = true;
                });
              }
              return context.signal;
            }
          });
        };
        const queryFn = context.options.queryFn || (() => Promise.reject("Missing queryFn for queryKey '" + context.options.queryHash + "'"));
        const buildNewPages = (pages, param, page, previous) => {
          newPageParams = previous ? [param, ...newPageParams] : [...newPageParams, param];
          return previous ? [page, ...pages] : [...pages, page];
        };
        const fetchPage = (pages, manual, param, previous) => {
          if (cancelled) {
            return Promise.reject("Cancelled");
          }
          if (typeof param === "undefined" && !manual && pages.length) {
            return Promise.resolve(pages);
          }
          const queryFnContext = {
            queryKey: context.queryKey,
            pageParam: param,
            meta: context.options.meta
          };
          addSignalProperty(queryFnContext);
          const queryFnResult = queryFn(queryFnContext);
          const promise2 = Promise.resolve(queryFnResult).then((page) => buildNewPages(pages, param, page, previous));
          return promise2;
        };
        let promise;
        if (!oldPages.length) {
          promise = fetchPage([]);
        } else if (isFetchingNextPage) {
          const manual = typeof pageParam !== "undefined";
          const param = manual ? pageParam : getNextPageParam(context.options, oldPages);
          promise = fetchPage(oldPages, manual, param);
        } else if (isFetchingPreviousPage) {
          const manual = typeof pageParam !== "undefined";
          const param = manual ? pageParam : getPreviousPageParam(context.options, oldPages);
          promise = fetchPage(oldPages, manual, param, true);
        } else {
          newPageParams = [];
          const manual = typeof context.options.getNextPageParam === "undefined";
          const shouldFetchFirstPage = refetchPage && oldPages[0] ? refetchPage(oldPages[0], 0, oldPages) : true;
          promise = shouldFetchFirstPage ? fetchPage([], manual, oldPageParams[0]) : Promise.resolve(buildNewPages([], oldPageParams[0], oldPages[0]));
          for (let i2 = 1; i2 < oldPages.length; i2++) {
            promise = promise.then((pages) => {
              const shouldFetchNextPage = refetchPage && oldPages[i2] ? refetchPage(oldPages[i2], i2, oldPages) : true;
              if (shouldFetchNextPage) {
                const param = manual ? oldPageParams[i2] : getNextPageParam(context.options, pages);
                return fetchPage(pages, manual, param);
              }
              return Promise.resolve(buildNewPages(pages, oldPageParams[i2], oldPages[i2]));
            });
          }
        }
        const finalPromise = promise.then((pages) => ({
          pages,
          pageParams: newPageParams
        }));
        return finalPromise;
      };
    }
  };
}
function getNextPageParam(options, pages) {
  return options.getNextPageParam == null ? void 0 : options.getNextPageParam(pages[pages.length - 1], pages);
}
function getPreviousPageParam(options, pages) {
  return options.getPreviousPageParam == null ? void 0 : options.getPreviousPageParam(pages[0], pages);
}
function hasNextPage(options, pages) {
  if (options.getNextPageParam && Array.isArray(pages)) {
    const nextPageParam = getNextPageParam(options, pages);
    return typeof nextPageParam !== "undefined" && nextPageParam !== null && nextPageParam !== false;
  }
  return;
}
function hasPreviousPage(options, pages) {
  if (options.getPreviousPageParam && Array.isArray(pages)) {
    const previousPageParam = getPreviousPageParam(options, pages);
    return typeof previousPageParam !== "undefined" && previousPageParam !== null && previousPageParam !== false;
  }
  return;
}

// node_modules/@tanstack/query-core/build/lib/queryClient.mjs
var QueryClient = class {
  constructor(config = {}) {
    this.queryCache = config.queryCache || new QueryCache();
    this.mutationCache = config.mutationCache || new MutationCache();
    this.logger = config.logger || defaultLogger;
    this.defaultOptions = config.defaultOptions || {};
    this.queryDefaults = [];
    this.mutationDefaults = [];
    this.mountCount = 0;
    if (config.logger) {
      this.logger.error("Passing a custom logger has been deprecated and will be removed in the next major version.");
    }
  }
  mount() {
    this.mountCount++;
    if (this.mountCount !== 1)
      return;
    this.unsubscribeFocus = focusManager.subscribe(() => {
      if (focusManager.isFocused()) {
        this.resumePausedMutations();
        this.queryCache.onFocus();
      }
    });
    this.unsubscribeOnline = onlineManager.subscribe(() => {
      if (onlineManager.isOnline()) {
        this.resumePausedMutations();
        this.queryCache.onOnline();
      }
    });
  }
  unmount() {
    var _this$unsubscribeFocu, _this$unsubscribeOnli;
    this.mountCount--;
    if (this.mountCount !== 0)
      return;
    (_this$unsubscribeFocu = this.unsubscribeFocus) == null ? void 0 : _this$unsubscribeFocu.call(this);
    this.unsubscribeFocus = void 0;
    (_this$unsubscribeOnli = this.unsubscribeOnline) == null ? void 0 : _this$unsubscribeOnli.call(this);
    this.unsubscribeOnline = void 0;
  }
  isFetching(arg1, arg2) {
    const [filters] = parseFilterArgs(arg1, arg2);
    filters.fetchStatus = "fetching";
    return this.queryCache.findAll(filters).length;
  }
  isMutating(filters) {
    return this.mutationCache.findAll({
      ...filters,
      fetching: true
    }).length;
  }
  getQueryData(queryKey, filters) {
    var _this$queryCache$find;
    return (_this$queryCache$find = this.queryCache.find(queryKey, filters)) == null ? void 0 : _this$queryCache$find.state.data;
  }
  ensureQueryData(arg1, arg2, arg3) {
    const parsedOptions = parseQueryArgs(arg1, arg2, arg3);
    const cachedData = this.getQueryData(parsedOptions.queryKey);
    return cachedData ? Promise.resolve(cachedData) : this.fetchQuery(parsedOptions);
  }
  getQueriesData(queryKeyOrFilters) {
    return this.getQueryCache().findAll(queryKeyOrFilters).map(({
      queryKey,
      state
    }) => {
      const data = state.data;
      return [queryKey, data];
    });
  }
  setQueryData(queryKey, updater, options) {
    const query = this.queryCache.find(queryKey);
    const prevData = query == null ? void 0 : query.state.data;
    const data = functionalUpdate(updater, prevData);
    if (typeof data === "undefined") {
      return void 0;
    }
    const parsedOptions = parseQueryArgs(queryKey);
    const defaultedOptions = this.defaultQueryOptions(parsedOptions);
    return this.queryCache.build(this, defaultedOptions).setData(data, {
      ...options,
      manual: true
    });
  }
  setQueriesData(queryKeyOrFilters, updater, options) {
    return notifyManager.batch(() => this.getQueryCache().findAll(queryKeyOrFilters).map(({
      queryKey
    }) => [queryKey, this.setQueryData(queryKey, updater, options)]));
  }
  getQueryState(queryKey, filters) {
    var _this$queryCache$find2;
    return (_this$queryCache$find2 = this.queryCache.find(queryKey, filters)) == null ? void 0 : _this$queryCache$find2.state;
  }
  removeQueries(arg1, arg2) {
    const [filters] = parseFilterArgs(arg1, arg2);
    const queryCache = this.queryCache;
    notifyManager.batch(() => {
      queryCache.findAll(filters).forEach((query) => {
        queryCache.remove(query);
      });
    });
  }
  resetQueries(arg1, arg2, arg3) {
    const [filters, options] = parseFilterArgs(arg1, arg2, arg3);
    const queryCache = this.queryCache;
    const refetchFilters = {
      type: "active",
      ...filters
    };
    return notifyManager.batch(() => {
      queryCache.findAll(filters).forEach((query) => {
        query.reset();
      });
      return this.refetchQueries(refetchFilters, options);
    });
  }
  cancelQueries(arg1, arg2, arg3) {
    const [filters, cancelOptions = {}] = parseFilterArgs(arg1, arg2, arg3);
    if (typeof cancelOptions.revert === "undefined") {
      cancelOptions.revert = true;
    }
    const promises = notifyManager.batch(() => this.queryCache.findAll(filters).map((query) => query.cancel(cancelOptions)));
    return Promise.all(promises).then(noop).catch(noop);
  }
  invalidateQueries(arg1, arg2, arg3) {
    const [filters, options] = parseFilterArgs(arg1, arg2, arg3);
    return notifyManager.batch(() => {
      var _ref, _filters$refetchType;
      this.queryCache.findAll(filters).forEach((query) => {
        query.invalidate();
      });
      if (filters.refetchType === "none") {
        return Promise.resolve();
      }
      const refetchFilters = {
        ...filters,
        type: (_ref = (_filters$refetchType = filters.refetchType) != null ? _filters$refetchType : filters.type) != null ? _ref : "active"
      };
      return this.refetchQueries(refetchFilters, options);
    });
  }
  refetchQueries(arg1, arg2, arg3) {
    const [filters, options] = parseFilterArgs(arg1, arg2, arg3);
    const promises = notifyManager.batch(() => this.queryCache.findAll(filters).filter((query) => !query.isDisabled()).map((query) => {
      var _options$cancelRefetc;
      return query.fetch(void 0, {
        ...options,
        cancelRefetch: (_options$cancelRefetc = options == null ? void 0 : options.cancelRefetch) != null ? _options$cancelRefetc : true,
        meta: {
          refetchPage: filters.refetchPage
        }
      });
    }));
    let promise = Promise.all(promises).then(noop);
    if (!(options != null && options.throwOnError)) {
      promise = promise.catch(noop);
    }
    return promise;
  }
  fetchQuery(arg1, arg2, arg3) {
    const parsedOptions = parseQueryArgs(arg1, arg2, arg3);
    const defaultedOptions = this.defaultQueryOptions(parsedOptions);
    if (typeof defaultedOptions.retry === "undefined") {
      defaultedOptions.retry = false;
    }
    const query = this.queryCache.build(this, defaultedOptions);
    return query.isStaleByTime(defaultedOptions.staleTime) ? query.fetch(defaultedOptions) : Promise.resolve(query.state.data);
  }
  prefetchQuery(arg1, arg2, arg3) {
    return this.fetchQuery(arg1, arg2, arg3).then(noop).catch(noop);
  }
  fetchInfiniteQuery(arg1, arg2, arg3) {
    const parsedOptions = parseQueryArgs(arg1, arg2, arg3);
    parsedOptions.behavior = infiniteQueryBehavior();
    return this.fetchQuery(parsedOptions);
  }
  prefetchInfiniteQuery(arg1, arg2, arg3) {
    return this.fetchInfiniteQuery(arg1, arg2, arg3).then(noop).catch(noop);
  }
  resumePausedMutations() {
    return this.mutationCache.resumePausedMutations();
  }
  getQueryCache() {
    return this.queryCache;
  }
  getMutationCache() {
    return this.mutationCache;
  }
  getLogger() {
    return this.logger;
  }
  getDefaultOptions() {
    return this.defaultOptions;
  }
  setDefaultOptions(options) {
    this.defaultOptions = options;
  }
  setQueryDefaults(queryKey, options) {
    const result = this.queryDefaults.find((x2) => hashQueryKey(queryKey) === hashQueryKey(x2.queryKey));
    if (result) {
      result.defaultOptions = options;
    } else {
      this.queryDefaults.push({
        queryKey,
        defaultOptions: options
      });
    }
  }
  getQueryDefaults(queryKey) {
    if (!queryKey) {
      return void 0;
    }
    const firstMatchingDefaults = this.queryDefaults.find((x2) => partialMatchKey(queryKey, x2.queryKey));
    if (true) {
      const matchingDefaults = this.queryDefaults.filter((x2) => partialMatchKey(queryKey, x2.queryKey));
      if (matchingDefaults.length > 1) {
        this.logger.error("[QueryClient] Several query defaults match with key '" + JSON.stringify(queryKey) + "'. The first matching query defaults are used. Please check how query defaults are registered. Order does matter here. cf. https://react-query.tanstack.com/reference/QueryClient#queryclientsetquerydefaults.");
      }
    }
    return firstMatchingDefaults == null ? void 0 : firstMatchingDefaults.defaultOptions;
  }
  setMutationDefaults(mutationKey, options) {
    const result = this.mutationDefaults.find((x2) => hashQueryKey(mutationKey) === hashQueryKey(x2.mutationKey));
    if (result) {
      result.defaultOptions = options;
    } else {
      this.mutationDefaults.push({
        mutationKey,
        defaultOptions: options
      });
    }
  }
  getMutationDefaults(mutationKey) {
    if (!mutationKey) {
      return void 0;
    }
    const firstMatchingDefaults = this.mutationDefaults.find((x2) => partialMatchKey(mutationKey, x2.mutationKey));
    if (true) {
      const matchingDefaults = this.mutationDefaults.filter((x2) => partialMatchKey(mutationKey, x2.mutationKey));
      if (matchingDefaults.length > 1) {
        this.logger.error("[QueryClient] Several mutation defaults match with key '" + JSON.stringify(mutationKey) + "'. The first matching mutation defaults are used. Please check how mutation defaults are registered. Order does matter here. cf. https://react-query.tanstack.com/reference/QueryClient#queryclientsetmutationdefaults.");
      }
    }
    return firstMatchingDefaults == null ? void 0 : firstMatchingDefaults.defaultOptions;
  }
  defaultQueryOptions(options) {
    if (options != null && options._defaulted) {
      return options;
    }
    const defaultedOptions = {
      ...this.defaultOptions.queries,
      ...this.getQueryDefaults(options == null ? void 0 : options.queryKey),
      ...options,
      _defaulted: true
    };
    if (!defaultedOptions.queryHash && defaultedOptions.queryKey) {
      defaultedOptions.queryHash = hashQueryKeyByOptions(defaultedOptions.queryKey, defaultedOptions);
    }
    if (typeof defaultedOptions.refetchOnReconnect === "undefined") {
      defaultedOptions.refetchOnReconnect = defaultedOptions.networkMode !== "always";
    }
    if (typeof defaultedOptions.useErrorBoundary === "undefined") {
      defaultedOptions.useErrorBoundary = !!defaultedOptions.suspense;
    }
    return defaultedOptions;
  }
  defaultMutationOptions(options) {
    if (options != null && options._defaulted) {
      return options;
    }
    return {
      ...this.defaultOptions.mutations,
      ...this.getMutationDefaults(options == null ? void 0 : options.mutationKey),
      ...options,
      _defaulted: true
    };
  }
  clear() {
    this.queryCache.clear();
    this.mutationCache.clear();
  }
};

// node_modules/@tanstack/query-core/build/lib/queryObserver.mjs
var QueryObserver = class extends Subscribable {
  constructor(client, options) {
    super();
    this.client = client;
    this.options = options;
    this.trackedProps = /* @__PURE__ */ new Set();
    this.selectError = null;
    this.bindMethods();
    this.setOptions(options);
  }
  bindMethods() {
    this.remove = this.remove.bind(this);
    this.refetch = this.refetch.bind(this);
  }
  onSubscribe() {
    if (this.listeners.size === 1) {
      this.currentQuery.addObserver(this);
      if (shouldFetchOnMount(this.currentQuery, this.options)) {
        this.executeFetch();
      }
      this.updateTimers();
    }
  }
  onUnsubscribe() {
    if (!this.hasListeners()) {
      this.destroy();
    }
  }
  shouldFetchOnReconnect() {
    return shouldFetchOn(this.currentQuery, this.options, this.options.refetchOnReconnect);
  }
  shouldFetchOnWindowFocus() {
    return shouldFetchOn(this.currentQuery, this.options, this.options.refetchOnWindowFocus);
  }
  destroy() {
    this.listeners = /* @__PURE__ */ new Set();
    this.clearStaleTimeout();
    this.clearRefetchInterval();
    this.currentQuery.removeObserver(this);
  }
  setOptions(options, notifyOptions) {
    const prevOptions = this.options;
    const prevQuery = this.currentQuery;
    this.options = this.client.defaultQueryOptions(options);
    if (typeof (options == null ? void 0 : options.isDataEqual) !== "undefined") {
      this.client.getLogger().error("The isDataEqual option has been deprecated and will be removed in the next major version. You can achieve the same functionality by passing a function as the structuralSharing option");
    }
    if (!shallowEqualObjects(prevOptions, this.options)) {
      this.client.getQueryCache().notify({
        type: "observerOptionsUpdated",
        query: this.currentQuery,
        observer: this
      });
    }
    if (typeof this.options.enabled !== "undefined" && typeof this.options.enabled !== "boolean") {
      throw new Error("Expected enabled to be a boolean");
    }
    if (!this.options.queryKey) {
      this.options.queryKey = prevOptions.queryKey;
    }
    this.updateQuery();
    const mounted = this.hasListeners();
    if (mounted && shouldFetchOptionally(this.currentQuery, prevQuery, this.options, prevOptions)) {
      this.executeFetch();
    }
    this.updateResult(notifyOptions);
    if (mounted && (this.currentQuery !== prevQuery || this.options.enabled !== prevOptions.enabled || this.options.staleTime !== prevOptions.staleTime)) {
      this.updateStaleTimeout();
    }
    const nextRefetchInterval = this.computeRefetchInterval();
    if (mounted && (this.currentQuery !== prevQuery || this.options.enabled !== prevOptions.enabled || nextRefetchInterval !== this.currentRefetchInterval)) {
      this.updateRefetchInterval(nextRefetchInterval);
    }
  }
  getOptimisticResult(options) {
    const query = this.client.getQueryCache().build(this.client, options);
    const result = this.createResult(query, options);
    if (shouldAssignObserverCurrentProperties(this, result, options)) {
      this.currentResult = result;
      this.currentResultOptions = this.options;
      this.currentResultState = this.currentQuery.state;
    }
    return result;
  }
  getCurrentResult() {
    return this.currentResult;
  }
  trackResult(result) {
    const trackedResult = {};
    Object.keys(result).forEach((key) => {
      Object.defineProperty(trackedResult, key, {
        configurable: false,
        enumerable: true,
        get: () => {
          this.trackedProps.add(key);
          return result[key];
        }
      });
    });
    return trackedResult;
  }
  getCurrentQuery() {
    return this.currentQuery;
  }
  remove() {
    this.client.getQueryCache().remove(this.currentQuery);
  }
  refetch({
    refetchPage,
    ...options
  } = {}) {
    return this.fetch({
      ...options,
      meta: {
        refetchPage
      }
    });
  }
  fetchOptimistic(options) {
    const defaultedOptions = this.client.defaultQueryOptions(options);
    const query = this.client.getQueryCache().build(this.client, defaultedOptions);
    query.isFetchingOptimistic = true;
    return query.fetch().then(() => this.createResult(query, defaultedOptions));
  }
  fetch(fetchOptions) {
    var _fetchOptions$cancelR;
    return this.executeFetch({
      ...fetchOptions,
      cancelRefetch: (_fetchOptions$cancelR = fetchOptions.cancelRefetch) != null ? _fetchOptions$cancelR : true
    }).then(() => {
      this.updateResult();
      return this.currentResult;
    });
  }
  executeFetch(fetchOptions) {
    this.updateQuery();
    let promise = this.currentQuery.fetch(this.options, fetchOptions);
    if (!(fetchOptions != null && fetchOptions.throwOnError)) {
      promise = promise.catch(noop);
    }
    return promise;
  }
  updateStaleTimeout() {
    this.clearStaleTimeout();
    if (isServer || this.currentResult.isStale || !isValidTimeout(this.options.staleTime)) {
      return;
    }
    const time = timeUntilStale(this.currentResult.dataUpdatedAt, this.options.staleTime);
    const timeout = time + 1;
    this.staleTimeoutId = setTimeout(() => {
      if (!this.currentResult.isStale) {
        this.updateResult();
      }
    }, timeout);
  }
  computeRefetchInterval() {
    var _this$options$refetch;
    return typeof this.options.refetchInterval === "function" ? this.options.refetchInterval(this.currentResult.data, this.currentQuery) : (_this$options$refetch = this.options.refetchInterval) != null ? _this$options$refetch : false;
  }
  updateRefetchInterval(nextInterval) {
    this.clearRefetchInterval();
    this.currentRefetchInterval = nextInterval;
    if (isServer || this.options.enabled === false || !isValidTimeout(this.currentRefetchInterval) || this.currentRefetchInterval === 0) {
      return;
    }
    this.refetchIntervalId = setInterval(() => {
      if (this.options.refetchIntervalInBackground || focusManager.isFocused()) {
        this.executeFetch();
      }
    }, this.currentRefetchInterval);
  }
  updateTimers() {
    this.updateStaleTimeout();
    this.updateRefetchInterval(this.computeRefetchInterval());
  }
  clearStaleTimeout() {
    if (this.staleTimeoutId) {
      clearTimeout(this.staleTimeoutId);
      this.staleTimeoutId = void 0;
    }
  }
  clearRefetchInterval() {
    if (this.refetchIntervalId) {
      clearInterval(this.refetchIntervalId);
      this.refetchIntervalId = void 0;
    }
  }
  createResult(query, options) {
    const prevQuery = this.currentQuery;
    const prevOptions = this.options;
    const prevResult = this.currentResult;
    const prevResultState = this.currentResultState;
    const prevResultOptions = this.currentResultOptions;
    const queryChange = query !== prevQuery;
    const queryInitialState = queryChange ? query.state : this.currentQueryInitialState;
    const prevQueryResult = queryChange ? this.currentResult : this.previousQueryResult;
    const {
      state
    } = query;
    let {
      dataUpdatedAt,
      error,
      errorUpdatedAt,
      fetchStatus,
      status
    } = state;
    let isPreviousData = false;
    let isPlaceholderData = false;
    let data;
    if (options._optimisticResults) {
      const mounted = this.hasListeners();
      const fetchOnMount = !mounted && shouldFetchOnMount(query, options);
      const fetchOptionally = mounted && shouldFetchOptionally(query, prevQuery, options, prevOptions);
      if (fetchOnMount || fetchOptionally) {
        fetchStatus = canFetch(query.options.networkMode) ? "fetching" : "paused";
        if (!dataUpdatedAt) {
          status = "loading";
        }
      }
      if (options._optimisticResults === "isRestoring") {
        fetchStatus = "idle";
      }
    }
    if (options.keepPreviousData && !state.dataUpdatedAt && prevQueryResult != null && prevQueryResult.isSuccess && status !== "error") {
      data = prevQueryResult.data;
      dataUpdatedAt = prevQueryResult.dataUpdatedAt;
      status = prevQueryResult.status;
      isPreviousData = true;
    } else if (options.select && typeof state.data !== "undefined") {
      if (prevResult && state.data === (prevResultState == null ? void 0 : prevResultState.data) && options.select === this.selectFn) {
        data = this.selectResult;
      } else {
        try {
          this.selectFn = options.select;
          data = options.select(state.data);
          data = replaceData(prevResult == null ? void 0 : prevResult.data, data, options);
          this.selectResult = data;
          this.selectError = null;
        } catch (selectError) {
          if (true) {
            this.client.getLogger().error(selectError);
          }
          this.selectError = selectError;
        }
      }
    } else {
      data = state.data;
    }
    if (typeof options.placeholderData !== "undefined" && typeof data === "undefined" && status === "loading") {
      let placeholderData;
      if (prevResult != null && prevResult.isPlaceholderData && options.placeholderData === (prevResultOptions == null ? void 0 : prevResultOptions.placeholderData)) {
        placeholderData = prevResult.data;
      } else {
        placeholderData = typeof options.placeholderData === "function" ? options.placeholderData() : options.placeholderData;
        if (options.select && typeof placeholderData !== "undefined") {
          try {
            placeholderData = options.select(placeholderData);
            this.selectError = null;
          } catch (selectError) {
            if (true) {
              this.client.getLogger().error(selectError);
            }
            this.selectError = selectError;
          }
        }
      }
      if (typeof placeholderData !== "undefined") {
        status = "success";
        data = replaceData(prevResult == null ? void 0 : prevResult.data, placeholderData, options);
        isPlaceholderData = true;
      }
    }
    if (this.selectError) {
      error = this.selectError;
      data = this.selectResult;
      errorUpdatedAt = Date.now();
      status = "error";
    }
    const isFetching = fetchStatus === "fetching";
    const isLoading = status === "loading";
    const isError2 = status === "error";
    const result = {
      status,
      fetchStatus,
      isLoading,
      isSuccess: status === "success",
      isError: isError2,
      isInitialLoading: isLoading && isFetching,
      data,
      dataUpdatedAt,
      error,
      errorUpdatedAt,
      failureCount: state.fetchFailureCount,
      failureReason: state.fetchFailureReason,
      errorUpdateCount: state.errorUpdateCount,
      isFetched: state.dataUpdateCount > 0 || state.errorUpdateCount > 0,
      isFetchedAfterMount: state.dataUpdateCount > queryInitialState.dataUpdateCount || state.errorUpdateCount > queryInitialState.errorUpdateCount,
      isFetching,
      isRefetching: isFetching && !isLoading,
      isLoadingError: isError2 && state.dataUpdatedAt === 0,
      isPaused: fetchStatus === "paused",
      isPlaceholderData,
      isPreviousData,
      isRefetchError: isError2 && state.dataUpdatedAt !== 0,
      isStale: isStale(query, options),
      refetch: this.refetch,
      remove: this.remove
    };
    return result;
  }
  updateResult(notifyOptions) {
    const prevResult = this.currentResult;
    const nextResult = this.createResult(this.currentQuery, this.options);
    this.currentResultState = this.currentQuery.state;
    this.currentResultOptions = this.options;
    if (shallowEqualObjects(nextResult, prevResult)) {
      return;
    }
    this.currentResult = nextResult;
    const defaultNotifyOptions = {
      cache: true
    };
    const shouldNotifyListeners = () => {
      if (!prevResult) {
        return true;
      }
      const {
        notifyOnChangeProps
      } = this.options;
      const notifyOnChangePropsValue = typeof notifyOnChangeProps === "function" ? notifyOnChangeProps() : notifyOnChangeProps;
      if (notifyOnChangePropsValue === "all" || !notifyOnChangePropsValue && !this.trackedProps.size) {
        return true;
      }
      const includedProps = new Set(notifyOnChangePropsValue != null ? notifyOnChangePropsValue : this.trackedProps);
      if (this.options.useErrorBoundary) {
        includedProps.add("error");
      }
      return Object.keys(this.currentResult).some((key) => {
        const typedKey = key;
        const changed = this.currentResult[typedKey] !== prevResult[typedKey];
        return changed && includedProps.has(typedKey);
      });
    };
    if ((notifyOptions == null ? void 0 : notifyOptions.listeners) !== false && shouldNotifyListeners()) {
      defaultNotifyOptions.listeners = true;
    }
    this.notify({
      ...defaultNotifyOptions,
      ...notifyOptions
    });
  }
  updateQuery() {
    const query = this.client.getQueryCache().build(this.client, this.options);
    if (query === this.currentQuery) {
      return;
    }
    const prevQuery = this.currentQuery;
    this.currentQuery = query;
    this.currentQueryInitialState = query.state;
    this.previousQueryResult = this.currentResult;
    if (this.hasListeners()) {
      prevQuery == null ? void 0 : prevQuery.removeObserver(this);
      query.addObserver(this);
    }
  }
  onQueryUpdate(action) {
    const notifyOptions = {};
    if (action.type === "success") {
      notifyOptions.onSuccess = !action.manual;
    } else if (action.type === "error" && !isCancelledError(action.error)) {
      notifyOptions.onError = true;
    }
    this.updateResult(notifyOptions);
    if (this.hasListeners()) {
      this.updateTimers();
    }
  }
  notify(notifyOptions) {
    notifyManager.batch(() => {
      if (notifyOptions.onSuccess) {
        var _this$options$onSucce, _this$options, _this$options$onSettl, _this$options2;
        (_this$options$onSucce = (_this$options = this.options).onSuccess) == null ? void 0 : _this$options$onSucce.call(_this$options, this.currentResult.data);
        (_this$options$onSettl = (_this$options2 = this.options).onSettled) == null ? void 0 : _this$options$onSettl.call(_this$options2, this.currentResult.data, null);
      } else if (notifyOptions.onError) {
        var _this$options$onError, _this$options3, _this$options$onSettl2, _this$options4;
        (_this$options$onError = (_this$options3 = this.options).onError) == null ? void 0 : _this$options$onError.call(_this$options3, this.currentResult.error);
        (_this$options$onSettl2 = (_this$options4 = this.options).onSettled) == null ? void 0 : _this$options$onSettl2.call(_this$options4, void 0, this.currentResult.error);
      }
      if (notifyOptions.listeners) {
        this.listeners.forEach(({
          listener
        }) => {
          listener(this.currentResult);
        });
      }
      if (notifyOptions.cache) {
        this.client.getQueryCache().notify({
          query: this.currentQuery,
          type: "observerResultsUpdated"
        });
      }
    });
  }
};
function shouldLoadOnMount(query, options) {
  return options.enabled !== false && !query.state.dataUpdatedAt && !(query.state.status === "error" && options.retryOnMount === false);
}
function shouldFetchOnMount(query, options) {
  return shouldLoadOnMount(query, options) || query.state.dataUpdatedAt > 0 && shouldFetchOn(query, options, options.refetchOnMount);
}
function shouldFetchOn(query, options, field) {
  if (options.enabled !== false) {
    const value = typeof field === "function" ? field(query) : field;
    return value === "always" || value !== false && isStale(query, options);
  }
  return false;
}
function shouldFetchOptionally(query, prevQuery, options, prevOptions) {
  return options.enabled !== false && (query !== prevQuery || prevOptions.enabled === false) && (!options.suspense || query.state.status !== "error") && isStale(query, options);
}
function isStale(query, options) {
  return query.isStaleByTime(options.staleTime);
}
function shouldAssignObserverCurrentProperties(observer, optimisticResult, options) {
  if (options.keepPreviousData) {
    return false;
  }
  if (options.placeholderData !== void 0) {
    return optimisticResult.isPlaceholderData;
  }
  if (!shallowEqualObjects(observer.getCurrentResult(), optimisticResult)) {
    return true;
  }
  return false;
}

// node_modules/@tanstack/query-core/build/lib/infiniteQueryObserver.mjs
var InfiniteQueryObserver = class extends QueryObserver {
  // Type override
  // Type override
  // Type override
  // eslint-disable-next-line @typescript-eslint/no-useless-constructor
  constructor(client, options) {
    super(client, options);
  }
  bindMethods() {
    super.bindMethods();
    this.fetchNextPage = this.fetchNextPage.bind(this);
    this.fetchPreviousPage = this.fetchPreviousPage.bind(this);
  }
  setOptions(options, notifyOptions) {
    super.setOptions({
      ...options,
      behavior: infiniteQueryBehavior()
    }, notifyOptions);
  }
  getOptimisticResult(options) {
    options.behavior = infiniteQueryBehavior();
    return super.getOptimisticResult(options);
  }
  fetchNextPage({
    pageParam,
    ...options
  } = {}) {
    return this.fetch({
      ...options,
      meta: {
        fetchMore: {
          direction: "forward",
          pageParam
        }
      }
    });
  }
  fetchPreviousPage({
    pageParam,
    ...options
  } = {}) {
    return this.fetch({
      ...options,
      meta: {
        fetchMore: {
          direction: "backward",
          pageParam
        }
      }
    });
  }
  createResult(query, options) {
    var _state$fetchMeta, _state$fetchMeta$fetc, _state$fetchMeta2, _state$fetchMeta2$fet, _state$data, _state$data2;
    const {
      state
    } = query;
    const result = super.createResult(query, options);
    const {
      isFetching,
      isRefetching
    } = result;
    const isFetchingNextPage = isFetching && ((_state$fetchMeta = state.fetchMeta) == null ? void 0 : (_state$fetchMeta$fetc = _state$fetchMeta.fetchMore) == null ? void 0 : _state$fetchMeta$fetc.direction) === "forward";
    const isFetchingPreviousPage = isFetching && ((_state$fetchMeta2 = state.fetchMeta) == null ? void 0 : (_state$fetchMeta2$fet = _state$fetchMeta2.fetchMore) == null ? void 0 : _state$fetchMeta2$fet.direction) === "backward";
    return {
      ...result,
      fetchNextPage: this.fetchNextPage,
      fetchPreviousPage: this.fetchPreviousPage,
      hasNextPage: hasNextPage(options, (_state$data = state.data) == null ? void 0 : _state$data.pages),
      hasPreviousPage: hasPreviousPage(options, (_state$data2 = state.data) == null ? void 0 : _state$data2.pages),
      isFetchingNextPage,
      isFetchingPreviousPage,
      isRefetching: isRefetching && !isFetchingNextPage && !isFetchingPreviousPage
    };
  }
};

// node_modules/@tanstack/query-core/build/lib/mutationObserver.mjs
var MutationObserver = class extends Subscribable {
  constructor(client, options) {
    super();
    this.client = client;
    this.setOptions(options);
    this.bindMethods();
    this.updateResult();
  }
  bindMethods() {
    this.mutate = this.mutate.bind(this);
    this.reset = this.reset.bind(this);
  }
  setOptions(options) {
    var _this$currentMutation;
    const prevOptions = this.options;
    this.options = this.client.defaultMutationOptions(options);
    if (!shallowEqualObjects(prevOptions, this.options)) {
      this.client.getMutationCache().notify({
        type: "observerOptionsUpdated",
        mutation: this.currentMutation,
        observer: this
      });
    }
    (_this$currentMutation = this.currentMutation) == null ? void 0 : _this$currentMutation.setOptions(this.options);
  }
  onUnsubscribe() {
    if (!this.hasListeners()) {
      var _this$currentMutation2;
      (_this$currentMutation2 = this.currentMutation) == null ? void 0 : _this$currentMutation2.removeObserver(this);
    }
  }
  onMutationUpdate(action) {
    this.updateResult();
    const notifyOptions = {
      listeners: true
    };
    if (action.type === "success") {
      notifyOptions.onSuccess = true;
    } else if (action.type === "error") {
      notifyOptions.onError = true;
    }
    this.notify(notifyOptions);
  }
  getCurrentResult() {
    return this.currentResult;
  }
  reset() {
    this.currentMutation = void 0;
    this.updateResult();
    this.notify({
      listeners: true
    });
  }
  mutate(variables, options) {
    this.mutateOptions = options;
    if (this.currentMutation) {
      this.currentMutation.removeObserver(this);
    }
    this.currentMutation = this.client.getMutationCache().build(this.client, {
      ...this.options,
      variables: typeof variables !== "undefined" ? variables : this.options.variables
    });
    this.currentMutation.addObserver(this);
    return this.currentMutation.execute();
  }
  updateResult() {
    const state = this.currentMutation ? this.currentMutation.state : getDefaultState2();
    const result = {
      ...state,
      isLoading: state.status === "loading",
      isSuccess: state.status === "success",
      isError: state.status === "error",
      isIdle: state.status === "idle",
      mutate: this.mutate,
      reset: this.reset
    };
    this.currentResult = result;
  }
  notify(options) {
    notifyManager.batch(() => {
      if (this.mutateOptions && this.hasListeners()) {
        if (options.onSuccess) {
          var _this$mutateOptions$o, _this$mutateOptions, _this$mutateOptions$o2, _this$mutateOptions2;
          (_this$mutateOptions$o = (_this$mutateOptions = this.mutateOptions).onSuccess) == null ? void 0 : _this$mutateOptions$o.call(_this$mutateOptions, this.currentResult.data, this.currentResult.variables, this.currentResult.context);
          (_this$mutateOptions$o2 = (_this$mutateOptions2 = this.mutateOptions).onSettled) == null ? void 0 : _this$mutateOptions$o2.call(_this$mutateOptions2, this.currentResult.data, null, this.currentResult.variables, this.currentResult.context);
        } else if (options.onError) {
          var _this$mutateOptions$o3, _this$mutateOptions3, _this$mutateOptions$o4, _this$mutateOptions4;
          (_this$mutateOptions$o3 = (_this$mutateOptions3 = this.mutateOptions).onError) == null ? void 0 : _this$mutateOptions$o3.call(_this$mutateOptions3, this.currentResult.error, this.currentResult.variables, this.currentResult.context);
          (_this$mutateOptions$o4 = (_this$mutateOptions4 = this.mutateOptions).onSettled) == null ? void 0 : _this$mutateOptions$o4.call(_this$mutateOptions4, void 0, this.currentResult.error, this.currentResult.variables, this.currentResult.context);
        }
      }
      if (options.listeners) {
        this.listeners.forEach(({
          listener
        }) => {
          listener(this.currentResult);
        });
      }
    });
  }
};

// node_modules/@tanstack/react-query/build/lib/reactBatchedUpdates.mjs
var ReactDOM = __toESM(require_react_dom(), 1);
var unstable_batchedUpdates2 = ReactDOM.unstable_batchedUpdates;

// node_modules/@tanstack/react-query/build/lib/setBatchUpdatesFn.mjs
notifyManager.setBatchNotifyFunction(unstable_batchedUpdates2);

// node_modules/@tanstack/react-query/build/lib/useQueries.mjs
var React5 = __toESM(require_react(), 1);

// node_modules/@tanstack/react-query/build/lib/useSyncExternalStore.mjs
var import_shim = __toESM(require_shim(), 1);
var useSyncExternalStore = import_shim.useSyncExternalStore;

// node_modules/@tanstack/react-query/build/lib/QueryClientProvider.mjs
var React = __toESM(require_react(), 1);
var defaultContext = React.createContext(void 0);
var QueryClientSharingContext = React.createContext(false);
function getQueryClientContext(context, contextSharing) {
  if (context) {
    return context;
  }
  if (contextSharing && typeof window !== "undefined") {
    if (!window.ReactQueryClientContext) {
      window.ReactQueryClientContext = defaultContext;
    }
    return window.ReactQueryClientContext;
  }
  return defaultContext;
}
var useQueryClient = ({
  context
} = {}) => {
  const queryClient = React.useContext(getQueryClientContext(context, React.useContext(QueryClientSharingContext)));
  if (!queryClient) {
    throw new Error("No QueryClient set, use QueryClientProvider to set one");
  }
  return queryClient;
};
var QueryClientProvider = ({
  client,
  children,
  context,
  contextSharing = false
}) => {
  React.useEffect(() => {
    client.mount();
    return () => {
      client.unmount();
    };
  }, [client]);
  if (contextSharing) {
    client.getLogger().error("The contextSharing option has been deprecated and will be removed in the next major version");
  }
  const Context = getQueryClientContext(context, contextSharing);
  return React.createElement(QueryClientSharingContext.Provider, {
    value: !context && contextSharing
  }, React.createElement(Context.Provider, {
    value: client
  }, children));
};

// node_modules/@tanstack/react-query/build/lib/isRestoring.mjs
var React2 = __toESM(require_react(), 1);
var IsRestoringContext = React2.createContext(false);
var useIsRestoring = () => React2.useContext(IsRestoringContext);
var IsRestoringProvider = IsRestoringContext.Provider;

// node_modules/@tanstack/react-query/build/lib/QueryErrorResetBoundary.mjs
var React3 = __toESM(require_react(), 1);
function createValue() {
  let isReset = false;
  return {
    clearReset: () => {
      isReset = false;
    },
    reset: () => {
      isReset = true;
    },
    isReset: () => {
      return isReset;
    }
  };
}
var QueryErrorResetBoundaryContext = React3.createContext(createValue());
var useQueryErrorResetBoundary = () => React3.useContext(QueryErrorResetBoundaryContext);

// node_modules/@tanstack/react-query/build/lib/errorBoundaryUtils.mjs
var React4 = __toESM(require_react(), 1);

// node_modules/@tanstack/react-query/build/lib/utils.mjs
function shouldThrowError(_useErrorBoundary, params) {
  if (typeof _useErrorBoundary === "function") {
    return _useErrorBoundary(...params);
  }
  return !!_useErrorBoundary;
}

// node_modules/@tanstack/react-query/build/lib/errorBoundaryUtils.mjs
var ensurePreventErrorBoundaryRetry = (options, errorResetBoundary) => {
  if (options.suspense || options.useErrorBoundary) {
    if (!errorResetBoundary.isReset()) {
      options.retryOnMount = false;
    }
  }
};
var useClearResetErrorBoundary = (errorResetBoundary) => {
  React4.useEffect(() => {
    errorResetBoundary.clearReset();
  }, [errorResetBoundary]);
};
var getHasError = ({
  result,
  errorResetBoundary,
  useErrorBoundary,
  query
}) => {
  return result.isError && !errorResetBoundary.isReset() && !result.isFetching && shouldThrowError(useErrorBoundary, [result.error, query]);
};

// node_modules/@tanstack/react-query/build/lib/suspense.mjs
var ensureStaleTime = (defaultedOptions) => {
  if (defaultedOptions.suspense) {
    if (typeof defaultedOptions.staleTime !== "number") {
      defaultedOptions.staleTime = 1e3;
    }
  }
};
var willFetch = (result, isRestoring) => result.isLoading && result.isFetching && !isRestoring;
var shouldSuspend = (defaultedOptions, result, isRestoring) => (defaultedOptions == null ? void 0 : defaultedOptions.suspense) && willFetch(result, isRestoring);
var fetchOptimistic = (defaultedOptions, observer, errorResetBoundary) => observer.fetchOptimistic(defaultedOptions).then(({
  data
}) => {
  defaultedOptions.onSuccess == null ? void 0 : defaultedOptions.onSuccess(data);
  defaultedOptions.onSettled == null ? void 0 : defaultedOptions.onSettled(data, null);
}).catch((error) => {
  errorResetBoundary.clearReset();
  defaultedOptions.onError == null ? void 0 : defaultedOptions.onError(error);
  defaultedOptions.onSettled == null ? void 0 : defaultedOptions.onSettled(void 0, error);
});

// node_modules/@tanstack/react-query/build/lib/useBaseQuery.mjs
var React6 = __toESM(require_react(), 1);
function useBaseQuery(options, Observer) {
  const queryClient = useQueryClient({
    context: options.context
  });
  const isRestoring = useIsRestoring();
  const errorResetBoundary = useQueryErrorResetBoundary();
  const defaultedOptions = queryClient.defaultQueryOptions(options);
  defaultedOptions._optimisticResults = isRestoring ? "isRestoring" : "optimistic";
  if (defaultedOptions.onError) {
    defaultedOptions.onError = notifyManager.batchCalls(defaultedOptions.onError);
  }
  if (defaultedOptions.onSuccess) {
    defaultedOptions.onSuccess = notifyManager.batchCalls(defaultedOptions.onSuccess);
  }
  if (defaultedOptions.onSettled) {
    defaultedOptions.onSettled = notifyManager.batchCalls(defaultedOptions.onSettled);
  }
  ensureStaleTime(defaultedOptions);
  ensurePreventErrorBoundaryRetry(defaultedOptions, errorResetBoundary);
  useClearResetErrorBoundary(errorResetBoundary);
  const [observer] = React6.useState(() => new Observer(queryClient, defaultedOptions));
  const result = observer.getOptimisticResult(defaultedOptions);
  useSyncExternalStore(React6.useCallback((onStoreChange) => {
    const unsubscribe = isRestoring ? () => void 0 : observer.subscribe(notifyManager.batchCalls(onStoreChange));
    observer.updateResult();
    return unsubscribe;
  }, [observer, isRestoring]), () => observer.getCurrentResult(), () => observer.getCurrentResult());
  React6.useEffect(() => {
    observer.setOptions(defaultedOptions, {
      listeners: false
    });
  }, [defaultedOptions, observer]);
  if (shouldSuspend(defaultedOptions, result, isRestoring)) {
    throw fetchOptimistic(defaultedOptions, observer, errorResetBoundary);
  }
  if (getHasError({
    result,
    errorResetBoundary,
    useErrorBoundary: defaultedOptions.useErrorBoundary,
    query: observer.getCurrentQuery()
  })) {
    throw result.error;
  }
  return !defaultedOptions.notifyOnChangeProps ? observer.trackResult(result) : result;
}

// node_modules/@tanstack/react-query/build/lib/useQuery.mjs
function useQuery(arg1, arg2, arg3) {
  const parsedOptions = parseQueryArgs(arg1, arg2, arg3);
  return useBaseQuery(parsedOptions, QueryObserver);
}

// node_modules/@tanstack/react-query/build/lib/Hydrate.mjs
var React7 = __toESM(require_react(), 1);

// node_modules/@tanstack/react-query/build/lib/useIsFetching.mjs
var React8 = __toESM(require_react(), 1);

// node_modules/@tanstack/react-query/build/lib/useIsMutating.mjs
var React9 = __toESM(require_react(), 1);

// node_modules/@tanstack/react-query/build/lib/useMutation.mjs
var React10 = __toESM(require_react(), 1);
function useMutation(arg1, arg2, arg3) {
  const options = parseMutationArgs(arg1, arg2, arg3);
  const queryClient = useQueryClient({
    context: options.context
  });
  const [observer] = React10.useState(() => new MutationObserver(queryClient, options));
  React10.useEffect(() => {
    observer.setOptions(options);
  }, [observer, options]);
  const result = useSyncExternalStore(React10.useCallback((onStoreChange) => observer.subscribe(notifyManager.batchCalls(onStoreChange)), [observer]), () => observer.getCurrentResult(), () => observer.getCurrentResult());
  const mutate = React10.useCallback((variables, mutateOptions) => {
    observer.mutate(variables, mutateOptions).catch(noop2);
  }, [observer]);
  if (result.error && shouldThrowError(observer.options.useErrorBoundary, [result.error])) {
    throw result.error;
  }
  return {
    ...result,
    mutate,
    mutateAsync: result.mutate
  };
}
function noop2() {
}

// node_modules/@tanstack/react-query/build/lib/useInfiniteQuery.mjs
function useInfiniteQuery(arg1, arg2, arg3) {
  const options = parseQueryArgs(arg1, arg2, arg3);
  return useBaseQuery(options, InfiniteQueryObserver);
}

// node_modules/@refinedev/devtools-internal/dist/esm/index.js
var import_error_stack_parser = __toESM(require_error_stack_parser());
var import_react = __toESM(require_react());
var v = "renderWithHooks";
var m = (r) => {
  let e = r.findIndex((s) => s.functionName === v);
  return e !== -1 ? r.slice(0, e) : r;
};
var i = false ? /node_modules\/refinedev\/(?<name>.*?)\// : /\/refine\/packages\/(?<name>.*?)\//;
var y = (r) => r ? !!r.match(i) : false;
var d2 = (r) => {
  var n;
  if (!r)
    return;
  let e = r.match(i), s = (n = e == null ? void 0 : e.groups) == null ? void 0 : n.name;
  if (s)
    return `@refinedev/${s}`;
};
function p2() {
  if (false)
    return [];
  try {
    let r = new Error(), e = import_error_stack_parser.default.parse(r);
    return m(e).map((t) => ({ file: t.fileName, line: t.lineNumber, column: t.columnNumber, function: t.functionName, isRefine: y(t.fileName), packageName: d2(t.fileName) })).filter((t) => t.function).slice(1);
  } catch {
    return [];
  }
}
var E = (r, e) => {
  if (p[r] === "auth")
    return null;
  if (r === "useCan")
    return e ? "key[1].resource" : "key[1]";
  if (p[r] === "audit-log")
    return r === "useLog" ? "variables.resource" : "key[1]";
  if (p[r] === "data") {
    if (r === "useCustom" || r === "useCustomMutation")
      return null;
    switch (r) {
      case "useList":
      case "useInfiniteList":
      case "useOne":
      case "useMany":
        return e ? "key[1]" : "key[2]";
      case "useCreate":
      case "useCreateMany":
      case "useDelete":
      case "useDeleteMany":
      case "useUpdate":
      case "useUpdateMany":
        return "variables.resource";
    }
  }
  return null;
};
function P(r, e) {
  if (false)
    return { hookName: "", trace: [], resourcePath: null, legacyKey: false };
  {
    let s = p2().slice(1), n = E(r, e);
    return { hookName: r, trace: s, resourcePath: n, legacyKey: e };
  }
}
var l = (r, e) => {
  let s = e == null ? void 0 : e.map((t) => `${t.file}:${t.line}:${t.column}#${t.function}-${t.packageName}-${t.isRefine ? 1 : 0}`);
  return JSON.stringify([...r ?? [], ...s ?? []]);
};
var R = (r) => (e) => {
  var n;
  if (!((n = e == null ? void 0 : e.meta) != null && n.trace))
    return;
  let s = e == null ? void 0 : e.meta;
  new Promise((t) => {
    var a, o2;
    d(r, T.ACTIVITY, { type: "mutation", identifier: l(e == null ? void 0 : e.options.mutationKey, (a = e == null ? void 0 : e.meta) == null ? void 0 : a.trace), key: e == null ? void 0 : e.options.mutationKey, status: e == null ? void 0 : e.state.status, state: e == null ? void 0 : e.state, variables: (o2 = e == null ? void 0 : e.state) == null ? void 0 : o2.variables, ...s }), t();
  });
};
var g = (r) => (e) => {
  var n;
  if (!((n = e == null ? void 0 : e.meta) != null && n.trace))
    return;
  let s = e == null ? void 0 : e.meta;
  new Promise((t) => {
    var a;
    d(r, T.ACTIVITY, { type: "query", identifier: l(e.queryKey, (a = e.meta) == null ? void 0 : a.trace), key: e.queryKey, status: e.state.status, state: e.state, ...s }), t();
  });
};
var C = false ? () => ({}) : (r) => {
  let { ws: e } = (0, import_react.useContext)(O), s = import_react.default.useRef(), n = import_react.default.useRef();
  return import_react.default.useEffect(() => {
    if (!e)
      return () => 0;
    let t = r.getQueryCache(), a = g(e);
    return t.getAll().forEach(a), s.current = t.subscribe(({ query: o2, type: c }) => (c === "added" || c === "updated") && a(o2)), () => {
      var o2;
      (o2 = s.current) == null || o2.call(s);
    };
  }, [e, r]), import_react.default.useEffect(() => {
    if (!e)
      return () => 0;
    let t = r.getMutationCache(), a = R(e);
    return t.getAll().forEach(a), n.current = t.subscribe(({ mutation: o2, type: c }) => (c === "added" || c === "updated") && a(o2)), () => {
      var o2;
      (o2 = n.current) == null || o2.call(n);
    };
  }, [e, r]), {};
};

// node_modules/@refinedev/core/dist/esm/index.js
var import_qs = __toESM(require_lib());
var import_warn_once = __toESM(require_warn_once());
var import_pluralize = __toESM(require_pluralize());
var import_react3 = __toESM(require_react());
var import_pluralize2 = __toESM(require_pluralize());
var import_react4 = __toESM(require_react());
var import_react5 = __toESM(require_react());
var import_react6 = __toESM(require_react());
var import_react7 = __toESM(require_react());
var import_react8 = __toESM(require_react());
var import_react9 = __toESM(require_react());
var import_react10 = __toESM(require_react());
var import_react11 = __toESM(require_react());
var import_react12 = __toESM(require_react());
var import_react13 = __toESM(require_react());
var import_qs2 = __toESM(require_lib());
var import_react14 = __toESM(require_react());
var import_qs3 = __toESM(require_lib());
var import_react15 = __toESM(require_react());
var import_react16 = __toESM(require_react());
var import_react17 = __toESM(require_react());
var import_react18 = __toESM(require_react());
var import_react19 = __toESM(require_react());
var import_react20 = __toESM(require_react());
var import_react21 = __toESM(require_react());
var import_react22 = __toESM(require_react());
var import_react23 = __toESM(require_react());
var import_react24 = __toESM(require_react());
var import_react25 = __toESM(require_react());
var import_react26 = __toESM(require_react());
var import_react27 = __toESM(require_react());
var import_react28 = __toESM(require_react());
var import_react29 = __toESM(require_react());
var import_react30 = __toESM(require_react());
var import_react31 = __toESM(require_react());
var import_react32 = __toESM(require_react());
var import_react33 = __toESM(require_react());
var import_react34 = __toESM(require_react());
var import_react35 = __toESM(require_react());
var import_react36 = __toESM(require_react());
var import_react37 = __toESM(require_react());
var import_react38 = __toESM(require_react());
var import_react39 = __toESM(require_react());
var import_react40 = __toESM(require_react());
var import_react41 = __toESM(require_react());
var import_react42 = __toESM(require_react());
var import_papaparse = __toESM(require_papaparse_min());
var import_warn_once2 = __toESM(require_warn_once());
var import_react43 = __toESM(require_react());
var import_warn_once3 = __toESM(require_warn_once());
var import_react44 = __toESM(require_react());
var import_react45 = __toESM(require_react());
var import_react46 = __toESM(require_react());
var import_react47 = __toESM(require_react());
var import_react48 = __toESM(require_react());
var import_warn_once4 = __toESM(require_warn_once());
var import_react49 = __toESM(require_react());
var import_papaparse2 = __toESM(require_papaparse_min());
var import_react50 = __toESM(require_react());
var import_react51 = __toESM(require_react());
var import_react52 = __toESM(require_react());
var import_react53 = __toESM(require_react());
var import_react54 = __toESM(require_react());
var import_react55 = __toESM(require_react());
var import_react56 = __toESM(require_react());
var import_react57 = __toESM(require_react());
var import_react58 = __toESM(require_react());
var import_qs4 = __toESM(require_lib());
var import_warn_once5 = __toESM(require_warn_once());
var import_react59 = __toESM(require_react());
var import_react60 = __toESM(require_react());
var import_react61 = __toESM(require_react());
var import_react62 = __toESM(require_react());
var import_warn_once6 = __toESM(require_warn_once());
var import_react63 = __toESM(require_react());
var import_react64 = __toESM(require_react());
var import_react65 = __toESM(require_react());
var import_react66 = __toESM(require_react());
var import_react67 = __toESM(require_react());
var import_react68 = __toESM(require_react());
var import_react69 = __toESM(require_react());
var import_react70 = __toESM(require_react());
var import_react71 = __toESM(require_react());
var import_react72 = __toESM(require_react());
var import_react73 = __toESM(require_react());
var import_react74 = __toESM(require_react());
var import_react75 = __toESM(require_react());
var import_react76 = __toESM(require_react());
var import_react77 = __toESM(require_react());
var import_react78 = __toESM(require_react());
var import_react79 = __toESM(require_react());
var import_react80 = __toESM(require_react());
var import_react81 = __toESM(require_react());
var import_react82 = __toESM(require_react());
var import_react83 = __toESM(require_react());
var import_react84 = __toESM(require_react());
var vn = Object.defineProperty;
var o = (e, t) => vn(e, "name", { value: t, configurable: true });
var Bt = o((e) => e / 1e3, "userFriendlySecond");
var tr = o((e, t = (r) => r) => {
  let [r, ...s] = e;
  return s.map((n) => fromPairs_default(zip_default(r, n))).map((n, i2, a) => t.call(void 0, n, i2, a));
}, "importCSVMapper");
var rr = o((e = "", t) => {
  let r = Nt(e);
  return t === "singular" ? import_pluralize.default.singular(r) : import_pluralize.default.plural(r);
}, "userFriendlyResourceName");
var Lo = o((e = {}) => e != null && e.id ? { ...e, id: decodeURIComponent(e.id) } : e, "handleUseParams");
var S = o((...e) => e.find((t) => typeof t < "u"), "pickNotDeprecated");
function Ye(e, t) {
  return e.findIndex((r, s) => s <= e.length - t.length && t.every((n, i2) => e[s + i2] === n));
}
o(Ye, "arrayFindIndex");
function En(e) {
  if (e[0] === "data") {
    let t = e.slice(1);
    if (t[2] === "many")
      t[2] = "getMany";
    else if (t[2] === "infinite")
      t[2] = "list";
    else if (t[2] === "one")
      t[2] = "detail";
    else if (t[1] === "custom") {
      let r = { ...t[2] };
      return delete r.method, delete r.url, [t[0], t[1], t[2].method, t[2].url, r];
    }
    return t;
  }
  if (e[0] === "audit" && e[2] === "list")
    return ["logList", e[1], e[3]];
  if (e[0] === "access" && e.length === 4)
    return ["useCan", { resource: e[1], action: e[2], ...e[3] }];
  if (e[0] === "auth") {
    if (Ye(e, ["auth", "login"]) !== -1)
      return ["useLogin"];
    if (Ye(e, ["auth", "logout"]) !== -1)
      return ["useLogout"];
    if (Ye(e, ["auth", "identity"]) !== -1)
      return ["getUserIdentity"];
    if (Ye(e, ["auth", "register"]) !== -1)
      return ["useRegister"];
    if (Ye(e, ["auth", "forgotPassword"]) !== -1)
      return ["useForgotPassword"];
    if (Ye(e, ["auth", "check"]) !== -1)
      return ["useAuthenticated", e[2]];
    if (Ye(e, ["auth", "onError"]) !== -1)
      return ["useCheckError"];
    if (Ye(e, ["auth", "permissions"]) !== -1)
      return ["usePermissions"];
    if (Ye(e, ["auth", "updatePassword"]) !== -1)
      return ["useUpdatePassword"];
  }
  return e;
}
o(En, "convertToLegacy");
var Ee = class {
  constructor(t = []) {
    this.segments = [];
    this.segments = t;
  }
  key() {
    return this.segments;
  }
  legacy() {
    return En(this.segments);
  }
  get(t) {
    return t ? this.legacy() : this.segments;
  }
};
o(Ee, "BaseKeyBuilder");
var We = class extends Ee {
  constructor(t) {
    super(t);
  }
  params(t) {
    return new Ee([...this.segments, t]);
  }
};
o(We, "ParamsKeyBuilder");
var or = class extends Ee {
  constructor(t) {
    super(t);
  }
  id(t) {
    return new We([...this.segments, t ? String(t) : void 0]);
  }
};
o(or, "DataIdRequiringKeyBuilder");
var sr = class extends Ee {
  constructor(t) {
    super(t);
  }
  ids(...t) {
    return new We([...this.segments, ...t.length ? [t.map((r) => String(r))] : []]);
  }
};
o(sr, "DataIdsRequiringKeyBuilder");
var nr = class extends Ee {
  constructor(t) {
    super(t);
  }
  action(t) {
    if (t === "one")
      return new or([...this.segments, t]);
    if (t === "many")
      return new sr([...this.segments, t]);
    if (["list", "infinite"].includes(t))
      return new We([...this.segments, t]);
    throw new Error("Invalid action type");
  }
};
o(nr, "DataResourceKeyBuilder");
var ar = class extends Ee {
  constructor(t) {
    super(t);
  }
  resource(t) {
    return new nr([...this.segments, t]);
  }
  mutation(t) {
    return new We([...t === "custom" ? this.segments : [this.segments[0]], t]);
  }
};
o(ar, "DataKeyBuilder");
var ir = class extends Ee {
  constructor(t) {
    super(t);
  }
  action(t) {
    return new We([...this.segments, t]);
  }
};
o(ir, "AuthKeyBuilder");
var ur = class extends Ee {
  constructor(t) {
    super(t);
  }
  action(t) {
    return new We([...this.segments, t]);
  }
};
o(ur, "AccessResourceKeyBuilder");
var cr = class extends Ee {
  constructor(t) {
    super(t);
  }
  resource(t) {
    return new ur([...this.segments, t]);
  }
};
o(cr, "AccessKeyBuilder");
var pr = class extends Ee {
  constructor(t) {
    super(t);
  }
  action(t) {
    return new We([...this.segments, t]);
  }
};
o(pr, "AuditActionKeyBuilder");
var dr = class extends Ee {
  constructor(t) {
    super(t);
  }
  resource(t) {
    return new pr([...this.segments, t]);
  }
  action(t) {
    return new We([...this.segments, t]);
  }
};
o(dr, "AuditKeyBuilder");
var bt = class extends Ee {
  constructor(t) {
    super(t);
  }
  data(t) {
    return new ar(["data", t || "default"]);
  }
  auth() {
    return new ir(["auth"]);
  }
  access() {
    return new cr(["access"]);
  }
  audit() {
    return new dr(["audit"]);
  }
};
o(bt, "KeyBuilder");
var Oe = o(() => new bt([]), "keys");
var Eo = o((e, t, r, s) => {
  let n = t || "default", i2 = { all: [n], resourceAll: [n, e || ""], list: (a) => [...i2.resourceAll, "list", { ...a, ...S(r, s) || {} }], many: (a) => [...i2.resourceAll, "getMany", a && a.map(String), { ...S(r, s) || {} }].filter((u) => u !== void 0), detail: (a) => [...i2.resourceAll, "detail", a == null ? void 0 : a.toString(), { ...S(r, s) || {} }], logList: (a) => ["logList", e, a, s].filter((u) => u !== void 0) };
  return i2;
}, "queryKeys");
var Je = o((e) => (t, r, s, n) => {
  let i2 = r || "default";
  return { all: Oe().data(i2).get(e), resourceAll: Oe().data(r).resource(t ?? "").get(e), list: (u) => Oe().data(r).resource(t ?? "").action("list").params({ ...u, ...S(s, n) || {} }).get(e), many: (u) => Oe().data(r).resource(t ?? "").action("many").ids(...u ?? []).params({ ...S(s, n) || {} }).get(e), detail: (u) => Oe().data(r).resource(t ?? "").action("one").id(u ?? "").params({ ...S(s, n) || {} }).get(e), logList: (u) => [...Oe().audit().resource(t).action("list").params(u).get(e), n].filter((p3) => p3 !== void 0) };
}, "queryKeysReplacement");
var Kr = o((e, t) => !e || !t ? false : !!e.find((r) => r === t), "hasPermission");
var vt = o((e) => e.startsWith(":"), "isParameter");
var Xe = o((e) => e.split("/").filter((r) => r !== ""), "splitToSegments");
var Uo = o((e, t) => {
  let r = Xe(e), s = Xe(t);
  return r.length === s.length;
}, "isSegmentCountsSame");
var he = o((e) => e.replace(/^\/|\/$/g, ""), "removeLeadingTrailingSlashes");
var Mo = o((e, t) => {
  let r = he(e), s = he(t);
  if (!Uo(r, s))
    return false;
  let n = Xe(r);
  return Xe(s).every((a, u) => vt(a) || a === n[u]);
}, "checkBySegments");
var wo = o((e, t, r) => {
  let s = he(r || ""), n = `${s}${s ? "/" : ""}${e}`;
  return t === "list" ? n = `${n}` : t === "create" ? n = `${n}/create` : t === "edit" ? n = `${n}/edit/:id` : t === "show" ? n = `${n}/show/:id` : t === "clone" && (n = `${n}/clone/:id`), `/${n.replace(/^\//, "")}`;
}, "getDefaultActionPath");
var Ae = o((e, t) => {
  var n, i2;
  let r = S((n = e.meta) == null ? void 0 : n.parent, (i2 = e.options) == null ? void 0 : i2.parent, e.parentName);
  return r ? t.find((a) => (a.identifier ?? a.name) === r) ?? { name: r } : void 0;
}, "getParentResource");
var Kt = o((e, t, r) => {
  let s = [], n = Ae(e, t);
  for (; n; )
    s.push(n), n = Ae(n, t);
  if (s.length !== 0)
    return `/${s.reverse().map((i2) => {
      var u;
      let a = r ? ((u = i2.options) == null ? void 0 : u.route) ?? i2.name : i2.name;
      return he(a);
    }).join("/")}`;
}, "getParentPrefixForResource");
var Pe = o((e, t, r) => {
  let s = [], n = ["list", "show", "edit", "create", "clone"], i2 = Kt(e, t, r);
  return n.forEach((a) => {
    var c, d3;
    let u = r && a === "clone" ? e.create : e[a], p3;
    typeof u == "function" || r ? p3 = wo(r ? ((c = e.meta) == null ? void 0 : c.route) ?? ((d3 = e.options) == null ? void 0 : d3.route) ?? e.name : e.name, a, r ? i2 : void 0) : typeof u == "string" ? p3 = u : typeof u == "object" && (p3 = u.path), p3 && s.push({ action: a, resource: e, route: `/${p3.replace(/^\//, "")}` });
  }), s;
}, "getActionRoutesFromResource");
var Io = o((e) => {
  var n;
  if (e.length === 0)
    return;
  if (e.length === 1)
    return e[0];
  let t = e.map((i2) => ({ ...i2, splitted: Xe(he(i2.route)) })), r = ((n = t[0]) == null ? void 0 : n.splitted.length) ?? 0, s = [...t];
  for (let i2 = 0; i2 < r; i2++) {
    let a = s.filter((u) => !vt(u.splitted[i2]));
    if (a.length !== 0) {
      if (a.length === 1) {
        s = a;
        break;
      }
      s = a;
    }
  }
  return s[0];
}, "pickMatchedRoute");
var So = o((e, t) => {
  let s = t.flatMap((i2) => Pe(i2, t)).filter((i2) => Mo(e, i2.route)), n = Io(s);
  return { found: !!n, resource: n == null ? void 0 : n.resource, action: n == null ? void 0 : n.action, matchedRoute: n == null ? void 0 : n.route };
}, "matchResourceFromRoute");
var lr = o((e, t) => {
  var n;
  let r, s = Kt(e, t, true);
  if (s) {
    let i2 = S(e.meta, e.options);
    r = `${s}/${(i2 == null ? void 0 : i2.route) ?? e.name}`;
  } else
    r = ((n = e.options) == null ? void 0 : n.route) ?? e.name;
  return `/${r.replace(/^\//, "")}`;
}, "routeGenerator");
var Fo = o((e) => {
  var a;
  let t = [], r = {}, s = {}, n, i2;
  for (let u = 0; u < e.length; u++) {
    n = e[u];
    let p3 = n.route ?? ((a = S(n == null ? void 0 : n.meta, n.options)) == null ? void 0 : a.route) ?? "";
    r[p3] = n, r[p3].children = [], s[n.name] = n, s[n.name].children = [];
  }
  for (let u in r)
    r.hasOwnProperty(u) && (i2 = r[u], i2.parentName && s[i2.parentName] ? s[i2.parentName].children.push(i2) : t.push(i2));
  return t;
}, "createTreeView");
var Nt = o((e) => (e = e.replace(/([a-z]{1})([A-Z]{1})/g, "$1-$2"), e = e.replace(/([A-Z]{1})([A-Z]{1})([a-z]{1})/g, "$1-$2$3"), e = e.toLowerCase().replace(/[_-]+/g, " ").replace(/\s{2,}/g, " ").trim(), e = e.charAt(0).toUpperCase() + e.slice(1), e), "humanizeString");
var Gr = o(({ children: e }) => import_react4.default.createElement("div", null, e), "DefaultLayout");
var Ue = { mutationMode: "pessimistic", syncWithLocation: false, undoableTimeout: 5e3, warnWhenUnsavedChanges: false, liveMode: "off", redirect: { afterCreate: "list", afterClone: "list", afterEdit: "list" }, overtime: { interval: 1e3 }, textTransformers: { humanize: Nt, plural: import_pluralize2.default.plural, singular: import_pluralize2.default.singular }, disableServerSideValidation: false };
var Me = import_react3.default.createContext({ hasDashboard: false, mutationMode: "pessimistic", warnWhenUnsavedChanges: false, syncWithLocation: false, undoableTimeout: 5e3, Title: void 0, Sider: void 0, Header: void 0, Footer: void 0, Layout: Gr, OffLayoutArea: void 0, liveMode: "off", onLiveEvent: void 0, options: Ue });
var Qo = o(({ hasDashboard: e, mutationMode: t, warnWhenUnsavedChanges: r, syncWithLocation: s, undoableTimeout: n, children: i2, DashboardPage: a, Title: u, Layout: p3 = Gr, Header: c, Sider: d3, Footer: f2, OffLayoutArea: R2, LoginPage: g2 = Hr, catchAll: x2, liveMode: E2 = "off", onLiveEvent: w, options: m2 }) => import_react3.default.createElement(Me.Provider, { value: { __initialized: true, hasDashboard: e, mutationMode: t, warnWhenUnsavedChanges: r, syncWithLocation: s, Title: u, undoableTimeout: n, Layout: p3, Header: c, Sider: d3, Footer: f2, OffLayoutArea: R2, DashboardPage: a, LoginPage: g2, catchAll: x2, liveMode: E2, onLiveEvent: w, options: m2 } }, i2), "RefineContextProvider");
var $r = o(({ options: e, disableTelemetry: t, liveMode: r, mutationMode: s, reactQueryClientConfig: n, reactQueryDevtoolConfig: i2, syncWithLocation: a, undoableTimeout: u, warnWhenUnsavedChanges: p3 } = {}) => {
  var R2, g2, x2, E2, w, m2, h, D;
  let c = { breadcrumb: e == null ? void 0 : e.breadcrumb, mutationMode: (e == null ? void 0 : e.mutationMode) ?? s ?? Ue.mutationMode, undoableTimeout: (e == null ? void 0 : e.undoableTimeout) ?? u ?? Ue.undoableTimeout, syncWithLocation: (e == null ? void 0 : e.syncWithLocation) ?? a ?? Ue.syncWithLocation, warnWhenUnsavedChanges: (e == null ? void 0 : e.warnWhenUnsavedChanges) ?? p3 ?? Ue.warnWhenUnsavedChanges, liveMode: (e == null ? void 0 : e.liveMode) ?? r ?? Ue.liveMode, redirect: { afterCreate: ((R2 = e == null ? void 0 : e.redirect) == null ? void 0 : R2.afterCreate) ?? Ue.redirect.afterCreate, afterClone: ((g2 = e == null ? void 0 : e.redirect) == null ? void 0 : g2.afterClone) ?? Ue.redirect.afterClone, afterEdit: ((x2 = e == null ? void 0 : e.redirect) == null ? void 0 : x2.afterEdit) ?? Ue.redirect.afterEdit }, overtime: (e == null ? void 0 : e.overtime) ?? Ue.overtime, textTransformers: { humanize: ((E2 = e == null ? void 0 : e.textTransformers) == null ? void 0 : E2.humanize) ?? Ue.textTransformers.humanize, plural: ((w = e == null ? void 0 : e.textTransformers) == null ? void 0 : w.plural) ?? Ue.textTransformers.plural, singular: ((m2 = e == null ? void 0 : e.textTransformers) == null ? void 0 : m2.singular) ?? Ue.textTransformers.singular }, disableServerSideValidation: (e == null ? void 0 : e.disableServerSideValidation) ?? Ue.disableServerSideValidation, projectId: e == null ? void 0 : e.projectId, useNewQueryKeys: e == null ? void 0 : e.useNewQueryKeys }, d3 = (e == null ? void 0 : e.disableTelemetry) ?? t ?? false, f2 = { clientConfig: ((h = e == null ? void 0 : e.reactQuery) == null ? void 0 : h.clientConfig) ?? n ?? {}, devtoolConfig: ((D = e == null ? void 0 : e.reactQuery) == null ? void 0 : D.devtoolConfig) ?? i2 ?? {} };
  return { optionsWithDefaults: c, disableTelemetryWithDefault: d3, reactQueryWithDefaults: f2 };
}, "handleRefineOptions");
var Wr = o(({ redirectFromProps: e, action: t, redirectOptions: r }) => {
  if (e || e === false)
    return e;
  switch (t) {
    case "clone":
      return r.afterClone;
    case "create":
      return r.afterCreate;
    case "edit":
      return r.afterEdit;
    default:
      return false;
  }
}, "redirectPage");
var mr = o(async (e, t, r) => {
  let s = [];
  for (let [n, i2] of e.entries())
    try {
      let a = await i2();
      s.push(t(a, n));
    } catch (a) {
      s.push(r(a, n));
    }
  return s;
}, "sequentialPromises");
var de = o((e, t = [], r = false) => {
  if (!e)
    return;
  if (r) {
    let n = t.find((a) => he(a.route ?? "") === he(e));
    return n || t.find((a) => a.name === e);
  }
  let s = t.find((n) => n.identifier === e);
  return s || (s = t.find((n) => n.name === e)), s;
}, "pickResource");
var z = o((e, t, r) => {
  if (t)
    return t;
  let s = de(e, r), n = S(s == null ? void 0 : s.meta, s == null ? void 0 : s.options);
  return n != null && n.dataProviderName ? n.dataProviderName : "default";
}, "pickDataProvider");
var qe = o(async (e) => ({ data: (await Promise.all(e)).map((t) => t.data) }), "handleMultiple");
var fr = o((e) => {
  let { pagination: t, cursor: r } = e;
  if (r != null && r.next)
    return r.next;
  let s = (t == null ? void 0 : t.current) || 1, n = (t == null ? void 0 : t.pageSize) || 10, i2 = Math.ceil((e.total || 0) / n);
  return s < i2 ? Number(s) + 1 : void 0;
}, "getNextPageParam");
var yr = o((e) => {
  let { pagination: t, cursor: r } = e;
  if (r != null && r.prev)
    return r.prev;
  let s = (t == null ? void 0 : t.current) || 1;
  return s === 1 ? void 0 : s - 1;
}, "getPreviousPageParam");
var gr = o((e) => {
  let t = [];
  return e.forEach((r) => {
    var s, n;
    t.push({ ...r, label: ((s = r.meta) == null ? void 0 : s.label) ?? ((n = r.options) == null ? void 0 : n.label), route: lr(r, e), canCreate: !!r.create, canEdit: !!r.edit, canShow: !!r.show, canDelete: r.canDelete });
  }), t;
}, "legacyResourceTransform");
var Vo = o((e) => Xe(he(e)).flatMap((r) => vt(r) ? [r.slice(1)] : []), "pickRouteParams");
var Bo = o((e, t = {}) => e.reduce((r, s) => {
  let n = t[s];
  return typeof n < "u" && (r[s] = n), r;
}, {}), "prepareRouteParams");
var we = o((e, t = {}, r = {}, s = {}) => {
  let n = Vo(e), i2 = Bo(n, { ...t, ...typeof (r == null ? void 0 : r.id) < "u" ? { id: r.id } : {}, ...typeof (r == null ? void 0 : r.action) < "u" ? { action: r.action } : {}, ...typeof (r == null ? void 0 : r.resource) < "u" ? { resource: r.resource } : {}, ...r == null ? void 0 : r.params, ...s });
  return e.replace(/:([^\/]+)/g, (a, u) => {
    let p3 = i2[u];
    return typeof p3 < "u" ? `${p3}` : a;
  });
}, "composeRoute");
var No = import_react5.default.createContext({});
var Ko = o(({ children: e, isProvided: t, ...r }) => {
  let { replace: s } = me(), n = o(async (p3) => {
    var c;
    try {
      return await ((c = r.login) == null ? void 0 : c.call(r, p3));
    } catch (d3) {
      return Promise.reject(d3);
    }
  }, "loginFunc"), i2 = o(async (p3) => {
    var c;
    try {
      return await ((c = r.register) == null ? void 0 : c.call(r, p3));
    } catch (d3) {
      return Promise.reject(d3);
    }
  }, "registerFunc"), a = o(async (p3) => {
    var c;
    try {
      return await ((c = r.logout) == null ? void 0 : c.call(r, p3));
    } catch (d3) {
      return Promise.reject(d3);
    }
  }, "logoutFunc"), u = o(async (p3) => {
    var c;
    try {
      return await ((c = r.checkAuth) == null ? void 0 : c.call(r, p3)), Promise.resolve();
    } catch (d3) {
      return d3 != null && d3.redirectPath && s(d3.redirectPath), Promise.reject(d3);
    }
  }, "checkAuthFunc");
  return import_react5.default.createElement(No.Provider, { value: { ...r, login: n, logout: a, checkAuth: u, register: i2, isProvided: t } }, e);
}, "LegacyAuthContextProvider");
var Go = import_react5.default.createContext({});
var Ho = o(({ children: e, isProvided: t, ...r }) => {
  let s = o(async (c) => {
    var d3;
    try {
      return await ((d3 = r.login) == null ? void 0 : d3.call(r, c));
    } catch (f2) {
      return console.warn("Unhandled Error in login: refine always expects a resolved promise.", f2), Promise.reject(f2);
    }
  }, "handleLogin"), n = o(async (c) => {
    var d3;
    try {
      return await ((d3 = r.register) == null ? void 0 : d3.call(r, c));
    } catch (f2) {
      return console.warn("Unhandled Error in register: refine always expects a resolved promise.", f2), Promise.reject(f2);
    }
  }, "handleRegister"), i2 = o(async (c) => {
    var d3;
    try {
      return await ((d3 = r.logout) == null ? void 0 : d3.call(r, c));
    } catch (f2) {
      return console.warn("Unhandled Error in logout: refine always expects a resolved promise.", f2), Promise.reject(f2);
    }
  }, "handleLogout"), a = o(async (c) => {
    var d3;
    try {
      let f2 = await ((d3 = r.check) == null ? void 0 : d3.call(r, c));
      return Promise.resolve(f2);
    } catch (f2) {
      return console.warn("Unhandled Error in check: refine always expects a resolved promise.", f2), Promise.reject(f2);
    }
  }, "handleCheck"), u = o(async (c) => {
    var d3;
    try {
      let f2 = await ((d3 = r.forgotPassword) == null ? void 0 : d3.call(r, c));
      return Promise.resolve(f2);
    } catch (f2) {
      return console.warn("Unhandled Error in forgotPassword: refine always expects a resolved promise.", f2), Promise.reject(f2);
    }
  }, "handleForgotPassword"), p3 = o(async (c) => {
    var d3;
    try {
      let f2 = await ((d3 = r.updatePassword) == null ? void 0 : d3.call(r, c));
      return Promise.resolve(f2);
    } catch (f2) {
      return console.warn("Unhandled Error in updatePassword: refine always expects a resolved promise.", f2), Promise.reject(f2);
    }
  }, "handleUpdatePassword");
  return import_react5.default.createElement(Go.Provider, { value: { ...r, login: s, logout: i2, check: a, register: n, forgotPassword: u, updatePassword: p3, isProvided: t } }, e);
}, "AuthBindingsContextProvider");
var ne = o(() => import_react5.default.useContext(No), "useLegacyAuthContext");
var le = o(() => import_react5.default.useContext(Go), "useAuthBindingsContext");
var Y = o(() => {
  let e = ne(), t = le();
  return t.isProvided ? { isLegacy: false, ...t } : e.isProvided ? { isLegacy: true, ...e, check: e.checkAuth, onError: e.checkError, getIdentity: e.getUserIdentity } : null;
}, "useActiveAuthProvider");
var Gt = o(({ hasPagination: e, pagination: t, configPagination: r } = {}) => {
  let s = e === false ? "off" : "server", n = (t == null ? void 0 : t.mode) ?? s, i2 = S(t == null ? void 0 : t.current, r == null ? void 0 : r.current) ?? 1, a = S(t == null ? void 0 : t.pageSize, r == null ? void 0 : r.pageSize) ?? 10;
  return { current: i2, pageSize: a, mode: n };
}, "handlePaginationParams");
var Tr = o((e) => {
  let [t, r] = (0, import_react6.useState)(false);
  return (0, import_react6.useEffect)(() => {
    let s = window.matchMedia(e);
    s.matches !== t && r(s.matches);
    let n = o(() => r(s.matches), "listener");
    return window.addEventListener("resize", n), () => window.removeEventListener("resize", n);
  }, [t, e]), t;
}, "useMediaQuery");
var Rr = o((e, t, r, s) => {
  let n = s ? e(t, s, r) : e(t, r), i2 = r ?? t;
  return n === t || typeof n > "u" ? i2 : n;
}, "safeTranslate");
function $o(e, t, r, s, n) {
  var R2;
  let i2 = { create: "Create new ", clone: `#${s ?? ""} Clone `, edit: `#${s ?? ""} Edit `, show: `#${s ?? ""} Show `, list: "" }, a = (t == null ? void 0 : t.identifier) ?? (t == null ? void 0 : t.name), u = (t == null ? void 0 : t.label) ?? ((R2 = t == null ? void 0 : t.meta) == null ? void 0 : R2.label) ?? rr(a, r === "list" ? "plural" : "singular"), p3 = n ?? u, c = Rr(e, "documentTitle.default", "refine"), d3 = Rr(e, "documentTitle.suffix", " | refine"), f2 = c;
  return r && a && (f2 = Rr(e, `documentTitle.${a}.${r}`, `${i2[r] ?? ""}${p3}${d3}`, { id: s })), f2;
}
o($o, "generateDefaultDocumentTitle");
var ze = o(() => {
  let { mutationMode: e, undoableTimeout: t } = (0, import_react7.useContext)(Me);
  return { mutationMode: e, undoableTimeout: t };
}, "useMutationMode");
var Or = import_react9.default.createContext({});
var Oo = o(({ children: e }) => {
  let [t, r] = (0, import_react9.useState)(false);
  return import_react9.default.createElement(Or.Provider, { value: { warnWhen: t, setWarnWhen: r } }, e);
}, "UnsavedWarnContextProvider");
var Ht = o(() => {
  let { warnWhenUnsavedChanges: e } = (0, import_react8.useContext)(Me), { warnWhen: t, setWarnWhen: r } = (0, import_react8.useContext)(Or);
  return { warnWhenUnsavedChanges: e, warnWhen: !!t, setWarnWhen: r ?? (() => {
  }) };
}, "useWarnAboutChange");
var zr = o(() => {
  let { syncWithLocation: e } = (0, import_react10.useContext)(Me);
  return { syncWithLocation: e };
}, "useSyncWithLocation");
var kn = o(() => {
  let { Title: e } = (0, import_react11.useContext)(Me);
  return e;
}, "useTitle");
var oe = o(() => {
  let { Footer: e, Header: t, Layout: r, OffLayoutArea: s, Sider: n, Title: i2, hasDashboard: a, mutationMode: u, syncWithLocation: p3, undoableTimeout: c, warnWhenUnsavedChanges: d3, DashboardPage: f2, LoginPage: R2, catchAll: g2, options: x2, __initialized: E2 } = (0, import_react12.useContext)(Me);
  return { __initialized: E2, Footer: e, Header: t, Layout: r, OffLayoutArea: s, Sider: n, Title: i2, hasDashboard: a, mutationMode: u, syncWithLocation: p3, undoableTimeout: c, warnWhenUnsavedChanges: d3, DashboardPage: f2, LoginPage: R2, catchAll: g2, options: x2 };
}, "useRefineContext");
var Lt = o(() => {
  let { options: { textTransformers: e } } = oe();
  return o((r = "", s) => {
    let n = e.humanize(r);
    return s === "singular" ? e.singular(n) : e.plural(n);
  }, "getFriendlyName");
}, "useUserFriendlyName");
var _o = o((e) => typeof e == "object" && e !== null, "isNested");
var Vn = o((e) => Array.isArray(e), "isArray");
var xr = o((e, t = "") => _o(e) ? Object.keys(e).reduce((r, s) => {
  let n = t.length ? t + "." : "";
  return _o(e[s]) && Object.keys(e[s]).length && (Vn(e[s]) && e[s].length ? e[s].forEach((i2, a) => {
    Object.assign(r, xr(i2, n + s + "." + a));
  }) : Object.assign(r, xr(e[s], n + s))), r[n + s] = e[s], r;
}, {}) : { [t]: e }, "flattenObjectKeys");
var jo = o((e) => e.split(".").map((t) => isNaN(Number(t)) ? t : Number(t)), "propertyPathToArray");
var _r = o((e, t, r) => {
  if (typeof window > "u")
    return;
  let s = new Blob([t], { type: r }), n = document.createElement("a");
  n.setAttribute("visibility", "hidden"), n.download = e;
  let i2 = URL.createObjectURL(s);
  n.href = i2, document.body.appendChild(n), n.click(), document.body.removeChild(n), setTimeout(() => {
    URL.revokeObjectURL(i2);
  });
}, "downloadInBrowser");
var Pr = o((e) => {
  let { current: t, pageSize: r, sorter: s, sorters: n, filters: i2 } = import_qs.default.parse(e.substring(1));
  return { parsedCurrent: t && Number(t), parsedPageSize: r && Number(r), parsedSorter: S(n, s) ?? [], parsedFilters: i2 ?? [] };
}, "parseTableParams");
var Nn = o((e) => {
  let t = import_qs.default.stringify(e);
  return Pr(`/${t}`);
}, "parseTableParamsFromQuery");
var hr = o((e) => {
  let t = { skipNulls: true, arrayFormat: "indices", encode: false }, { pagination: r, sorter: s, sorters: n, filters: i2, ...a } = e;
  return import_qs.default.stringify({ ...a, ...r || {}, sorters: S(n, s), filters: i2 }, t);
}, "stringifyTableParams");
var Yo = o((e, t) => e.operator !== "and" && e.operator !== "or" && t.operator !== "and" && t.operator !== "or" ? ("field" in e ? e.field : void 0) == ("field" in t ? t.field : void 0) && e.operator == t.operator : ("key" in e ? e.key : void 0) == ("key" in t ? t.key : void 0) && e.operator == t.operator, "compareFilters");
var Jo = o((e, t) => e.field == t.field, "compareSorters");
var Et = o((e, t, r = []) => (t.filter((n) => (n.operator === "or" || n.operator === "and") && !n.key).length > 1 && (0, import_warn_once.default)(true, `[conditionalFilters]: You have created multiple Conditional Filters at the top level, this requires the key parameter. 
For more information, see https://refine.dev/docs/advanced-tutorials/data-provider/handling-filters/#top-level-multiple-conditional-filters-usage`), unionWith_default(e, t, r, Yo).filter((n) => n.value !== void 0 && n.value !== null && (n.operator !== "or" || n.operator === "or" && n.value.length !== 0) && (n.operator !== "and" || n.operator === "and" && n.value.length !== 0))), "unionFilters");
var Cr = o((e, t) => unionWith_default(e, t, Jo).filter((r) => r.order !== void 0 && r.order !== null), "unionSorters");
var br = o((e, t) => [...differenceWith_default(t, e, Yo), ...e], "setInitialFilters");
var vr = o((e, t) => [...differenceWith_default(t, e, Jo), ...e], "setInitialSorters");
var Kn = o((e, t) => {
  if (!t)
    return;
  let r = t.find((s) => s.field === e);
  if (r)
    return r.order;
}, "getDefaultSortOrder");
var Gn = o((e, t, r = "eq") => {
  let s = t == null ? void 0 : t.find((n) => {
    if (n.operator !== "or" && n.operator !== "and" && "field" in n) {
      let { operator: i2, field: a } = n;
      return a === e && i2 === r;
    }
  });
  if (s)
    return s.value || [];
}, "getDefaultFilter");
var Hn = o((e) => new Promise((t, r) => {
  let s = new FileReader(), n = o(() => {
    s.result && (s.removeEventListener("load", n, false), t(s.result));
  }, "resultHandler");
  s.addEventListener("load", n, false), s.readAsDataURL(e.originFileObj), s.onerror = (i2) => (s.removeEventListener("load", n, false), r(i2));
}), "file2Base64");
var O2 = o(() => {
  let { options: { useNewQueryKeys: e } } = oe();
  return { keys: Oe, preferLegacyKeys: !e };
}, "useKeys");
function $n({ v3LegacyAuthProviderCompatible: e = false, options: t } = {}) {
  let { getPermissions: r } = ne(), { getPermissions: s } = le(), { keys: n, preferLegacyKeys: i2 } = O2(), a = useQuery({ queryKey: n().auth().action("permissions").get(i2), queryFn: s ?? (() => Promise.resolve(void 0)), enabled: !e && !!s, ...e ? {} : t, meta: { ...e ? {} : t == null ? void 0 : t.meta, ...P("usePermissions", i2) } }), u = useQuery({ queryKey: [...n().auth().action("permissions").get(i2), "v3LegacyAuthProviderCompatible"], queryFn: r ?? (() => Promise.resolve(void 0)), enabled: e && !!r, ...e ? t : {}, meta: { ...e ? t == null ? void 0 : t.meta : {}, ...P("usePermissions", i2) } });
  return e ? u : a;
}
o($n, "usePermissions");
function Xr({ v3LegacyAuthProviderCompatible: e = false, queryOptions: t } = {}) {
  let { getUserIdentity: r } = ne(), { getIdentity: s } = le(), { keys: n, preferLegacyKeys: i2 } = O2(), a = useQuery({ queryKey: n().auth().action("identity").get(i2), queryFn: s ?? (() => Promise.resolve({})), enabled: !e && !!s, retry: false, ...e === true ? {} : t, meta: { ...e === true ? {} : t == null ? void 0 : t.meta, ...P("useGetIdentity", i2) } }), u = useQuery({ queryKey: [...n().auth().action("identity").get(i2), "v3LegacyAuthProviderCompatible"], queryFn: r ?? (() => Promise.resolve({})), enabled: e && !!r, retry: false, ...e ? t : {}, meta: { ...e ? t == null ? void 0 : t.meta : {}, ...P("useGetIdentity", i2) } });
  return e ? u : a;
}
o(Xr, "useGetIdentity");
var Ut = o(() => {
  let e = useQueryClient(), { keys: t, preferLegacyKeys: r } = O2();
  return o(async () => {
    await Promise.all(["check", "identity", "permissions"].map((n) => e.invalidateQueries(t().auth().action(n).get(r))));
  }, "invalidate");
}, "useInvalidateAuthStore");
function Dr({ v3LegacyAuthProviderCompatible: e, mutationOptions: t } = {}) {
  let r = Ut(), s = ee(), n = fe(), { push: i2 } = me(), { open: a, close: u } = Ie(), { logout: p3 } = ne(), { logout: c } = le(), { keys: d3, preferLegacyKeys: f2 } = O2(), R2 = useMutation({ mutationKey: d3().auth().action("logout").get(f2), mutationFn: c, onSuccess: async (x2, E2) => {
    let { success: w, error: m2, redirectTo: h, successNotification: D } = x2, { redirectPath: I2 } = E2 ?? {}, T2 = I2 ?? h;
    w && (u == null || u("useLogout-error"), D && (a == null || a(On(D)))), (m2 || !w) && (a == null || a(Zr(m2))), T2 !== false && (s === "legacy" ? i2(T2 ?? "/login") : T2 && n({ to: T2 })), await r();
  }, onError: (x2) => {
    a == null || a(Zr(x2));
  }, ...e === true ? {} : t, meta: { ...e === true ? {} : t == null ? void 0 : t.meta, ...P("useLogout", f2) } }), g2 = useMutation({ mutationKey: [...d3().auth().action("logout").get(f2), "v3LegacyAuthProviderCompatible"], mutationFn: p3, onSuccess: async (x2, E2) => {
    let w = (E2 == null ? void 0 : E2.redirectPath) ?? x2;
    if (w !== false) {
      if (w) {
        s === "legacy" ? i2(w) : n({ to: w });
        return;
      }
      s === "legacy" ? i2("/login") : n({ to: "/login" }), await r();
    }
  }, onError: (x2) => {
    a == null || a(Zr(x2));
  }, ...e ? t : {}, meta: { ...e ? t == null ? void 0 : t.meta : {}, ...P("useLogout", f2) } });
  return e ? g2 : R2;
}
o(Dr, "useLogout");
var Zr = o((e) => ({ key: "useLogout-error", type: "error", message: (e == null ? void 0 : e.name) || "Logout Error", description: (e == null ? void 0 : e.message) || "Something went wrong during logout" }), "buildNotification");
var On = o((e) => ({ message: e.message, description: e.description, key: "logout-success", type: "success" }), "buildSuccessNotification");
function $t({ v3LegacyAuthProviderCompatible: e, mutationOptions: t } = {}) {
  let r = Ut(), s = ee(), n = fe(), { replace: i2 } = me(), a = ge(), { useLocation: u } = ae(), { search: p3 } = u(), { close: c, open: d3 } = Ie(), { login: f2 } = ne(), { login: R2 } = le(), { keys: g2, preferLegacyKeys: x2 } = O2(), E2 = import_react13.default.useMemo(() => {
    var h;
    return s === "legacy" ? import_qs2.default.parse(p3, { ignoreQueryPrefix: true }).to : (h = a.params) == null ? void 0 : h.to;
  }, [s, a.params, p3]), w = useMutation({ mutationKey: g2().auth().action("login").get(x2), mutationFn: R2, onSuccess: async ({ success: h, redirectTo: D, error: I2, successNotification: T2 }) => {
    h && (c == null || c("login-error"), T2 && (d3 == null || d3(jn(T2)))), (I2 || !h) && (d3 == null || d3(Yr(I2))), E2 && h ? s === "legacy" ? i2(E2) : n({ to: E2, type: "replace" }) : D ? s === "legacy" ? i2(D) : n({ to: D, type: "replace" }) : s === "legacy" && i2("/"), await r();
  }, onError: (h) => {
    d3 == null || d3(Yr(h));
  }, ...e === true ? {} : t, meta: { ...e === true ? {} : t == null ? void 0 : t.meta, ...P("useLogin", x2) } }), m2 = useMutation({ mutationKey: [...g2().auth().action("login").get(x2), "v3LegacyAuthProviderCompatible"], mutationFn: f2, onSuccess: async (h) => {
    E2 && i2(E2), h !== false && !E2 && (typeof h == "string" ? s === "legacy" ? i2(h) : n({ to: h, type: "replace" }) : s === "legacy" ? i2("/") : n({ to: "/", type: "replace" })), await r(), c == null || c("login-error");
  }, onError: (h) => {
    d3 == null || d3(Yr(h));
  }, ...e ? t : {}, meta: { ...e ? t == null ? void 0 : t.meta : {}, ...P("useLogin", x2) } });
  return e ? m2 : w;
}
o($t, "useLogin");
var Yr = o((e) => ({ message: (e == null ? void 0 : e.name) || "Login Error", description: (e == null ? void 0 : e.message) || "Invalid credentials", key: "login-error", type: "error" }), "buildNotification");
var jn = o((e) => ({ message: e.message, description: e.description, key: "login-success", type: "success" }), "buildSuccessNotification");
function qr({ v3LegacyAuthProviderCompatible: e, mutationOptions: t } = {}) {
  let r = Ut(), s = ee(), n = fe(), { replace: i2 } = me(), { register: a } = ne(), { register: u } = le(), { close: p3, open: c } = Ie(), { keys: d3, preferLegacyKeys: f2 } = O2(), R2 = useMutation({ mutationKey: d3().auth().action("register").get(f2), mutationFn: u, onSuccess: async ({ success: x2, redirectTo: E2, error: w, successNotification: m2 }) => {
    x2 && (p3 == null || p3("register-error"), m2 && (c == null || c(Xn(m2)))), (w || !x2) && (c == null || c(Jr(w))), E2 ? s === "legacy" ? i2(E2) : n({ to: E2, type: "replace" }) : s === "legacy" && i2("/"), await r();
  }, onError: (x2) => {
    c == null || c(Jr(x2));
  }, ...e === true ? {} : t, meta: { ...e === true ? {} : t == null ? void 0 : t.meta, ...P("useRegister", f2) } }), g2 = useMutation({ mutationKey: [...d3().auth().action("register").get(f2), "v3LegacyAuthProviderCompatible"], mutationFn: a, onSuccess: async (x2) => {
    x2 !== false && (x2 ? s === "legacy" ? i2(x2) : n({ to: x2, type: "replace" }) : s === "legacy" ? i2("/") : n({ to: "/", type: "replace" }), await r(), p3 == null || p3("register-error"));
  }, onError: (x2) => {
    c == null || c(Jr(x2));
  }, ...e ? t : {}, meta: { ...e ? t == null ? void 0 : t.meta : {}, ...P("useRegister", f2) } });
  return e ? g2 : R2;
}
o(qr, "useRegister");
var Jr = o((e) => ({ message: (e == null ? void 0 : e.name) || "Register Error", description: (e == null ? void 0 : e.message) || "Error while registering", key: "register-error", type: "error" }), "buildNotification");
var Xn = o((e) => ({ message: e.message, description: e.description, key: "register-success", type: "success" }), "buildSuccessNotification");
function to({ v3LegacyAuthProviderCompatible: e, mutationOptions: t } = {}) {
  let r = ee(), s = fe(), { replace: n } = me(), { forgotPassword: i2 } = ne(), { forgotPassword: a } = le(), { close: u, open: p3 } = Ie(), { keys: c, preferLegacyKeys: d3 } = O2(), f2 = useMutation({ mutationKey: c().auth().action("forgotPassword").get(d3), mutationFn: a, onSuccess: ({ success: g2, redirectTo: x2, error: E2, successNotification: w }) => {
    g2 && (u == null || u("forgot-password-error"), w && (p3 == null || p3(Zn(w)))), (E2 || !g2) && (p3 == null || p3(eo(E2))), x2 && (r === "legacy" ? n(x2) : s({ to: x2, type: "replace" }));
  }, onError: (g2) => {
    p3 == null || p3(eo(g2));
  }, ...e === true ? {} : t, meta: { ...e === true ? {} : t == null ? void 0 : t.meta, ...P("useForgotPassword", d3) } }), R2 = useMutation({ mutationKey: [...c().auth().action("forgotPassword").get(d3), "v3LegacyAuthProviderCompatible"], mutationFn: i2, onSuccess: (g2) => {
    g2 !== false && g2 && (r === "legacy" ? n(g2) : s({ to: g2, type: "replace" })), u == null || u("forgot-password-error");
  }, onError: (g2) => {
    p3 == null || p3(eo(g2));
  }, ...e ? t : {}, meta: { ...e ? t == null ? void 0 : t.meta : {}, ...P("useForgotPassword", d3) } });
  return e ? R2 : f2;
}
o(to, "useForgotPassword");
var eo = o((e) => ({ message: (e == null ? void 0 : e.name) || "Forgot Password Error", description: (e == null ? void 0 : e.message) || "Error while resetting password", key: "forgot-password-error", type: "error" }), "buildNotification");
var Zn = o((e) => ({ message: e.message, description: e.description, key: "forgot-password-success", type: "success" }), "buildSuccessNotification");
function oo({ v3LegacyAuthProviderCompatible: e, mutationOptions: t } = {}) {
  let r = ee(), s = fe(), { replace: n } = me(), { updatePassword: i2 } = ne(), { updatePassword: a } = le(), { close: u, open: p3 } = Ie(), { keys: c, preferLegacyKeys: d3 } = O2(), f2 = ge(), { useLocation: R2 } = ae(), { search: g2 } = R2(), x2 = import_react14.default.useMemo(() => r === "legacy" ? import_qs3.default.parse(g2, { ignoreQueryPrefix: true }) ?? {} : f2.params ?? {}, [g2, f2, r]), E2 = useMutation({ mutationKey: c().auth().action("updatePassword").get(d3), mutationFn: async (m2) => a == null ? void 0 : a({ ...x2, ...m2 }), onSuccess: ({ success: m2, redirectTo: h, error: D, successNotification: I2 }) => {
    m2 && (u == null || u("update-password-error"), I2 && (p3 == null || p3(qn(I2)))), (D || !m2) && (p3 == null || p3(ro(D))), h && (r === "legacy" ? n(h) : s({ to: h, type: "replace" }));
  }, onError: (m2) => {
    p3 == null || p3(ro(m2));
  }, ...e === true ? {} : t, meta: { ...e === true ? {} : t == null ? void 0 : t.meta, ...P("useUpdatePassword", d3) } }), w = useMutation({ mutationKey: [...c().auth().action("updatePassword").get(d3), "v3LegacyAuthProviderCompatible"], mutationFn: async (m2) => i2 == null ? void 0 : i2({ ...x2, ...m2 }), onSuccess: (m2) => {
    m2 !== false && m2 && (r === "legacy" ? n(m2) : s({ to: m2, type: "replace" })), u == null || u("update-password-error");
  }, onError: (m2) => {
    p3 == null || p3(ro(m2));
  }, ...e ? t : {}, meta: { ...e ? t == null ? void 0 : t.meta : {}, ...P("useUpdatePassword", d3) } });
  return e ? w : E2;
}
o(oo, "useUpdatePassword");
var ro = o((e) => ({ message: (e == null ? void 0 : e.name) || "Update Password Error", description: (e == null ? void 0 : e.message) || "Error while updating password", key: "update-password-error", type: "error" }), "buildNotification");
var qn = o((e) => ({ message: e.message, description: e.description, key: "update-password-success", type: "success" }), "buildSuccessNotification");
function Lr({ v3LegacyAuthProviderCompatible: e = false, params: t } = {}) {
  let { checkAuth: r } = ne(), { check: s } = le(), { keys: n, preferLegacyKeys: i2 } = O2(), a = useQuery({ queryKey: n().auth().action("check").params(t).get(i2), queryFn: async () => await (s == null ? void 0 : s(t)) ?? {}, retry: false, enabled: !e, meta: { ...P("useIsAuthenticated", i2) } }), u = useQuery({ queryKey: [...n().auth().action("check").params(t).get(i2), "v3LegacyAuthProviderCompatible"], queryFn: async () => await (r == null ? void 0 : r(t)) ?? {}, retry: false, enabled: e, meta: { ...P("useIsAuthenticated", i2) } });
  return e ? u : a;
}
o(Lr, "useIsAuthenticated");
var ea = Lr;
function ie({ v3LegacyAuthProviderCompatible: e = false } = {}) {
  let t = ee(), r = fe(), { replace: s } = me(), { checkError: n } = ne(), { onError: i2 } = le(), { keys: a, preferLegacyKeys: u } = O2(), { mutate: p3 } = Dr({ v3LegacyAuthProviderCompatible: !!e }), { mutate: c } = Dr({ v3LegacyAuthProviderCompatible: !!e }), d3 = useMutation({ mutationKey: a().auth().action("onError").get(u), mutationFn: i2, onSuccess: ({ logout: R2, redirectTo: g2 }) => {
    if (R2) {
      c({ redirectPath: g2 });
      return;
    }
    if (g2) {
      t === "legacy" ? s(g2) : r({ to: g2, type: "replace" });
      return;
    }
  }, meta: { ...P("useOnError", u) } }), f2 = useMutation({ mutationKey: [...a().auth().action("onError").get(u), "v3LegacyAuthProviderCompatible"], mutationFn: n, onError: (R2) => {
    p3({ redirectPath: R2 });
  }, meta: { ...P("useOnError", u) } });
  return e ? f2 : d3;
}
o(ie, "useOnError");
var ta = ie;
var so = o(() => {
  let { isProvided: e } = ne(), { isProvided: t } = le();
  return !!(t || e);
}, "useIsExistAuthentication");
var te = o(({ isLoading: e, interval: t, onInterval: r }) => {
  let [s, n] = (0, import_react15.useState)(void 0), { options: i2 } = oe(), { overtime: a } = i2, u = t ?? a.interval, p3 = r ?? (a == null ? void 0 : a.onInterval);
  return (0, import_react15.useEffect)(() => {
    let c;
    return e && (c = setInterval(() => {
      n((d3) => d3 === void 0 ? u : d3 + u);
    }, u)), () => {
      clearInterval(c), n(void 0);
    };
  }, [e, u]), (0, import_react15.useEffect)(() => {
    p3 && s && p3(s);
  }, [s]), { elapsedTime: s };
}, "useLoadingOvertime");
var Wt = o(({ resource: e, config: t, filters: r, hasPagination: s, pagination: n, sorters: i2, queryOptions: a, successNotification: u, errorNotification: p3, meta: c, metaData: d3, liveMode: f2, onLiveEvent: R2, liveParams: g2, dataProviderName: x2, overtimeOptions: E2 } = {}) => {
  let { resources: w, resource: m2, identifier: h } = $(e), D = se(), I2 = _(), T2 = Y(), { mutate: y2 } = ie({ v3LegacyAuthProviderCompatible: !!(T2 != null && T2.isLegacy) }), l2 = ue(), C2 = J(), { keys: v2, preferLegacyKeys: P2 } = O2(), U = z(h, x2, w), b = S(c, d3), M = S(r, t == null ? void 0 : t.filters), k = S(i2, t == null ? void 0 : t.sort), L = S(s, t == null ? void 0 : t.hasPagination), Q = Gt({ pagination: n, configPagination: t == null ? void 0 : t.pagination, hasPagination: L }), F = Q.mode === "server", G = C2({ resource: m2, meta: b }), N = { meta: G, metaData: G, filters: M, hasPagination: F, pagination: Q, sorters: k, config: { ...t, sort: k } }, B = (a == null ? void 0 : a.enabled) === void 0 || (a == null ? void 0 : a.enabled) === true, { getList: j } = D(U);
  it({ resource: h, types: ["*"], params: { meta: G, metaData: G, pagination: Q, hasPagination: F, sort: k, sorters: k, filters: M, subscriptionType: "useList", ...g2 }, channel: `resources/${m2 == null ? void 0 : m2.name}`, enabled: B, liveMode: f2, onLiveEvent: R2, dataProviderName: U, meta: { ...c, dataProviderName: x2 } });
  let K = useQuery({ queryKey: v2().data(U).resource(h ?? "").action("list").params({ ...b || {}, filters: M, hasPagination: F, ...F && { pagination: Q }, ...i2 && { sorters: i2 }, ...(t == null ? void 0 : t.sort) && { sort: t == null ? void 0 : t.sort } }).get(P2), queryFn: ({ queryKey: V, pageParam: H, signal: Z }) => j({ resource: (m2 == null ? void 0 : m2.name) ?? "", pagination: Q, hasPagination: F, filters: M, sort: k, sorters: k, meta: { ...G, queryContext: { queryKey: V, pageParam: H, signal: Z } }, metaData: { ...G, queryContext: { queryKey: V, pageParam: H, signal: Z } } }), ...a, enabled: typeof (a == null ? void 0 : a.enabled) < "u" ? a == null ? void 0 : a.enabled : !!(m2 != null && m2.name), select: (V) => {
    var Ce;
    let H = V, { current: Z, mode: X, pageSize: q } = Q;
    return X === "client" && (H = { ...H, data: H.data.slice((Z - 1) * q, Z * q), total: H.total }), a != null && a.select ? (Ce = a == null ? void 0 : a.select) == null ? void 0 : Ce.call(a, H) : H;
  }, onSuccess: (V) => {
    var Z;
    (Z = a == null ? void 0 : a.onSuccess) == null || Z.call(a, V);
    let H = typeof u == "function" ? u(V, N, h) : u;
    l2(H);
  }, onError: (V) => {
    var Z;
    y2(V), (Z = a == null ? void 0 : a.onError) == null || Z.call(a, V);
    let H = typeof p3 == "function" ? p3(V, N, h) : p3;
    l2(H, { key: `${h}-useList-notification`, message: I2("notifications.error", { statusCode: V.statusCode }, `Error (status code: ${V.statusCode})`), description: V.message, type: "error" });
  }, meta: { ...a == null ? void 0 : a.meta, ...P("useList", P2) } }), { elapsedTime: W } = te({ isLoading: K.isFetching, interval: E2 == null ? void 0 : E2.interval, onInterval: E2 == null ? void 0 : E2.onInterval });
  return { ...K, overtime: { elapsedTime: W } };
}, "useList");
var Ot = o(({ resource: e, id: t, queryOptions: r, successNotification: s, errorNotification: n, meta: i2, metaData: a, liveMode: u, onLiveEvent: p3, liveParams: c, dataProviderName: d3, overtimeOptions: f2 }) => {
  let { resources: R2, resource: g2, identifier: x2 } = $(e), E2 = se(), w = _(), m2 = Y(), { mutate: h } = ie({ v3LegacyAuthProviderCompatible: !!(m2 != null && m2.isLegacy) }), D = ue(), I2 = J(), { keys: T2, preferLegacyKeys: y2 } = O2(), l2 = S(i2, a), C2 = z(x2, d3, R2), { getOne: v2 } = E2(C2), P2 = I2({ resource: g2, meta: l2 });
  it({ resource: x2, types: ["*"], channel: `resources/${g2 == null ? void 0 : g2.name}`, params: { ids: t ? [t] : [], id: t, meta: P2, metaData: P2, subscriptionType: "useOne", ...c }, enabled: typeof (r == null ? void 0 : r.enabled) < "u" ? r == null ? void 0 : r.enabled : typeof (g2 == null ? void 0 : g2.name) < "u" && typeof t < "u", liveMode: u, onLiveEvent: p3, dataProviderName: C2, meta: { ...i2, dataProviderName: d3 } });
  let U = useQuery({ queryKey: T2().data(C2).resource(x2 ?? "").action("one").id(t ?? "").params({ ...l2 || {} }).get(y2), queryFn: ({ queryKey: M, pageParam: k, signal: L }) => v2({ resource: (g2 == null ? void 0 : g2.name) ?? "", id: t, meta: { ...P2, queryContext: { queryKey: M, pageParam: k, signal: L } }, metaData: { ...P2, queryContext: { queryKey: M, pageParam: k, signal: L } } }), ...r, enabled: typeof (r == null ? void 0 : r.enabled) < "u" ? r == null ? void 0 : r.enabled : typeof t < "u", onSuccess: (M) => {
    var L;
    (L = r == null ? void 0 : r.onSuccess) == null || L.call(r, M);
    let k = typeof s == "function" ? s(M, { id: t, ...P2 }, x2) : s;
    D(k);
  }, onError: (M) => {
    var L;
    h(M), (L = r == null ? void 0 : r.onError) == null || L.call(r, M);
    let k = typeof n == "function" ? n(M, { id: t, ...P2 }, x2) : n;
    D(k, { key: `${t}-${x2}-getOne-notification`, message: w("notifications.error", { statusCode: M.statusCode }, `Error (status code: ${M.statusCode})`), description: M.message, type: "error" });
  }, meta: { ...r == null ? void 0 : r.meta, ...P("useOne", y2) } }), { elapsedTime: b } = te({ isLoading: U.isFetching, interval: f2 == null ? void 0 : f2.interval, onInterval: f2 == null ? void 0 : f2.onInterval });
  return { ...U, overtime: { elapsedTime: b } };
}, "useOne");
var no = o(({ resource: e, ids: t, queryOptions: r, successNotification: s, errorNotification: n, meta: i2, metaData: a, liveMode: u, onLiveEvent: p3, liveParams: c, dataProviderName: d3, overtimeOptions: f2 }) => {
  let { resources: R2, resource: g2, identifier: x2 } = $(e), E2 = se(), w = _(), m2 = Y(), { mutate: h } = ie({ v3LegacyAuthProviderCompatible: !!(m2 != null && m2.isLegacy) }), D = ue(), I2 = J(), { keys: T2, preferLegacyKeys: y2 } = O2(), l2 = S(i2, a), C2 = z(x2, d3, R2), v2 = (r == null ? void 0 : r.enabled) === void 0 || (r == null ? void 0 : r.enabled) === true, { getMany: P2, getOne: U } = E2(C2), b = I2({ resource: g2, meta: l2 });
  it({ resource: x2, types: ["*"], params: { ids: t, meta: b, metaData: b, subscriptionType: "useMany", ...c }, channel: `resources/${g2.name}`, enabled: v2, liveMode: u, onLiveEvent: p3, dataProviderName: C2, meta: { ...i2, dataProviderName: d3 } });
  let M = useQuery({ queryKey: T2().data(C2).resource(x2).action("many").ids(...t).params({ ...l2 || {} }).get(y2), queryFn: ({ queryKey: L, pageParam: Q, signal: F }) => P2 ? P2({ resource: g2 == null ? void 0 : g2.name, ids: t, meta: { ...b, queryContext: { queryKey: L, pageParam: Q, signal: F } }, metaData: { ...b, queryContext: { queryKey: L, pageParam: Q, signal: F } } }) : qe(t.map((G) => U({ resource: g2 == null ? void 0 : g2.name, id: G, meta: { ...b, queryContext: { queryKey: L, pageParam: Q, signal: F } }, metaData: { ...b, queryContext: { queryKey: L, pageParam: Q, signal: F } } }))), ...r, onSuccess: (L) => {
    var F;
    (F = r == null ? void 0 : r.onSuccess) == null || F.call(r, L);
    let Q = typeof s == "function" ? s(L, t, x2) : s;
    D(Q);
  }, onError: (L) => {
    var F;
    h(L), (F = r == null ? void 0 : r.onError) == null || F.call(r, L);
    let Q = typeof n == "function" ? n(L, t, x2) : n;
    D(Q, { key: `${t[0]}-${x2}-getMany-notification`, message: w("notifications.error", { statusCode: L.statusCode }, `Error (status code: ${L.statusCode})`), description: L.message, type: "error" });
  }, meta: { ...r == null ? void 0 : r.meta, ...P("useMany", y2) } }), { elapsedTime: k } = te({ isLoading: M.isFetching, interval: f2 == null ? void 0 : f2.interval, onInterval: f2 == null ? void 0 : f2.onInterval });
  return { ...M, overtime: { elapsedTime: k } };
}, "useMany");
var Er = import_react16.default.createContext({ notifications: [], notificationDispatch: () => false });
var pa = [];
var da = o((e, t) => {
  switch (t.type) {
    case "ADD":
      return [...e.filter((s) => !(isEqual_default(s.id, t.payload.id) && s.resource == t.payload.resource)), { ...t.payload, isRunning: true }];
    case "REMOVE":
      return e.filter((s) => !(isEqual_default(s.id, t.payload.id) && s.resource == t.payload.resource));
    case "DECREASE_NOTIFICATION_SECOND":
      return e.map((s) => isEqual_default(s.id, t.payload.id) && s.resource == t.payload.resource ? { ...s, seconds: t.payload.seconds - 1e3 } : s);
    default:
      return e;
  }
}, "undoableQueueReducer");
var uo = o(({ children: e }) => {
  let [t, r] = (0, import_react16.useReducer)(da, pa), s = { notifications: t, notificationDispatch: r };
  return import_react16.default.createElement(Er.Provider, { value: s }, e, typeof window < "u" && t.map((n) => import_react16.default.createElement(Rs, { key: `${n.id}-${n.resource}-queue`, notification: n })));
}, "UndoableQueueContextProvider");
var Ur = o(({ mutationOptions: e, overtimeOptions: t } = {}) => {
  let { resources: r, select: s } = $(), n = useQueryClient(), i2 = se(), { mutationMode: a, undoableTimeout: u } = ze(), p3 = _(), c = Y(), { mutate: d3 } = ie({ v3LegacyAuthProviderCompatible: !!(c != null && c.isLegacy) }), f2 = Be(), { log: R2 } = Ne(), { notificationDispatch: g2 } = Ze(), x2 = ue(), E2 = be(), w = J(), { options: { textTransformers: m2 } } = oe(), { keys: h, preferLegacyKeys: D } = O2(), I2 = useMutation({ mutationFn: ({ id: y2, values: l2, resource: C2, mutationMode: v2, undoableTimeout: P2, onCancel: U, meta: b, metaData: M, dataProviderName: k }) => {
    let { resource: L, identifier: Q } = s(C2), F = w({ resource: L, meta: S(b, M) }), G = v2 ?? a, N = P2 ?? u;
    return G !== "undoable" ? i2(z(Q, k, r)).update({ resource: L.name, id: y2, variables: l2, meta: F, metaData: F }) : new Promise((j, K) => {
      let W = o(() => {
        i2(z(Q, k, r)).update({ resource: L.name, id: y2, variables: l2, meta: F, metaData: F }).then((H) => j(H)).catch((H) => K(H));
      }, "doMutation"), V = o(() => {
        K({ message: "mutationCancelled" });
      }, "cancelMutation");
      U && U(V), g2({ type: "ADD", payload: { id: y2, resource: Q, cancelMutation: V, doMutation: W, seconds: N, isSilent: !!U } });
    });
  }, onMutate: async ({ resource: y2, id: l2, mutationMode: C2, values: v2, dataProviderName: P2, meta: U, metaData: b, optimisticUpdateMap: M = { list: true, many: true, detail: true } }) => {
    let { identifier: k } = s(y2), { gqlMutation: L, gqlQuery: Q, ...F } = S(U, b) ?? {}, G = Je(D)(k, z(k, P2, r), F), N = h().data(z(k, P2, r)).resource(k), B = n.getQueriesData(N.get(D)), j = C2 ?? a;
    return await n.cancelQueries(N.get(D), void 0, { silent: true }), j !== "pessimistic" && (M.list && n.setQueriesData(N.action("list").params(F ?? {}).get(D), (K) => {
      if (typeof M.list == "function")
        return M.list(K, v2, l2);
      if (!K)
        return null;
      let W = K.data.map((V) => {
        var H;
        return ((H = V.id) == null ? void 0 : H.toString()) === (l2 == null ? void 0 : l2.toString()) ? { id: l2, ...V, ...v2 } : V;
      });
      return { ...K, data: W };
    }), M.many && n.setQueriesData(N.action("many").get(D), (K) => {
      if (typeof M.many == "function")
        return M.many(K, v2, l2);
      if (!K)
        return null;
      let W = K.data.map((V) => {
        var H;
        return ((H = V.id) == null ? void 0 : H.toString()) === (l2 == null ? void 0 : l2.toString()) && (V = { id: l2, ...V, ...v2 }), V;
      });
      return { ...K, data: W };
    }), M.detail && n.setQueriesData(N.action("one").id(l2).params(F ?? {}).get(D), (K) => typeof M.detail == "function" ? M.detail(K, v2, l2) : K ? { ...K, data: { ...K.data, ...v2 } } : null)), { previousQueries: B, queryKey: G };
  }, onSettled: (y2, l2, { id: C2, resource: v2, dataProviderName: P2, invalidates: U = ["list", "many", "detail"] }) => {
    let { identifier: b } = s(v2);
    E2({ resource: b, dataProviderName: z(b, P2, r), invalidates: U, id: C2 }), g2({ type: "REMOVE", payload: { id: C2, resource: b } });
  }, onSuccess: (y2, { id: l2, resource: C2, successNotification: v2, dataProviderName: P2, values: U, meta: b, metaData: M }, k) => {
    var Z;
    let { resource: L, identifier: Q } = s(C2), F = m2.singular(Q), G = z(Q, P2, r), N = w({ resource: L, meta: S(b, M) }), B = typeof v2 == "function" ? v2(y2, { id: l2, values: U }, Q) : v2;
    x2(B, { key: `${l2}-${Q}-notification`, description: p3("notifications.success", "Successful"), message: p3("notifications.editSuccess", { resource: p3(`${Q}.${Q}`, F) }, `Successfully updated ${F}`), type: "success" }), f2 == null || f2({ channel: `resources/${L.name}`, type: "updated", payload: { ids: (Z = y2.data) != null && Z.id ? [y2.data.id] : void 0 }, date: /* @__PURE__ */ new Date(), meta: { ...N, dataProviderName: G } });
    let j;
    if (k) {
      let X = n.getQueryData(k.queryKey.detail(l2));
      j = Object.keys(U || {}).reduce((q, Ce) => {
        var De;
        return q[Ce] = (De = X == null ? void 0 : X.data) == null ? void 0 : De[Ce], q;
      }, {});
    }
    let { fields: K, operation: W, variables: V, ...H } = N || {};
    R2 == null || R2.mutate({ action: "update", resource: L.name, data: U, previousData: j, meta: { id: l2, dataProviderName: G, ...H } });
  }, onError: (y2, { id: l2, resource: C2, errorNotification: v2, values: P2 }, U) => {
    let { identifier: b } = s(C2);
    if (U)
      for (let M of U.previousQueries)
        n.setQueryData(M[0], M[1]);
    if (y2.message !== "mutationCancelled") {
      d3 == null || d3(y2);
      let M = m2.singular(b), k = typeof v2 == "function" ? v2(y2, { id: l2, values: P2 }, b) : v2;
      x2(k, { key: `${l2}-${b}-notification`, message: p3("notifications.editError", { resource: p3(`${b}.${b}`, M), statusCode: y2.statusCode }, `Error when updating ${M} (status code: ${y2.statusCode})`), description: y2.message, type: "error" });
    }
  }, mutationKey: h().data().mutation("update").get(D), ...e, meta: { ...e == null ? void 0 : e.meta, ...P("useUpdate", D) } }), { elapsedTime: T2 } = te({ isLoading: I2.isLoading, interval: t == null ? void 0 : t.interval, onInterval: t == null ? void 0 : t.onInterval });
  return { ...I2, overtime: { elapsedTime: T2 } };
}, "useUpdate");
var zt = o(({ mutationOptions: e, overtimeOptions: t } = {}) => {
  let r = Y(), { mutate: s } = ie({ v3LegacyAuthProviderCompatible: !!(r != null && r.isLegacy) }), n = se(), i2 = be(), { resources: a, select: u } = $(), p3 = _(), c = Be(), { log: d3 } = Ne(), f2 = ue(), R2 = J(), { options: { textTransformers: g2 } } = oe(), { keys: x2, preferLegacyKeys: E2 } = O2(), w = useMutation({ mutationFn: ({ resource: h, values: D, meta: I2, metaData: T2, dataProviderName: y2 }) => {
    let { resource: l2, identifier: C2 } = u(h), v2 = R2({ resource: l2, meta: S(I2, T2) });
    return n(z(C2, y2, a)).create({ resource: l2.name, variables: D, meta: v2, metaData: v2 });
  }, onSuccess: (h, { resource: D, successNotification: I2, dataProviderName: T2, invalidates: y2 = ["list", "many"], values: l2, meta: C2, metaData: v2 }) => {
    var B, j;
    let { resource: P2, identifier: U } = u(D), b = g2.singular(U), M = z(U, T2, a), k = R2({ resource: P2, meta: S(C2, v2) }), L = typeof I2 == "function" ? I2(h, l2, U) : I2;
    f2(L, { key: `create-${U}-notification`, message: p3("notifications.createSuccess", { resource: p3(`${U}.${U}`, b) }, `Successfully created ${b}`), description: p3("notifications.success", "Success"), type: "success" }), i2({ resource: U, dataProviderName: M, invalidates: y2 }), c == null || c({ channel: `resources/${P2.name}`, type: "created", payload: { ids: (B = h == null ? void 0 : h.data) != null && B.id ? [h.data.id] : void 0 }, date: /* @__PURE__ */ new Date(), meta: { ...k, dataProviderName: M } });
    let { fields: Q, operation: F, variables: G, ...N } = k || {};
    d3 == null || d3.mutate({ action: "create", resource: P2.name, data: l2, meta: { dataProviderName: M, id: ((j = h == null ? void 0 : h.data) == null ? void 0 : j.id) ?? void 0, ...N } });
  }, onError: (h, { resource: D, errorNotification: I2, values: T2 }) => {
    s(h);
    let { identifier: y2 } = u(D), l2 = g2.singular(y2), C2 = typeof I2 == "function" ? I2(h, T2, y2) : I2;
    f2(C2, { key: `create-${y2}-notification`, description: h.message, message: p3("notifications.createError", { resource: p3(`${y2}.${y2}`, l2), statusCode: h.statusCode }, `There was an error creating ${l2} (status code: ${h.statusCode})`), type: "error" });
  }, mutationKey: x2().data().mutation("create").get(E2), ...e, meta: { ...e == null ? void 0 : e.meta, ...P("useCreate", E2) } }), { elapsedTime: m2 } = te({ isLoading: w.isLoading, interval: t == null ? void 0 : t.interval, onInterval: t == null ? void 0 : t.onInterval });
  return { ...w, overtime: { elapsedTime: m2 } };
}, "useCreate");
var Pa = o(({ mutationOptions: e, overtimeOptions: t } = {}) => {
  let r = Y(), { mutate: s } = ie({ v3LegacyAuthProviderCompatible: !!(r != null && r.isLegacy) }), n = se(), { resources: i2, select: a } = $(), u = useQueryClient(), { mutationMode: p3, undoableTimeout: c } = ze(), { notificationDispatch: d3 } = Ze(), f2 = _(), R2 = Be(), { log: g2 } = Ne(), x2 = ue(), E2 = be(), w = J(), { options: { textTransformers: m2 } } = oe(), { keys: h, preferLegacyKeys: D } = O2(), I2 = useMutation({ mutationFn: ({ id: y2, mutationMode: l2, undoableTimeout: C2, resource: v2, onCancel: P2, meta: U, metaData: b, dataProviderName: M, values: k }) => {
    let { resource: L, identifier: Q } = a(v2), F = w({ resource: L, meta: S(U, b) }), G = l2 ?? p3, N = C2 ?? c;
    return G !== "undoable" ? n(z(Q, M, i2)).deleteOne({ resource: L.name, id: y2, meta: F, metaData: F, variables: k }) : new Promise((j, K) => {
      let W = o(() => {
        n(z(Q, M, i2)).deleteOne({ resource: L.name, id: y2, meta: F, metaData: F, variables: k }).then((H) => j(H)).catch((H) => K(H));
      }, "doMutation"), V = o(() => {
        K({ message: "mutationCancelled" });
      }, "cancelMutation");
      P2 && P2(V), d3({ type: "ADD", payload: { id: y2, resource: Q, cancelMutation: V, doMutation: W, seconds: N, isSilent: !!P2 } });
    });
  }, onMutate: async ({ id: y2, resource: l2, mutationMode: C2, dataProviderName: v2, meta: P2, metaData: U }) => {
    let { identifier: b } = a(l2), { gqlMutation: M, gqlQuery: k, ...L } = S(P2, U) ?? {}, Q = Je(D)(b, z(b, v2, i2), L), F = h().data(z(b, v2, i2)).resource(b), G = C2 ?? p3;
    await u.cancelQueries(F.get(D), void 0, { silent: true });
    let N = u.getQueriesData(F.get(D));
    return G !== "pessimistic" && (u.setQueriesData(F.action("list").params(L ?? {}).get(D), (B) => B ? { data: B.data.filter((K) => {
      var W;
      return ((W = K.id) == null ? void 0 : W.toString()) !== y2.toString();
    }), total: B.total - 1 } : null), u.setQueriesData(F.action("many").get(D), (B) => {
      if (!B)
        return null;
      let j = B.data.filter((K) => {
        var W;
        return ((W = K.id) == null ? void 0 : W.toString()) !== (y2 == null ? void 0 : y2.toString());
      });
      return { ...B, data: j };
    })), { previousQueries: N, queryKey: Q };
  }, onSettled: (y2, l2, { id: C2, resource: v2, dataProviderName: P2, invalidates: U = ["list", "many"] }) => {
    let { identifier: b } = a(v2);
    E2({ resource: b, dataProviderName: z(b, P2, i2), invalidates: U }), d3({ type: "REMOVE", payload: { id: C2, resource: b } });
  }, onSuccess: (y2, { id: l2, resource: C2, successNotification: v2, dataProviderName: P2, meta: U, metaData: b }, M) => {
    let { resource: k, identifier: L } = a(C2), Q = m2.singular(L), F = z(L, P2, i2), G = w({ resource: k, meta: S(U, b) });
    u.removeQueries(M == null ? void 0 : M.queryKey.detail(l2));
    let N = typeof v2 == "function" ? v2(y2, l2, L) : v2;
    x2(N, { key: `${l2}-${L}-notification`, description: f2("notifications.success", "Success"), message: f2("notifications.deleteSuccess", { resource: f2(`${L}.${L}`, Q) }, `Successfully deleted a ${Q}`), type: "success" }), R2 == null || R2({ channel: `resources/${k.name}`, type: "deleted", payload: { ids: [l2] }, date: /* @__PURE__ */ new Date(), meta: { ...G, dataProviderName: F } });
    let { fields: B, operation: j, variables: K, ...W } = G || {};
    g2 == null || g2.mutate({ action: "delete", resource: k.name, meta: { id: l2, dataProviderName: F, ...W } }), u.removeQueries(M == null ? void 0 : M.queryKey.detail(l2));
  }, onError: (y2, { id: l2, resource: C2, errorNotification: v2 }, P2) => {
    let { identifier: U } = a(C2);
    if (P2)
      for (let b of P2.previousQueries)
        u.setQueryData(b[0], b[1]);
    if (y2.message !== "mutationCancelled") {
      s(y2);
      let b = m2.singular(U), M = typeof v2 == "function" ? v2(y2, l2, U) : v2;
      x2(M, { key: `${l2}-${U}-notification`, message: f2("notifications.deleteError", { resource: b, statusCode: y2.statusCode }, `Error (status code: ${y2.statusCode})`), description: y2.message, type: "error" });
    }
  }, mutationKey: h().data().mutation("delete").get(D), ...e, meta: { ...e == null ? void 0 : e.meta, ...P("useDelete", D) } }), { elapsedTime: T2 } = te({ isLoading: I2.isLoading, interval: t == null ? void 0 : t.interval, onInterval: t == null ? void 0 : t.onInterval });
  return { ...I2, overtime: { elapsedTime: T2 } };
}, "useDelete");
var co = o(({ mutationOptions: e, overtimeOptions: t } = {}) => {
  let r = se(), { resources: s, select: n } = $(), i2 = _(), a = Be(), u = ue(), p3 = be(), { log: c } = Ne(), d3 = J(), { options: { textTransformers: f2 } } = oe(), { keys: R2, preferLegacyKeys: g2 } = O2(), x2 = useMutation({ mutationFn: ({ resource: w, values: m2, meta: h, metaData: D, dataProviderName: I2 }) => {
    let { resource: T2, identifier: y2 } = n(w), l2 = d3({ resource: T2, meta: S(h, D) }), C2 = r(z(y2, I2, s));
    return C2.createMany ? C2.createMany({ resource: T2.name, variables: m2, meta: l2, metaData: l2 }) : qe(m2.map((v2) => C2.create({ resource: T2.name, variables: v2, meta: l2, metaData: l2 })));
  }, onSuccess: (w, { resource: m2, successNotification: h, dataProviderName: D, invalidates: I2 = ["list", "many"], values: T2, meta: y2, metaData: l2 }) => {
    let { resource: C2, identifier: v2 } = n(m2), P2 = f2.plural(v2), U = z(v2, D, s), b = d3({ resource: C2, meta: S(y2, l2) }), M = typeof h == "function" ? h(w, T2, v2) : h;
    u(M, { key: `createMany-${v2}-notification`, message: i2("notifications.createSuccess", { resource: i2(`${v2}.${v2}`, v2) }, `Successfully created ${P2}`), description: i2("notifications.success", "Success"), type: "success" }), p3({ resource: v2, dataProviderName: U, invalidates: I2 });
    let k = w == null ? void 0 : w.data.filter((N) => (N == null ? void 0 : N.id) !== void 0).map((N) => N.id);
    a == null || a({ channel: `resources/${C2.name}`, type: "created", payload: { ids: k }, date: /* @__PURE__ */ new Date(), meta: { ...b, dataProviderName: U } });
    let { fields: L, operation: Q, variables: F, ...G } = b || {};
    c == null || c.mutate({ action: "createMany", resource: C2.name, data: T2, meta: { dataProviderName: U, ids: k, ...G } });
  }, onError: (w, { resource: m2, errorNotification: h, values: D }) => {
    let { identifier: I2 } = n(m2), T2 = typeof h == "function" ? h(w, D, I2) : h;
    u(T2, { key: `createMany-${I2}-notification`, description: w.message, message: i2("notifications.createError", { resource: i2(`${I2}.${I2}`, I2), statusCode: w.statusCode }, `There was an error creating ${I2} (status code: ${w.statusCode}`), type: "error" });
  }, mutationKey: R2().data().mutation("createMany").get(g2), ...e, meta: { ...e == null ? void 0 : e.meta, ...P("useCreateMany", g2) } }), { elapsedTime: E2 } = te({ isLoading: x2.isLoading, interval: t == null ? void 0 : t.interval, onInterval: t == null ? void 0 : t.onInterval });
  return { ...x2, overtime: { elapsedTime: E2 } };
}, "useCreateMany");
var La = o(({ mutationOptions: e, overtimeOptions: t } = {}) => {
  let { resources: r, select: s } = $(), n = useQueryClient(), i2 = se(), a = _(), { mutationMode: u, undoableTimeout: p3 } = ze(), c = Y(), { mutate: d3 } = ie({ v3LegacyAuthProviderCompatible: !!(c != null && c.isLegacy) }), { notificationDispatch: f2 } = Ze(), R2 = Be(), g2 = ue(), x2 = be(), { log: E2 } = Ne(), w = J(), { options: { textTransformers: m2 } } = oe(), { keys: h, preferLegacyKeys: D } = O2(), I2 = useMutation({ mutationFn: ({ ids: y2, values: l2, resource: C2, onCancel: v2, mutationMode: P2, undoableTimeout: U, meta: b, metaData: M, dataProviderName: k }) => {
    let { resource: L, identifier: Q } = s(C2), F = w({ resource: L, meta: S(b, M) }), G = P2 ?? u, N = U ?? p3, B = i2(z(Q, k, r)), j = o(() => B.updateMany ? B.updateMany({ resource: L.name, ids: y2, variables: l2, meta: F, metaData: F }) : qe(y2.map((W) => B.update({ resource: L.name, id: W, variables: l2, meta: F, metaData: F }))), "mutationFn");
    return G !== "undoable" ? j() : new Promise((W, V) => {
      let H = o(() => {
        j().then((X) => W(X)).catch((X) => V(X));
      }, "doMutation"), Z = o(() => {
        V({ message: "mutationCancelled" });
      }, "cancelMutation");
      v2 && v2(Z), f2({ type: "ADD", payload: { id: y2, resource: Q, cancelMutation: Z, doMutation: H, seconds: N, isSilent: !!v2 } });
    });
  }, onMutate: async ({ resource: y2, ids: l2, values: C2, mutationMode: v2, dataProviderName: P2, meta: U, metaData: b, optimisticUpdateMap: M = { list: true, many: true, detail: true } }) => {
    let { identifier: k } = s(y2), { gqlMutation: L, gqlQuery: Q, ...F } = S(U, b) ?? {}, G = Je(D)(k, z(k, P2, r), F), N = h().data(z(k, P2, r)).resource(k), B = v2 ?? u;
    await n.cancelQueries(N.get(D), void 0, { silent: true });
    let j = n.getQueriesData(N.get(D));
    if (B !== "pessimistic" && (M.list && n.setQueriesData(N.action("list").params(F ?? {}).get(D), (K) => {
      if (typeof M.list == "function")
        return M.list(K, C2, l2);
      if (!K)
        return null;
      let W = K.data.map((V) => V.id !== void 0 && l2.filter((H) => H !== void 0).map(String).includes(V.id.toString()) ? { ...V, ...C2 } : V);
      return { ...K, data: W };
    }), M.many && n.setQueriesData(N.action("many").get(D), (K) => {
      if (typeof M.many == "function")
        return M.many(K, C2, l2);
      if (!K)
        return null;
      let W = K.data.map((V) => V.id !== void 0 && l2.filter((H) => H !== void 0).map(String).includes(V.id.toString()) ? { ...V, ...C2 } : V);
      return { ...K, data: W };
    }), M.detail))
      for (let K of l2)
        n.setQueriesData(N.action("one").id(K).params(F ?? {}).get(D), (W) => {
          if (typeof M.detail == "function")
            return M.detail(W, C2, K);
          if (!W)
            return null;
          let V = { ...W.data, ...C2 };
          return { ...W, data: V };
        });
    return { previousQueries: j, queryKey: G };
  }, onSettled: (y2, l2, { ids: C2, resource: v2, dataProviderName: P2 }) => {
    let { identifier: U } = s(v2);
    x2({ resource: U, invalidates: ["list", "many"], dataProviderName: z(U, P2, r) }), C2.forEach((b) => x2({ resource: U, invalidates: ["detail"], dataProviderName: z(U, P2, r), id: b })), f2({ type: "REMOVE", payload: { id: C2, resource: U } });
  }, onSuccess: (y2, { ids: l2, resource: C2, meta: v2, metaData: P2, dataProviderName: U, successNotification: b, values: M }, k) => {
    let { resource: L, identifier: Q } = s(C2), F = m2.singular(Q), G = z(Q, U, r), N = w({ resource: L, meta: S(v2, P2) }), B = typeof b == "function" ? b(y2, { ids: l2, values: M }, Q) : b;
    g2(B, { key: `${l2}-${Q}-notification`, description: a("notifications.success", "Successful"), message: a("notifications.editSuccess", { resource: a(`${Q}.${Q}`, Q) }, `Successfully updated ${F}`), type: "success" }), R2 == null || R2({ channel: `resources/${L.name}`, type: "updated", payload: { ids: l2.map(String) }, date: /* @__PURE__ */ new Date(), meta: { ...N, dataProviderName: G } });
    let j = [];
    k && l2.forEach((Z) => {
      let X = n.getQueryData(k.queryKey.detail(Z));
      j.push(Object.keys(M || {}).reduce((q, Ce) => {
        var De;
        return q[Ce] = (De = X == null ? void 0 : X.data) == null ? void 0 : De[Ce], q;
      }, {}));
    });
    let { fields: K, operation: W, variables: V, ...H } = N || {};
    E2 == null || E2.mutate({ action: "updateMany", resource: L.name, data: M, previousData: j, meta: { ids: l2, dataProviderName: G, ...H } });
  }, onError: (y2, { ids: l2, resource: C2, errorNotification: v2, values: P2 }, U) => {
    let { identifier: b } = s(C2);
    if (U)
      for (let M of U.previousQueries)
        n.setQueryData(M[0], M[1]);
    if (y2.message !== "mutationCancelled") {
      d3 == null || d3(y2);
      let M = m2.singular(b), k = typeof v2 == "function" ? v2(y2, { ids: l2, values: P2 }, b) : v2;
      g2(k, { key: `${l2}-${b}-updateMany-error-notification`, message: a("notifications.editError", { resource: M, statusCode: y2.statusCode }, `Error when updating ${M} (status code: ${y2.statusCode})`), description: y2.message, type: "error" });
    }
  }, mutationKey: h().data().mutation("updateMany").get(D), ...e, meta: { ...e == null ? void 0 : e.meta, ...P("useUpdateMany", D) } }), { elapsedTime: T2 } = te({ isLoading: I2.isLoading, interval: t == null ? void 0 : t.interval, onInterval: t == null ? void 0 : t.onInterval });
  return { ...I2, overtime: { elapsedTime: T2 } };
}, "useUpdateMany");
var wa = o(({ mutationOptions: e, overtimeOptions: t } = {}) => {
  let r = Y(), { mutate: s } = ie({ v3LegacyAuthProviderCompatible: !!(r != null && r.isLegacy) }), { mutationMode: n, undoableTimeout: i2 } = ze(), a = se(), { notificationDispatch: u } = Ze(), p3 = _(), c = Be(), d3 = ue(), f2 = be(), { log: R2 } = Ne(), { resources: g2, select: x2 } = $(), E2 = useQueryClient(), w = J(), { options: { textTransformers: m2 } } = oe(), { keys: h, preferLegacyKeys: D } = O2(), I2 = useMutation({ mutationFn: ({ resource: y2, ids: l2, mutationMode: C2, undoableTimeout: v2, onCancel: P2, meta: U, metaData: b, dataProviderName: M, values: k }) => {
    let { resource: L, identifier: Q } = x2(y2), F = w({ resource: L, meta: S(U, b) }), G = C2 ?? n, N = v2 ?? i2, B = a(z(Q, M, g2)), j = o(() => B.deleteMany ? B.deleteMany({ resource: L.name, ids: l2, meta: F, metaData: F, variables: k }) : qe(l2.map((W) => B.deleteOne({ resource: L.name, id: W, meta: F, metaData: F, variables: k }))), "mutationFn");
    return G !== "undoable" ? j() : new Promise((W, V) => {
      let H = o(() => {
        j().then((X) => W(X)).catch((X) => V(X));
      }, "doMutation"), Z = o(() => {
        V({ message: "mutationCancelled" });
      }, "cancelMutation");
      P2 && P2(Z), u({ type: "ADD", payload: { id: l2, resource: Q, cancelMutation: Z, doMutation: H, seconds: N, isSilent: !!P2 } });
    });
  }, onMutate: async ({ ids: y2, resource: l2, mutationMode: C2, dataProviderName: v2, meta: P2, metaData: U }) => {
    let { identifier: b } = x2(l2), { gqlMutation: M, gqlQuery: k, ...L } = S(P2, U) ?? {}, Q = Je(D)(b, z(b, v2, g2), L), F = h().data(z(b, v2, g2)).resource(b), G = C2 ?? n;
    await E2.cancelQueries(F.get(D), void 0, { silent: true });
    let N = E2.getQueriesData(F.get(D));
    if (G !== "pessimistic") {
      E2.setQueriesData(F.action("list").params(L ?? {}).get(D), (B) => B ? { data: B.data.filter((K) => K.id && !y2.map(String).includes(K.id.toString())), total: B.total - 1 } : null), E2.setQueriesData(F.action("many").get(D), (B) => {
        if (!B)
          return null;
        let j = B.data.filter((K) => K.id ? !y2.map(String).includes(K.id.toString()) : false);
        return { ...B, data: j };
      });
      for (let B of y2)
        E2.setQueriesData(F.action("one").id(B).params(L).get(D), (j) => !j || j.data.id == B ? null : { ...j });
    }
    return { previousQueries: N, queryKey: Q };
  }, onSettled: (y2, l2, { resource: C2, ids: v2, dataProviderName: P2, invalidates: U = ["list", "many"] }) => {
    let { identifier: b } = x2(C2);
    f2({ resource: b, dataProviderName: z(b, P2, g2), invalidates: U }), u({ type: "REMOVE", payload: { id: v2, resource: b } });
  }, onSuccess: (y2, { ids: l2, resource: C2, meta: v2, metaData: P2, dataProviderName: U, successNotification: b }, M) => {
    let { resource: k, identifier: L } = x2(C2), Q = z(L, U, g2), F = w({ resource: k, meta: S(v2, P2) });
    l2.forEach((W) => E2.removeQueries(M == null ? void 0 : M.queryKey.detail(W)));
    let G = typeof b == "function" ? b(y2, l2, L) : b;
    d3(G, { key: `${l2}-${L}-notification`, description: p3("notifications.success", "Success"), message: p3("notifications.deleteSuccess", { resource: p3(`${L}.${L}`, L) }, `Successfully deleted ${L}`), type: "success" }), c == null || c({ channel: `resources/${k.name}`, type: "deleted", payload: { ids: l2 }, date: /* @__PURE__ */ new Date(), meta: { ...F, dataProviderName: Q } });
    let { fields: N, operation: B, variables: j, ...K } = F || {};
    R2 == null || R2.mutate({ action: "deleteMany", resource: k.name, meta: { ids: l2, dataProviderName: Q, ...K } }), l2.forEach((W) => E2.removeQueries(M == null ? void 0 : M.queryKey.detail(W)));
  }, onError: (y2, { ids: l2, resource: C2, errorNotification: v2 }, P2) => {
    let { identifier: U } = x2(C2);
    if (P2)
      for (let b of P2.previousQueries)
        E2.setQueryData(b[0], b[1]);
    if (y2.message !== "mutationCancelled") {
      s(y2);
      let b = m2.singular(U), M = typeof v2 == "function" ? v2(y2, l2, U) : v2;
      d3(M, { key: `${l2}-${U}-notification`, message: p3("notifications.deleteError", { resource: b, statusCode: y2.statusCode }, `Error (status code: ${y2.statusCode})`), description: y2.message, type: "error" });
    }
  }, mutationKey: h().data().mutation("deleteMany").get(D), ...e, meta: { ...e == null ? void 0 : e.meta, ...P("useDeleteMany", D) } }), { elapsedTime: T2 } = te({ isLoading: I2.isLoading, interval: t == null ? void 0 : t.interval, onInterval: t == null ? void 0 : t.onInterval });
  return { ...I2, overtime: { elapsedTime: T2 } };
}, "useDeleteMany");
var Ia = o((e) => {
  var n;
  let t = se(), { resource: r } = $(), { getApiUrl: s } = t(e ?? ((n = S(r == null ? void 0 : r.meta, r == null ? void 0 : r.options)) == null ? void 0 : n.dataProviderName));
  return s();
}, "useApiUrl");
var Aa = o(({ url: e, method: t, config: r, queryOptions: s, successNotification: n, errorNotification: i2, meta: a, metaData: u, dataProviderName: p3, overtimeOptions: c }) => {
  let d3 = se(), f2 = Y(), { mutate: R2 } = ie({ v3LegacyAuthProviderCompatible: !!(f2 != null && f2.isLegacy) }), g2 = _(), x2 = ue(), E2 = J(), { keys: w, preferLegacyKeys: m2 } = O2(), h = S(a, u), { custom: D } = d3(p3), I2 = E2({ meta: h });
  if (D) {
    let T2 = useQuery({ queryKey: w().data(p3).mutation("custom").params({ method: t, url: e, ...r, ...h || {} }).get(m2), queryFn: ({ queryKey: l2, pageParam: C2, signal: v2 }) => D({ url: e, method: t, ...r, meta: { ...I2, queryContext: { queryKey: l2, pageParam: C2, signal: v2 } }, metaData: { ...I2, queryContext: { queryKey: l2, pageParam: C2, signal: v2 } } }), ...s, onSuccess: (l2) => {
      var v2;
      (v2 = s == null ? void 0 : s.onSuccess) == null || v2.call(s, l2);
      let C2 = typeof n == "function" ? n(l2, { ...r, ...I2 }) : n;
      x2(C2);
    }, onError: (l2) => {
      var v2;
      R2(l2), (v2 = s == null ? void 0 : s.onError) == null || v2.call(s, l2);
      let C2 = typeof i2 == "function" ? i2(l2, { ...r, ...I2 }) : i2;
      x2(C2, { key: `${t}-notification`, message: g2("notifications.error", { statusCode: l2.statusCode }, `Error (status code: ${l2.statusCode})`), description: l2.message, type: "error" });
    }, meta: { ...s == null ? void 0 : s.meta, ...P("useCustom", m2) } }), { elapsedTime: y2 } = te({ isLoading: T2.isFetching, interval: c == null ? void 0 : c.interval, onInterval: c == null ? void 0 : c.onInterval });
    return { ...T2, overtime: { elapsedTime: y2 } };
  } else
    throw Error("Not implemented custom on data provider.");
}, "useCustom");
var Va = o(({ mutationOptions: e, overtimeOptions: t } = {}) => {
  let r = Y(), { mutate: s } = ie({ v3LegacyAuthProviderCompatible: !!(r != null && r.isLegacy) }), n = ue(), i2 = se(), a = _(), u = J(), { keys: p3, preferLegacyKeys: c } = O2(), d3 = useMutation(({ url: R2, method: g2, values: x2, meta: E2, metaData: w, dataProviderName: m2, config: h }) => {
    let D = u({ meta: S(E2, w) }), { custom: I2 } = i2(m2);
    if (I2)
      return I2({ url: R2, method: g2, payload: x2, meta: D, metaData: D, headers: { ...h == null ? void 0 : h.headers } });
    throw Error("Not implemented custom on data provider.");
  }, { onSuccess: (R2, { successNotification: g2, config: x2, meta: E2, metaData: w }) => {
    let m2 = typeof g2 == "function" ? g2(R2, { ...x2, ...S(E2, w) || {} }) : g2;
    n(m2);
  }, onError: (R2, { errorNotification: g2, method: x2, config: E2, meta: w, metaData: m2 }) => {
    s(R2);
    let h = typeof g2 == "function" ? g2(R2, { ...E2, ...S(w, m2) || {} }) : g2;
    n(h, { key: `${x2}-notification`, message: a("notifications.error", { statusCode: R2.statusCode }, `Error (status code: ${R2.statusCode})`), description: R2.message, type: "error" });
  }, mutationKey: p3().data().mutation("customMutation").get(c), ...e, meta: { ...e == null ? void 0 : e.meta, ...P("useCustomMutation", c) } }), { elapsedTime: f2 } = te({ isLoading: d3.isLoading, interval: t == null ? void 0 : t.interval, onInterval: t == null ? void 0 : t.onInterval });
  return { ...d3, overtime: { elapsedTime: f2 } };
}, "useCustomMutation");
var Ba = o(() => ({}), "defaultDataProvider");
var _t = import_react18.default.createContext(Ba());
var Ps = o(({ children: e, ...t }) => {
  let r;
  return !t.getList || !t.getOne ? r = t : r = { default: t }, import_react18.default.createElement(_t.Provider, { value: r }, e);
}, "DataContextProvider");
var se = o(() => {
  let e = (0, import_react17.useContext)(_t);
  return (0, import_react17.useCallback)((r) => {
    if (r) {
      if (!(e == null ? void 0 : e[r]))
        throw new Error(`"${r}" Data provider not found`);
      return e[r];
    }
    if (e.default)
      return e.default;
    throw new Error('There is no "default" data provider. Please pass dataProviderName.');
  }, [e]);
}, "useDataProvider");
var $a = o(({ resource: e, config: t, filters: r, hasPagination: s, pagination: n, sorters: i2, queryOptions: a, successNotification: u, errorNotification: p3, meta: c, metaData: d3, liveMode: f2, onLiveEvent: R2, liveParams: g2, dataProviderName: x2, overtimeOptions: E2 }) => {
  let { resources: w, resource: m2, identifier: h } = $(e), D = se(), I2 = _(), T2 = Y(), { mutate: y2 } = ie({ v3LegacyAuthProviderCompatible: !!(T2 != null && T2.isLegacy) }), l2 = ue(), C2 = J(), { keys: v2, preferLegacyKeys: P2 } = O2(), U = z(h, x2, w), b = S(c, d3), M = S(r, t == null ? void 0 : t.filters), k = S(i2, t == null ? void 0 : t.sort), L = S(s, t == null ? void 0 : t.hasPagination), Q = Gt({ pagination: n, configPagination: t == null ? void 0 : t.pagination, hasPagination: L }), F = Q.mode === "server", G = { meta: b, metaData: b, filters: M, hasPagination: F, pagination: Q, sorters: k, config: { ...t, sort: k } }, N = (a == null ? void 0 : a.enabled) === void 0 || (a == null ? void 0 : a.enabled) === true, B = C2({ resource: m2, meta: b }), { getList: j } = D(U);
  it({ resource: h, types: ["*"], params: { meta: B, metaData: B, pagination: Q, hasPagination: F, sort: k, sorters: k, filters: M, subscriptionType: "useList", ...g2 }, channel: `resources/${m2.name}`, enabled: N, liveMode: f2, onLiveEvent: R2, dataProviderName: U, meta: { ...B, dataProviderName: x2 } });
  let K = useInfiniteQuery({ queryKey: v2().data(U).resource(h).action("infinite").params({ ...b || {}, filters: M, hasPagination: F, ...F && { pagination: Q }, ...i2 && { sorters: i2 }, ...(t == null ? void 0 : t.sort) && { sort: t == null ? void 0 : t.sort } }).get(P2), queryFn: ({ queryKey: V, pageParam: H, signal: Z }) => {
    let X = { ...Q, current: H };
    return j({ resource: m2.name, pagination: X, hasPagination: F, filters: M, sort: k, sorters: k, meta: { ...B, queryContext: { queryKey: V, pageParam: H, signal: Z } }, metaData: { ...B, queryContext: { queryKey: V, pageParam: H, signal: Z } } }).then(({ data: q, total: Ce, ...De }) => ({ data: q, total: Ce, pagination: X, ...De }));
  }, getNextPageParam: (V) => fr(V), getPreviousPageParam: (V) => yr(V), ...a, onSuccess: (V) => {
    var Z;
    (Z = a == null ? void 0 : a.onSuccess) == null || Z.call(a, V);
    let H = typeof u == "function" ? u(V, G, h) : u;
    l2(H);
  }, onError: (V) => {
    var Z;
    y2(V), (Z = a == null ? void 0 : a.onError) == null || Z.call(a, V);
    let H = typeof p3 == "function" ? p3(V, G, h) : p3;
    l2(H, { key: `${h}-useInfiniteList-notification`, message: I2("notifications.error", { statusCode: V.statusCode }, `Error (status code: ${V.statusCode})`), description: V.message, type: "error" });
  }, meta: { ...a == null ? void 0 : a.meta, ...P("useInfiniteList", P2) } }), { elapsedTime: W } = te({ isLoading: K.isFetching, interval: E2 == null ? void 0 : E2.interval, onInterval: E2 == null ? void 0 : E2.onInterval });
  return { ...K, overtime: { elapsedTime: W } };
}, "useInfiniteList");
var et = import_react19.default.createContext(void 0);
var Cs = o(({ liveProvider: e, children: t }) => import_react19.default.createElement(et.Provider, { value: e }, t), "LiveContextProvider");
var bs = o((e) => {
  let t = (0, import_react24.useRef)(e);
  return isEqual_default(t.current, e) || (t.current = e), t.current;
}, "useMemoized");
var Mr = o((e, t) => {
  let r = bs(t);
  return (0, import_react23.useMemo)(e, r);
}, "useDeepMemo");
var ut = import_react22.default.createContext({ resources: [] });
var Ds = o(({ resources: e, children: t }) => {
  let r = Mr(() => gr(e ?? []), [e]);
  return import_react22.default.createElement(ut.Provider, { value: { resources: r } }, t);
}, "ResourceContextProvider");
var Es = import_react25.default.createContext("new");
var Us = Es.Provider;
var ee = o(() => import_react25.default.useContext(Es), "useRouterType");
var Ms = {};
var tt = (0, import_react27.createContext)(Ms);
var ws = o(({ children: e, router: t }) => import_react27.default.createElement(tt.Provider, { value: t ?? Ms }, e), "RouterBindingsProvider");
var po = o(() => {
  let e = (0, import_react28.useContext)(tt);
  return import_react28.default.useMemo(() => (e == null ? void 0 : e.parse) ?? (() => () => ({})), [e == null ? void 0 : e.parse])();
}, "useParse");
var ge = o(() => {
  let e = po();
  return import_react26.default.useMemo(() => e(), [e]);
}, "useParsed");
function $(e) {
  let { resources: t } = (0, import_react21.useContext)(ut), r = ee(), s = ge(), n = { resourceName: e && typeof e != "string" ? e.resourceName : e, resourceNameOrRouteName: e && typeof e != "string" ? e.resourceNameOrRouteName : e, recordItemId: e && typeof e != "string" ? e.recordItemId : void 0 }, i2 = o((f2, R2 = true) => {
    let x2 = de(f2, t, r === "legacy");
    if (x2)
      return { resource: x2, identifier: x2.identifier ?? x2.name };
    if (R2) {
      let E2 = { name: f2, identifier: f2 }, w = E2.identifier ?? E2.name;
      return { resource: E2, identifier: w };
    }
  }, "select"), a = Is(), { useParams: u } = ae(), p3 = u();
  if (r === "legacy") {
    let f2 = n.resourceNameOrRouteName ? n.resourceNameOrRouteName : p3.resource, R2 = f2 ? a(f2) : void 0, g2 = (n == null ? void 0 : n.recordItemId) ?? p3.id, x2 = p3.action, E2 = (n == null ? void 0 : n.resourceName) ?? (R2 == null ? void 0 : R2.name), w = (R2 == null ? void 0 : R2.identifier) ?? (R2 == null ? void 0 : R2.name);
    return { resources: t, resource: R2, resourceName: E2, id: g2, action: x2, select: i2, identifier: w };
  }
  let c, d3 = typeof e == "string" ? e : n == null ? void 0 : n.resourceNameOrRouteName;
  if (d3) {
    let f2 = de(d3, t);
    f2 ? c = f2 : c = { name: d3 };
  } else
    s != null && s.resource && (c = s.resource);
  return { resources: t, resource: c, resourceName: c == null ? void 0 : c.name, id: s.id, action: s.action, select: i2, identifier: (c == null ? void 0 : c.identifier) ?? (c == null ? void 0 : c.name) };
}
o($, "useResource");
var Is = o(() => {
  let { resources: e } = (0, import_react29.useContext)(ut);
  return (0, import_react29.useCallback)((r) => {
    let s = de(r, e, true);
    return s || { name: r, route: r };
  }, [e]);
}, "useResourceWithRoute");
var be = o(() => {
  let { resources: e } = $(), t = useQueryClient(), { keys: r, preferLegacyKeys: s } = O2();
  return (0, import_react20.useCallback)(async ({ resource: i2, dataProviderName: a, invalidates: u, id: p3, invalidationFilters: c = { type: "all", refetchType: "active" }, invalidationOptions: d3 = { cancelRefetch: false } }) => {
    if (u === false)
      return;
    let f2 = z(i2, a, e), R2 = r().data(f2).resource(i2 ?? "");
    await Promise.all(u.map((g2) => {
      switch (g2) {
        case "all":
          return t.invalidateQueries(r().data(f2).get(s), c, d3);
        case "list":
          return t.invalidateQueries(R2.action("list").get(s), c, d3);
        case "many":
          return t.invalidateQueries(R2.action("many").get(s), c, d3);
        case "resourceAll":
          return t.invalidateQueries(R2.get(s), c, d3);
        case "detail":
          return t.invalidateQueries(R2.action("one").id(p3 || "").get(s), c, d3);
        default:
          return;
      }
    }));
  }, []);
}, "useInvalidate");
var it = o(({ resource: e, params: t, channel: r, types: s, enabled: n = true, liveMode: i2, onLiveEvent: a, dataProviderName: u, meta: p3 }) => {
  var m2;
  let { resource: c, identifier: d3 } = $(e), f2 = (0, import_react30.useContext)(et), { liveMode: R2, onLiveEvent: g2 } = (0, import_react30.useContext)(Me), x2 = i2 ?? R2, E2 = be(), w = u ?? (p3 == null ? void 0 : p3.dataProviderName) ?? ((m2 = c == null ? void 0 : c.meta) == null ? void 0 : m2.dataProviderName);
  (0, import_react30.useEffect)(() => {
    let h, D = o((I2) => {
      x2 === "auto" && E2({ resource: d3, dataProviderName: w, invalidates: ["resourceAll"], invalidationFilters: { type: "active", refetchType: "active" }, invalidationOptions: { cancelRefetch: false } }), a == null || a(I2), g2 == null || g2(I2);
    }, "callback");
    return x2 && x2 !== "off" && n && (h = f2 == null ? void 0 : f2.subscribe({ channel: r, params: { resource: c == null ? void 0 : c.name, ...t }, types: s, callback: D, dataProviderName: w, meta: { ...p3, dataProviderName: w } })), () => {
      h && (f2 == null || f2.unsubscribe(h));
    };
  }, [n]);
}, "useResourceSubscription");
var Fs = o((e) => {
  let { liveMode: t } = (0, import_react31.useContext)(Me);
  return e ?? t;
}, "useLiveMode");
var vx = o(({ params: e, channel: t, types: r = ["*"], enabled: s = true, onLiveEvent: n, dataProviderName: i2 = "default", meta: a }) => {
  let u = (0, import_react32.useContext)(et);
  (0, import_react32.useEffect)(() => {
    let p3;
    return s && (p3 = u == null ? void 0 : u.subscribe({ channel: t, params: e, types: r, callback: n, dataProviderName: i2, meta: { ...a, dataProviderName: i2 } })), () => {
      p3 && (u == null || u.unsubscribe(p3));
    };
  }, [s]);
}, "useSubscription");
var Be = o(() => {
  let e = (0, import_react33.useContext)(et);
  return e == null ? void 0 : e.publish;
}, "usePublish");
var Ze = o(() => {
  let { notifications: e, notificationDispatch: t } = (0, import_react34.useContext)(Er);
  return { notifications: e, notificationDispatch: t };
}, "useCancelNotification");
var jt = (0, import_react36.createContext)({});
var As = o(({ open: e, close: t, children: r }) => import_react36.default.createElement(jt.Provider, { value: { open: e, close: t } }, r), "NotificationContextProvider");
var Ie = o(() => {
  let { open: e, close: t } = (0, import_react35.useContext)(jt);
  return { open: e, close: t };
}, "useNotification");
var ue = o(() => {
  let { open: e } = Ie();
  return (0, import_react37.useCallback)((r, s) => {
    r !== false && (r ? e == null || e(r) : s && (e == null || e(s)));
  }, []);
}, "useHandleNotification");
var Ke = import_react39.default.createContext({});
var Qs = o(({ children: e, i18nProvider: t }) => import_react39.default.createElement(Ke.Provider, { value: { i18nProvider: t } }, e), "TranslationContextProvider");
var yi = o(() => {
  let { i18nProvider: e } = (0, import_react38.useContext)(Ke);
  return (0, import_react38.useCallback)((t) => e == null ? void 0 : e.changeLocale(t), []);
}, "useSetLocale");
var _ = o(() => {
  let { i18nProvider: e } = (0, import_react40.useContext)(Ke);
  return (0, import_react40.useMemo)(() => {
    function r(s, n, i2) {
      return (e == null ? void 0 : e.translate(s, n, i2)) ?? i2 ?? (typeof n == "string" && typeof i2 > "u" ? n : s);
    }
    return o(r, "translate"), r;
  }, [e]);
}, "useTranslate");
var Pi = o(() => {
  let { i18nProvider: e } = (0, import_react41.useContext)(Ke);
  return (0, import_react41.useCallback)(() => e == null ? void 0 : e.getLocale(), []);
}, "useGetLocale");
var TP = o(({ resourceName: e, resource: t, sorter: r, sorters: s, filters: n, maxItemCount: i2, pageSize: a = 20, mapData: u = o((E2) => E2, "mapData"), exportOptions: p3, unparseConfig: c, meta: d3, metaData: f2, dataProviderName: R2, onError: g2, download: x2 } = {}) => {
  let [E2, w] = (0, import_react42.useState)(false), m2 = se(), h = J(), { resource: D, resources: I2, identifier: T2 } = $(S(t, e)), l2 = `${Lt()(T2, "plural")}-${(/* @__PURE__ */ new Date()).toLocaleString()}`, { getList: C2 } = m2(z(T2, R2, I2)), v2 = h({ resource: D, meta: S(d3, f2) });
  return { isLoading: E2, triggerExport: o(async () => {
    w(true);
    let U = [], b = 1, M = true;
    for (; M; )
      try {
        let { data: F, total: G } = await C2({ resource: (D == null ? void 0 : D.name) ?? "", filters: n, sort: S(s, r), sorters: S(s, r), pagination: { current: b, pageSize: a, mode: "server" }, meta: v2, metaData: v2 });
        b++, U.push(...F), i2 && U.length >= i2 && (U = U.slice(0, i2), M = false), G === U.length && (M = false);
      } catch (F) {
        w(false), M = false, g2 == null || g2(F);
        return;
      }
    let k = typeof c < "u" && c !== null;
    (0, import_warn_once2.default)(k && typeof p3 < "u" && p3 !== null, `[useExport]: resource: "${T2}" 

Both \`unparseConfig\` and \`exportOptions\` are set, \`unparseConfig\` will take precedence`);
    let L = { filename: l2, useKeysAsHeaders: true, useBom: true, title: "My Generated Report", quoteStrings: '"', ...p3 };
    (0, import_warn_once2.default)((p3 == null ? void 0 : p3.decimalSeparator) !== void 0, `[useExport]: resource: "${T2}" 

Use of \`decimalSeparator\` no longer supported, please use \`mapData\` instead.

See https://refine.dev/docs/api-reference/core/hooks/import-export/useExport/`), k ? c = { quotes: true, ...c } : c = { columns: L.useKeysAsHeaders ? void 0 : L.headers, delimiter: L.fieldSeparator, header: L.showLabels || L.useKeysAsHeaders, quoteChar: L.quoteStrings, quotes: true };
    let Q = (0, import_papaparse.unparse)(U.map(u), c);
    if (L.showTitle && (Q = L.title + `\r

` + Q), typeof window < "u" && Q.length > 0 && (x2 ?? true)) {
      let F = L.useTextFile ? ".txt" : ".csv", G = `text/${L.useTextFile ? "plain" : "csv"};charset=utf8;`, N = `${(L.filename ?? "download").replace(/ /g, "_")}${F}`;
      _r(N, `${L != null && L.useBom ? "\uFEFF" : ""}${Q}`, G);
    }
    return w(false), Q;
  }, "triggerExport") };
}, "useExport");
var SP = o(({ resource: e, action: t, id: r, onMutationSuccess: s, onMutationError: n, redirect: i2, successNotification: a, errorNotification: u, meta: p3, metaData: c, queryMeta: d3, mutationMeta: f2, mutationMode: R2, liveMode: g2, onLiveEvent: x2, liveParams: E2, undoableTimeout: w, dataProviderName: m2, invalidates: h, queryOptions: D, createMutationOptions: I2, updateMutationOptions: T2, overtimeOptions: y2, autoSave: l2, optimisticUpdateMap: C2 } = {}) => {
  let v2 = be(), { options: P2 } = oe(), U = J(), { resource: b, id: M, action: k, identifier: L } = $(e), { identifier: Q } = $(), G = o(() => {
    let ye = r ?? M;
    return e && e !== Q ? r : ye;
  }, "getDefaultId")(), [N, B] = import_react43.default.useState(G);
  import_react43.default.useEffect(() => {
    B(G);
  }, [G]);
  let K = o(() => t || (k === "edit" || k === "clone" ? k : "create"), "getAction")(), W = U({ resource: b, meta: S(p3, c) }), { mutationMode: V } = ze(), H = R2 ?? V, Z = K === "create", X = K === "edit", q = K === "clone";
  (0, import_warn_once3.default)((q || X) && !!e && !r && (D == null ? void 0 : D.enabled) !== false, `[useForm]: action: "${K}", resource: "${L}", id: ${N} 

If you don't use the \`setId\` method to set the \`id\`, you should pass the \`id\` prop to \`useForm\`. Otherwise, \`useForm\` will not be able to infer the \`id\` from the current URL. 

See https://refine.dev/docs/api-reference/core/hooks/useForm/#resource`);
  let Ce = Wr({ redirectFromProps: i2, action: K, redirectOptions: P2.redirect }), Ft = Ot({ resource: L, id: N ?? "", queryOptions: { enabled: N !== void 0 && (X || q), ...D }, liveMode: g2, onLiveEvent: x2, liveParams: E2, meta: { ...W, ...d3 }, metaData: { ...W, ...d3 }, dataProviderName: m2 }), { isFetching: pt } = Ft, dt = zt({ mutationOptions: I2 }), { mutate: Rt, isLoading: xt } = dt, Pt = Ur({ mutationOptions: T2 }), { mutate: Yt, isLoading: Vr } = Pt, je = Ur({}), { setWarnWhen: At } = Ht(), $e = Bs(), Jt = o(async (ye) => {
    At(false);
    let Qe = o((Fe) => {
      $e({ redirect: Ce, resource: b, id: Fe, meta: S(p3, c) });
    }, "onSuccess");
    return H !== "pessimistic" && setTimeout(() => {
      Qe();
    }), new Promise((Fe, st) => {
      if (H !== "pessimistic" && Fe(), !!b)
        return Rt({ values: ye, resource: L ?? b.name, successNotification: a, errorNotification: u, meta: { ...W, ...f2 }, metaData: { ...W, ...f2 }, dataProviderName: m2, invalidates: h }, { onSuccess: (Le, nt, at) => {
          var Qt;
          s && s(Le, ye, at);
          let mt = (Qt = Le == null ? void 0 : Le.data) == null ? void 0 : Qt.id;
          Qe(mt), Fe(Le);
        }, onError: (Le, nt, at) => {
          if (n)
            return n(Le, ye, at);
          st();
        } });
    });
  }, "onFinishCreate");
  import_react43.default.useEffect(() => () => {
    l2 != null && l2.invalidateOnUnmount && je.status === "success" && v2({ id: N, invalidates: h || ["list", "many", "detail"], dataProviderName: m2, resource: L });
  }, [l2 == null ? void 0 : l2.invalidateOnUnmount, je.status]);
  let lt = o((ye) => {
    if (!b || !X)
      return;
    let Qe = { id: N ?? "", values: ye, resource: L ?? b.name, successNotification: false, errorNotification: false, meta: { ...W, ...f2 }, metaData: { ...W, ...f2 }, dataProviderName: m2, invalidates: [], mutationMode: "pessimistic" };
    return je.mutate(Qe, { onSuccess: (Fe, st, Le) => {
      s && s(Fe, ye, Le, true);
    }, onError: (Fe, st, Le) => {
      if (n)
        return n(Fe, ye, Le, true);
    } });
  }, "onFinishAutoSaveMutation"), ht = debounce_default((ye) => lt(ye), (l2 == null ? void 0 : l2.debounce) || 1e3), kt = Z || q ? { formLoading: pt || xt, mutationResult: dt, onFinish: Jt } : { formLoading: pt || Vr, mutationResult: Pt, onFinish: o(async (ye) => {
    if (At(false), !b)
      return;
    let Qe = { id: N ?? "", values: ye, resource: L ?? b.name, mutationMode: H, undoableTimeout: w, successNotification: a, errorNotification: u, meta: { ...W, ...f2 }, metaData: { ...W, ...f2 }, dataProviderName: m2, invalidates: h, optimisticUpdateMap: C2 }, Fe = o(() => {
      $e({ redirect: Ce, resource: b, id: N, meta: S(p3, c) });
    }, "onSuccess");
    return H !== "pessimistic" && setTimeout(() => {
      Fe();
    }), new Promise((st, Le) => (H !== "pessimistic" && st(), setTimeout(() => {
      Yt(Qe, { onSuccess: (nt, at, mt) => {
        s && s(nt, ye, mt), H === "pessimistic" && Fe(), st(nt);
      }, onError: (nt, at, mt) => {
        if (n)
          return n(nt, ye, mt);
        Le();
      } });
    })));
  }, "onFinishUpdate") }, { elapsedTime: Nr } = te({ isLoading: kt.mutationResult.isLoading || Ft.isFetching, interval: y2 == null ? void 0 : y2.interval, onInterval: y2 == null ? void 0 : y2.onInterval });
  return { ...kt, queryResult: Ft, onFinishAutoSave: ht, autoSaveProps: { status: je.status, data: je.data, error: je.error }, id: N, setId: B, redirect: (ye, Qe) => {
    $e({ redirect: ye !== void 0 ? ye : X ? "list" : "edit", resource: b, id: Qe ?? N, meta: S(p3, c) });
  }, overtime: { elapsedTime: Nr } };
}, "useForm");
var Bs = o(() => {
  let { show: e, edit: t, list: r, create: s } = me();
  return (0, import_react44.useCallback)(({ redirect: i2, resource: a, id: u, meta: p3 = {} }) => {
    if (i2 && a)
      return a.show && i2 === "show" && u ? e(a, u, void 0, p3) : a.edit && i2 === "edit" && u ? t(a, u, void 0, p3) : a.create && i2 === "create" ? s(a, void 0, p3) : r(a, "push", p3);
  }, []);
}, "useRedirectionAfterSubmission");
var ft = o(() => {
  let e = ee(), { resource: t, resources: r } = $(), s = ge();
  return import_react46.default.useCallback(({ resource: i2, action: a, meta: u }) => {
    var R2;
    let p3 = i2 || t;
    if (!p3)
      return;
    let d3 = (R2 = Pe(p3, r, e === "legacy").find((g2) => g2.action === a)) == null ? void 0 : R2.route;
    return d3 ? we(d3, p3 == null ? void 0 : p3.meta, s, u) : void 0;
  }, [r, t, s]);
}, "useGetToPath");
var fe = o(() => {
  let e = (0, import_react45.useContext)(tt), { select: t } = $(), r = ft(), n = import_react45.default.useMemo(() => (e == null ? void 0 : e.go) ?? (() => () => {
  }), [e == null ? void 0 : e.go])();
  return (0, import_react45.useCallback)((a) => {
    if (typeof a.to != "object")
      return n({ ...a, to: a.to });
    let { resource: u } = t(a.to.resource);
    wi(a.to, u);
    let p3 = r({ resource: u, action: a.to.action, meta: { id: a.to.id, ...a.to.meta } });
    return n({ ...a, to: p3 });
  }, [t, n]);
}, "useGo");
var wi = o((e, t) => {
  if (!(e != null && e.action) || !(e != null && e.resource))
    throw new Error('[useGo]: "action" or "resource" is required.');
  if (["edit", "show", "clone"].includes(e == null ? void 0 : e.action) && !e.id)
    throw new Error(`[useGo]: [action: ${e.action}] requires an "id" for resource [resource: ${e.resource}]`);
  if (!t[e.action])
    throw new Error(`[useGo]: [action: ${e.action}] is not defined for [resource: ${e.resource}]`);
}, "handleResourceErrors");
var mo = o(() => {
  let e = (0, import_react47.useContext)(tt);
  return import_react47.default.useMemo(() => (e == null ? void 0 : e.back) ?? (() => () => {
  }), [e == null ? void 0 : e.back])();
}, "useBack");
var me = o(() => {
  let { resources: e } = $(), t = ee(), { useHistory: r } = ae(), s = r(), n = ge(), i2 = fe(), a = mo(), u = o((T2, y2 = "push") => {
    t === "legacy" ? s[y2](T2) : i2({ to: T2, type: y2 });
  }, "handleUrl"), p3 = o((T2, y2 = {}) => {
    var l2;
    if (t === "legacy") {
      let C2 = typeof T2 == "string" ? de(T2, e, true) ?? { name: T2, route: T2 } : T2, v2 = Pe(C2, e, true).find((P2) => P2.action === "create");
      return v2 ? we(v2.route, C2 == null ? void 0 : C2.meta, n, y2) : "";
    } else {
      let C2 = typeof T2 == "string" ? de(T2, e) ?? { name: T2 } : T2, v2 = (l2 = Pe(C2, e).find((P2) => P2.action === "create")) == null ? void 0 : l2.route;
      return v2 ? i2({ to: we(v2, C2 == null ? void 0 : C2.meta, n, y2), type: "path" }) : "";
    }
  }, "createUrl"), c = o((T2, y2, l2 = {}) => {
    var v2;
    let C2 = encodeURIComponent(y2);
    if (t === "legacy") {
      let P2 = typeof T2 == "string" ? de(T2, e, true) ?? { name: T2, route: T2 } : T2, U = Pe(P2, e, true).find((b) => b.action === "edit");
      return U ? we(U.route, P2 == null ? void 0 : P2.meta, n, { ...l2, id: C2 }) : "";
    } else {
      let P2 = typeof T2 == "string" ? de(T2, e) ?? { name: T2 } : T2, U = (v2 = Pe(P2, e).find((b) => b.action === "edit")) == null ? void 0 : v2.route;
      return U ? i2({ to: we(U, P2 == null ? void 0 : P2.meta, n, { ...l2, id: C2 }), type: "path" }) : "";
    }
  }, "editUrl"), d3 = o((T2, y2, l2 = {}) => {
    var v2;
    let C2 = encodeURIComponent(y2);
    if (t === "legacy") {
      let P2 = typeof T2 == "string" ? de(T2, e, true) ?? { name: T2, route: T2 } : T2, U = Pe(P2, e, true).find((b) => b.action === "clone");
      return U ? we(U.route, P2 == null ? void 0 : P2.meta, n, { ...l2, id: C2 }) : "";
    } else {
      let P2 = typeof T2 == "string" ? de(T2, e) ?? { name: T2 } : T2, U = (v2 = Pe(P2, e).find((b) => b.action === "clone")) == null ? void 0 : v2.route;
      return U ? i2({ to: we(U, P2 == null ? void 0 : P2.meta, n, { ...l2, id: C2 }), type: "path" }) : "";
    }
  }, "cloneUrl"), f2 = o((T2, y2, l2 = {}) => {
    var v2;
    let C2 = encodeURIComponent(y2);
    if (t === "legacy") {
      let P2 = typeof T2 == "string" ? de(T2, e, true) ?? { name: T2, route: T2 } : T2, U = Pe(P2, e, true).find((b) => b.action === "show");
      return U ? we(U.route, P2 == null ? void 0 : P2.meta, n, { ...l2, id: C2 }) : "";
    } else {
      let P2 = typeof T2 == "string" ? de(T2, e) ?? { name: T2 } : T2, U = (v2 = Pe(P2, e).find((b) => b.action === "show")) == null ? void 0 : v2.route;
      return U ? i2({ to: we(U, P2 == null ? void 0 : P2.meta, n, { ...l2, id: C2 }), type: "path" }) : "";
    }
  }, "showUrl"), R2 = o((T2, y2 = {}) => {
    var l2;
    if (t === "legacy") {
      let C2 = typeof T2 == "string" ? de(T2, e, true) ?? { name: T2, route: T2 } : T2, v2 = Pe(C2, e, true).find((P2) => P2.action === "list");
      return v2 ? we(v2.route, C2 == null ? void 0 : C2.meta, n, y2) : "";
    } else {
      let C2 = typeof T2 == "string" ? de(T2, e) ?? { name: T2 } : T2, v2 = (l2 = Pe(C2, e).find((P2) => P2.action === "list")) == null ? void 0 : l2.route;
      return v2 ? i2({ to: we(v2, C2 == null ? void 0 : C2.meta, n, y2), type: "path" }) : "";
    }
  }, "listUrl");
  return { create: o((T2, y2 = "push", l2 = {}) => {
    u(p3(T2, l2), y2);
  }, "create"), createUrl: p3, edit: o((T2, y2, l2 = "push", C2 = {}) => {
    u(c(T2, y2, C2), l2);
  }, "edit"), editUrl: c, clone: o((T2, y2, l2 = "push", C2 = {}) => {
    u(d3(T2, y2, C2), l2);
  }, "clone"), cloneUrl: d3, show: o((T2, y2, l2 = "push", C2 = {}) => {
    u(f2(T2, y2, C2), l2);
  }, "show"), showUrl: f2, list: o((T2, y2 = "push", l2 = {}) => {
    u(R2(T2, l2), y2);
  }, "list"), listUrl: R2, push: o((T2, ...y2) => {
    t === "legacy" ? s.push(T2, ...y2) : i2({ to: T2, type: "push" });
  }, "push"), replace: o((T2, ...y2) => {
    t === "legacy" ? s.replace(T2, ...y2) : i2({ to: T2, type: "replace" });
  }, "replace"), goBack: o(() => {
    t === "legacy" ? s.goBack() : a();
  }, "goBack") };
}, "useNavigation");
var hh = o(({ resource: e, id: t, successNotification: r, errorNotification: s, meta: n, metaData: i2, liveMode: a, onLiveEvent: u, dataProviderName: p3, queryOptions: c, overtimeOptions: d3 } = {}) => {
  let { resource: f2, id: R2, identifier: g2 } = $(e), { identifier: x2 } = $(), E2 = J(), m2 = o(() => {
    let l2 = t ?? R2;
    return e && e !== x2 ? t : l2;
  }, "getDefaultId")(), [h, D] = (0, import_react48.useState)(m2);
  import_react48.default.useEffect(() => {
    D(m2);
  }, [m2]);
  let I2 = E2({ resource: f2, meta: S(n, i2) });
  (0, import_warn_once4.default)(!!e && !t, `[useShow]: resource: "${g2}", id: ${t} 

If you don't use the \`setShowId\` method to set the \`showId\`, you should pass the \`id\` prop to \`useShow\`. Otherwise, \`useShow\` will not be able to infer the \`id\` from the current URL. 

See https://refine.dev/docs/api-reference/core/hooks/show/useShow/#resource`);
  let T2 = Ot({ resource: g2, id: h ?? "", queryOptions: { enabled: h !== void 0, ...c }, successNotification: r, errorNotification: s, meta: I2, metaData: I2, liveMode: a, onLiveEvent: u, dataProviderName: p3 }), { elapsedTime: y2 } = te({ isLoading: T2.isFetching, interval: d3 == null ? void 0 : d3.interval, onInterval: d3 == null ? void 0 : d3.onInterval });
  return { queryResult: T2, showId: h, setShowId: D, overtime: { elapsedTime: y2 } };
}, "useShow");
var wh = o(({ resourceName: e, resource: t, mapData: r = o((d3) => d3, "mapData"), paparseOptions: s, batchSize: n = Number.MAX_SAFE_INTEGER, onFinish: i2, meta: a, metaData: u, onProgress: p3, dataProviderName: c } = {}) => {
  let [d3, f2] = (0, import_react49.useState)(0), [R2, g2] = (0, import_react49.useState)(0), [x2, E2] = (0, import_react49.useState)(false), { resource: w, identifier: m2 } = $(t ?? e), h = J(), D = co(), I2 = zt(), T2 = h({ resource: w, meta: S(a, u) }), y2;
  n === 1 ? y2 = I2 : y2 = D;
  let l2 = o(() => {
    g2(0), f2(0), E2(false);
  }, "handleCleanup"), C2 = o((P2) => {
    let U = { succeeded: P2.filter((b) => b.type === "success"), errored: P2.filter((b) => b.type === "error") };
    i2 == null || i2(U), E2(false);
  }, "handleFinish");
  (0, import_react49.useEffect)(() => {
    p3 == null || p3({ totalAmount: R2, processedAmount: d3 });
  }, [R2, d3]);
  let v2 = o(({ file: P2 }) => (l2(), new Promise((U) => {
    E2(true), (0, import_papaparse2.parse)(P2, { complete: async ({ data: b }) => {
      let M = tr(b, r);
      if (g2(M.length), n === 1) {
        let k = M.map((Q) => o(async () => ({ response: await I2.mutateAsync({ resource: m2 ?? "", values: Q, successNotification: false, errorNotification: false, dataProviderName: c, meta: T2, metaData: T2 }), value: Q }), "fn")), L = await mr(k, ({ response: Q, value: F }) => (f2((G) => G + 1), { response: [Q.data], type: "success", request: [F] }), (Q, F) => ({ response: [Q], type: "error", request: [M[F]] }));
        U(L);
      } else {
        let k = chunk_default(M, n), L = k.map((F) => o(async () => ({ response: await D.mutateAsync({ resource: m2 ?? "", values: F, successNotification: false, errorNotification: false, dataProviderName: c, meta: T2, metaData: T2 }), value: F, currentBatchLength: F.length }), "fn")), Q = await mr(L, ({ response: F, currentBatchLength: G, value: N }) => (f2((B) => B + G), { response: F.data, type: "success", request: N }), (F, G) => ({ response: [F], type: "error", request: k[G] }));
        U(Q);
      }
    }, ...s });
  }).then((U) => (C2(U), U))), "handleChange");
  return { inputProps: { type: "file", accept: ".csv", onChange: (P2) => {
    P2.target.files && P2.target.files.length > 0 && v2({ file: P2.target.files[0] });
  } }, mutationResult: y2, isLoading: x2, handleChange: v2 };
}, "useImport");
var Ah = o(({ defaultVisible: e = false } = {}) => {
  let [t, r] = (0, import_react50.useState)(e), s = (0, import_react50.useCallback)(() => r(true), [t]), n = (0, import_react50.useCallback)(() => r(false), [t]);
  return { visible: t, show: s, close: n };
}, "useModal");
var Ki = o(({ resource: e, action: t, meta: r, legacy: s }) => ft()({ resource: e, action: t, meta: r, legacy: s }), "useToPath");
var yt = o(() => {
  let e = (0, import_react51.useContext)(tt);
  return e != null && e.Link ? e.Link : o(({ to: r, ...s }) => import_react51.default.createElement("a", { href: r, ...s }), "FallbackLink");
}, "useLink");
var rt = { useHistory: () => false, useLocation: () => false, useParams: () => ({}), Prompt: () => null, Link: () => null };
var $i = import_react53.default.createContext(rt);
var Xt = $i;
var Gs = o(({ children: e, useHistory: t, useLocation: r, useParams: s, Prompt: n, Link: i2, routes: a }) => import_react53.default.createElement(Xt.Provider, { value: { useHistory: t ?? rt.useHistory, useLocation: r ?? rt.useLocation, useParams: s ?? rt.useParams, Prompt: n ?? rt.Prompt, Link: i2 ?? rt.Link, routes: a ?? rt.routes } }, e), "LegacyRouterContextProvider");
var ae = o(() => {
  let e = (0, import_react52.useContext)(Xt), { useHistory: t, useLocation: r, useParams: s, Prompt: n, Link: i2, routes: a } = e ?? rt;
  return { useHistory: t, useLocation: r, useParams: s, Prompt: n, Link: i2, routes: a };
}, "useRouterContext");
var ct = import_react55.default.createContext({ options: { buttons: { enableAccessControl: true, hideIfUnauthorized: false } } });
var $s = o(({ can: e, children: t, options: r }) => import_react55.default.createElement(ct.Provider, { value: { can: e, options: r ? { ...r, buttons: { enableAccessControl: true, hideIfUnauthorized: false, ...r.buttons } } : { buttons: { enableAccessControl: true, hideIfUnauthorized: false }, queryOptions: void 0 } } }, t), "AccessControlContextProvider");
var Mt = o((e) => {
  if (!e)
    return;
  let { icon: t, list: r, edit: s, create: n, show: i2, clone: a, children: u, meta: p3, options: c, ...d3 } = e, { icon: f2, ...R2 } = p3 ?? {}, { icon: g2, ...x2 } = c ?? {};
  return { ...d3, ...p3 ? { meta: R2 } : {}, ...c ? { options: x2 } : {} };
}, "sanitizeResource");
var Ws = o(({ action: e, resource: t, params: r, queryOptions: s }) => {
  let { can: n, options: i2 } = (0, import_react54.useContext)(ct), { keys: a, preferLegacyKeys: u } = O2(), { queryOptions: p3 } = i2 || {}, c = { ...p3, ...s }, { resource: d3, ...f2 } = r ?? {}, R2 = Mt(d3), g2 = useQuery({ queryKey: a().access().resource(t).action(e).params({ params: { ...f2, resource: R2 }, enabled: c == null ? void 0 : c.enabled }).get(u), queryFn: () => (n == null ? void 0 : n({ action: e, resource: t, params: { ...f2, resource: R2 } })) ?? Promise.resolve({ can: true }), enabled: typeof n < "u", ...c, meta: { ...c == null ? void 0 : c.meta, ...P("useCan", u) }, retry: false });
  return typeof n > "u" ? { data: { can: true } } : g2;
}, "useCan");
var bC = o(() => {
  let { can: e } = import_react56.default.useContext(ct);
  return { can: import_react56.default.useMemo(() => e ? o(async ({ params: s, ...n }) => {
    let i2 = s != null && s.resource ? Mt(s.resource) : void 0;
    return e({ ...n, ...s ? { params: { ...s, resource: i2 } } : {} });
  }, "canWithSanitizedResource") : void 0, [e]) };
}, "useCanWithoutCache");
var QC = o((e) => {
  let [t, r] = (0, import_react57.useState)([]), [s, n] = (0, import_react57.useState)([]), [i2, a] = (0, import_react57.useState)([]), { resource: u, sort: p3, sorters: c, filters: d3 = [], optionLabel: f2 = "title", optionValue: R2 = "id", debounce: g2 = 300, successNotification: x2, errorNotification: E2, defaultValueQueryOptions: w, queryOptions: m2, fetchSize: h, pagination: D, hasPagination: I2 = false, liveMode: T2, defaultValue: y2 = [], onLiveEvent: l2, onSearch: C2, liveParams: v2, meta: P2, metaData: U, dataProviderName: b, overtimeOptions: M } = e, { resource: k, identifier: L } = $(u), F = J()({ resource: k, meta: S(P2, U) }), G = Array.isArray(y2) ? y2 : [y2], N = (0, import_react57.useCallback)((X) => {
    a(X.data.map((q) => ({ label: get_default(q, f2), value: get_default(q, R2) })));
  }, [f2, R2]), B = w ?? m2, j = no({ resource: L, ids: G, queryOptions: { ...B, enabled: G.length > 0 && ((B == null ? void 0 : B.enabled) ?? true), onSuccess: (X) => {
    var q;
    N(X), (q = B == null ? void 0 : B.onSuccess) == null || q.call(B, X);
  } }, meta: F, metaData: F, liveMode: "off", dataProviderName: b }), K = (0, import_react57.useCallback)((X) => {
    n(X.data.map((q) => ({ label: get_default(q, f2), value: get_default(q, R2) })));
  }, [f2, R2]), W = Wt({ resource: L, sorters: S(c, p3), filters: d3.concat(t), pagination: { current: D == null ? void 0 : D.current, pageSize: (D == null ? void 0 : D.pageSize) ?? h, mode: D == null ? void 0 : D.mode }, hasPagination: I2, queryOptions: { ...m2, onSuccess: (X) => {
    var q;
    K(X), (q = m2 == null ? void 0 : m2.onSuccess) == null || q.call(m2, X);
  } }, successNotification: x2, errorNotification: E2, meta: F, metaData: F, liveMode: T2, liveParams: v2, onLiveEvent: l2, dataProviderName: b }), V = o((X) => {
    if (C2) {
      r(C2(X));
      return;
    }
    if (X)
      r([{ field: f2, operator: "contains", value: X }]);
    else {
      r([]);
      return;
    }
  }, "onSearch"), { elapsedTime: H } = te({ isLoading: W.isFetching || j.isFetching, interval: M == null ? void 0 : M.interval, onInterval: M == null ? void 0 : M.onInterval }), Z = (0, import_react57.useMemo)(() => uniqBy_default([...s, ...i2], "value"), [s, i2]);
  return { queryResult: W, defaultValueQueryResult: j, options: Z, onSearch: debounce_default(V, g2), overtime: { elapsedTime: H } };
}, "useSelect");
var Xs = [];
var Zs = [];
function YC({ initialCurrent: e, initialPageSize: t, hasPagination: r = true, pagination: s, initialSorter: n, permanentSorter: i2 = Zs, defaultSetFilterBehavior: a, initialFilter: u, permanentFilter: p3 = Xs, filters: c, sorters: d3, syncWithLocation: f2, resource: R2, successNotification: g2, errorNotification: x2, queryOptions: E2, liveMode: w, onLiveEvent: m2, liveParams: h, meta: D, metaData: I2, dataProviderName: T2, overtimeOptions: y2 } = {}) {
  var Qt, ho, Co, bo, vo;
  let { syncWithLocation: l2 } = zr(), C2 = f2 ?? l2, v2 = Fs(w), P2 = ee(), { useLocation: U } = ae(), { search: b, pathname: M } = U(), k = J(), L = ge(), Q = ((c == null ? void 0 : c.mode) || "server") === "server", F = ((d3 == null ? void 0 : d3.mode) || "server") === "server", G = r === false ? "off" : "server", N = ((s == null ? void 0 : s.mode) ?? G) !== "off", B = S(s == null ? void 0 : s.current, e), j = S(s == null ? void 0 : s.pageSize, t), K = S(D, I2), { parsedCurrent: W, parsedPageSize: V, parsedSorter: H, parsedFilters: Z } = Pr(b ?? "?"), X = S(c == null ? void 0 : c.initial, u), q = S(c == null ? void 0 : c.permanent, p3) ?? Xs, Ce = S(d3 == null ? void 0 : d3.initial, n), De = S(d3 == null ? void 0 : d3.permanent, i2) ?? Zs, Ft = S(c == null ? void 0 : c.defaultBehavior, a) ?? "merge", pt, dt, Rt, xt;
  C2 ? (pt = ((Qt = L == null ? void 0 : L.params) == null ? void 0 : Qt.current) || W || B || 1, dt = ((ho = L == null ? void 0 : L.params) == null ? void 0 : ho.pageSize) || V || j || 10, Rt = ((Co = L == null ? void 0 : L.params) == null ? void 0 : Co.sorters) || (H.length ? H : Ce), xt = ((bo = L == null ? void 0 : L.params) == null ? void 0 : bo.filters) || (Z.length ? Z : X)) : (pt = B || 1, dt = j || 10, Rt = Ce, xt = X);
  let { replace: Pt } = me(), Yt = fe(), { resource: Vr, identifier: je } = $(R2), At = k({ resource: Vr, meta: K });
  import_react58.default.useEffect(() => {
    (0, import_warn_once5.default)(typeof je > "u", "useTable: `resource` is not defined.");
  }, [je]);
  let [$e, Jt] = (0, import_react58.useState)(vr(De, Rt ?? [])), [lt, ht] = (0, import_react58.useState)(br(q, xt ?? [])), [Ct, Br] = (0, import_react58.useState)(pt), [ot, kt] = (0, import_react58.useState)(dt), Nr = o(({ pagination: { current: xe, pageSize: Ve }, sorter: qt, filters: er }) => {
    if (P2 === "new")
      return Yt({ type: "path", options: { keepHash: true, keepQuery: true }, query: { ...N ? { current: xe, pageSize: Ve } : {}, sorters: qt, filters: er, ...ye() } }) ?? "";
    {
      let Vt = import_qs4.default.parse(b == null ? void 0 : b.substring(1)), bn = hr({ pagination: { pageSize: Ve, current: xe }, sorters: $e ?? qt, filters: er, ...Vt });
      return `${M ?? ""}?${bn ?? ""}`;
    }
  }, "createLinkForSyncWithLocation");
  (0, import_react58.useEffect)(() => {
    b === "" && (Br(pt), kt(dt), Jt(vr(De, Rt ?? [])), ht(br(q, xt ?? [])));
  }, [b]);
  let ye = o(() => {
    if (P2 === "new") {
      let { sorters: xe, filters: Ve, pageSize: qt, current: er, ...Vt } = (L == null ? void 0 : L.params) ?? {};
      return Vt;
    } else {
      let { sorter: xe, filters: Ve, pageSize: qt, current: er, ...Vt } = import_qs4.default.parse(b, { ignoreQueryPrefix: true });
      return Vt;
    }
  }, "currentQueryParams");
  (0, import_react58.useEffect)(() => {
    if (C2) {
      let xe = ye();
      if (P2 === "new")
        Yt({ type: "replace", options: { keepQuery: true }, query: { ...N ? { pageSize: ot, current: Ct } : {}, sorters: differenceWith_default($e, De, isEqual_default), filters: differenceWith_default(lt, q, isEqual_default) } });
      else {
        let Ve = hr({ ...N ? { pagination: { pageSize: ot, current: Ct } } : {}, sorters: differenceWith_default($e, De, isEqual_default), filters: differenceWith_default(lt, q, isEqual_default), ...xe });
        return Pt == null ? void 0 : Pt(`${M}?${Ve}`, void 0, { shallow: true });
      }
    }
  }, [C2, Ct, ot, $e, lt]);
  let Qe = Wt({ resource: je, hasPagination: r, pagination: { current: Ct, pageSize: ot, mode: s == null ? void 0 : s.mode }, filters: Q ? Et(q, lt) : void 0, sorters: F ? Cr(De, $e) : void 0, queryOptions: E2, successNotification: g2, errorNotification: x2, meta: At, metaData: At, liveMode: v2, liveParams: h, onLiveEvent: m2, dataProviderName: T2 }), Fe = o((xe) => {
    ht((Ve) => Et(q, xe, Ve));
  }, "setFiltersAsMerge"), st = o((xe) => {
    ht(Et(q, xe));
  }, "setFiltersAsReplace"), Le = o((xe) => {
    ht((Ve) => Et(q, xe(Ve)));
  }, "setFiltersWithSetter"), nt = o((xe, Ve = Ft) => {
    typeof xe == "function" ? Le(xe) : Ve === "replace" ? st(xe) : Fe(xe);
  }, "setFiltersFn"), at = o((xe) => {
    Jt(() => Cr(De, xe));
  }, "setSortWithUnion"), { elapsedTime: mt } = te({ isLoading: Qe.isFetching, interval: y2 == null ? void 0 : y2.interval, onInterval: y2 == null ? void 0 : y2.onInterval });
  return { tableQueryResult: Qe, sorters: $e, setSorters: at, sorter: $e, setSorter: at, filters: lt, setFilters: nt, current: Ct, setCurrent: Br, pageSize: ot, setPageSize: kt, pageCount: ot ? Math.ceil((((vo = Qe.data) == null ? void 0 : vo.total) ?? 0) / ot) : 1, createLinkForSyncWithLocation: Nr, overtime: { elapsedTime: mt } };
}
o(YC, "useTable");
var gt = import_react60.default.createContext({});
var Js = o(({ create: e, get: t, update: r, children: s }) => import_react60.default.createElement(gt.Provider, { value: { create: e, get: t, update: r } }, s), "AuditLogContextProvider");
var Ne = o(({ logMutationOptions: e, renameMutationOptions: t } = {}) => {
  let r = useQueryClient(), s = (0, import_react59.useContext)(gt), { keys: n, preferLegacyKeys: i2 } = O2(), a = Y(), { resources: u } = (0, import_react59.useContext)(ut), { data: p3, refetch: c, isLoading: d3 } = Xr({ v3LegacyAuthProviderCompatible: !!(a != null && a.isLegacy), queryOptions: { enabled: !!(s != null && s.create) } }), f2 = useMutation(async (g2) => {
    var m2, h, D, I2, T2;
    let x2 = de(g2.resource, u), E2 = S((m2 = x2 == null ? void 0 : x2.meta) == null ? void 0 : m2.audit, (h = x2 == null ? void 0 : x2.options) == null ? void 0 : h.audit, (I2 = (D = x2 == null ? void 0 : x2.options) == null ? void 0 : D.auditLog) == null ? void 0 : I2.permissions);
    if (E2 && !Kr(E2, g2.action))
      return;
    let w;
    return d3 && (s != null && s.create) && (w = await c()), await ((T2 = s.create) == null ? void 0 : T2.call(s, { ...g2, author: p3 ?? (w == null ? void 0 : w.data) }));
  }, { mutationKey: n().audit().action("log").get(), ...e, meta: { ...e == null ? void 0 : e.meta, ...P("useLog", i2) } }), R2 = useMutation(async (g2) => {
    var x2;
    return await ((x2 = s.update) == null ? void 0 : x2.call(s, g2));
  }, { onSuccess: (g2) => {
    g2 != null && g2.resource && r.invalidateQueries(n().audit().resource((g2 == null ? void 0 : g2.resource) ?? "").action("list").get(i2));
  }, mutationKey: n().audit().action("rename").get(), ...t, meta: { ...t == null ? void 0 : t.meta, ...P("useLog", i2) } });
  return { log: f2, rename: R2 };
}, "useLog");
var vb = o(({ resource: e, action: t, meta: r, author: s, metaData: n, queryOptions: i2 }) => {
  let { get: a } = (0, import_react61.useContext)(gt), { keys: u, preferLegacyKeys: p3 } = O2();
  return useQuery({ queryKey: u().audit().resource(e).action("list").params(r).get(p3), queryFn: () => (a == null ? void 0 : a({ resource: e, action: t, author: s, meta: r, metaData: n })) ?? Promise.resolve([]), enabled: typeof a < "u", ...i2, retry: false, meta: { ...i2 == null ? void 0 : i2.meta, ...P("useLogList", p3) } });
}, "useLogList");
var Bb = o(({ meta: e = {} } = {}) => {
  let t = ee(), { i18nProvider: r } = (0, import_react62.useContext)(Ke), s = ge(), n = _(), { resources: i2, resource: a, action: u } = $(), { options: { textTransformers: p3 } } = oe(), c = [];
  if (!(a != null && a.name))
    return { breadcrumbs: c };
  let d3 = o((f2) => {
    var g2, x2, E2, w, m2, h;
    let R2 = typeof f2 == "string" ? de(f2, i2, t === "legacy") ?? { name: f2 } : f2;
    if (R2) {
      let D = S((g2 = R2 == null ? void 0 : R2.meta) == null ? void 0 : g2.parent, R2 == null ? void 0 : R2.parentName);
      D && d3(D);
      let I2 = Pe(R2, i2, t === "legacy").find((l2) => l2.action === "list"), T2 = (x2 = I2 == null ? void 0 : I2.resource) != null && x2.list ? I2 == null ? void 0 : I2.route : void 0, y2 = T2 ? t === "legacy" ? T2 : we(T2, R2 == null ? void 0 : R2.meta, s, e) : void 0;
      c.push({ label: S((E2 = R2.meta) == null ? void 0 : E2.label, (w = R2.options) == null ? void 0 : w.label) ?? n(`${R2.name}.${R2.name}`, p3.humanize(R2.name)), href: y2, icon: S((m2 = R2.meta) == null ? void 0 : m2.icon, (h = R2.options) == null ? void 0 : h.icon, R2.icon) });
    }
  }, "addBreadcrumb");
  if (d3(a), u && u !== "list") {
    let f2 = `actions.${u}`, R2 = n(f2);
    typeof r < "u" && R2 === f2 ? ((0, import_warn_once6.default)(true, `[useBreadcrumb]: Breadcrumb missing translate key for the "${u}" action. Please add "actions.${u}" key to your translation file.
For more information, see https://refine.dev/docs/api-reference/core/hooks/useBreadcrumb/#i18n-support`), c.push({ label: n(`buttons.${u}`, p3.humanize(u)) })) : c.push({ label: n(f2, p3.humanize(u)) });
  }
  return { breadcrumbs: c };
}, "useBreadcrumb");
var wt = o((e, t, r = false) => {
  let s = [], n = Ae(e, t);
  for (; n; )
    s.push(n), n = Ae(n, t);
  return s.reverse(), `/${[...s, e].map((a) => he((r ? a.route : void 0) ?? a.identifier ?? a.name)).join("/").replace(/^\//, "")}`;
}, "createResourceKey");
var rn = o((e, t = false) => {
  let r = { item: { name: "__root__" }, children: {} };
  e.forEach((n) => {
    let i2 = [], a = Ae(n, e);
    for (; a; )
      i2.push(a), a = Ae(a, e);
    i2.reverse();
    let u = r;
    i2.forEach((c) => {
      let d3 = (t ? c.route : void 0) ?? c.identifier ?? c.name;
      u.children[d3] || (u.children[d3] = { item: c, children: {} }), u = u.children[d3];
    });
    let p3 = (t ? n.route : void 0) ?? n.identifier ?? n.name;
    u.children[p3] || (u.children[p3] = { item: n, children: {} });
  });
  let s = o((n) => {
    let i2 = [];
    return Object.keys(n.children).forEach((a) => {
      let u = wt(n.children[a].item, e, t), p3 = { ...n.children[a].item, key: u, children: s(n.children[a]) };
      i2.push(p3);
    }), i2;
  }, "flatten");
  return s(r);
}, "createTree");
var on = o((e) => e.split("?")[0].split("#")[0].replace(/(.+)(\/$)/, "$1"), "getCleanPath");
var nu = o(({ meta: e, hideOnMissingParameter: t } = { hideOnMissingParameter: true }) => {
  let r = _(), s = ft(), n = ee(), { resource: i2, resources: a } = $(), { pathname: u } = ge(), { useLocation: p3 } = ae(), { pathname: c } = p3(), d3 = Lt(), R2 = `/${((n === "legacy" ? on(c) : u ? on(u) : void 0) ?? "").replace(/^\//, "")}`, g2 = i2 ? wt(i2, a, n === "legacy") : R2 ?? "", x2 = import_react63.default.useMemo(() => {
    if (!i2)
      return [];
    let m2 = Ae(i2, a), h = [wt(i2, a)];
    for (; m2; )
      h.push(wt(m2, a)), m2 = Ae(m2, a);
    return h;
  }, []), E2 = import_react63.default.useCallback((m2) => {
    var D, I2, T2, y2, l2, C2;
    if ((((D = m2 == null ? void 0 : m2.meta) == null ? void 0 : D.hide) ?? ((I2 = m2 == null ? void 0 : m2.options) == null ? void 0 : I2.hide)) || !(m2 != null && m2.list) && m2.children.length === 0)
      return;
    let h = m2.list ? s({ resource: m2, action: "list", legacy: n === "legacy", meta: e }) : void 0;
    if (!(t && h && h.match(/(\/|^):(.+?)(\/|$){1}/)))
      return { ...m2, route: h, icon: S((T2 = m2.meta) == null ? void 0 : T2.icon, (y2 = m2.options) == null ? void 0 : y2.icon, m2.icon), label: S((l2 = m2 == null ? void 0 : m2.meta) == null ? void 0 : l2.label, (C2 = m2 == null ? void 0 : m2.options) == null ? void 0 : C2.label) ?? r(`${m2.name}.${m2.name}`, d3(m2.name, "plural")) };
  }, [n, e, s, r, t]), w = import_react63.default.useMemo(() => {
    let m2 = rn(a, n === "legacy"), h = o((D) => D.flatMap((I2) => {
      let T2 = h(I2.children), y2 = E2({ ...I2, children: T2 });
      return y2 ? [y2] : [];
    }), "prepare");
    return h(m2);
  }, [a, n, E2]);
  return { defaultOpenKeys: x2, selectedKey: g2, menuItems: w };
}, "useMenu");
var J = o(() => {
  let { params: e } = ge();
  return o(({ resource: r, meta: s } = {}) => {
    let { meta: n } = Mt(r) ?? { meta: {} }, { filters: i2, sorters: a, current: u, pageSize: p3, ...c } = e ?? {};
    return { ...n, ...c, ...s };
  }, "getMetaFn");
}, "useMeta");
var uu = o(() => {
  let [e, t] = (0, import_react2.useState)(), r = _(), { push: s } = me(), n = fe(), i2 = ee(), { resource: a, action: u } = $();
  return (0, import_react2.useEffect)(() => {
    a && u && t(r("pages.error.info", { action: u, resource: a.name }, `You may have forgotten to add the "${u}" component to "${a.name}" resource.`));
  }, [a, u]), import_react2.default.createElement(import_react2.default.Fragment, null, import_react2.default.createElement("h1", null, r("pages.error.404", void 0, "Sorry, the page you visited does not exist.")), e && import_react2.default.createElement("p", null, e), import_react2.default.createElement("button", { onClick: () => {
    i2 === "legacy" ? s("/") : n({ to: "/" });
  } }, r("pages.error.backHome", void 0, "Back Home")));
}, "ErrorComponent");
var Hr = o(() => {
  let [e, t] = (0, import_react64.useState)(""), [r, s] = (0, import_react64.useState)(""), n = _(), i2 = Y(), { mutate: a } = $t({ v3LegacyAuthProviderCompatible: !!(i2 != null && i2.isLegacy) });
  return import_react64.default.createElement(import_react64.default.Fragment, null, import_react64.default.createElement("h1", null, n("pages.login.title", "Sign in your account")), import_react64.default.createElement("form", { onSubmit: (u) => {
    u.preventDefault(), a({ username: e, password: r });
  } }, import_react64.default.createElement("table", null, import_react64.default.createElement("tbody", null, import_react64.default.createElement("tr", null, import_react64.default.createElement("td", null, n("pages.login.username", void 0, "username"), ":"), import_react64.default.createElement("td", null, import_react64.default.createElement("input", { type: "text", size: 20, autoCorrect: "off", spellCheck: false, autoCapitalize: "off", autoFocus: true, required: true, value: e, onChange: (u) => t(u.target.value) }))), import_react64.default.createElement("tr", null, import_react64.default.createElement("td", null, n("pages.login.password", void 0, "password"), ":"), import_react64.default.createElement("td", null, import_react64.default.createElement("input", { type: "password", required: true, size: 20, value: r, onChange: (u) => s(u.target.value) }))))), import_react64.default.createElement("br", null), import_react64.default.createElement("input", { type: "submit", value: "login" })));
}, "LoginPage");
var nn = o(({ providers: e, registerLink: t, forgotPasswordLink: r, rememberMe: s, contentProps: n, wrapperProps: i2, renderContent: a, formProps: u, title: p3 = void 0, hideForm: c }) => {
  let d3 = ee(), f2 = yt(), { Link: R2 } = ae(), g2 = d3 === "legacy" ? R2 : f2, [x2, E2] = (0, import_react66.useState)(""), [w, m2] = (0, import_react66.useState)(""), [h, D] = (0, import_react66.useState)(false), I2 = _(), T2 = Y(), { mutate: y2 } = $t({ v3LegacyAuthProviderCompatible: !!(T2 != null && T2.isLegacy) }), l2 = o((P2, U) => import_react66.default.createElement(g2, { to: P2 }, U), "renderLink"), C2 = o(() => e ? e.map((P2) => import_react66.default.createElement("div", { key: P2.name, style: { display: "flex", alignItems: "center", justifyContent: "center", marginBottom: "1rem" } }, import_react66.default.createElement("button", { onClick: () => y2({ providerName: P2.name }), style: { display: "flex", alignItems: "center" } }, P2 == null ? void 0 : P2.icon, P2.label ?? import_react66.default.createElement("label", null, P2.label)))) : null, "renderProviders"), v2 = import_react66.default.createElement("div", { ...n }, import_react66.default.createElement("h1", { style: { textAlign: "center" } }, I2("pages.login.title", "Sign in to your account")), C2(), !c && import_react66.default.createElement(import_react66.default.Fragment, null, import_react66.default.createElement("hr", null), import_react66.default.createElement("form", { onSubmit: (P2) => {
    P2.preventDefault(), y2({ email: x2, password: w, remember: h });
  }, ...u }, import_react66.default.createElement("div", { style: { display: "flex", flexDirection: "column", padding: 25 } }, import_react66.default.createElement("label", { htmlFor: "email-input" }, I2("pages.login.fields.email", "Email")), import_react66.default.createElement("input", { id: "email-input", name: "email", type: "text", size: 20, autoCorrect: "off", spellCheck: false, autoCapitalize: "off", required: true, value: x2, onChange: (P2) => E2(P2.target.value) }), import_react66.default.createElement("label", { htmlFor: "password-input" }, I2("pages.login.fields.password", "Password")), import_react66.default.createElement("input", { id: "password-input", type: "password", name: "password", required: true, size: 20, value: w, onChange: (P2) => m2(P2.target.value) }), s ?? import_react66.default.createElement(import_react66.default.Fragment, null, import_react66.default.createElement("label", { htmlFor: "remember-me-input" }, I2("pages.login.buttons.rememberMe", "Remember me"), import_react66.default.createElement("input", { id: "remember-me-input", name: "remember", type: "checkbox", size: 20, checked: h, value: h.toString(), onChange: () => {
    D(!h);
  } }))), import_react66.default.createElement("br", null), r ?? l2("/forgot-password", I2("pages.login.buttons.forgotPassword", "Forgot password?")), import_react66.default.createElement("input", { type: "submit", value: I2("pages.login.signin", "Sign in") }), t ?? import_react66.default.createElement("span", null, I2("pages.login.buttons.noAccount", "Don’t have an account?"), " ", l2("/register", I2("pages.login.register", "Sign up")))))), t !== false && c && import_react66.default.createElement("div", { style: { textAlign: "center" } }, I2("pages.login.buttons.noAccount", "Don’t have an account?"), " ", l2("/register", I2("pages.login.register", "Sign up"))));
  return import_react66.default.createElement("div", { ...i2 }, a ? a(v2, p3) : v2);
}, "LoginPage");
var un = o(({ providers: e, loginLink: t, wrapperProps: r, contentProps: s, renderContent: n, formProps: i2, title: a = void 0, hideForm: u }) => {
  let p3 = ee(), c = yt(), { Link: d3 } = ae(), f2 = p3 === "legacy" ? d3 : c, [R2, g2] = (0, import_react67.useState)(""), [x2, E2] = (0, import_react67.useState)(""), w = _(), m2 = Y(), { mutate: h, isLoading: D } = qr({ v3LegacyAuthProviderCompatible: !!(m2 != null && m2.isLegacy) }), I2 = o((l2, C2) => import_react67.default.createElement(f2, { to: l2 }, C2), "renderLink"), T2 = o(() => e ? e.map((l2) => import_react67.default.createElement("div", { key: l2.name, style: { display: "flex", alignItems: "center", justifyContent: "center", marginBottom: "1rem" } }, import_react67.default.createElement("button", { onClick: () => h({ providerName: l2.name }), style: { display: "flex", alignItems: "center" } }, l2 == null ? void 0 : l2.icon, l2.label ?? import_react67.default.createElement("label", null, l2.label)))) : null, "renderProviders"), y2 = import_react67.default.createElement("div", { ...s }, import_react67.default.createElement("h1", { style: { textAlign: "center" } }, w("pages.register.title", "Sign up for your account")), T2(), !u && import_react67.default.createElement(import_react67.default.Fragment, null, import_react67.default.createElement("hr", null), import_react67.default.createElement("form", { onSubmit: (l2) => {
    l2.preventDefault(), h({ email: R2, password: x2 });
  }, ...i2 }, import_react67.default.createElement("div", { style: { display: "flex", flexDirection: "column", padding: 25 } }, import_react67.default.createElement("label", { htmlFor: "email-input" }, w("pages.register.fields.email", "Email")), import_react67.default.createElement("input", { id: "email-input", name: "email", type: "email", size: 20, autoCorrect: "off", spellCheck: false, autoCapitalize: "off", required: true, value: R2, onChange: (l2) => g2(l2.target.value) }), import_react67.default.createElement("label", { htmlFor: "password-input" }, w("pages.register.fields.password", "Password")), import_react67.default.createElement("input", { id: "password-input", name: "password", type: "password", required: true, size: 20, value: x2, onChange: (l2) => E2(l2.target.value) }), import_react67.default.createElement("input", { type: "submit", value: w("pages.register.buttons.submit", "Sign up"), disabled: D }), t ?? import_react67.default.createElement(import_react67.default.Fragment, null, import_react67.default.createElement("span", null, w("pages.login.buttons.haveAccount", "Have an account?"), " ", I2("/login", w("pages.login.signin", "Sign in"))))))), t !== false && u && import_react67.default.createElement("div", { style: { textAlign: "center" } }, w("pages.login.buttons.haveAccount", "Have an account?"), " ", I2("/login", w("pages.login.signin", "Sign in"))));
  return import_react67.default.createElement("div", { ...r }, n ? n(y2, a) : y2);
}, "RegisterPage");
var cn = o(({ loginLink: e, wrapperProps: t, contentProps: r, renderContent: s, formProps: n, title: i2 = void 0 }) => {
  let a = _(), u = ee(), p3 = yt(), { Link: c } = ae(), d3 = u === "legacy" ? c : p3, [f2, R2] = (0, import_react68.useState)(""), { mutate: g2, isLoading: x2 } = to(), E2 = o((m2, h) => import_react68.default.createElement(d3, { to: m2 }, h), "renderLink"), w = import_react68.default.createElement("div", { ...r }, import_react68.default.createElement("h1", { style: { textAlign: "center" } }, a("pages.forgotPassword.title", "Forgot your password?")), import_react68.default.createElement("hr", null), import_react68.default.createElement("form", { onSubmit: (m2) => {
    m2.preventDefault(), g2({ email: f2 });
  }, ...n }, import_react68.default.createElement("div", { style: { display: "flex", flexDirection: "column", padding: 25 } }, import_react68.default.createElement("label", { htmlFor: "email-input" }, a("pages.forgotPassword.fields.email", "Email")), import_react68.default.createElement("input", { id: "email-input", name: "email", type: "mail", autoCorrect: "off", spellCheck: false, autoCapitalize: "off", required: true, value: f2, onChange: (m2) => R2(m2.target.value) }), import_react68.default.createElement("input", { type: "submit", disabled: x2, value: a("pages.forgotPassword.buttons.submit", "Send reset instructions") }), import_react68.default.createElement("br", null), e ?? import_react68.default.createElement("span", null, a("pages.register.buttons.haveAccount", "Have an account? "), " ", E2("/login", a("pages.login.signin", "Sign in"))))));
  return import_react68.default.createElement("div", { ...t }, s ? s(w, i2) : w);
}, "ForgotPasswordPage");
var dn = o(({ wrapperProps: e, contentProps: t, renderContent: r, formProps: s, title: n = void 0 }) => {
  let i2 = _(), a = Y(), { mutate: u, isLoading: p3 } = oo({ v3LegacyAuthProviderCompatible: !!(a != null && a.isLegacy) }), [c, d3] = (0, import_react69.useState)(""), [f2, R2] = (0, import_react69.useState)(""), g2 = import_react69.default.createElement("div", { ...t }, import_react69.default.createElement("h1", { style: { textAlign: "center" } }, i2("pages.updatePassword.title", "Update Password")), import_react69.default.createElement("hr", null), import_react69.default.createElement("form", { onSubmit: (x2) => {
    x2.preventDefault(), u({ password: c, confirmPassword: f2 });
  }, ...s }, import_react69.default.createElement("div", { style: { display: "flex", flexDirection: "column", padding: 25 } }, import_react69.default.createElement("label", { htmlFor: "password-input" }, i2("pages.updatePassword.fields.password", "New Password")), import_react69.default.createElement("input", { id: "password-input", name: "password", type: "password", required: true, size: 20, value: c, onChange: (x2) => d3(x2.target.value) }), import_react69.default.createElement("label", { htmlFor: "confirm-password-input" }, i2("pages.updatePassword.fields.confirmPassword", "Confirm New Password")), import_react69.default.createElement("input", { id: "confirm-password-input", name: "confirmPassword", type: "password", required: true, size: 20, value: f2, onChange: (x2) => R2(x2.target.value) }), import_react69.default.createElement("input", { type: "submit", disabled: p3, value: i2("pages.updatePassword.buttons.submit", "Update") }))));
  return import_react69.default.createElement("div", { ...e }, r ? r(g2, n) : g2);
}, "UpdatePasswordPage");
var pu = o((e) => {
  let { type: t } = e;
  return import_react65.default.createElement(import_react65.default.Fragment, null, o(() => {
    switch (t) {
      case "register":
        return import_react65.default.createElement(un, { ...e });
      case "forgotPassword":
        return import_react65.default.createElement(cn, { ...e });
      case "updatePassword":
        return import_react65.default.createElement(dn, { ...e });
      default:
        return import_react65.default.createElement(nn, { ...e });
    }
  }, "renderView")());
}, "AuthPage");
var Ro = o(() => import_react70.default.createElement(import_react70.default.Fragment, null, import_react70.default.createElement("h1", null, "Welcome on board"), import_react70.default.createElement("p", null, "Your configuration is completed."), import_react70.default.createElement("p", null, "Now you can get started by adding your resources to the", " ", import_react70.default.createElement("code", null, "`resources`"), " property of ", import_react70.default.createElement("code", null, "`<Refine>`")), import_react70.default.createElement("div", { style: { display: "flex", gap: 8 } }, import_react70.default.createElement("a", { href: "https://refine.dev", target: "_blank", rel: "noreferrer" }, import_react70.default.createElement("button", null, "Documentation")), import_react70.default.createElement("a", { href: "https://refine.dev/examples", target: "_blank", rel: "noreferrer" }, import_react70.default.createElement("button", null, "Examples")), import_react70.default.createElement("a", { href: "https://discord.gg/refine", target: "_blank", rel: "noreferrer" }, import_react70.default.createElement("button", null, "Community")))), "ReadyPage");
var lu = [{ title: "Documentation", description: "Learn about the technical details of using Refine in your projects.", link: "https://refine.dev/docs", iconUrl: "https://refine.ams3.cdn.digitaloceanspaces.com/welcome-page/book.svg" }, { title: "Tutorial", description: "Learn how to use Refine by building a fully-functioning CRUD app, from scratch to full launch.", link: "https://refine.dev/tutorial", iconUrl: "https://refine.ams3.cdn.digitaloceanspaces.com/welcome-page/hat.svg" }, { title: "Templates", description: "Explore a range of pre-built templates, perfect everything from admin panels to dashboards and CRMs.", link: "https://refine.dev/templates", iconUrl: "https://refine.ams3.cdn.digitaloceanspaces.com/welcome-page/application.svg" }, { title: "Community", description: "Join our Discord community and keep up with the latest news.", link: "https://discord.gg/refine", iconUrl: "https://refine.ams3.cdn.digitaloceanspaces.com/welcome-page/discord.svg" }];
var ln = o(() => {
  let e = Tr("(max-width: 1010px)"), t = Tr("(max-width: 650px)"), r = o(() => t ? "1, 280px" : e ? "2, 280px" : "4, 1fr", "getGridTemplateColumns"), s = o(() => t ? "32px" : e ? "40px" : "48px", "getHeaderFontSize"), n = o(() => t ? "16px" : e ? "20px" : "24px", "getSubHeaderFontSize");
  return import_react72.default.createElement("div", { style: { position: "fixed", zIndex: 10, inset: 0, overflow: "auto", width: "100dvw", height: "100dvh" } }, import_react72.default.createElement("div", { style: { overflow: "hidden", position: "relative", backgroundSize: "cover", backgroundRepeat: "no-repeat", background: t ? "url(https://refine.ams3.cdn.digitaloceanspaces.com/website/static/assets/landing-noise.webp), radial-gradient(88.89% 50% at 50% 100%, rgba(38, 217, 127, 0.10) 0%, rgba(38, 217, 127, 0.00) 100%), radial-gradient(88.89% 50% at 50% 0%, rgba(71, 235, 235, 0.15) 0%, rgba(71, 235, 235, 0.00) 100%), #1D1E30" : e ? "url(https://refine.ams3.cdn.digitaloceanspaces.com/website/static/assets/landing-noise.webp), radial-gradient(66.67% 50% at 50% 100%, rgba(38, 217, 127, 0.10) 0%, rgba(38, 217, 127, 0.00) 100%), radial-gradient(66.67% 50% at 50% 0%, rgba(71, 235, 235, 0.15) 0%, rgba(71, 235, 235, 0.00) 100%), #1D1E30" : "url(https://refine.ams3.cdn.digitaloceanspaces.com/website/static/assets/landing-noise.webp), radial-gradient(35.56% 50% at 50% 100%, rgba(38, 217, 127, 0.12) 0%, rgba(38, 217, 127, 0) 100%), radial-gradient(35.56% 50% at 50% 0%, rgba(71, 235, 235, 0.18) 0%, rgba(71, 235, 235, 0) 100%), #1D1E30", minHeight: "100%", minWidth: "100%", fontFamily: "Arial", color: "#FFFFFF" } }, import_react72.default.createElement("div", { style: { zIndex: 2, position: "absolute", width: t ? "400px" : "800px", height: "552px", opacity: "0.5", background: "url(https://refine.ams3.cdn.digitaloceanspaces.com/assets/welcome-page-hexagon.png)", backgroundRepeat: "no-repeat", backgroundSize: "contain", top: "0", left: "50%", transform: "translateX(-50%)" } }), import_react72.default.createElement("div", { style: { height: t ? "40px" : "80px" } }), import_react72.default.createElement("div", { style: { display: "flex", justifyContent: "center" } }, import_react72.default.createElement("div", { style: { backgroundRepeat: "no-repeat", backgroundSize: t ? "112px 58px" : "224px 116px", backgroundImage: "url(https://refine.ams3.cdn.digitaloceanspaces.com/assets/refine-logo.svg)", width: t ? 112 : 224, height: t ? 58 : 116 } })), import_react72.default.createElement("div", { style: { height: t ? "120px" : e ? "200px" : "30vh", minHeight: t ? "120px" : "200px" } }), import_react72.default.createElement("div", { style: { display: "flex", flexDirection: "column", gap: "16px", textAlign: "center" } }, import_react72.default.createElement("h1", { style: { fontSize: s(), fontWeight: 700, margin: "0px" } }, "Welcome Aboard!"), import_react72.default.createElement("h4", { style: { fontSize: n(), fontWeight: 400, margin: "0px" } }, "Your configuration is completed.")), import_react72.default.createElement("div", { style: { height: "64px" } }), import_react72.default.createElement("div", { style: { display: "grid", gridTemplateColumns: `repeat(${r()})`, justifyContent: "center", gap: "48px", paddingRight: "16px", paddingLeft: "16px", paddingBottom: "32px", maxWidth: "976px", margin: "auto" } }, lu.map((i2) => import_react72.default.createElement(mu, { key: `welcome-page-${i2.title}`, card: i2 })))));
}, "ConfigSuccessPage");
var mu = o(({ card: e }) => {
  let { title: t, description: r, iconUrl: s, link: n } = e, [i2, a] = (0, import_react72.useState)(false);
  return import_react72.default.createElement("div", { style: { display: "flex", flexDirection: "column", gap: "16px" } }, import_react72.default.createElement("div", { style: { display: "flex", alignItems: "center" } }, import_react72.default.createElement("a", { onPointerEnter: () => a(true), onPointerLeave: () => a(false), style: { display: "flex", alignItems: "center", color: "#fff", textDecoration: "none" }, href: n }, import_react72.default.createElement("div", { style: { width: "16px", height: "16px", backgroundPosition: "center", backgroundSize: "contain", backgroundRepeat: "no-repeat", backgroundImage: `url(${s})` } }), import_react72.default.createElement("span", { style: { fontSize: "16px", fontWeight: 700, marginLeft: "13px", marginRight: "14px" } }, t), import_react72.default.createElement("svg", { style: { transition: "transform 0.5s ease-in-out, opacity 0.2s ease-in-out", ...i2 && { transform: "translateX(4px)", opacity: 1 } }, width: "12", height: "8", fill: "none", opacity: "0.5", xmlns: "http://www.w3.org/2000/svg" }, import_react72.default.createElement("path", { d: "M7.293.293a1 1 0 0 1 1.414 0l3 3a1 1 0 0 1 0 1.414l-3 3a1 1 0 0 1-1.414-1.414L8.586 5H1a1 1 0 0 1 0-2h7.586L7.293 1.707a1 1 0 0 1 0-1.414Z", fill: "#fff" })))), import_react72.default.createElement("span", { style: { fontSize: "12px", opacity: 0.5, lineHeight: "16px" } }, r));
}, "Card");
var mn = o(() => import_react73.default.createElement("div", { style: { position: "fixed", zIndex: 11, inset: 0, overflow: "auto", width: "100dvw", height: "100dvh" } }, import_react73.default.createElement("div", { style: { width: "100%", height: "100%", display: "flex", justifyContent: "center", alignItems: "center", padding: "24px", background: "#14141FBF", backdropFilter: "blur(3px)" } }, import_react73.default.createElement("div", { style: { maxWidth: "640px", width: "100%", background: "#1D1E30", borderRadius: "16px", border: "1px solid #303450", boxShadow: "0px 0px 120px -24px #000000" } }, import_react73.default.createElement("div", { style: { padding: "16px 20px", borderBottom: "1px solid #303450", display: "flex", alignItems: "center", gap: "8px", position: "relative" } }, import_react73.default.createElement(yu, { style: { position: "absolute", left: 0, top: 0 } }), import_react73.default.createElement("div", { style: { lineHeight: "24px", fontSize: "16px", color: "#FFFFFF", display: "flex", alignItems: "center", gap: "16px" } }, import_react73.default.createElement(gu, null), import_react73.default.createElement("span", { style: { fontWeight: 400 } }, "Configuration Error"))), import_react73.default.createElement("div", { style: { padding: "20px", color: "#A3ADC2", lineHeight: "20px", fontSize: "14px", display: "flex", flexDirection: "column", gap: "20px" } }, import_react73.default.createElement("p", { style: { margin: 0, padding: 0, lineHeight: "28px", fontSize: "16px" } }, import_react73.default.createElement("code", { style: { display: "inline-block", background: "#30345080", padding: "0 4px", lineHeight: "24px", fontSize: "16px", borderRadius: "4px", color: "#FFFFFF" } }, "<Refine />"), " ", "is not initialized. Please make sure you have it mounted in your app and placed your components inside it."), import_react73.default.createElement("div", null, import_react73.default.createElement(fu, null)))))), "ConfigErrorPage");
var fu = o(() => import_react73.default.createElement("pre", { style: { display: "block", overflowX: "auto", borderRadius: "8px", fontSize: "14px", lineHeight: "24px", backgroundColor: "#14141F", color: "#E5ECF2", padding: "16px", margin: "0", maxHeight: "400px", overflow: "auto" } }, import_react73.default.createElement("span", { style: { color: "#FF7B72" } }, "import"), " ", "{", " Refine, WelcomePage ", "}", " ", import_react73.default.createElement("span", { style: { color: "#FF7B72" } }, "from"), " ", import_react73.default.createElement("span", { style: { color: "#A5D6FF" } }, '"@refinedev/core"'), ";", `
`, `
`, import_react73.default.createElement("span", { style: { color: "#FF7B72" } }, "export"), " ", import_react73.default.createElement("span", { style: { color: "#FF7B72" } }, "default"), " ", import_react73.default.createElement("span", null, import_react73.default.createElement("span", { style: { color: "#FF7B72" } }, "function"), " ", import_react73.default.createElement("span", { style: { color: "#FFA657" } }, "App"), "(", import_react73.default.createElement("span", { style: { color: "rgb(222, 147, 95)" } }), ")", " "), "{", `
`, "  ", import_react73.default.createElement("span", { style: { color: "#FF7B72" } }, "return"), " (", `
`, "    ", import_react73.default.createElement("span", null, import_react73.default.createElement("span", { style: { color: "#79C0FF" } }, "<", import_react73.default.createElement("span", { style: { color: "#79C0FF" } }, "Refine"), `
`, "      ", import_react73.default.createElement("span", { style: { color: "#E5ECF2", opacity: 0.6 } }, "// ", import_react73.default.createElement("span", null, "...")), `
`, "    ", ">"), `
`, "      ", import_react73.default.createElement("span", { style: { opacity: 0.6 } }, "{", "/* ... */", "}"), `
`, "      ", import_react73.default.createElement("span", { style: { color: "#79C0FF" } }, "<", import_react73.default.createElement("span", { style: { color: "#79C0FF" } }, "WelcomePage"), " />"), `
`, "      ", import_react73.default.createElement("span", { style: { opacity: 0.6 } }, "{", "/* ... */", "}"), `
`, "    ", import_react73.default.createElement("span", { style: { color: "#79C0FF" } }, "</", import_react73.default.createElement("span", { style: { color: "#79C0FF" } }, "Refine"), ">")), `
`, "  ", ");", `
`, "}"), "ExampleImplementation");
var yu = o((e) => import_react73.default.createElement("svg", { xmlns: "http://www.w3.org/2000/svg", width: 204, height: 56, viewBox: "0 0 204 56", fill: "none", ...e }, import_react73.default.createElement("path", { fill: "url(#welcome-page-error-gradient-a)", d: "M12 0H0v12L12 0Z" }), import_react73.default.createElement("path", { fill: "url(#welcome-page-error-gradient-b)", d: "M28 0h-8L0 20v8L28 0Z" }), import_react73.default.createElement("path", { fill: "url(#welcome-page-error-gradient-c)", d: "M36 0h8L0 44v-8L36 0Z" }), import_react73.default.createElement("path", { fill: "url(#welcome-page-error-gradient-d)", d: "M60 0h-8L0 52v4h4L60 0Z" }), import_react73.default.createElement("path", { fill: "url(#welcome-page-error-gradient-e)", d: "M68 0h8L20 56h-8L68 0Z" }), import_react73.default.createElement("path", { fill: "url(#welcome-page-error-gradient-f)", d: "M92 0h-8L28 56h8L92 0Z" }), import_react73.default.createElement("path", { fill: "url(#welcome-page-error-gradient-g)", d: "M100 0h8L52 56h-8l56-56Z" }), import_react73.default.createElement("path", { fill: "url(#welcome-page-error-gradient-h)", d: "M124 0h-8L60 56h8l56-56Z" }), import_react73.default.createElement("path", { fill: "url(#welcome-page-error-gradient-i)", d: "M140 0h-8L76 56h8l56-56Z" }), import_react73.default.createElement("path", { fill: "url(#welcome-page-error-gradient-j)", d: "M132 0h8L84 56h-8l56-56Z" }), import_react73.default.createElement("path", { fill: "url(#welcome-page-error-gradient-k)", d: "M156 0h-8L92 56h8l56-56Z" }), import_react73.default.createElement("path", { fill: "url(#welcome-page-error-gradient-l)", d: "M164 0h8l-56 56h-8l56-56Z" }), import_react73.default.createElement("path", { fill: "url(#welcome-page-error-gradient-m)", d: "M188 0h-8l-56 56h8l56-56Z" }), import_react73.default.createElement("path", { fill: "url(#welcome-page-error-gradient-n)", d: "M204 0h-8l-56 56h8l56-56Z" }), import_react73.default.createElement("defs", null, import_react73.default.createElement("radialGradient", { id: "welcome-page-error-gradient-a", cx: 0, cy: 0, r: 1, gradientTransform: "scale(124)", gradientUnits: "userSpaceOnUse" }, import_react73.default.createElement("stop", { stopColor: "#FF4C4D", stopOpacity: 0.1 }), import_react73.default.createElement("stop", { offset: 1, stopColor: "#FF4C4D", stopOpacity: 0 })), import_react73.default.createElement("radialGradient", { id: "welcome-page-error-gradient-b", cx: 0, cy: 0, r: 1, gradientTransform: "scale(124)", gradientUnits: "userSpaceOnUse" }, import_react73.default.createElement("stop", { stopColor: "#FF4C4D", stopOpacity: 0.1 }), import_react73.default.createElement("stop", { offset: 1, stopColor: "#FF4C4D", stopOpacity: 0 })), import_react73.default.createElement("radialGradient", { id: "welcome-page-error-gradient-c", cx: 0, cy: 0, r: 1, gradientTransform: "scale(124)", gradientUnits: "userSpaceOnUse" }, import_react73.default.createElement("stop", { stopColor: "#FF4C4D", stopOpacity: 0.1 }), import_react73.default.createElement("stop", { offset: 1, stopColor: "#FF4C4D", stopOpacity: 0 })), import_react73.default.createElement("radialGradient", { id: "welcome-page-error-gradient-d", cx: 0, cy: 0, r: 1, gradientTransform: "scale(124)", gradientUnits: "userSpaceOnUse" }, import_react73.default.createElement("stop", { stopColor: "#FF4C4D", stopOpacity: 0.1 }), import_react73.default.createElement("stop", { offset: 1, stopColor: "#FF4C4D", stopOpacity: 0 })), import_react73.default.createElement("radialGradient", { id: "welcome-page-error-gradient-e", cx: 0, cy: 0, r: 1, gradientTransform: "scale(124)", gradientUnits: "userSpaceOnUse" }, import_react73.default.createElement("stop", { stopColor: "#FF4C4D", stopOpacity: 0.1 }), import_react73.default.createElement("stop", { offset: 1, stopColor: "#FF4C4D", stopOpacity: 0 })), import_react73.default.createElement("radialGradient", { id: "welcome-page-error-gradient-f", cx: 0, cy: 0, r: 1, gradientTransform: "scale(124)", gradientUnits: "userSpaceOnUse" }, import_react73.default.createElement("stop", { stopColor: "#FF4C4D", stopOpacity: 0.1 }), import_react73.default.createElement("stop", { offset: 1, stopColor: "#FF4C4D", stopOpacity: 0 })), import_react73.default.createElement("radialGradient", { id: "welcome-page-error-gradient-g", cx: 0, cy: 0, r: 1, gradientTransform: "scale(124)", gradientUnits: "userSpaceOnUse" }, import_react73.default.createElement("stop", { stopColor: "#FF4C4D", stopOpacity: 0.1 }), import_react73.default.createElement("stop", { offset: 1, stopColor: "#FF4C4D", stopOpacity: 0 })), import_react73.default.createElement("radialGradient", { id: "welcome-page-error-gradient-h", cx: 0, cy: 0, r: 1, gradientTransform: "scale(124)", gradientUnits: "userSpaceOnUse" }, import_react73.default.createElement("stop", { stopColor: "#FF4C4D", stopOpacity: 0.1 }), import_react73.default.createElement("stop", { offset: 1, stopColor: "#FF4C4D", stopOpacity: 0 })), import_react73.default.createElement("radialGradient", { id: "welcome-page-error-gradient-i", cx: 0, cy: 0, r: 1, gradientTransform: "scale(124)", gradientUnits: "userSpaceOnUse" }, import_react73.default.createElement("stop", { stopColor: "#FF4C4D", stopOpacity: 0.1 }), import_react73.default.createElement("stop", { offset: 1, stopColor: "#FF4C4D", stopOpacity: 0 })), import_react73.default.createElement("radialGradient", { id: "welcome-page-error-gradient-j", cx: 0, cy: 0, r: 1, gradientTransform: "scale(124)", gradientUnits: "userSpaceOnUse" }, import_react73.default.createElement("stop", { stopColor: "#FF4C4D", stopOpacity: 0.1 }), import_react73.default.createElement("stop", { offset: 1, stopColor: "#FF4C4D", stopOpacity: 0 })), import_react73.default.createElement("radialGradient", { id: "welcome-page-error-gradient-k", cx: 0, cy: 0, r: 1, gradientTransform: "scale(124)", gradientUnits: "userSpaceOnUse" }, import_react73.default.createElement("stop", { stopColor: "#FF4C4D", stopOpacity: 0.1 }), import_react73.default.createElement("stop", { offset: 1, stopColor: "#FF4C4D", stopOpacity: 0 })), import_react73.default.createElement("radialGradient", { id: "welcome-page-error-gradient-l", cx: 0, cy: 0, r: 1, gradientTransform: "scale(124)", gradientUnits: "userSpaceOnUse" }, import_react73.default.createElement("stop", { stopColor: "#FF4C4D", stopOpacity: 0.1 }), import_react73.default.createElement("stop", { offset: 1, stopColor: "#FF4C4D", stopOpacity: 0 })), import_react73.default.createElement("radialGradient", { id: "welcome-page-error-gradient-m", cx: 0, cy: 0, r: 1, gradientTransform: "scale(124)", gradientUnits: "userSpaceOnUse" }, import_react73.default.createElement("stop", { stopColor: "#FF4C4D", stopOpacity: 0.1 }), import_react73.default.createElement("stop", { offset: 1, stopColor: "#FF4C4D", stopOpacity: 0 })), import_react73.default.createElement("radialGradient", { id: "welcome-page-error-gradient-n", cx: 0, cy: 0, r: 1, gradientTransform: "scale(124)", gradientUnits: "userSpaceOnUse" }, import_react73.default.createElement("stop", { stopColor: "#FF4C4D", stopOpacity: 0.1 }), import_react73.default.createElement("stop", { offset: 1, stopColor: "#FF4C4D", stopOpacity: 0 })))), "ErrorGradient");
var gu = o((e) => import_react73.default.createElement("svg", { xmlns: "http://www.w3.org/2000/svg", width: 16, height: 16, viewBox: "0 0 16 16", fill: "none", ...e }, import_react73.default.createElement("path", { fill: "#FF4C4D", fillRule: "evenodd", d: "M8 16A8 8 0 1 0 8 0a8 8 0 0 0 0 16Z", clipRule: "evenodd" }), import_react73.default.createElement("path", { fill: "#fff", fillRule: "evenodd", d: "M7 8a1 1 0 1 0 2 0V5a1 1 0 1 0-2 0v3Zm0 3a1 1 0 1 1 2 0 1 1 0 0 1-2 0Z", clipRule: "evenodd" })), "ErrorIcon");
var Tu = o(() => {
  let { __initialized: e } = oe();
  return import_react71.default.createElement(import_react71.default.Fragment, null, import_react71.default.createElement(ln, null), !e && import_react71.default.createElement(mn, null));
}, "WelcomePage");
var Ru = "4.47.2";
var fn = o(() => {
  var h;
  let e = so(), t = (0, import_react76.useContext)(gt), r = (0, import_react76.useContext)(et), s = (0, import_react76.useContext)(Xt), n = (0, import_react76.useContext)(_t), { i18nProvider: i2 } = (0, import_react76.useContext)(Ke), a = (0, import_react76.useContext)(jt), u = (0, import_react76.useContext)(ct), { resources: p3 } = $(), c = oe(), d3 = !!t.create || !!t.get || !!t.update, f2 = !!(r != null && r.publish) || !!(r != null && r.subscribe) || !!(r != null && r.unsubscribe), R2 = !!s.useHistory || !!s.Link || !!s.Prompt || !!s.useLocation || !!s.useParams, g2 = !!n, x2 = !!(i2 != null && i2.changeLocale) || !!(i2 != null && i2.getLocale) || !!(i2 != null && i2.translate), E2 = !!a.close || !!a.open, w = !!u.can, m2 = (h = c == null ? void 0 : c.options) == null ? void 0 : h.projectId;
  return { providers: { auth: e, auditLog: d3, live: f2, router: R2, data: g2, i18n: x2, notification: E2, accessControl: w }, version: Ru, resourceCount: p3.length, projectId: m2 };
}, "useTelemetryData");
var xu = o((e) => {
  try {
    let t = JSON.stringify(e || {});
    return typeof btoa < "u" ? btoa(t) : Buffer.from(t).toString("base64");
  } catch {
    return;
  }
}, "encode");
var Pu = o((e) => {
  let t = new Image();
  t.src = e;
}, "throughImage");
var hu = o((e) => {
  fetch(e);
}, "throughFetch");
var Cu = o((e) => {
  typeof Image < "u" ? Pu(e) : typeof fetch < "u" && hu(e);
}, "transport");
var gn = o(() => {
  let e = fn(), t = import_react75.default.useRef(false);
  return import_react75.default.useEffect(() => {
    if (t.current)
      return;
    let r = xu(e);
    r && (Cu(`https://telemetry.refine.dev/telemetry?payload=${r}`), t.current = true);
  }, []), null;
}, "Telemetry");
var Tn = o((e) => {
  let t = ["go", "parse", "back", "Link"], r = Object.keys(e).filter((n) => !t.includes(n));
  return r.length > 0 ? (console.warn(`Unsupported properties are found in \`routerProvider\` prop. You provided \`${r.join(", ")}\`. Supported properties are \`${t.join(", ")}\`. You may wanted to use \`legacyRouterProvider\` prop instead.`), true) : false;
}, "checkRouterPropMisuse");
var xn = o((e) => {
  let t = import_react77.default.useRef(false);
  import_react77.default.useEffect(() => {
    t.current === false && e && Tn(e) && (t.current = true);
  }, [e]);
}, "useRouterMisuseWarning");
var Du = o(({ legacyAuthProvider: e, authProvider: t, dataProvider: r, legacyRouterProvider: s, routerProvider: n, notificationProvider: i2, accessControlProvider: a, auditLogProvider: u, resources: p3, DashboardPage: c, ReadyPage: d3, LoginPage: f2, catchAll: R2, children: g2, liveProvider: x2, i18nProvider: E2, Title: w, Layout: m2, Sider: h, Header: D, Footer: I2, OffLayoutArea: T2, onLiveEvent: y2, options: l2 }) => {
  let { optionsWithDefaults: C2, disableTelemetryWithDefault: v2, reactQueryWithDefaults: P2 } = $r({ options: l2 }), U = Mr(() => {
    var L;
    return P2.clientConfig instanceof QueryClient ? P2.clientConfig : new QueryClient({ ...P2.clientConfig, defaultOptions: { ...P2.clientConfig.defaultOptions, queries: { refetchOnWindowFocus: false, keepPreviousData: true, ...(L = P2.clientConfig.defaultOptions) == null ? void 0 : L.queries } } });
  }, [P2.clientConfig]);
  C(U);
  let M = import_react74.default.useMemo(() => typeof i2 == "function" ? i2 : () => i2 ?? {}, [i2])();
  if (xn(n), s && !n && (p3 ?? []).length === 0)
    return d3 ? import_react74.default.createElement(d3, null) : import_react74.default.createElement(Ro, null);
  let { RouterComponent: k = import_react74.default.Fragment } = n ? {} : s ?? {};
  return import_react74.default.createElement(QueryClientProvider, { client: U }, import_react74.default.createElement(As, { ...M }, import_react74.default.createElement(Ko, { ...e ?? {}, isProvided: !!e }, import_react74.default.createElement(Ho, { ...t ?? {}, isProvided: !!t }, import_react74.default.createElement(Ps, { ...r }, import_react74.default.createElement(Cs, { liveProvider: x2 }, import_react74.default.createElement(Us, { value: s && !n ? "legacy" : "new" }, import_react74.default.createElement(ws, { router: n }, import_react74.default.createElement(Gs, { ...s }, import_react74.default.createElement(Ds, { resources: p3 ?? [] }, import_react74.default.createElement(Qs, { i18nProvider: E2 }, import_react74.default.createElement($s, { ...a ?? {} }, import_react74.default.createElement(Js, { ...u ?? {} }, import_react74.default.createElement(uo, null, import_react74.default.createElement(Qo, { mutationMode: C2.mutationMode, warnWhenUnsavedChanges: C2.warnWhenUnsavedChanges, syncWithLocation: C2.syncWithLocation, Title: w, undoableTimeout: C2.undoableTimeout, catchAll: R2, DashboardPage: c, LoginPage: f2, Layout: m2, Sider: h, Footer: I2, Header: D, OffLayoutArea: T2, hasDashboard: !!c, liveMode: C2.liveMode, onLiveEvent: y2, options: C2 }, import_react74.default.createElement(Oo, null, import_react74.default.createElement(k, null, g2, !v2 && import_react74.default.createElement(gn, null), import_react74.default.createElement(xo, null))))))))))))))))));
}, "Refine");
var Rs = o(({ notification: e }) => {
  let t = _(), { notificationDispatch: r } = Ze(), { open: s } = Ie(), [n, i2] = (0, import_react78.useState)(), a = o(() => {
    if (e.isRunning === true && (e.seconds === 0 && e.doMutation(), e.isSilent || s == null || s({ key: `${e.id}-${e.resource}-notification`, type: "progress", message: t("notifications.undoable", { seconds: Bt(e.seconds) }, `You have ${Bt(e.seconds)} seconds to undo`), cancelMutation: e.cancelMutation, undoableTimeout: Bt(e.seconds) }), e.seconds > 0)) {
      n && clearTimeout(n);
      let u = setTimeout(() => {
        r({ type: "DECREASE_NOTIFICATION_SECOND", payload: { id: e.id, seconds: e.seconds, resource: e.resource } });
      }, 1e3);
      i2(u);
    }
  }, "cancelNotification");
  return (0, import_react78.useEffect)(() => {
    a();
  }, [e]), null;
}, "UndoableQueue");
var Mu = o(({ children: e, Layout: t, Sider: r, Header: s, Title: n, Footer: i2, OffLayoutArea: a }) => {
  let { Layout: u, Footer: p3, Header: c, Sider: d3, Title: f2, OffLayoutArea: R2 } = oe();
  return import_react79.default.createElement(t ?? u, { Sider: r ?? d3, Header: s ?? c, Footer: i2 ?? p3, Title: n ?? f2, OffLayoutArea: a ?? R2 }, e, import_react79.default.createElement(wu, null));
}, "LayoutWrapper");
var wu = o(() => {
  let { Prompt: e } = ae(), t = _(), { warnWhen: r, setWarnWhen: s } = Ht(), n = o((i2) => (i2.preventDefault(), i2.returnValue = t("warnWhenUnsavedChanges", "Are you sure you want to leave? You have unsaved changes."), i2.returnValue), "warnWhenListener");
  return (0, import_react79.useEffect)(() => (r && window.addEventListener("beforeunload", n), window.removeEventListener("beforeunload", n)), [r]), import_react79.default.createElement(e, { when: r, message: t("warnWhenUnsavedChanges", "Are you sure you want to leave? You have unsaved changes."), setWarnWhen: s });
}, "UnsavedPrompt");
function Iu({ redirectOnFail: e = true, appendCurrentPathToQuery: t = true, children: r, fallback: s, loading: n }) {
  var D;
  let i2 = Y(), a = ee(), u = !!(i2 != null && i2.isProvided), p3 = !!(i2 != null && i2.isLegacy), c = a === "legacy", d3 = ge(), f2 = fe(), { useLocation: R2 } = ae(), g2 = R2(), { isFetching: x2, isSuccess: E2, data: { authenticated: w, redirectTo: m2 } = {} } = Lr({ v3LegacyAuthProviderCompatible: p3 }), h = u ? p3 ? E2 : w : true;
  if (!u)
    return import_react80.default.createElement(import_react80.default.Fragment, null, r ?? null);
  if (x2)
    return import_react80.default.createElement(import_react80.default.Fragment, null, n ?? null);
  if (h)
    return import_react80.default.createElement(import_react80.default.Fragment, null, r ?? null);
  {
    if (typeof s < "u")
      return import_react80.default.createElement(import_react80.default.Fragment, null, s ?? null);
    let I2 = p3 ? typeof e == "string" ? e : "/login" : typeof e == "string" ? e : m2, T2 = `${c ? g2 == null ? void 0 : g2.pathname : d3.pathname}`.replace(/(\?.*|#.*)$/, "");
    if (I2)
      if (c) {
        let y2 = t ? `?to=${encodeURIComponent(T2)}` : "";
        return import_react80.default.createElement(Fu, { to: `${I2}${y2}` });
      } else
        return import_react80.default.createElement(Su, { config: { to: I2, query: t ? { to: (D = d3.params) != null && D.to ? d3.params.to : f2({ to: T2, options: { keepQuery: true }, type: "path" }) } : void 0, type: "replace" } });
    return null;
  }
}
o(Iu, "Authenticated");
var Su = o(({ config: e }) => {
  let t = fe();
  return import_react80.default.useEffect(() => {
    t(e);
  }, [t, e]), null;
}, "Redirect");
var Fu = o(({ to: e }) => {
  let { replace: t } = me();
  return import_react80.default.useEffect(() => {
    t(e);
  }, [t, e]), null;
}, "RedirectLegacy");
var xo = o(() => {
  let { useLocation: e } = ae(), { checkAuth: t } = ne(), r = e();
  return (0, import_react81.useEffect)(() => {
    t == null || t().catch(() => false);
  }, [r == null ? void 0 : r.pathname]), null;
}, "RouteChangeHandler");
var Qu = o(({ resource: e, action: t, params: r, fallback: s, onUnauthorized: n, children: i2, queryOptions: a, ...u }) => {
  let { resource: p3, id: c, action: d3 } = $(e), { identifier: f2 } = $(), g2 = o(() => {
    let h = (r == null ? void 0 : r.id) ?? c;
    return e && e !== f2 ? r == null ? void 0 : r.id : h;
  }, "getDefaultId")(), x2 = e ?? (p3 == null ? void 0 : p3.name), E2 = t ?? d3 ?? "", w = r ?? { id: g2, resource: p3 }, { data: m2 } = Ws({ resource: x2, action: E2, params: w, queryOptions: a });
  return (0, import_react82.useEffect)(() => {
    n && (m2 == null ? void 0 : m2.can) === false && n({ resource: x2, action: E2, reason: m2 == null ? void 0 : m2.reason, params: w });
  }, [m2 == null ? void 0 : m2.can]), m2 != null && m2.can ? import_react82.default.isValidElement(i2) ? import_react82.default.cloneElement(i2, u) : import_react82.default.createElement(import_react82.default.Fragment, null, i2) : (m2 == null ? void 0 : m2.can) === false ? import_react82.default.createElement(import_react82.default.Fragment, null, s ?? null) : null;
}, "CanAccess");
var hn = [`
    .bg-top-announcement {
        border-bottom: 1px solid rgba(71, 235, 235, 0.15);
        background: radial-gradient(
                218.19% 111.8% at 0% 0%,
                rgba(71, 235, 235, 0.1) 0%,
                rgba(71, 235, 235, 0.2) 100%
            ),
            #14141f;
    }
    `, `
    .top-announcement-mask {
        mask-image: url(https://refine.ams3.cdn.digitaloceanspaces.com/website/static/assets/hexagon.svg);
        -webkit-mask-image: url(https://refine.ams3.cdn.digitaloceanspaces.com/website/static/assets/hexagon.svg);
        mask-repeat: repeat;
        -webkit-mask-repeat: repeat;
        background: rgba(71, 235, 235, 0.25);
    }
    `, `
    .banner {
        display: flex;
        @media (max-width: 1000px) {
            display: none;
        }
    }`, `
    .gh-link, .gh-link:hover, .gh-link:active, .gh-link:visited, .gh-link:focus {
        text-decoration: none;
        z-index: 9;
    }
    `, `
    @keyframes top-announcement-glow {
        0% {
            opacity: 1;
        }

        100% {
            opacity: 0;
        }
    }
    `];
var Bu = "If you find Refine useful, you can contribute to its growth by giving it a star on GitHub";
var Nu = o(() => ((0, import_react83.useEffect)(() => {
  let e = document.createElement("style");
  document.head.appendChild(e), hn.forEach((t) => {
    var r;
    return (r = e.sheet) == null ? void 0 : r.insertRule(t, e.sheet.cssRules.length);
  });
}, []), import_react83.default.createElement("div", { className: "banner bg-top-announcement", style: { width: "100%", height: "48px" } }, import_react83.default.createElement("div", { style: { position: "relative", display: "flex", justifyContent: "center", alignItems: "center", paddingLeft: "200px", width: "100%", maxWidth: "100vw", height: "100%", borderBottom: "1px solid #47ebeb26" } }, import_react83.default.createElement("div", { className: "top-announcement-mask", style: { position: "absolute", left: 0, top: 0, width: "100%", height: "100%", borderBottom: "1px solid #47ebeb26" } }, import_react83.default.createElement("div", { style: { position: "relative", width: "960px", height: "100%", display: "flex", justifyContent: "space-between", margin: "0 auto" } }, import_react83.default.createElement("div", { style: { width: "calc(50% - 300px)", height: "100%", position: "relative" } }, import_react83.default.createElement(kr, { style: { animationDelay: "1.5s", position: "absolute", top: "2px", right: "220px" }, id: "1" }), import_react83.default.createElement(kr, { style: { animationDelay: "1s", position: "absolute", top: "8px", right: "100px", transform: "rotate(180deg)" }, id: "2" }), import_react83.default.createElement(Cn, { style: { position: "absolute", right: "10px" }, id: "3" })), import_react83.default.createElement("div", { style: { width: "calc(50% - 300px)", height: "100%", position: "relative" } }, import_react83.default.createElement(kr, { style: { animationDelay: "2s", position: "absolute", top: "6px", right: "180px", transform: "rotate(180deg)" }, id: "4" }), import_react83.default.createElement(kr, { style: { animationDelay: "0.5s", transitionDelay: "1.3s", position: "absolute", top: "2px", right: "40px" }, id: "5" }), import_react83.default.createElement(Cn, { style: { position: "absolute", right: "-70px" }, id: "6" })))), import_react83.default.createElement(Ku, { text: Bu })))), "GitHubBanner");
var Ku = o(({ text: e }) => import_react83.default.createElement("a", { className: "gh-link", href: "https://s.refine.dev/github-support", target: "_blank", rel: "noreferrer", style: { position: "absolute", height: "100%", padding: "0 60px", display: "flex", flexWrap: "nowrap", whiteSpace: "nowrap", justifyContent: "center", alignItems: "center", backgroundImage: "linear-gradient(90deg, rgba(31, 63, 72, 0.00) 0%, #1F3F48 10%, #1F3F48 90%, rgba(31, 63, 72, 0.00) 100%)" } }, import_react83.default.createElement("div", { style: { color: "#fff", display: "flex", flexDirection: "row", gap: "8px" } }, import_react83.default.createElement("span", { style: { display: "flex", flexDirection: "row", justifyContent: "center", alignItems: "center" } }, "⭐️"), import_react83.default.createElement("span", { className: "text", style: { fontSize: "16px", lineHeight: "24px" } }, e), import_react83.default.createElement("span", { style: { display: "flex", flexDirection: "row", justifyContent: "center", alignItems: "center" } }, "⭐️"))), "Text");
var kr = o(({ style: e, ...t }) => import_react83.default.createElement("svg", { xmlns: "http://www.w3.org/2000/svg", width: 80, height: 40, fill: "none", style: { opacity: 1, animation: "top-announcement-glow 1s ease-in-out infinite alternate", ...e } }, import_react83.default.createElement("circle", { cx: 40, r: 40, fill: `url(#${t.id}-a)`, fillOpacity: 0.5 }), import_react83.default.createElement("defs", null, import_react83.default.createElement("radialGradient", { id: `${t.id}-a`, cx: 0, cy: 0, r: 1, gradientTransform: "matrix(0 40 -40 0 40 0)", gradientUnits: "userSpaceOnUse" }, import_react83.default.createElement("stop", { stopColor: "#47EBEB" }), import_react83.default.createElement("stop", { offset: 1, stopColor: "#47EBEB", stopOpacity: 0 })))), "GlowSmall");
var Cn = o(({ style: e, ...t }) => import_react83.default.createElement("svg", { xmlns: "http://www.w3.org/2000/svg", width: 120, height: 48, fill: "none", ...t, style: { opacity: 1, animation: "top-announcement-glow 1s ease-in-out infinite alternate", ...e } }, import_react83.default.createElement("circle", { cx: 60, cy: 24, r: 60, fill: `url(#${t.id}-a)`, fillOpacity: 0.5 }), import_react83.default.createElement("defs", null, import_react83.default.createElement("radialGradient", { id: `${t.id}-a`, cx: 0, cy: 0, r: 1, gradientTransform: "matrix(0 60 -60 0 60 24)", gradientUnits: "userSpaceOnUse" }, import_react83.default.createElement("stop", { stopColor: "#47EBEB" }), import_react83.default.createElement("stop", { offset: 1, stopColor: "#47EBEB", stopOpacity: 0 })))), "GlowBig");
var Gu = o(({ status: e, elements: { success: t = import_react84.default.createElement(Qr, { key: "autoSave.success", defaultMessage: "saved" }), error: r = import_react84.default.createElement(Qr, { key: "autoSave.error", defaultMessage: "auto save failure" }), loading: s = import_react84.default.createElement(Qr, { key: "autoSave.loading", defaultMessage: "saving..." }), idle: n = import_react84.default.createElement(Qr, { key: "autoSave.idle", defaultMessage: "waiting for changes" }) } = {} }) => {
  switch (e) {
    case "success":
      return import_react84.default.createElement(import_react84.default.Fragment, null, t);
    case "error":
      return import_react84.default.createElement(import_react84.default.Fragment, null, r);
    case "loading":
      return import_react84.default.createElement(import_react84.default.Fragment, null, s);
    default:
      return import_react84.default.createElement(import_react84.default.Fragment, null, n);
  }
}, "AutoSaveIndicator");
var Qr = o(({ key: e, defaultMessage: t }) => {
  let r = _();
  return import_react84.default.createElement("span", null, r(e, t));
}, "Message");

export {
  require_shim,
  useQueryClient,
  require_lib,
  tr,
  rr,
  Lo,
  S,
  bt,
  Oe,
  Eo,
  So,
  lr,
  Fo,
  z,
  fr,
  yr,
  gr,
  Y,
  $o,
  ze,
  Ht,
  zr,
  kn,
  oe,
  Lt,
  xr,
  jo,
  Pr,
  Nn,
  hr,
  Et,
  Cr,
  br,
  vr,
  Kn,
  Gn,
  Hn,
  $n,
  Xr,
  Dr,
  $t,
  qr,
  to,
  oo,
  Lr,
  ea,
  ie,
  ta,
  so,
  te,
  Wt,
  Ot,
  no,
  Ur,
  zt,
  Pa,
  co,
  La,
  wa,
  Ia,
  Aa,
  Va,
  se,
  $a,
  ut,
  ee,
  po,
  ge,
  $,
  Is,
  be,
  it,
  Fs,
  vx,
  Be,
  Ze,
  Ie,
  ue,
  Ke,
  yi,
  _,
  Pi,
  TP,
  SP,
  Bs,
  ft,
  fe,
  mo,
  me,
  hh,
  wh,
  Ah,
  Ki,
  yt,
  ae,
  ct,
  Ws,
  bC,
  QC,
  YC,
  Ne,
  vb,
  Bb,
  nu,
  J,
  uu,
  Hr,
  pu,
  Ro,
  Tu,
  Du,
  Rs,
  Mu,
  Iu,
  xo,
  Qu,
  Nu,
  Gu
};
/*! Bundled license information:

use-sync-external-store/cjs/use-sync-external-store-shim.development.js:
  (**
   * @license React
   * use-sync-external-store-shim.development.js
   *
   * Copyright (c) Facebook, Inc. and its affiliates.
   *
   * This source code is licensed under the MIT license found in the
   * LICENSE file in the root directory of this source tree.
   *)

papaparse/papaparse.min.js:
  (* @license
  Papa Parse
  v5.4.1
  https://github.com/mholt/PapaParse
  License: MIT
  *)
*/
//# sourceMappingURL=chunk-T43E27SF.js.map
